# agents/index — a generated, line-numbered map of the code
Purpose: find the right LINE of a large file without reading the file. Read this when you arrive cold, once; after that grep the index, never read it whole. Skip it when you already know the file and the symbol — `grep -n` the source directly.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `9836e6a`). Do not edit; rebuild with `npm run agents:index`.

## How to read it cheaply
1. You know the name: `grep -rn "bookClass" agents/index/` — one hit per fact, e.g. `symbols/app.js.md:L4147  async bookClass(eventId, btn, studioId)  +286`. (For the declaration alone, `grep -n` on the source is as cheap.)
2. You know what it DOES, not what it is called: a symbol carries the first sentence of the comment DIRECTLY above it, when it has one — `grep -rni "waitlist place" agents/index/symbols/` (8 hits). About 50% of the symbols have none — the longest functions of js/app.js among them (`renderMyBookings`, `bookClass`, `showBikePicker`, `openClassDetail`), because their comment sits inside the body or behind a blank line or a marker. No hit is not "no such code": grep agents/architecture/, then the source.
3. Open the SOURCE at that line with a small window (`+N` is the length of the declaration in lines); widen only if you must.
4. For "who else touches this" use the cross-reference files below (events, storage keys, globals, API calls, ids) before you grep the whole tree.
5. You know the RULE and want the test that guards it: `grep -n -i "<words of the rule>" agents/index/test-sections.md` — a hit is a section title with its line, under the `## <file>` heading of its suite. For a rule of the Android app's Java, java.md lists the JVM tests by name.
A symbols file is also the fastest way to see the SHAPE of a module: its headings are the file's own banner comments.

## How fresh it is
Line 4 of every file names the commit the rebuild was made ON TOP OF — "working tree" = built over uncommitted changes, which is the normal case, because the index is rebuilt and committed together with the change it describes. So the hash is normally the PARENT of the commit that carries these lines; it cannot be compared with `git log` to judge freshness. A file is rewritten only when its content changes, so an old hash means "unchanged since", not "stale". Only a check tells you:
- `npm run agents:check` — exits 1 and lists the files a rebuild would change. It is NOT part of `npm run ci` or of the pre-commit hook; .github/workflows/ci.yml runs it as a NON-BLOCKING step, so a stale index shows as a warning on the commit, never as a red build.
- `npm run agents:index` — rebuilds (`node agents/tools/build-index.mjs --force` also restamps every header).
Rebuild after an edit that adds or removes lines in js/, css/, the HTML shells, ios-app/www/native-bridge.js, Swift, the Android app's Java (ios-app/android/app/src), tests/unit.js or tests/suites — every symbol below the edit moves — and commit the result with it. A drifted index is not an error: `npm test` only prints an advisory line (tests/suites/15-agents-index.js), and the header on line 4 tells you how old it is — when in doubt rebuild first, it takes a second.
The folder is wholly generated: never put a hand-written file in agents/index/ — the next rebuild deletes every `.md` it did not write.

## Files
| File | Lines | What it answers |
|---|---|---|
| [pure-blocks.md](pure-blocks.md) | 97 | `pure:<name>` regions: line ranges, what they declare, which suites load them, which ones need fakes |
| [events.md](events.md) | 78 | `PsycleEvents` names → emitters and listeners |
| [storage-keys.md](storage-keys.md) | 310 | `psycle_…` keys → read / write / remove sites, and the four key lists |
| [globals.md](globals.md) | 208 | `window.<name> =` assignments → file:line, in load order; which sites WRAP a function |
| [api-calls.md](api-calls.md) | 106 | `METHOD path` → call sites, writes first, with `retries:` |
| [css-sections.md](css-sections.md) | 241 | stylesheet sections with line ranges; custom properties css/theme.css defines |
| [dom-ids.md](dom-ids.md) | 160 | ids in the HTML shells and ids created by JS → files that reference them |
| [tests.md](tests.md) | 534 | each suite: what it covers, pure blocks it loads, functions and source text it slices by anchor |
| [test-sections.md](test-sections.md) | 861 | which test guards a rule: every `t.section(…)` title of tests/unit.js and tests/suites/*.js, with its line |
| [swift.md](swift.md) | 268 | Swift types and functions with line numbers; the stored and computed properties of value types |
| [java.md](java.md) | 444 | the Android app's Java (app/src main, debug, test): types, methods, `static final` constants and `@Test` names, with line numbers |

## symbols/ — one file per script, in load order
| Index | Source | Source lines | Symbols | Shape |
|---|---|---|---|---|
| [symbols/state.js.md](symbols/state.js.md) | js/state.js | 228 | 8 | top level + 2 IIFEs |
| [symbols/security.js.md](symbols/security.js.md) | js/security.js | 549 | 42 | one IIFE |
| [symbols/theme.js.md](symbols/theme.js.md) | js/theme.js | 801 | 53 | top level + 2 IIFEs |
| [symbols/facets.js.md](symbols/facets.js.md) | js/facets.js | 134 | 8 | one IIFE |
| [symbols/app.js.md](symbols/app.js.md) | js/app.js | 14116 | 699 | top level + 5 IIFEs |
| [symbols/reliability.js.md](symbols/reliability.js.md) | js/reliability.js | 1142 | 58 | one IIFE |
| [symbols/interactions.js.md](symbols/interactions.js.md) | js/interactions.js | 721 | 52 | one IIFE |
| [symbols/performance.js.md](symbols/performance.js.md) | js/performance.js | 251 | 20 | top level + 3 IIFEs |
| [symbols/calendar.js.md](symbols/calendar.js.md) | js/calendar.js | 384 | 16 | top level + 1 IIFE |
| [symbols/features.js.md](symbols/features.js.md) | js/features.js | 1051 | 54 | one IIFE |
| [symbols/tabs.js.md](symbols/tabs.js.md) | js/tabs.js | 4580 | 182 | one IIFE |
| [symbols/settings.js.md](symbols/settings.js.md) | js/settings.js | 2121 | 99 | one IIFE |
| [symbols/explore.js.md](symbols/explore.js.md) | js/explore.js | 1243 | 49 | one IIFE |
| [symbols/api-client.js.md](symbols/api-client.js.md) | js/api-client.js | 179 | 4 | one IIFE |
| [symbols/diagnostic.js.md](symbols/diagnostic.js.md) | js/diagnostic.js | 519 | 31 | one IIFE |
| [symbols/native-bridge.js.md](symbols/native-bridge.js.md) | ios-app/www/native-bridge.js | 2652 | 170 | one IIFE |
| [symbols/sw.js.md](symbols/sw.js.md) | sw.js | 143 | 3 | top level |

## What the parser does and does not see
- Line-based regexes over a small lexer that blanks comments, strings, template literals and regex literals and counts brackets. No AST.
- "Top level" = bracket depth 0, OR one level inside a top-level IIFE (`(function () { … })();`). Most modules wrap everything in one IIFE; for those, the declarations directly inside it are what you would call the module's top level.
- Listed: `function`, `async function`, `class`, `const|let|var` (function-valued ones with their parameters), `window.x = function | arrow | { … } | anotherName`, top-level IIFEs of ten lines or more. NOT listed: functions nested inside functions, members of object literals (an exported object shows its keys), callbacks, anything inside `document.addEventListener(…, function () { … })`.
- Java (java.md) is read statement by statement at member level, after comments, strings and character literals are blanked: a `{` opens a type, a method body or an initialiser, and only a type's body is read further — so the members of an anonymous or local class are not listed. Swift (swift.md) is read line by line. The Android project's XML, Gradle files and manifest are not indexed.
- A section title (test-sections.md) is the string literal `t.section(…)` was called with; a call inside a string or a comment is not a call.
- Comments are quoted from the source (first sentence, clipped). In symbols/app.js.md they are clipped harder to keep the file under 60 KB — symbols are never dropped.
- Line numbers are exact for the commit in the header and drift with every edit above them. If a line does not show the symbol, grep the source and rebuild the index.
