# Test sections — every `t.section(…)` title, with its line
Purpose: which test guards a rule. A section title says what the group of assertions under it proves; this file lists every one, per file, with its line. Use it with ONE grep: `grep -n -i "<words of the rule>" agents/index/test-sections.md` — the hit is `L<line>  <title>`, and the nearest `## <file>` heading above it (`grep -n` prints both line numbers, so compare them) is the suite to open at that line. Skip it when you want what a suite LOADS or SLICES: that is tests.md.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `0fc5967`). Do not edit; rebuild with `npm run agents:index`.

695 sections in 80 files: tests/unit.js (its built-in checks, which run first), then tests/suites/*.js in filename order, which is the order `npm test` runs them in. Format: `L<line>  <title>`. A title is the string the call was given, adjacent literals joined; one built from a variable is printed as written between the parentheses (`'Text tokens — ' + id`). The assertions of a section run from its line to the next section's. No hit is not "no test": a rule may be held by an assertion's own message — `grep -rn -i "<words>" tests/suites/`.

## tests/unit.js
L331  escapeHTML (js/security.js)
L361  escapeForJsString (js/security.js)
L375  PsycleAPI.categorizeError (js/api-client.js)
L403  PsycleDiag.record + checkContract (js/diagnostic.js)
L444  Europe/London class-time resolution (ios-app/www/native-bridge.js)
L473  Waitlist pure helpers (js/app.js waitlist:pure block)

## tests/suites/00-harness.js
L30  Suite harness

## tests/suites/10a-time24.js
L25  10a: _clock24 — zero-padded 24-hour "HH:MM", from digits the caller already has
L39  10a: _clockOf — the time of a class, cut from its wall-clock DIGITS
L59  10a: the formatter rides with every pure block that prints a time
L90  10a: the class card — "18:30" over "45 min" (the approved boards), no am / pm under the digits
L100  10a: "Free cancel until Sun 19:30" — _cancelDeadline's label
L114  10a: countdown chip — "Tomorrow 07:00"; a countdown is not a clock time and is untouched
L128  10a: the clash line, the day strip's spoken dot, the Time row
L161  10a: the other modules print through the same formatter
L199  10a: the welcome's illustrations are 24-hour too
L258  10a: the guard can see what it is looking for
L283  10a: no 12-hour marker in shipped source

## tests/suites/10b-themes.js
L37  10b themes: five themes, the two retired ones gone from the registry
L45  10b themes: a retired id reads as the theme on the same base (pure:theme-retired)
L62  10b themes: a saved / deep-linked retired theme — migrated, rewritten once, never "follow the system"
L134  10b themes: iOS — the Preferences mirror follows the rewrite
L177  10b themes: both first-paint scripts carry the same five themes and the same retired map
L210  10b themes: a settings backup that names a retired theme is accepted and mapped
L240  10b themes: no stylesheet, script or page names a retired theme
L267  10b themes: the picker draws five chips, from the registry
L287  10b themes: Terminal and Handheld — five ranges in two rows on a phone, nothing clipped at rest
L363  10b themes: the favourite star is a token — --fav, per theme, >= 3:1 as a graphic

## tests/suites/10c-polish.js
L65  10c: the caution mark — one drawn line mark, decorative, never an emoji
L76  10c: class sheet — the clash is a row like its neighbours, led by the caution mark
L91  10c: seat picker — the clash chip is the same quiet line, in the header's ink
L122  10c: confirm dialogs — the warn line is the quiet line; the late-cancel warning alone keeps a ground
L160  10c: usual-week sheet — a row's warning is the quiet line (pure:usual-week-note)
L187  10c: the welcome's illustration draws the clash line as the app now does
L197  10c: the caution line is readable in every theme — text >= 4.5:1, the mark >= 3:1 on every ground it sits on
L255  10c: pill — down the list it steps aside; up, at the top and at the end it is back (pure:pill-scroll)
L298  10c: pill — at rest it never sits on a Book button (pure:pill-rest)
L338  10c: pill — wired with passive listeners, one frame at a time, and never out of a keyboard's or a screen reader's reach
L531  10c: desktop Discover — filters in a pinned left column, the timetable on the right
L587  10c: desktop — the class sheet is a dialog with a width of its own; the picker sits beside it
L606  10c: "Clear filters" is there only while a filter is on

## tests/suites/11-native-snapshot.js
L39  Native snapshot: class type + colour fields (pure, native-bridge.js)
L167  Native snapshot: every class entry carries its type and the member's colours
L202  Native snapshot: a waitlist place is still never a class on the widget
L224  Native snapshot: fallbacks — no colour engine, no classTypeKey
L254  Native snapshot: rewritten when the member changes their class colours
L310  Swift parity: the fallback palette is the app's DEFAULTS, value for value
L327  Swift parity: the class-type words are CATEGORY_MAP's
L340  Swift parity: the neutral surface and the inks are Cloud's and Graphite's
L355  Swift parity: the eight pictograms are the web marks, number for number
L426  Swift contract: 24-hour times through one formatter; no emoji, no SF Symbol look-alikes
L446  Swift contract: old payloads still decode; the new files are in their targets
L488  Swift contract: snapshot times are READ with the fixed formatter too (a 12-hour phone, a non-Gregorian calendar)
L511  Swift contract: the countdown turns to "Now", the Live Activity paints its own ground, a name is never cut mid-word

## tests/suites/12-brand-mark.js
L6  Brand mark: one geometry everywhere it is drawn

## tests/suites/13-safe-area.js
L8  Safe areas: the last stylesheet never drops an inset an older sheet set

## tests/suites/14a-usual-week-sheet.js
L30  14a horizon: what is open, what is listed, what the next release brings (OBSERVED 2026-09-19 — advisory)
L58  14a seats: 1–4, default 1, coerced where it is read, never above the class's own max_bookable_slots
L104  14a ranges: the next 7 days, the next THREE weeks — and the newly opened batch first, when there is one
L143  14a spots: usual → preferred → closest to the usual → first free; never an avoided spot while another is free
L179  14a plan: ONE advisory line when the ticked seats are more than Psycle says are left — never a block
L219  14a run: exactly the spots that were shown — and what a refusal, a gone spot and a changed booking do
L267  14a plan: the range is chosen once the bookings are in; rows say their seats and what may not be open yet
L318  14a spots read: one GET /events/{id}, the member's own habits, nothing written
L355  14a sheet: what starts ticked, what the button counts, what each result says
L423  14a picker: choose-only mode — "Use bike 12", never a booking
L533  14a choose again: a tick survives only on a row that is still what the member ticked — and at THEIR seat count
L575  14a seat step: for a class ALREADY held, the seat held before can wear the ✓ — that is never "booked"
L679  14a plan: a usual class on a day Psycle has not LISTED yet says so — not "No matching class that day"
L692  14a sheet: only what must stay pinned is pinned; the money lines are spoken; nothing is clipped
L740  14a sheet: an open, idle sheet takes the reminder tap — it lists afresh with the newly opened dates first
L751  14a sheet: a tick that takes room from the list never leaves the row just ticked under its fold

## tests/suites/14b-usual-week-card.js
L74  13b: collapsed or not — psycle_usual_week_collapsed is '1' | absent, read defensively, default EXPANDED
L90  13b: "2 seats" on an entry that books more than one — an integer 1–4, anything else is 1
L118  13b: the card is a disclosure — a real button in a real heading, the ONE primary beside it, the body it controls
L141  13b: the primary reads as a review, not a spend — and keeps every hook other code reads
L157  13b: stored text is escaped, stored junk never throws, and × still removes the row it sits on
L237  13b: a repaint with nothing new to say writes nothing — the buttons stay the same nodes
L262  13b: the toggle flips IN PLACE — same button node, its state announces it, the choice is stored
L291  13b: a folded card is folded from its FIRST paint, and survives re-renders and relaunches
L319  13b: storage that throws never breaks the card
L338  13b: css — the fold, the disclosure's target and ring, tokens only, inks that hold on the card
L416  13b: wiring

## tests/suites/14c-weekly-reminder.js
L29  Monday reminder: it fires at 12:00 London — the release itself — wherever the phone is
L62  Monday reminder: the copy says what the tap opens
L196  Monday reminder (wired): eight rolling one-shots, re-armed at launch, idempotent
L221  Monday reminder (wired): the body follows the usual week being saved or cleared
L264  Monday reminder (wired): the tap lands where the body said it would
L281  Monday reminder (wired): offered once, in the app, right after a usual week is saved
L458  Reminder tap: which dates a Monday release opened (observed model, advisory only)
L497  Reminder tap with a usual week: My Bookings, then the REVIEW sheet on the newest dates — nothing is booked by the tap
L709  Reminder: what the app says about it, everywhere

## tests/suites/15-agents-index.js
L20  agents/index: the generated code index is present, stamped, and points at real lines

## tests/suites/16-agents-docs.js
L18  Agents docs: a lean entry, a thin pointer, every topic file routed
L75  Agents docs: the reading guide and agents/README.md list what is on disk, with honest sizes
L198  Agents docs: a folder guide in each working folder, a thin pointer beside it
L226  Agents docs: the handover opens with its session log; it and the backlog are listed
L249  Agents docs: only names that exist — a code name a guide gives is in the code

## tests/suites/17-leftovers.js
L32  Date picker: stepping a month keeps keyboard focus in the calendar
L91  Date picker: picking a day with the keyboard leaves focus on the calendar button
L140  Next-class pill: the last class leaves the page once the pill is hidden
L184  Seat picker: a map wider than the sheet shows that it scrolls
L209  Older sheets: the booked button, its radius and the class sheet's corners read the tokens
L222  Date row: the calendar button grows into a pill when it carries a picked date
L239  "Same class next week": the carrier button is in the page but never on screen

## tests/suites/18-android.js
L128  Android Back: what one press does (pure:android-back), every combination
L190  Android Back: the actor closes the TOP layer only, through that layer's own closer
L321  Android Back: focus is handed back where the closer itself hands it nowhere (keyboard, switch access, TalkBack)
L342  Android Back: never "yes" — a confirm gets its Cancel, the welcome its own Back or Skip
L380  Android Back: a request in flight swallows the press — but a dialog that waits on the member is answered first
L446  Android Back: what the actor assumes about the shipped bookClass — busy under its own confirm; "Book anyway" goes on to a confirm, never to a POST
L517  Android app: no service worker (its files are in the bundle)
L538  Android copy: a widget, reminders and a countdown in its notifications — no Lock Screen widget, no Live Activity, no Siri, no iPhone
L637  Android app: a share card goes out through the native sheet — never a "saved" toast over a download that cannot happen
L668  Android bridge: notification channels once, and every reminder filed under one
L707  Android bridge: a notification tap can be FORGED there (any app can start the exported launcher with the plugin's extras)
L738  Android bridge: the status bar wears the theme's ground
L769  Android bridge: an app built before the widget twins — the missing plugins are skipped without a word
L812  iPhone bridge: not one call more, not one field more
L839  Android calendar: the ownership marker is read from `description`; a row Android still lists after a delete is not a live event
L939  Narrow phones: the welcome's time tile fits from 320 wide (most Android phones are 360)

## tests/suites/19-android-project.js
L163  Android: the XML reader this suite uses can fail
L217  Android: the generated project is committed, and its ids are the app's
L271  Android manifest: no backup of the app's data, no cleartext traffic, every named resource exists
L351  Android BACK: MainActivity asks window._psycleAndroidBack, then moves the task back — it never finishes
L380  Android signing: nothing secret is tracked, nothing secret can be, no password in a Gradle file
L413  ios-app/package.json: `sync` is still iOS-only, the Android scripts exist, the lockfile still agrees
L439  CI: the JVM tests block (the countdown's are proved to have RUN), a debug APK is compiled, the emulator smoke is advisory, never taps, photographs the widget preview, then posts the countdown notification and reads it back; the bootstrap is gone
L664  Android resources: every XML parses, every file has a name aapt accepts
L687  Android resources: every @type/name that a resource, the manifest or MainActivity names is in res/
L743  Android seams: what the bridge names, the project holds
L792  Android widget: one provider, declared the way a launcher needs it
L841  Android widget: layouts a launcher may inflate, one pictogram per class type, the empty state's words
L940  Android widget: every PendingIntent immutable, an inexact alarm only, an explicit tap, nothing newer than API 22 unguarded
L1136  Android widget: the three plugins the bridge finds by name, registered before super.onCreate; the tap's id is untrusted
L1186  Android widget: a pure snapshot class with JVM tests CI runs, no new runtime dependency, the preview in DEBUG builds only
L1255  Android countdown: a pure planner, with JVM tests of its own that CI proves ran
L1289  Android countdown: ONE silent, ongoing notification on its OWN channel — private, with a public version — from a notifier that never asks
L1349  Android countdown: a receiver that is NOT exported, an inexact alarm, no service, no new permission — and what runs the plan
L1424  Android countdown: the debug hook that posts a REAL one on CI's emulator — DEBUG builds only, and only for adb

## tests/suites/1f-contrast-tokens.js
L53  Contrast — sanity
L62  'Text tokens — ' + id
L99  'Text tokens — ' + id
L119  Accent ink
L183  Danger pair
L222  Theme selectors
L245  Tokenised components

## tests/suites/1g-layout-fixes.js
L21  Sub-filter pills: stylesheet owns the look
L84  Date row: calendar first, presets matched by label
L107  Membership theme picker: tracks can shrink, names are contained
L121  Class card: action pill is compact and wraps by width
L139  Class card: the busy label never re-flows the card

## tests/suites/20-android-widget.js
L76  Android widget: the snapshot goes out through the twins — three keys, then ONE reload
L112  Android widget: what a reader finds — the seven facts, colours optional, never a waitlist place or a started class
L173  Android widget: a tap lands as on the iPhone — My Bookings, then that class
L188  Android widget: a HOSTILE tap — the id began as an intent extra any app can set
L219  Android widget: an app built before the twins — skipped without a word, sign-out included
L297  Android widget: a deliberate sign-out empties it; an expired session does not
L335  Android widget: the iPhone app is untouched, and the Android app makes the iPhone's widget calls
L361  Android widget: the names the bridge reaches for are the names the Android project registers
L404  Android widget: the palette its JVM tests judge for contrast is js/theme.js's, value for value

## tests/suites/21-android-countdown.js
L90  Android countdown: every snapshot pass hands over the switch — '1' by default, before the ONE reload, once per pass
L130  Android countdown: '0' when Class reminders are off — the member's word alone, never the permission
L154  Android countdown: flipping Class reminders re-writes the switch at once — it does not wait for a booking change
L249  Android countdown: an app built before the key, or before the twins — served without a word
L304  Android countdown: the iPhone app never writes the key — its calls and its copy are what they were
L372  Android countdown: the bridge's key is the Android project's, and the one platform test sits in its own helper

## tests/suites/22-play-release.js
L73  Play release: the detectors refuse what they should
L88  Play release: the workflow is manual, main-only, read-only, and cannot leak or skip the signing key
L208  Play release: no keystore and no service-account key is tracked
L224  Play release: privacy.html is a page of text — no analytics, nothing external, one owner placeholder, not in the app shell
L371  Play release: the deploy guide names the workflow, the secrets and the privacy page, and quotes real figures of this project
L407  Play release: the icon, the feature graphic, the render script and the --play screenshot canvas

## tests/suites/23-release-polish.js
L28  Release polish: what a member never reads
L64  Release polish: the class reminder body is one list
L75  Release polish: the plan line never goes negative
L81  Release polish: the privacy policy is in the app; Diagnostics is not advertised

## tests/suites/24-tip-jar.js
L29  Tip jar: one allow-list, three ids, the same on every side
L38  Tip jar: what the store answered → the rows drawn
L54  Tip jar: the section — native only, never empty, the price as text, one purchase at a time
L104  Tip jar: no other way of paying, anywhere in the app
L117  Tip jar: the native sides, as far as they can be read

## tests/suites/25-ci-cost.js
L20  CI cost: which native builds a push needs (the rules, run)
L43  CI cost: a name that only LOOKS like a native path builds nothing
L53  CI cost: unsure means BUILD
L68  CI cost: the workflow — when it starts, and what a newer push does
L85  CI cost: the workflow — the native jobs read what changed
L118  CI cost: the script is checked like the rest

## tests/suites/2e-css-layout.js
L54  Tap targets: token + hit areas that move nothing
L94  Tap targets: no area reaches a neighbouring control
L159  Desktop Discover: spacing, aligned chrome, reachable chips
L218  Radius: redesign.css reads the tokens, Handheld zeroes them all
L231  Card entrance: first screen only, nothing held after it ends
L257  Instructor modal: the favourite star sits on its own line under the profile header
L271  Light bases: "Cancel booking" label contrast

## tests/suites/3d-leftovers.js
L35  Reference data: what the cache patch decides (js/performance.js)
L51  Reference data: init asks only once the later modules have wrapped apiFetch (js/app.js)
L148  Cost tracker: the forecast never asks for more than the cards say is left (js/tabs.js)
L212  My Bookings: "has it started?" is asked of the gym clock, not the device (js/app.js)
L276  Tab badge, Stats "Upcoming" and the next-class pill agree with the My Bookings list
L339  Bookings fetch: the stale-place cutoff reads the gym clock through app.js's own resolver
L391  bookClass: what GET /events/{id} says about availability reaches the list
L443  bookClass: a seat-map studio whose cached record lost its layout
L486  Tap targets: the gap between Refresh and Clear filters is never Clear filters'
L515  Travel notice: inside the Discover panel whichever of app.js / tabs.js gets there first

## tests/suites/4f-cleanup-pwa.js
L28  Clean-up: deleted dead code stays deleted
L41  Clean-up: the action log records what the dead wrappers claimed to
L85  Service worker: a stalled network cannot hold a cached page hostage
L217  First paint: the saved (or system) theme before any stylesheet or script file
L286  login.html wears the app's theme (inlined tokens = css/theme.css)
L328  PWA icons: real PNGs, linked and listed
L369  Calendar file export in the iOS app (js/calendar.js)

## tests/suites/6e-design-tokens.js
L78  Toast: error and success are told apart
L120  Late-cancel note: one caution treatment
L156  Seat map: every state tokenised at source
L251  Display face: never on the 12px section eyebrows
L261  Pull-to-refresh: parked below the status bar

## tests/suites/7a-final-polish.js
L35  History top-up: may it start? (pure:history-topup)
L102  History top-up: "too many to fetch quietly" is recorded, and handed to the member
L179  Saved details: does this class need re-reading before its studio is used?
L194  Saved details: one bounded re-read of THAT class
L226  Class sheet: the booked button of a saved-details seat
L266  Change spot: a saved-details seat
L302  Latest tap wins: a class tapped while a saved-details seat re-reads is not overtaken by it
L381  Date picker: Escape closes it and hands focus back to its button
L440  Next-class pill: hidden means gone — no text, no room kept for it
L504  Usual week: the studio gets a line of its own

## tests/suites/8a-filters.js
L56  Filters bar: the active filters, one removable chip each (js/app.js pure:filter-summary)
L153  Filters bar: updateFiltersSummary paints the count, the name and the chips
L207  Filters bar: a removed chip hands its focus on
L226  Filters bar: × goes through the panel's own control
L282  Filters bar: collapsed at launch, a class on the panel, nothing stored
L316  Filters bar: markup — date row exposed, one real button, every panel id kept
L348  Filters bar: CSS — collapsed below 1024px only, chips truncate, the dropdown is not clipped

## tests/suites/8b-day-pager.js
L27  Day pager: the days of a range
L75  Day pager: what a day is called
L103  Day pager: an empty day that may not say "No classes"
L137  Day pager: stepping, edges and the way off an empty day
L151  Day pager: which day is on screen
L166  Day pager: render() → _pagerModelFor keeps / resets the day (shipped lines)
L215  Day pager: the one-time "Swipe to change day" hint
L230  Swipe: sideways intent, release and edges (pure:swipe-nav)
L243  Swipe: window._psycleSwipe over a fake element (shipped lines)
L327  Day pager: a change of day never searches, never saves, never rebuilds under a busy button
L384  Day pager: a strip rebuilt while Discover is hidden gets its scroll offset back
L465  Day pager: a "find it" shortcut is a fresh view, not the day last swiped to
L480  Day pager: CSS — vertical scrolling is the browser's, tokens only

## tests/suites/8c-stats-pages.js
L24  Stats pages: three groups a member would name the same way
L56  Stats pages: which page opens
L62  Stats pages: the tab list keys
L72  Stats pages: a page with nothing on it
L169  Stats switcher: a real tab list
L194  Stats switcher: opening a page
L255  Stats switcher: a new page starts at its top, and the switcher stays under the thumb
L284  Stats switcher: keyboard
L295  Stats pages: the hero states and the quiet line
L343  Stats pages: wiring
L378  Stats switcher CSS: tokens only, pinned, fingertip-tall
L422  Stats switcher: readable in every theme

## tests/suites/8d-welcome.js
L27  Welcome: who sees it (pure)
L45  Welcome: four pages, a title and one sentence each (the first: the board's two-sentence headline)
L79  Welcome: swipe arithmetic (pure)
L106  Welcome: the launch path (shipped code, fake page)
L267  Welcome: the overlay (shipped code, small fake DOM)
L537  Welcome: contracts with the rest of the app
L572  Welcome: CSS

## tests/suites/8e-declutter.js
L39  Declutter: subtitles that restate a heading or explain the machinery are gone
L44  Declutter: no exclamation marks, filler or decorative emoji in headings, hints and empty states
L51  Declutter: a deleted element takes its CSS rule with it
L77  Declutter: streak cards say numbers, not cheers
L122  Declutter: a usual-slot card leads with the slot
L143  Declutter: headings and heroes
L161  Declutter: the lines a member would miss are still there, word for word

## tests/suites/8f-wave8-seams.js
L33  Wave 8 seams: the shared ids are real, and looked for where the other lanes look
L65  Wave 8 seams: the welcome pages and the shared swipe helper turn on the same movement
L84  Wave 8 seams: the welcome and the "Swipe to change day" hint never share a page session
L123  Wave 8 seams: Stats sub-pages are swiped through the shared helper + showStatsPage
L174  Wave 8 seams: the Filters bar chips are re-read on the way back to Discover
L183  Wave 8 seams: an empty Discover list says why ONCE (the declutter rule, in the day pager's region)

## tests/suites/9a-foundation.js
L61  Crisp Colour faces: Sofia Sans + Sofia Sans Condensed, self-hosted, through the two font tokens
L103  Tokens: the StyleGuide scale, the glow recipe, the chrome of Cloud and Graphite
L149  Class colours: the palette and the defaults
L176  Class colours: every swatch × role × base against the ink it carries
L258  Class colours: stored data is untrusted
L283  Class colours: the plan apply() writes
L321  Class colours: window.PsycleClassColours against a fake page
L400  css/crisp.css: linked last, five sections, ONE mapping block, tokens only
L492  Class colours arrive with the deferred theme.js: nothing class-coloured paints before them
L516  Pictograms: classPictogram(key, size) — one family, eight marks
L543  CATEGORY_MAP: the literal class colours are gone
L555  psycle_class_colours: exported, imported like the theme, mirrored on iOS

## tests/suites/9b-discover.js
L42  Day strip: a pill is a small word over the day of the month
L61  Day strip: the class-colour dot marks a day on which you hold a SEAT
L104  Day strip: dots are painted in place, from memory only, once /bookings has really loaded
L144  Class card: one anatomy, coloured by class type — and the booking contracts untouched
L212  Filters: colour means class type — only a class-type chip or pill wears one
L230  Header and tab bar: the wordmark, soft round controls, the boards' four marks
L253  crisp:9b-discover — the card's rules, the glow's three places, tap targets, motion
L326  Class card: the action drops to a row of its own before the tile is left alone on the title row
L359  Skeleton and empty states follow the card
L371  Contrast: the ink / ground pairs of the chrome, in every theme

## tests/suites/9c-sheets.js
L47  Sheets: line marks (_uiIcon) — the pictograms' family, never an emoji
L61  Class sheet: the note beside Book says only what Psycle's numbers can back (_sheetPlanNote)
L90  Seat picker: the confirm button names what it books (_pickerConfirmLabel)
L103  Dialogs: a destructive choice is a calm outline; filled red only for a late-cancel charge or a deletion (_confirmTone)
L143  Seat picker: data-ct on the sheet, round seats, a ring on your usual, a tick on a seat you hold
L221  Class sheet: time first, the class's colour, one action, no emoji
L298  Booked sheet: the tick in the class colour, your seat with the glow, neutral buttons
L322  Find similar / instructor / history: line marks and class colour, no emoji, no inline colour
L358  crisp:9c-sheets — shapes, the glow's four homes, and every ink on its ground in every theme

## tests/suites/9d-bookings.js
L58  9d: the day on the card is read off the digits ("Thu 24"), never the device zone
L69  9d: the primary and ONE companion stay on the card; the rest sit behind More
L90  9d: the More button says what is behind it; the menu steps with the arrow keys
L103  9d: plan usage — "8 of 12 this month · Resets 12 Oct"
L200  9d: the REAL card — tinted by class type, time first, the hooks other code reads still there
L230  9d: the glow is sparing — the NEXT class you hold a seat in, and nothing else
L244  9d: actions — Cancel + one companion on the card, a real disclosure for the rest
L283  9d: the plan-usage bar in the REAL render — one period, and two that fold
L317  9d: the saved copy is the same card with nothing on it that acts
L349  9d: usual week — a compact class component per entry, coloured by the TYPE alone
L394  9d: the next-class pill — class tile + seat in the class colour, API text escaped
L429  9d: More — aria-expanded is the ONLY thing that changes; focus in, focus back, one at a time
L486  9d: More → "Add spot" → the picker or confirm closes: focus goes to the More button, not to <body>
L514  9d: Find similar — a named group that takes the keyboard, and gives it back
L554  9d: Find similar — chosen from a closed menu, the popup still has somewhere to hang
L578  9d: css/crisp.css — scoped, aria-driven, the pill never over the last card
L665  9d: the calendar row carries no emoji
L673  9d: the list ends in ONE footer row — the calendar action(s), then "View full history"

## tests/suites/9e-stats-membership.js
L35  Stats charts: class times are read off their digits (pure:stats-charts)
L85  Class colours control: the model (pure:class-colour-control, on the real palette)
L125  Class colours control: the shipped markup
L183  No class colour is named in markup
L211  css/crisp.css (9e): inks on fills hold in every theme
L266  css/crisp.css (9e): fingertips, focus, motion, no glass
L286  Share images: the new chrome, faces and class colours
L310  login.html: the Crisp chrome, fonts and pill button — CSP and framebust untouched

## tests/suites/9f-one-card.js
L43  9f: the specificity reader agrees with the cascade on the selectors that matter here
L52  9f: no older sheet outranks the shared card root on what it paints (the tint must survive in every theme)
L71  9f: what the card shares is said once; My Bookings adds only its own
L84  9f: ONE time block and ONE title row — Discover, My Bookings, the saved copy and the Class colours preview
L125  9f: the tab hero has ONE look, keyed on its host class — a fourth host cannot be missed
L142  9f: the favourite STAR has ONE look, keyed on its own class — and it is the only mark on an instructor
L161  9f: one prefix, two meanings — ".cc-" is the class CARD (9b) and the class COLOURS control (9e)
L173  9f: the two small handoffs taken at integration

## tests/suites/a11y.js
L17  A11y: how long a toast stays up
L28  A11y: announcements that arrive together
L37  A11y: what a picker seat tells a screen reader
L55  A11y: toast() and announce() (sliced from js/app.js)
L125  A11y: the session-expired banner is announced once
L156  A11y: confirmModal ignores an auto-repeated Enter (sliced from js/app.js)
L235  A11y: overlays — Escape, Tab, focus in and back (sliced from js/app.js)
L542  A11y: the history-sync prompt waits for every other overlay
L602  A11y: Enter / Space on role="button" elements (sliced from js/app.js)
L649  A11y: static attributes the handling relies on
L693  A11y: which open dialog is on top (pure)
L702  A11y: a toast raised while a dialog is open is ALSO written inside it

## tests/suites/add-spot-studio.js
L67  "+ Add spot" on a card painted from saved details: the studio comes from the class just read
L97  "Not now" on "Book another space?" hands "+ Add spot" back as it was
L124  A studio that cannot be resolved never reaches a booking confirm
L163  A studio _studioMap already knows is left alone
L177  _studioFromEventDetail (pure:book-fresh)

## tests/suites/booking-extras.js
L31  Countdown chip: counts from the real (London) start, like the late-cancel warning
L47  Countdown chip: today / tomorrow are the gym's calendar days
L58  Countdown chip: falls back to the device clock when it has to
L104  Class history: a booking no longer truncates a synced history (#32)
L129  Instructor modal: "View schedule" lands on Discover (#31)
L146  Haptics: no tick on programmatic searches, one on a seat pick (#94)

## tests/suites/booking-races.js
L89  Book: two taps in flight — only the LATEST opens a picker
L197  Book: a picker somebody else opened meanwhile is never replaced — and a closed one never blocks
L230  Book: a class-sheet tap that had to wait for its class does not overtake a LATER tap
L274  Book: a failed tap says something a member can act on
L318  Cancel wording: never a retry nobody will make, never the raw error
L354  _friendlyError: follows categorizeError only where it can be trusted
L386  Failure toasts on the booking / waitlist / swap paths carry no raw error text
L433  Cancel: a DELETE that throws — truthful toast, /bookings re-read, nothing invented
L487  Cancel: a DELETE answered 5xx — never the raw "Server Error", and /bookings is re-read
L542  Seat maps: layout labels are escaped, and only NUMBERS reach an inline handler
L570  Bike picker: the dismiss button never reads as "cancel my booking"

## tests/suites/booking.js
L12  Booking: first tap after the usual bike was auto-selected
L21  Booking: merging new seats into what is already held
L46  Booking: which record id may a per-seat cancel DELETE?
L61  Booking: can a whole cancel list every seat's record yet?
L73  Booking: what a /bookings re-read says about an unanswered POST
L169  Booking: "+ Add spot" keeps the seat already held (#52/#71)
L191  Booking: a confirmed seat marks its card at once, not when the refetch lands
L217  Booking: a refused POST never leaves the button reading as booked
L248  Booking: a POST that never answered is verified, never guessed (#25)
L315  Booking: a 409 / "already" is ambiguous until /bookings answers (#25)
L370  Booking: a partly-landed POST gates only the seat still in question
L400  Booking: a refused add-a-seat says why (this API refuses with a 500)
L416  Booking: a session that dies mid-verify is "unconfirmed", never a verified "no seat"
L432  Booking: no-layout count bodies are judged by record count
L515  Cancel: the My Bookings seat × only ever DELETEs that seat's own record
L557  Cancel: "Cancel all" removes one record per seat — or nothing
L656  Change spot: never swaps off a record id it cannot prove is that seat's
L695  Change spot: what is left of a booking once a seat has been released
L705  Change spot: a successful swap is recorded locally (the refetch may never land)
L739  Change spot: a 404 on the old seat's record is checked against /bookings
L761  Change spot: a new-seat POST that never answered is verified before the old seat is re-POSTed
L839  Booking: a Book tap in an unverified session waits for the member's bookings
L873  Booking: no Book tap goes ahead until a /bookings snapshot has been applied
L911  "Book my usual week" never runs before the member's bookings have loaded
L951  Class sheet: Book / booked through a detached button
L1039  Offline queue: a 409 on replay is checked against /bookings, never assumed booked

## tests/suites/bookings-card.js
L16  Bookings card: _cancelDeadline (12h late-cancel rule) — no resolver (device-local fallback)
L80  Bookings card: _cancelDeadline — web build (no bridge), device abroad
L112  Bookings card: _cancelDeadline — iOS bridge present, device abroad (New York)
L160  Bookings card: _eventCacheEntry (rebook-next-week cache seed)
L185  Bookings card: markup the handlers depend on
L210  Bookings card: time left is rounded once (never "9h 60m")
L272  Bookings card: swipe-to-cancel targets the markup renderMyBookings emits
L472  Find similar: a search it starts is never shrunk or mislabelled by a stale date preset
L517  Find similar: a shortcut keeps none of the filters Discover was left on
L550  Find similar: "Same class next week" with no exact match shows its alternatives
L623  Find similar: the tap that dismisses the popup does nothing else
L667  Day groups: keyed by the date, whichever form start_at takes

## tests/suites/bookings-load.js
L7  Bookings: load state (fetchMyBookings sliced from js/app.js)
L130  Bookings: a loaded list whose classes could not be loaded is not "Nothing booked"
L223  Bookings: load state is what My Bookings and checkAuth rely on

## tests/suites/calendar-safety.js
L27  Calendar safety: pure ownership decisions (native-bridge.js, no Capacitor)
L188  Calendar permission: the calendar alone is asked for — never Reminders
L211  Calendar safety: psycleCountForeignEvents is read-only and honest
L230  Calendar safety: a pick is not consent — only ownedAck:true hands a calendar over
L302  Calendar safety: the old-target sweep honours the OLD calendar's ack
L341  Calendar safety: no reconcile, no refresh and no ack with an expired session
L369  Calendar safety: Settings asks before handing a calendar over (js/settings.js)
L500  Calendar safety: "Re-sync now" asks too while the calendar is un-owned
L539  Calendar safety: the buttons never call a skipped reconcile a sync
L575  Calendar safety: "Remove duplicates" on a calendar that was never handed over

## tests/suites/clash.js
L28  Clash: overlapping a seat already held
L39  Clash: touching end-to-start is not a clash
L44  Clash: a missing duration counts as 45 minutes
L53  Clash: what is ignored
L65  Clash: space- and T-form start_at read the same
L78  Clash: a tight change between two locations
L94  Clash: which one is reported
L161  Clash: said inside the confirms bookClass already shows (no extra gate)
L185  Clash: the just-fetched event time wins over the cached one
L200  Clash: joining a waitlist (Psycle can turn the place into a chargeable seat)
L220  Clash: the lookup is advisory

## tests/suites/copy.js
L18  Copy: _plural
L36  Copy: counts are no longer glued to a fixed plural
L43  Copy: one vocabulary
L63  Copy: the late-cancel message wears one class everywhere
L72  Share images: palette (pure:share)
L178  Share images: stats card
L262  Share images: year wrap

## tests/suites/data-owner.js
L43  Data owner: the plan
L50  Data owner: legacy install is adopted, never wiped
L62  Data owner: same member is a no-op
L72  Data owner: A → B stashes A's hand-entered keys and clears what Psycle can rebuild
L106  Data owner: A → B takes the history top-up's notes with the history
L122  Data owner: B → A restores A's, stashes B's
L144  Data owner: an unstamped install whose HISTORY is stamped someone else's is a switch, not an adopt
L195  Data owner: at most two members are kept, the oldest goes
L211  Data owner: a corrupt stash is tolerated
L233  Data owner: a full localStorage is tolerated — and costs nobody their data
L264  Data owner: a switch cut short is resumed, not repeated over half-moved keys
L288  Data owner: _applyProfile moves the data before anything repaints (sliced from js/app.js)
L365  Stats: signed out, the previous member's numbers are behind the Sign-in hero

## tests/suites/diag-wiring.js
L39  Diagnostics wiring: every PsycleDiag.<name> a caller uses is exported by js/diagnostic.js
L48  Diagnostics wiring: a recorded sample stores field names only
L56  Diagnostics wiring: app.js _recordShape (sliced from js/app.js)
L90  Diagnostics wiring: complete, live-shaped responses do not trip safe mode
L115  Diagnostics wiring: bookings, events and the profile are sampled where they are read

## tests/suites/discover-journeys.js
L23  Release: the latest / next Monday 12:00 Europe/London (js/app.js pure:window)
L60  Release: a timetable fetched before it is never fresh
L78  Filters: 'Next week' = the Monday–Sunday after today, from any weekday
L104  Discover: "Next week" before Monday noon says when it opens
L143  Release: one timer looks at noon; the Monday-reminder tap shows the dates that just opened
L285  Focused searches: every "find it" shortcut starts from a clean slate
L386  Focused searches: the next tap does not save the shortcut's filters over the member's own
L466  Focused searches: the callers in features.js / tabs.js / explore.js
L508  Stats: "You might like" / "New to you" (moved from under the Discover timetable)

## tests/suites/discover.js
L20  Discover: spots left comes from capacity − occupancy
L36  Discover: a count is only printed while it is recent (30 minutes)
L50  Discover: time-of-day bands are cut from the start_at string
L86  Discover: new cards merge into a day in one forward walk
L143  Discover: render() batches an empty grid through the GLOBAL eventCard
L165  Discover: the last-results write is debounced
L193  Discover: the Time row filters last, and never hides your own class
L220  Discover: "Available only" brings back your own full class once the bookings land
L264  Discover: Time row pills, persistence and the Tonight preset
L401  Discover: the card prints "Only N left" — and only from recent data
L493  Discover: a card re-synced in place never reads "Only N left" beside "Join waitlist"
L553  Class detail sheet: the third row is availability, from recent data
L564  Discover: Time row markup, sticky day headers, pressed state

## tests/suites/event-details.js
L14  Saved class details: pure helpers (js/app.js pure:event-details block)
L97  Saved class details: fetchMyBookings paints from them, blocks only on unknown classes, re-reads after the render
L275  Saved class details: where the key may and may not travel

## tests/suites/facets-core.js
L18  PsycleFacets.run (js/facets.js): matching
L55  PsycleFacets.run: counts narrow by the OTHER filters, never by their own
L74  PsycleFacets.run: search text, date window, fixed option lists, day groups
L99  getCategory / slotLabel (js/app.js pure:core): real Psycle class names
L125  Category → facet counts, in the shape the app feeds PsycleFacets
L141  pluralizeSlotLabel / formatSlots / _parseSlots (js/app.js pure:core)
L157  Local-day helpers (js/app.js pure:core) — runner pinned to New York
L170  _searchSignature (js/app.js pure:core): recent searches dedupe whatever order they were built in

## tests/suites/filters.js
L11  Filters: date arithmetic (js/app.js pure:filters)
L23  Filters: date presets → {startDate, daysAhead, label}
L49  Filters: fetch-window end date
L70  Filters: date row state
L84  Filters: saved date filter → restored state
L116  Filters: /events paging
L176  Filters: sub-filters only apply while their parent category is selected
L209  Filters: launch ends in the saved state whichever of init / restoreFilters runs first
L282  Filters: favourites are pre-selected on a first run only
L304  Filters: the launch search only yields to a search that could load

## tests/suites/history-sync.js
L93  History top-up: when it may start
L139  History top-up: never into another member's history
L162  History top-up: quiet, and only what is new
L194  History sync: a write that lands while it runs is kept
L205  History sync: the buttons behave as before

## tests/suites/history.js
L36  History reconcile: a seat booked elsewhere reaches history
L59  History reconcile: cancelled elsewhere
L88  History reconcile: what an untrusted snapshot would do (why the caller guards)
L96  History reconcile: its own cancellations are reversible
L110  History reconcile: shape, cap and hostile input
L179  History wiring: a trusted snapshot is reconciled once
L204  History wiring: snapshots that must NOT be trusted
L237  History wiring: the history belongs to ONE member of this install
L275  History wiring: Stats is repainted only when it is showing and something changed

## tests/suites/import-validate.js
L16  Stored data: ids
L25  Stored data: class history
L51  Stored data: bike prefs
L56  Stored ids never break out of markup (the real instrLink, the chips, the history modal)
L163  Import: a real export onto an empty device
L173  Import: unknown keys, prototype-pollution keys and the token are ignored
L204  Import: wrong shapes and oversized values are skipped and reported — the rest still imports
L249  Import: a markup payload in an otherwise valid file is defused on the way in
L261  Import: onto a device that already has data it only ADDS — nothing is replaced
L298  Import: the flow around the plan (js/settings.js)

## tests/suites/ios-bridge.js
L250  iOS bridge: status bar style follows the theme base
L275  iOS bridge: launch restore (Preferences → localStorage)
L318  iOS bridge: a tapped notification lands somewhere
L447  iOS bridge: class-reminder permission ask (first booking only)
L598  iOS bridge: a cancelled class takes its delivered banner with it
L620  Class-reminder switch (js/tabs.js)
L707  Settings export in the iOS app (js/settings.js)

## tests/suites/ios-polish.js
L113  iOS polish: reminder/widget decisions (pure, native-bridge.js)
L140  iOS polish: every held class gets a reminder, not just the next 5 (R2-23)
L179  iOS polish: a held class whose details failed to load is unknown, not gone (R2-16)
L296  iOS polish: the Monday-reminder tap asks Discover for a fresh timetable (R2-17)
L348  iOS polish: copy names the right app and the right moment (R2-21)
L374  iOS polish: the pull-to-refresh pill hides above the safe area (R2-1)
L407  iOS polish: the Live Activity shows the seats held NOW (R2-31, Swift source contract)

## tests/suites/notify.js
L23  Notify plan: what is dropped without asking Psycle
L43  Notify plan: at most five, and a long list is not starved
L57  Notify: only Psycle SAYING there is a spot counts
L140  Notify check: when it may talk to Psycle at all
L169  Notify check: GET only, five at most, every two minutes at most
L204  Notify check: pruning
L229  Notify check: a spot opened
L335  Notify dialog: never over another dialog, never lost
L398  Notify: the bell says what it really does
L402  Notify bell: the watch is saved at the tap, whatever the browser's permission prompt does

## tests/suites/offline-queue.js
L29  Offline queue: what happens to a queued CANCEL
L49  Offline queue: what happens to a queued BOOKING
L74  Offline queue: when is a replay the member's live intent?
L82  Offline queue: naming a class nothing else can name
L180  Offline queue: a cancel made offline in this session goes out on reconnect
L202  Offline queue: a cancel stranded by a closed app is sent on the next launch
L225  Offline queue: a cancel is only given up on when Psycle refuses the cancel itself
L245  Offline queue: a booking stranded by a closed app is never sent blind
L322  Offline queue: the "Offline booking" dialog names the seat as the rest of the app does, after a relaunch too
L357  Offline queue: a booking made offline in this session
L520  Offline queue: owners, sign-out, unverified sessions
L559  Offline queue: My Bookings status line

## tests/suites/offline.js
L16  Saved copy of My Bookings: pure helpers (js/app.js pure:offline block)
L103  Saved copy of My Bookings: painted only while Psycle has not answered; never fed back into state
L226  Saved copy of My Bookings: written from confirmed lists, deleted with the session
L293  fetchWithRetry: a browser that knows it is offline does not retry
L319  Static lists: the 24h TTL is honoured (js/performance.js)
L404  Token expiry: the warn timer cannot overflow setTimeout (js/security.js)
L440  login.html: a real form, a timeout, and a lockout that only counts real refusals
L535  Service worker: a new version reloads only when nobody is looking, otherwise offers it

## tests/suites/owner-tools.js
L62  Build id: web reads the content-hashed service-worker cache name
L81  Build id: falls back to the shipped sw.js
L93  Build id: the iOS app never trusts Cache Storage
L98  Build id: failures are not remembered, and never hang a report
L115  Build id: a worker that took over mid-session is called out
L134  Bug report: names the build, waits for it, and the cancel toast points nowhere
L146  Copy diagnostics: embeds the prefetched native report, never a promise
L190  Copy diagnostics: the status line tells the truth
L243  iOS diagnostic report: names the build and the device
L274  Favourites filter: the one quick filter, with its own spoken name

## tests/suites/predict.js
L21  Book again: the habit itself (js/app.js pure:predict)
L32  Book again: a booking is not attendance
L43  Book again: never for the class already booked
L66  Book again: predictNextClass() reads the seats actually held

## tests/suites/reliability.js
L114  apiFetch (js/reliability.js): a POST is never re-sent; idempotent verbs retry
L179  POST call sites: only the slot replay opts into retries
L235  Offline queue: a COUNT body is sent once, then /bookings decides — never a blind re-send
L268  Offline queue: a SLOT body may retry (same seat → 409), and a 409 is an answer
L289  Offline queue: replayed once per reconnect, and never for a class that has begun
L325  Offline queue: cancels, and what submitBooking queues

## tests/suites/render-perf.js
L37  Minute tick: the countdown chip is re-read from data-start, built as the card builds it
L63  Minute tick: a list is only rebuilt when a card changed by itself
L119  My Bookings: the same list is not written twice
L161  My Bookings: a list that stays up never strands a button
L210  My Bookings: a folded billing period stays folded across a rebuild
L227  My Bookings: the real render, called twice, writes once
L276  Stats: the history is parsed once and shared, and a writer invalidates it by writing
L317  Stats: "Your routine" names the weekday without an Intl formatter per entry
L341  Class History: a first screenful, then the rest in slices
L421  Explore: identical sections are not rebuilt; a real change keeps the carousel where it was
L481  Instructor photos: a circle while loading, and when the photo cannot load

## tests/suites/retry-signout.js
L17  Retry: may this request be sent again? (pure:retry-auth)
L92  Retry: a sign-out during the backoff stops the old token going out again
L129  Retry: what is NOT stopped
L170  Offline queue: nobody signed in is told about "pending" actions
L202  Offline queue: the status line is for a signed-in member only (source)

## tests/suites/session.js
L12  Session: pure helpers (js/app.js pure:session block)
L47  Session: checkAuth / refreshProfile (sliced from js/app.js)
L275  Session: sign-out and expiry (sliced from js/app.js)
L463  Session: cross-module contracts
L479  Session: what counts as a profile body

## tests/suites/shell.js
L31  psycle-finder.html: every module, once, in a fixed order
L52  tests/smoke.html: the same scripts, in the same order
L59  tests/smoke.html: cannot reach the live API
L90  tests/smoke.html: a module that throws while loading fails the page
L112  CI: the smoke step can fail, and typecheck no longer hides a missing tsc

## tests/suites/signin-storage.js
L61  Storage full: the quota-aware setter frees the app's own caches, in order
L95  Storage full: the token save never rejects, never reads "signed out", never leaves an older token behind
L153  Token expiry by the device clock asks Psycle — it no longer ends the session by itself
L199  login.html: a full localStorage is not a "Network error"
L254  login.html: the page that sees the password is locked down
L280  Favourites: read again after the iOS Preferences restore; one change is applied to what is STORED
L351  Timetable cache: never the tenant that fills the bucket

## tests/suites/waitlist-polish.js
L42  Waitlist: the offer-check window no longer stops at midnight
L92  Waitlist: phase (auto → offers → closed) — the copy decision table
L110  Waitlist: a remembered place that ended without a seat is reported by the diff
L164  Waitlist: remembered places survive an unreadable /waitlists even when Discover never cached the class
L188  Waitlist: a slow /waitlists is "late", not "failed" — and the stand-ins give way when it lands
L223  Waitlist: "ended without a booking" is said once, only when the waitlist has just closed on the place
L330  My Bookings card: the Waitlisted status line follows Psycle's clock
L385  Clash: a held waitlist place is a possible seat — opt-in, amber, never a hard clash
L428  Late-cancel wording: one "starts in…" phrase for the cancel dialog, the claim confirm and the announcement
L438  Claim spot: the confirm names the fallback seat it would double up with, and the real cancel terms
L497  "You're in" announcement: the overlap with a held seat and the real cancel terms
L556  Class sheet: a place with a spot to claim leads with Claim, not a ticked button that opens "Leave the waitlist?"
L652  Find similar: "Same class next week" does not call a waitlist place "booked"
L685  Offer re-check: only looks, only when someone can see it, and never repaints for nothing

## tests/suites/wave7-leftovers.js
L39  Stats: "This month" / "All time" count classes TAKEN, not bookings still to come
L116  Stats: "Instructor variety" has a column per MONTH — not per unreadable date
L135  Discover: a restored date pill is brought into view inside its row
L207  Handheld seat tiles are square; Stats sections share one inset on a phone
L227  Waitlist / claim / leave copy: a class more than 6 days off says its month
L242  The Monday-reminder tap shows the dates that opened without saving them

## tests/suites/weekly-template.js
L26  Usual week: times are read as London wall clock, from the digits
L34  Usual week: "now" is London's, whatever zone the device is in
L43  Usual week: which dates a tap means (London's today; no "Monday noon" rule any more)
L53  Usual week: the date an entry falls on
L75  Usual week matching: weekday + time, within a tolerance
L90  Usual week matching: a clock-change week is a non-event
L103  Usual week matching: the instructor changed
L115  Usual week matching: class full
L119  Usual week matching: already booked
L127  Usual week matching: clashes and studios without a spot map
L150  Usual week: saving it from the seats held over the next 7 days
L212  Usual week run: never a one-tap spend
L221  Usual week run: sequential, and the waitlist only where it was ticked
L236  Usual week run: what stops it, and what does not
L310  Usual week seat: a class that filled up is NOT turned into a waitlist place
L331  Usual week seat: never a spot nobody saw
L353  Usual week seat: a studio with no spot map is booked by COUNT — and only when that is positively known
L369  Usual week seat: the studio record has no seat map (a list response replaced it)
L388  Usual week seat: submitBooking's label contract tells "taken" from "refused" from "may still land"
L408  Usual week: joinWaitlist flags "couldn't tell" on the opts it was given
L438  Usual week plan: GETs only, every entry listed with its state
L515  Usual week run: the "Booked!" sheet it dismisses is really gone
L626  Usual week wiring

## tests/suites/widget-link.js
L18  Widget link: parsing psync://bookings?event=<id>
L45  Widget link: handler routes through the notification-tap router
L90  Widget link: cold launch — the retained tap is replayed into the real bridge
L195  Widget link: a tap after iOS killed the WebView process (a dead page's listener is still registered)
L273  Widget link: the Swift and JS sides name the same things

## tests/suites/window.js
L15  Window: keys and coverage (js/app.js pure:window)
L37  Window: day bound used by render() and the facet counts
L53  Window: freshness (15 minutes)
L64  Window: what the persisted cache can give a launch
L75  Window: the range a revalidate fetches
L86  Window: roll-forward on a new day
L107  Window: has a class started since the list was built?
L123  Window: a revalidate never commits a truncated window
L383  Window: search() serves in-window ranges without a fetch
L476  Window: the date row rolls forward once per new day
L569  Discover: why the list is empty, and the ways out
L609  PsycleAPI.categorizeError: errors search() actually throws (js/api-client.js)
L627  Discover: pull-to-refresh and the empty state have something to do
L670  Discover: a background re-render is quiet
L696  Discover: coming back online refreshes it
L702  Discover: the session-expired search error offers Sign in

## tests/suites/year-review.js
L30  Year in review: only classes that have started are counted
L74  Year in review: the summary the button, the modal and the share image all read
