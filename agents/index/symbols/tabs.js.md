# js/tabs.js — symbols
Purpose: the shape of `js/tabs.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `2301a76`). Do not edit; rebuild with `npm run agents:index`.

4590 lines · 183 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


### Tab Navigation (L20)
L22  var TABS
L23  var _currentTab
L25  initTabs()  +253
L279  window.switchTab(tab, noHash)  +73
L366  _stillToCome(startAt, now)  — Is this held class still to come?
L372  updateTabBadge()
L434  var _origRender  — Also update badge when renderMyBookings is called directly
L435  _patchRender()

### Stats sub-pages (L447)
L461  var STATS_PAGES  {stats-pages}  — THE grouping.
L469  var STATS_PAGE_DEFAULT  {stats-pages}
L470  var STATS_PAGE_KEY  {stats-pages}
L472  _statsPageById(id)  {stats-pages}
L478  _statsPageId(raw)  {stats-pages}  — Whatever was stored or passed in → a real page id.
L481  _statsPageOfSection(sectionId)  {stats-pages}
L489  _statsPageForKey(currentId, key)  {stats-pages}  — A key pressed in the tab list → the page to move to, or null when the key is not the tab list's.
L500  _statsPageStep(currentId, dir)  {stats-pages}  — The neighbour a horizontal swipe leads to (dir: +1 = next, -1 = previous).
L506  _statsPageIsEmpty(id, shown)  {stats-pages}  — Is there nothing on this page?
L515  _statsEmptyState(id, shown, o)  {stats-pages}  — What a page with nothing on it says: ONE line — never a blank page, never a paragraph.
L526  _statsScrollTarget(current, pageTop, barHeight, pinTop)  {stats-pages}  — Where the scroller goes when a page opens.
L534  var _statsPageNow  — The page on screen.
L539  var _statsPainted  — Pages built since the last renderInsights(): a page is built when it is first opened, and not again until the data is…
L541  var _statsHeroOnly  — Signed out / can't-reach-Psycle with nothing on record: the hero has the tab.
L546  var _STATS_SECTION_PAINT  — Which function fills which section.
L560  _statsPagesHtml(sectionHtml, current)  — The switcher and the three page wrappers, from the table above.
L580  _wireStatsPages(panel)  — Once, from initTabs.
L611  _paintStatsPage(id)  — Build the sections of one page, unless they were built since the last renderInsights().
L623  _applyStatsPage()  — Selected tab, roving tabindex, and which page is displayed.
L639  _syncStatsEmpty()  — The one quiet line of a page with nothing on it (pure:stats-pages decides).
L667  _syncStatsPin()  — Wider than a phone the document scrolls and the tab bar is what sticks to the top: the switcher pins beneath it,…
L678  _scrollToStatsPageTop(page)  — The DOM half of _statsScrollTarget.
L701  _syncStatsPages(heroOnly)  — renderInsights' half of the pages: every call is a fresh look at the data, so nothing built before it is trusted — and…
L715  window.showStatsPage(id)  — Open one of the Stats sub-pages ('overview' | 'habits' | 'instructors').
L732  window._statsPageStep(dir)  — For a swipe helper: the page a swipe in `dir` (+1 / -1) would open, or null.
L745  _statsSwipeMayStart(target)  — Swipe left / right between the pages.
L758  _wireStatsSwipe(panel)  — Once, from initTabs — bound to the panel, which outlives every repaint of its sections.

### Render insights tab content (L772)
L780  var _historyRaw, _historyParsed  — One Stats paint asks getFullHistory() ~11 times (every section below) and the string runs to 400 KB.
L786  getFullHistory()  — Get the full class history from localStorage (synced + locally tracked).
L803  var _tokenReadable  — The stored token is only readable once security.js has decrypted it (on iOS: after the Preferences restore).
L809  window.renderInsights()  +41

### Quick Stats (L851)
L853  renderQuickStats()  +128

### Weekly Calendar View (L982)
L984  var _weekOffset
L986  window.weekNav(dir)
L993  window.planDay(dateStr)  — Interactive weekly planner: tap an empty day to search for classes on that date
L1006  renderWeekView()  +112

### Your usual week (weekly template — UI only) (L1119)
L1126  var UW_DAYS
L1130  _uwDateLabel(dateStr)  — 'YYYY-MM-DD' → "Mon 21 Sep".
L1137  _uwPlural(n, one, many)
L1145  _uwTypeOf(label)  {usual-week-crisp}  — A template entry's label is "Type · Instructor" (app.js _templateFromSeats).
L1151  _uwTime(totalMin)  {usual-week-crisp}  — Minutes after midnight → "18:30", through app.js's one formatter (_clock24, pure:clock).
L1159  _uwClassMark(typeName, keyOf, pictogram)  {usual-week-crisp}  — The tile + data-ct of a class type.
L1170  _uwTimeHtml(totalMin)
L1174  _uwMark(typeName)
L1180  _usualWeekTemplate()
L1191  _collectDisplayedWeekTemplate()  — Template entries from the REAL seats held over the next 7 days (it used to read the week shown in the Discover…
L1200  _usualWeekFromHistory()  — Nothing booked this week (a holiday, or the first Monday with the app): fall back to the slots history shows 2+ times.
L1221  _uwLog(action)
L1225  async window.saveWeekAsTemplate()  +42
L1269  async window.clearUsualWeek()  — The card's own handlers (window.clearWeeklyTemplate is app.js's storage call).
L1287  window.removeUsualWeekEntry(idx)
L1301  _placeUsualWeekCard(card)  — Right after #rebookHint — a node app.js removes and re-creates in front of #upcomingPanel whenever it likes.
L1311  var _usualWeekWatch  — The hint is re-made outside any event this module hears (first paint), so watch the tab's own children — not its…
L1312  _watchUsualWeekPlacement(card)
L1325  var UW_COLLAPSED_KEY  {usual-week-card}
L1331  _uwCollapsedFrom(stored, mem)  {usual-week-card}  — Stored '1' = collapsed; anything else there (absent, junk, an import's "true") = expanded.
L1339  _uwCardSeats(value)  {usual-week-card}  — How many seats an entry books: an integer 1–4 (Psycle's own limit per booking); anything else stored there reads as 1.…
L1344  _uwToggleWord(collapsed)  {usual-week-card}  — The word that shows the head is a control (no chevron: the Filters bar's rule).
L1350  _uwCardHtml(template, collapsed)  +49  {usual-week-card}  — The whole card for a saved week.
L1405  var _uwCollapsedMem  — Collapsed or not: localStorage psycle_usual_week_collapsed ('1' | absent), read at every paint — never cached: in the…
L1406  _uwIsCollapsed()
L1411  _uwRememberCollapsed(collapsed)
L1424  window.toggleUsualWeek()  — The head's button.
L1451  _uwFocusedButton(card, doc)  — Which of the card's buttons holds focus (-1: none), and putting it back on the button in that place once the card has…
L1454  _uwRefocusButton(card, idx)
L1462  renderUsualWeekCard()  +38
L1500  window.renderUsualWeekCard = renderUsualWeekCard
L1517  var UW_NOT_OPEN  {usual-week-sheet}  — What the sheet says about a planned row (state → copy).
L1518  var UW_NOT_LISTED  {usual-week-sheet}
L1521  var UW_WAITLIST_NOTE  {usual-week-sheet}  — What joining a waitlist from here means (it leads with a space: it follows the sheet's money sentence).
L1522  _uwPlanNote(row)  +30  {usual-week-sheet}
L1554  _uwTopUpNeed(row)  {usual-week-sheet}  — "1 of 2 seats held": the seat(s) still missing — all a top-up may ask for.
L1561  _uwTickOf(row, need)  {usual-week-sheet}  — "Choose again" lists the same dates afresh.
L1574  _uwKeepTick(kept, row)  {usual-week-sheet}  — …and a kept tick stands only on a row that is still THAT: by template index alone, a seat that went while the class…
L1591  _uwResultNote(result, info)  {usual-week-sheet}  — …and about what happened to a ticked row once the run reached it.
L1614  _uwSeatOptions(saved, max)  {usual-week-sheet}  — The seat counts a row offers: 1 · 2, and 3 · 4 only when the saved entry asked for them — never above the class's own…
L1625  _uwConfirmLabel(t)  {usual-week-sheet}  — The confirm button counts what it will SPEND: "Book 3 classes · 4 seats".
L1637  _uwRangeLabel(range, fmt)  {usual-week-sheet}  — A date range of the sheet's control, as it is labelled.
L1650  _uwLabelHalves(label)  {usual-week-sheet}  — "Newly opened · Fri 25 Sept – Thu 1 Oct" as two halves that each stay whole.
L1664  _uwScrollTopToReveal(scrollTop, viewH, row, used)  {usual-week-sheet}  — The list's scrollTop once the sheet has re-flowed around the row the member is on.
L1686  _uwRowHtml(row, note, checkbox, spots)  — One row of the sheet.
L1717  _uwNoteInner(note)  {usual-week-note}  — What a row's note holds.
L1726  _uwFocusBack(prev, doc)  — Where focus goes when the sheet closes: what held it on open, if that is a real element still in the page — else the…
L1746  _runUsualWeekSheet(opts)  +620  — The sheet between "Book my usual week" and any booking.
L2371  var _templateWeekRunning  — Opens the REVIEW sheet — it books nothing by itself.
L2372  async window.bookTemplateWeek(opts)  +42
L2417  _statsTile(ct)  — The small pictogram tile that leads a class name on a Stats card (app.js classPictogram; css/crisp.css .ct-tile).

### Recommendations (L2422)
L2424  renderRecommendations()  +58

### Habit-slot alerts (Feature 1) (L2483)
L2488  var DAY_NAMES_FULL
L2492  _nextWeekdayDateStr(dow)  — Local-date "YYYY-MM-DD" for the next occurrence of a weekday (0=Sun..6=Sat).
L2502  renderHabitSlots()  +66

### Streaks & milestones (Feature 3) (L2598)
L2607  _weekIndex(date)  — Monday-anchored week index: whole weeks since a fixed epoch Monday.
L2614  renderStreaks()  +82
L2707  var HEAT_BANDS  {stats-charts}  — Five bands of the day, as the board draws them.
L2714  var HEAT_DAYS  {stats-charts}
L2717  _wallParts(s)  {stats-charts}  — 'YYYY-MM-DD HH:MM…' (space or T) → { day: 0–6 Monday first, hour } | null.
L2726  _heatBandIndex(hour)  {stats-charts}
L2733  _heatLevel(count, max)  {stats-charts}  — 0 = never; 1–4 = quarters of the busiest cell (the old grid's own steps).
L2741  _heatmapModel(starts)  {stats-charts}  — Class start times → { total, max, counts[band][day], levels[band][day], top: the (up to three) busiest cells, busiest…
L2764  _heatmapLabel(model)  {stats-charts}  — What the grid says to someone who cannot see it.
L2773  _streakWeeks(weeks, thisWeek, n)  {stats-charts}  — The streak's bars: the last `n` weeks, oldest first.
L2784  _streakWeeksLabel(bars)  {stats-charts}
L2794  _classTypeBreakdown(counts, cats)  {stats-charts}  — counts by CATEGORY_MAP key + the category list → what the class-type bar draws: rows with a class, biggest first (ct =…

### Activity Heatmap (L2808)
L2812  renderHeatmap()  +55

### Support Psync: optional tips (L2872)
L2886  var TIP_PRODUCTS  {tips}  — The allow-list, cheapest first: the same three ids as TipJarPlugin.swift and PsyncTips.java.
L2895  _tipRows(answer)  {tips}  — What the store answered → the rows to draw, in OUR order.
L2909  _tipOutcome(status)  {tips}  — The plugin's status → what to say.
L2916  _isTipId(id)  {tips}
L2921  _tipJar()
L2928  var _tipAnswer
L2929  var _tipAsking
L2930  var _tipBusy
L2932  renderSupportPsync()
L2957  async _leaveTip(id)
L2979  renderMembershipInfo()  +167

### Cost Per Class Tracker (L3147)
L3149  renderCostTracker()  +94
L3245  var FORECAST_TARGET_GBP  {cost-forecast}
L3246  var FORECAST_MAX_PER_DAY  {cost-forecast}
L3250  _bookableMore(made, max, daysLeft)  {cost-forecast}  — The most classes a "Book N more" line may ask for: what the plan still holds (max 0 = unlimited) and what fits in the…
L3260  _forecastVerdict(made, max, priceGbp, costAtMax, projected, projectedCost, daysLeft)  {cost-forecast}  — The forecast's one-line verdict.
L3283  _forecastHtml(sub, made, max, priceGbp, costAtMax)  +47  — Returns the forecast block HTML (or '' if there isn't enough to forecast).

### Theme picker (Membership → App) (L3331)
L3333  renderThemePicker()
L3358  window._pickTheme(id)

### Class colours (Membership → Appearance) (L3363)
L3372  var CC_INTENSITY_LABELS  {class-colour-control}
L3378  var CC_SWATCH_INK  {class-colour-control}  — What the preview card says for each class type (decoration — never timetable data).
L3379  var CC_PREVIEW  {class-colour-control}
L3384  _ccHas(obj, k)  {class-colour-control}
L3397  _ccControlModel(api, cats, theme, open)  +40  {class-colour-control}  — Everything the control draws, from what the engine says.
L3441  _ccRadioTarget(count, index, key)  {class-colour-control}  — A key pressed on radio `index` of `count` → the radio to move to (and check: in a radio group selection follows…
L3451  var _ccOpenKey
L3453  _ccThemeNow()
L3460  var CC_TICK
L3462  _ccControlHtml(m)  +52
L3518  renderClassColours()  — Rebuilt whole on every change (it is small), with the focus put back where it was: a radio group rebuilt under the…
L3540  window.renderClassColours = renderClassColours
L3542  _ccSet(partial, said)
L3553  _wireClassColours(box)  +42  — Once per container (it outlives every repaint of its contents).
L3606  window.renderThemePicker = renderThemePicker
L3607  window.renderReminderRow = renderReminderRow

### Weekly reminder row (the native apps only — needs the native bridge) (L3609)
L3614  var _classReminderGranted  — Last known answer to "may Psync send notifications?" for the class switch.
L3626  _classReminderSwitch(prefOn, granted, android)  {reminder-row}  — What the class-reminder switch shows.
L3642  _reminderAndroid()  — These rows exist in both native apps.
L3647  _osSettingsWords(text)
L3651  renderReminderRow()
L3676  _paintReminderRow(row)
L3699  async window._toggleClassReminders()
L3726  async window._toggleReminder()
L3738  _formatGbp(amount)
L3742  _daysLeftInBillingPeriod(sub)

### Class Type Distribution (L3759)
L3761  renderClassTypeDistribution()  +50

### Lapsed Favourites (L3812)
L3814  renderLapsedFavourites()  +54

### Instructor Variety Trend (L3869)
L3871  renderVarietyTrend()  +52

### Year in Review (Feature 4) (L3925)
L3937  _yearReviewRows(history, year, nowMs, startMs)  {year-review}  — The rows a year's wrap may count: this year's, not cancelled — and not still to come.
L3946  _takenRows(history, nowMs, startMs)  {year-review}  — The same test over every year: what the Stats tiles ("This month", "All time") and the "Share my stats" image may…
L3953  _historyStartMs()  — app.js's London resolver (h.date is gym wall clock); the device-local parse only when app.js is absent — the same pair…
L3960  _hasTakenHistory()  — Is there a class TAKEN to show?
L3965  _computeYearReview(year)  +62  — Aggregate this year's attended history into a tidy summary object.
L4028  renderYearReview()
L4049  window.openYearReview()  +47

### Share images (year wrap + stats card): shared helpers (L4097)
L4108  async _shareAsTextOnAndroid(title, text)  — The ANDROID app's web view has no Web Share API and no download manager: navigator.share is missing, and the <a…
L4124  var SHARE_FALLBACK  {share}  — Cloud, whole.
L4127  var SHARE_TOKENS  {share}  — Small grey labels read `muted` (--text-muted), never --text-faint: faint is under 3:1 in Handheld, and these labels…
L4130  var SHARE_FOOTER  {share}  — One credit line for both images (they were signed "Psycle Companion" and "Psycle Class Finder" — neither is the app's…
L4134  var SHARE_SCALE  {share}  — Export scale: a fixed integer, not devicePixelRatio — the PNG is looked at on OTHER devices, and a fractional ratio…
L4137  _sharePaletteFrom(read)  {share}  — read(tokenName) → the custom property's text, e.g. ' #efeee9'.
L4148  _sharePalette()
L4158  _shareCanvas(W, H)  — A W x H surface exported at SHARE_SCALE: every coordinate below stays in 640-wide layout units, the pixels are 3x (a…
L4173  var SHARE_WORDMARK_FONT  — Wait for the display face so the wordmark is not drawn in a fallback — but never for long: navigator.share() spends…
L4174  var SHARE_DISPLAY
L4175  var SHARE_BODY
L4176  _shareFontReady()
L4190  _shareWordmark(ctx, pal, y, label)  — "Psync" + the image's section label, on one baseline.
L4204  _shareClassColour(cat, pal)  — The class-type colour in effect, as a literal a canvas can paint: the member's own swatch through the engine…
L4213  async window.shareYearReview()  +114

### Share Insights Card (L4329)
L4350  async window.shareInsights()  +229
