# js/facets.js — symbols
Purpose: the shape of `js/facets.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

134 lines · 8 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.

L28  var DEFAULT_ACCESSORS
L36  var DIMENSIONS
L39  asArray(v)  — Coerce a selection (array | string | null/undefined) to an array.
L50  run(classes, filters, opts)  +56
L108  groupByDay(list, getStart)  — Bucket events by local YYYY-MM-DD, day-ascending, each day time-sorted.
L124  uniq(a)
L125  assign(t)
L133  root.PsycleFacets = {…}  — keys: run
