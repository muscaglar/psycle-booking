# ios-app/www/native-bridge.js — symbols
Purpose: the shape of `ios-app/www/native-bridge.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `9836e6a`). Do not edit; rebuild with `npm run agents:index`.

2652 lines · 170 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## Native Bridge — Capacitor integration layer (L1)

### Gym Time (Europe/London) (L16)
L37  var GYM_TZ
L38  var _gymWallFmt
L48  _gymWallToUtcMs(y, mo, d, h, mi, s)  — Absolute UTC ms for a wall-clock time in the gym's zone (DST-correct, independent of the device timezone).
L79  _classStartMs(startAt)  — Absolute UTC ms of an API class time.
L95  window._psycleClassStartMs = _classStartMs

### Calendar ownership decisions (pure) (L97)
L107  _calForeignEvents(events, calId, markerOf)  — Unmarked events in calendar `calId` — what a full-ownership reconcile would delete that isn't ours.
L114  _calSweepVictimIds(events, calId, ownedAck, markerOf)  — Native ids to clear out of `calId` when it stops being the sync target.
L137  _calAckCovers(stored, calId)  — Does the stored ownership ack hand over calendar `calId`?
L140  window._psycleCalForeignEvents = _calForeignEvents
L141  window._psycleCalSweepVictimIds = _calSweepVictimIds
L142  window._psycleCalAckCovers = _calAckCovers

### Platform (L152)
L170  var PLATFORM  — Both native apps run this file.
L174  var IS_ANDROID
L176  var Preferences
L177  var Haptics
L178  var Share

### Fix Login Flow (L193)
L196  window.openLoginPopup()  — window.open() in WKWebView opens Safari, breaking postMessage.

### Fix window.open calls (L200)
L204  var Browser  — calendar.js uses window.open for Google Calendar and ICS blob.
L205  var _origWindowOpen
L206  window.open(url, target)

### Persistent Storage (L221)
L226  var RETIRED_KEYS
L227  var SYNC_KEYS  +45
L278  async restoreFromNative()  — On startup: restore from native storage to localStorage.
L301  syncToNative(key, value)  — Sync localStorage changes to native storage
L311  var _origSetItem  — Intercept localStorage.setItem and removeItem
L312  var _origRemoveItem

### Native Share (L361)
L364  async window.nativeShare(title, text, url)

### Native Calendar Integration (L375)
L414  var Calendar
L415  var CAL_EVENT_MAP_KEY
L416  var CAL_ID_KEY
L417  var CAL_TARGET_KEY
L418  var CAL_MODE_KEY
L419  var CAL_ENABLED_KEY
L427  var CAL_OWNED_ACK_KEY  — '2:<calendarId>' once the user has confirmed that target under the full-ownership contract — only then may the…
L428  _calIsOwned(calId)
L431  var PSYCLE_CAL_TITLE
L432  var PSYCLE_EVENT_MARKER
L434  _loadCalMap()
L437  _saveCalMap(map)
L446  _nativeSlotLabel(typeName)  — Slot label for a class type.

### Permissions (L451)
L453  var _calPermissionGranted
L455  async _ensureCalendarPermission()  +30

### Dedicated Psycle Calendar (L486)
L488  async _listNativeCalendars()
L503  hasChosenCalendar()  — Has the user explicitly chosen a calendar to write into?
L507  async _resolveTargetCalendarId()

### Add / Update / Remove Events (L518)
L520  _buildCalEventData(eventId)  +55

### v6-plugin-correct primitives (L576)
L581  _markerEventId(ev)  — Extract the Psycle event id from a native event's notes, or null.
L588  _nativeStartMs(ev)  — Millisecond start time of a native event, across plugin shapes.
L596  _createdEventId(result)  — The created-event id from a createEvent() result, across versions.
L608  async _deleteNativeEvents(ids)  — Delete native events by id.
L640  var _androidDeletedEventIds  — ANDROID ONLY.
L641  _androidRememberDeleted(ids)
L648  async _listNativeEvents(startMs, endMs)  — Normalized listEventsInRange (ms timestamps in, array out).
L660  calendarSyncEnabled()

### Hook into booking / cancel / seat-cancel flows (L668)
L675  var _origSubmitBookingNative
L686  var _origConfirmUnbookNative
L694  var _origUpcomingCancelNative
L702  var _origCancelBikeSlotNative
L710  var _origUpcomingSeatCancelNative

### Full Calendar Reconcile (L718)
L735  async syncAllBookingsToCalendar()  +118
L855  var _origRenderNative  — Run sync after bookings load
L856  var _calSynced
L872  async window.psycleResyncCalendar(opts)  — Force a fresh sync.
L903  async window.psycleCleanupDuplicates()  — Settings → "Remove duplicates".
L912  window.syncAllBookingsToCalendar = syncAllBookingsToCalendar

### Public API for Settings UI (L914)
L917  async window.psycleListCalendars()  — List iOS calendars the user can choose from.
L935  window.psycleGetCalendarConfig()  — Get the current calendar sync config for the Settings UI.
L955  async window.psycleCountForeignEvents(calId)  — READ-ONLY: how many upcoming events in calendar `calId` are NOT Psycle bookings — what handing that calendar over…
L980  async window.psycleSetCalendarConfig(cfg)  +62  — Update the calendar target.

### Weekly Booking Reminder (L1044)
L1049  var LocalNotifications

### Android: notification channels, the small icon and its tint (L1051)
L1069  var ANDROID_CHANNEL_CLASSES  — Android 8+ files every notification under a CHANNEL, which is what the member sees (and can silence) in the system's…
L1070  var ANDROID_CHANNEL_NEW_DATES
L1071  var ANDROID_NOTIF_ICON
L1075  var ANDROID_NOTIF_TINT  — The mark's ink.
L1076  var _androidChannels
L1078  _ensureAndroidChannels()
L1102  _forAndroid(notification, channelId)  — The Android-only fields of one notification.
L1116  var REMINDER_IDS  — Eight rolling one-shot notifications (the next 8 Mondays).
L1121  var WEEKLY_REMINDER_HOUR, WEEKLY_REMINDER_MINUTE  {weekly-reminder}  — WHEN it fires and WHAT it says.
L1122  var WEEKLY_TEMPLATE_KEY  {weekly-reminder}
L1131  _nextMondaysNoonLondon(count, now, wallToUtcMs)  {weekly-reminder}  — The next `count` occurrences of Monday 12:00 Europe/London as absolute Date instants, DST-correct for any device…
L1149  _hasUsualWeek(raw)  {weekly-reminder}  — Is a usual week saved?
L1159  _weeklyReminderCopy(hasUsualWeek)  {weekly-reminder}  — It fires AT the release, so it says the dates ARE open (and still reads true when the banner is seen later).
L1169  _weeklyTapTab(hasUsualWeek)  {weekly-reminder}  — Where the tap lands at once (the web layer's _onBookingWeekOpened does the rest when it can): the usual-week review…
L1178  _weeklyOfferDecision(f)  {weekly-reminder}  — Put the in-app "remind you on Mondays?" question (_offerWeeklyReminder)?
L1187  _usualWeekSaved()
L1191  var REMINDER_PREF
L1195  var _weeklyArmedWithWeek  — Which body the armed reminders carry (true = "Book your usual week…"), null while nothing is known to be armed — so a…
L1197  async _cancelReminders()
L1211  async scheduleWeeklyReminder(interactive)  +42
L1256  var _weeklyChain  — One pass at a time: each cancels the eight ids and schedules them again, and two interleaved passes could leave the…
L1257  _scheduleWeeklySerial(interactive)
L1266  window._nativeReminder = {…}  — keys: isOn, enable, disable — Exposed to the web layer (Membership tab toggle).
L1291  _rearmWeeklyReminder()  — The body names what the tap opens, so it follows the usual week.

### Notification Action Buttons (Book / Cancel / Snooze) (L1312)
L1324  registerNotificationActions()
L1346  var TAP_ROUTE_WAIT_MS  — Land a tapped notification on what it was about.
L1347  var _cancelTapRoute
L1358  _askDiscoverToRefresh()  — The Monday reminder exists for one moment: the release of new dates.
L1367  _routeNotificationTap(eventId)  +53
L1440  handleNotificationAction(notification)  +36  — Route a tapped notification / action button.
L1495  _parseWidgetLink(url)  {widget-link}  — A Home/Lock Screen widget tap opens psync://bookings?event=<id> (minted in PsycleWidget.swift).
L1514  handleWidgetURL(info)  {widget-link}  — Same landing as a class-reminder tap: My Bookings, then that class's sheet (only while the seat is still held).
L1531  var PsycleDeepLink  — The in-app PsycleDeepLink plugin RETAINS the URL until this listener attaches — a widget tap usually cold-launches the…

### Widget / Live Activity / Siri Snapshot (L1539)
L1574  var WIDGET_APP_GROUP  — App Group container id.
L1576  var WIDGET_NEXT_KEY
L1577  var WIDGET_WEEK_KEY
L1582  var WIDGET_UPCOMING_KEY  — Next few classes (same shape as widget_next_class, array of up to 5) so the widget can build a MULTI-ENTRY timeline…
L1587  _prefSet(key, value)  — Standard Preferences plugin: lands in UserDefaults.standard under "<group>.<key>" (NOT extension-readable — kept for…
L1600  _appGroupSet(key, value)  — App Group mirror: writes the SAME logical value under the BARE key into the shared App Group suite, so a native…
L1609  _writeSnapshotKey(key, value)
L1624  var WIDGET_UPCOMING_MAX  {ios-polish}  — The widget timeline shows the next few classes; reminders cover every class held.
L1625  var CLASS_REMINDER_MAX  {ios-polish}
L1629  _isHeldSeat(booking)  {ios-polish}  — A real seat, as opposed to a waitlist place — true even when the class's details failed to load this pass (that makes…
L1638  _evtFromSavedItem(item)  {ios-polish}  — Stand-in for an _eventCache entry, built from app.js's saved copy of My Bookings (psycle_bookings_snapshot), which…
L1656  _staleReminderIds(map, wanted, keep)  {ios-polish}  — Event ids whose armed reminder should go: the class is no longer wanted, or its time moved (it is re-armed for the new…
L1678  var SNAPSHOT_NEUTRAL_TINT  {native-snapshot}  — The card ground at intensity "off": the app's own neutral surface (Cloud / Graphite, css/theme.css) — the widget's…
L1682  _snapHex(value)  {native-snapshot}  — '#2d5fd6' → '#2D5FD6'; anything that is not six hex digits → null.
L1692  _snapClassTypeKey(typeName, keyFn)  {native-snapshot}  — "RIDE: 45" → 'ride', through the app's OWN classTypeKey (js/app.js) — the same answer data-ct gets.
L1712  _snapClassColourFields(key, engine)  {native-snapshot}  — The colour fields for one class type, read from the app's colour engine (`engine` = window.PsycleClassColours: get() →…
L1745  _savedItemsById()  — app.js's saved copy of My Bookings, by event id ({} when there is none).
L1760  _snapshotEventFor(eventId, known)  +35  — Resolve a display-ready event object from the cache, or null.
L1807  var _snapServerConfirmed  — Only trust an EMPTY bookings map after the server confirmed it (bookings:loaded fired this session).
L1809  var _signOutPass  — True only while the clearToken wrapper (a deliberate sign-out) runs its pass.
L1812  var _snapColourSig  — The class colours the last WRITTEN snapshot carries (see the 'classcolours:changed' listener below), and that…
L1813  var _colourSnapTimer
L1814  _classColourSig()
L1823  updateWidgetSnapshot()  +128
L1951  window.updateWidgetSnapshot = updateWidgetSnapshot

### Class-start reminders (T-90 minutes) (L1953)
L1965  var CLASS_REMINDER_PREF
L1966  var CLASS_REMINDER_MAP
L1975  _classReminderBody(c)  — The reminder's body: the instructor, the place and — on the iPhone — what a tap is for.
L1981  _classRemindersEnabled()

### The Android countdown's switch (L1985)
L2000  var COUNTDOWN_ENABLED_KEY  — The Android app has no Live Activity.
L2001  var _countdownSwitchSends
L2002  _androidCountdownSwitch()
L2016  _androidCountdownFlipped(sendsBefore)  — The member flipped Class reminders.
L2027  _loadReminderMap()
L2030  _saveReminderMap(map)
L2034  _reminderIdFor(eventId, map)
L2048  var _reminderChain  — Serialized: overlapping snapshot passes (booking events fire in quick succession) must not interleave the…
L2052  _scheduleClassReminders(upcomingList, unknownSeats)  — `unknownSeats` = {eventId: true} for held seats this pass knew nothing about (see _staleReminderIds) — captured with…
L2059  async _scheduleClassRemindersInner(upcomingList, unknownSeats)  +51
L2112  window._nativeClassReminders = {…}  +33  — keys: isOn, hasPermission, enable, disable — Settings toggle API (rendered by tabs.js next to the weekly reminder).

### First-booking ask (L2146)
L2152  var CLASS_REMINDER_ASKED  — The pref defaults ON, but only the Settings switch ever requested notification permission — and it looked on already,…
L2153  var ASK_POLL_MS
L2154  var ASK_GIVE_UP_MS
L2155  var _reminderAskBusy
L2156  var _reminderAskVoid
L2165  var ASK_BLOCKING_IDS  — Anything the ask would land on top of.
L2170  _askBlocked()
L2179  _askStillWanted()
L2183  async _maybeAskClassReminders()  +56

### Usual-week ask: the Monday reminder, offered in context (L2248)
L2259  var WEEKLY_REMINDER_ASKED  — The weekly reminder defaults OFF and its switch sits in Settings, where nobody looks for it.
L2260  var _weeklyAskBusy
L2261  var _weeklyAskSince
L2263  _weeklyOfferFacts(permission)
L2272  async window._offerWeeklyReminder(reason)  +56
L2338  var _origClearTokenNative  — Deliberate sign-out must blank the snapshot and cancel pending class reminders — otherwise the previous account's…
L2380  var _calReconcileTimer, _calReconcileInFlight, _calReconcilePending  — Auto-reconcile the calendar whenever bookings change — covers classes cancelled on the web / another device and…
L2383  async _runCalSync()  — Single-flight calendar sync.
L2392  _scheduleCalReconcile()
L2400  var _calServerConfirmedEmpty  — Tracks whether the latest bookings state came from a successful server fetch.

### Status Bar (L2423)
L2434  _themeGround(registry, themeId, base)  {android-bridge}  — The ground of the theme in force, for the Android status bar: the `bg` of its APP_THEMES entry (js/theme.js) — the…
L2448  updateStatusBar()  +36
L2486  var observer  — Watch for theme changes

## Analytics / Diagnostics (developer-only, no third-party services) (L2491)
L2495  var ACTION_LOG_NATIVE_KEY
L2496  var MAX_NATIVE_ACTION_LOG
L2500  var _origPushAction  — Persist action log to Capacitor Preferences (survives iOS storage purges).
L2509  _persistActionLogToNative()
L2528  async window.getDiagnosticReport()  +121  — getDiagnosticReport — comprehensive diagnostics string for developer use.
