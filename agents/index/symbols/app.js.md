# js/app.js — symbols
Purpose: the shape of `js/app.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `db0805f`). Do not edit; rebuild with `npm run agents:index`.

14287 lines · 707 symbols. Top level, plus the declarations one level inside its top-level IIFEs. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.

L29  const DIRECT_API
L30  const PROXY
L31  const IS_FILE
L36  localDateStr(d = new Date())  {core}  — Format a Date as YYYY-MM-DD in LOCAL time.
L39  const today  {core}
L43  parsePsycleDate(v)  {core}  — Parse an API datetime that may be 'YYYY-MM-DD HH:MM:SS' — iOS WebKit…
L59  let _gymWallFmt  {gym-time}  — Psycle is a UK gym: a naive 'YYYY-MM-DD HH:MM[:SS]' from the API is…
L65  _gymWallToUtcMs(y, mo, d, h, mi, s)  {gym-time}  — UTC ms for a London wall-clock reading (DST-correct, device zone…
L89  _gymClassStartMs(startAt)  {gym-time}  — Absolute UTC ms of an API class time.
L108  apiUrl(path)
L114  getBearerToken()
L119  apiFetch(path, opts = {})

### Category mapping for smart filters (L154)
L163  const CATEGORY_MAP  {core}  — A category no longer carries a colour of its own: colour means class…
L188  getCategory(typeName)  {core}
L210  slotLabel(typeName)  {core}  — Get the slot label for a class type.
L230  var CLASS_PICTOGRAMS  {class-type}  — Crisp Colour: a class type is shown by its COLOUR (data-ct →…
L242  classTypeKey(typeName)  {class-type}  — "RIDE: 45" → "ride".
L254  classPictogram(key, size)  {class-type}  — The pictogram of a category, as an SVG string:…
L276  _ccTimeHTML(o)  {class-type}  — THE TIME BLOCK of the class card — built here, ONCE, for every…
L289  slotLabelForEvent(eventId)  — Get slot label from an event ID via the cache
L299  pluralizeSlotLabel(label)  {core}  — Pluralize a slot noun, preserving case: Bench -> Benches; Bike ->…
L312  _plural(n, one, many)  {core,copy}  — A count with its noun: "0 classes", "1 class", "2 classes".
L323  formatSlots(label, slots)  {core}  — Format a booked slot list with the correct noun for the class type.
L333  instrLink(name, instrId)  — Wrap an instructor name in a clickable link that opens their profile…

### Strength sub-filter (L353)
L354  const STRENGTH_SUBS

### Reformer sub-filter (variants of the Pilates category) (L361)
L365  const REFORMER_SUBS  — Mirrors STRENGTH_SUBS: shown when Pilates is selected, matches the…
L377  _repaintKeepingFocus(box, html, selector)  — Every pill row is rebuilt from state on each press, which destroys…
L386  renderReformerSubPills()
L401  toggleReformerSub(key)
L413  renderStrengthSubPills()
L426  toggleStrengthSub(key)
L444  _parseSlots(raw)  {core}  — Extract slot numbers from any API format: [7, 15] → [7, 15] [{id:7,…
L461  let _bookingsSeq  — Supersede guard: fetchMyBookings is fired concurrently from many…
L468  let _bookingsLoadState  — An empty _myBookings alone can't tell "nothing booked" from…
L473  _noteBookingsLoadFailed()  — A /bookings attempt failed (never called for a superseded one — the…
L482  let _bookingsLocalWriteAt  — Local writes to _myBookings (book / join / leave / cancel / claim).
L483  _noteLocalBookingWrite()
L489  async _hydrateEventDetails(ids)  — Hydrate _eventCache for the given event ids from GET /events/{id}.
L521  _seedEventCacheFromEntries(map, entries)  — A waitlist entry already carries its class — seed _eventCache from…

### Saved class details (My Bookings' first paint) (L531)
L542  const EVENT_DETAILS_MAX_ITEMS  {event-details}
L543  const EVENT_DETAILS_MAX_AGE_MS  {event-details}
L550  _slimEventDetails(evt, id)  {event-details}  — One class → the slim record kept between launches, or null.
L578  _eventCacheEntryFromSnapshot(item, nowMs)  {event-details}  — A stored item → an _eventCache record, or null: malformed, or not…
L588  _coerceEventDetailsSnapshot(raw, nowMs)  {event-details}  — What is in storage → { [eventId]: _eventCache record }, or null.
L604  _buildEventDetailsSnapshot(bookings, eventCache, nowMs)  {event-details}  — The record to store: the classes in `bookings` and no others, so it…
L619  _eventDetailsDiffer(a, b)  {event-details}  — Did anything a card, the calendar event or the widget shows about a…
L629  const EVENT_DETAILS_KEY
L633  _seedEventCacheFromSnapshot(ids)  — Stand-in details for `ids` — only ever ids the server's list has…
L642  _saveEventDetailsSnapshot()  — Only from a list Psycle confirmed this session (as the saved copy):…
L655  _refreshSeededEventDetails(next, ids)  — Re-read the classes this pass painted from saved details.
L691  _resyncDiscoverButtons(allowRelease)  — Re-apply the held state to every rendered Discover button, and…
L712  _dialogOpen()  — True while a user-facing dialog is up (a background announcement…
L718  let _allocAnnounceTimer
L719  let _announceShowing
L720  const _pendingAnnounce
L727  _persistPlacesNow(diffAllocated)  — Persist the place memory from the LIVE state at write time (never…
L749  _announceAllocations(eventIds, diffToPersist)  +68  — Announce waitlist places Psycle turned into real (chargeable) seats.
L830  const _placesLeftHere  — The other ending: a remembered place that is now neither a place nor…
L831  const _endedPlacesTold
L832  _noteEndedPlaces(ids)
L856  const WAITLISTS_DEADLINE_MS
L861  async fetchMyBookings()  +187  — Resolves to true when THIS call's response was applied to…
L1048  let _waitlistsUnavailable
L1053  async retryBookingsLoad(btn)  — The Retry button on My Bookings' "Couldn't load your bookings" hero.
L1086  _toastDuration(text)  {a11y}  — How long a toast stays up.
L1095  _joinAnnouncements(queue)  {a11y}  — Messages that reach announce() within one tick are spoken together,…
L1109  _topDialogIndex(dialogs)  {a11y}  — Which of the open aria-modal dialogs is on top: the highest z-index…
L1123  const _srQueue  — Say something to screen-reader users.
L1124  const _srTimers
L1125  const _srInDialog
L1126  announce(text, assertive)  +33
L1166  _dialogLiveRegion(id, assertive)  — A screen reader treats everything outside an open aria-modal dialog…
L1197  toast(msg, type = 'info')  — Toast (toastTimer managed by state.js)

### Discover empty state: signed-out users get one clear action (L1209)
L1210  updateDiscoverEmptyState()
L1234  _sessionStateFor(hasToken, status)  {session}  — What one /profile attempt says about the session.
L1245  _authGateViewFor(hasToken, unverified)  {session}  — Which hero a tab shows while there is no currentUser: 'signin' no…
L1253  _pickActiveSubscription(subs)  {session}  — The subscription behind the usage bar / Membership card: prefer a…
L1262  _shouldOfferHistorySync(s)  {session}  — The first-run "sync my history" prompt is an offer, not a nag: never…
L1270  _profileFrom(data)  {session}  — The member in a /profile body — `{data: {...}}` or the bare object —…
L1290  let _authInFlight  — Auth check ONE event tells every module the session changed:…
L1291  let _lastProfileId
L1292  let _authUnverified
L1293  let _authSettledOnce
L1294  let _gearIconHTML
L1296  _emitAuthChanged(signedIn)
L1323  const DATA_OWNER_KEY  {data-owner}  — Whose data is on this device.
L1324  const ACCOUNT_STASH_KEY  {data-owner}
L1325  const ACCOUNT_STASH_MAX_OWNERS  {data-owner}
L1326  const ACCOUNT_STASH_MAX_VALUE  {data-owner}
L1327  const ACCOUNT_STASH_KEYS  {data-owner}
L1331  const ACCOUNT_CLEAR_KEYS  {data-owner}
L1345  const HISTORY_OWNER_STAMP_KEY  {data-owner}
L1348  _dataOwnerPlan(storedOwner, newId)  {data-owner}  — 'none' (no id to go by) · 'adopt' · 'same' · 'switch'
L1355  _isStashOwnerId(id)  {data-owner}  — An owner id the stash can be keyed by (anything else is dropped on…
L1362  _parseAccountStash(raw)  {data-owner}  — The stash as stored → { ownerId: { savedAt, pending, keys: { key:…
L1389  _swapAccountData(store, newId, nowIso, write)  +75  {data-owner}  — Move the stored per-account data to `newId`.
L1470  _claimDataOwner(id)  — _applyProfile's one call into the above, plus the in-memory copies…
L1485  _applyProfile(data)  +42  — Apply a GET /profile body to app state.
L1532  async checkAuth()  — Single-flight per token: 'online' and visibilitychange fire together…
L1546  async _checkAuthOnce(token)  +75
L1629  const PROFILE_REFRESH_MIN_GAP_MS  — Plan usage ("7 of 12 classes"), credits and cost-per-class all come…
L1630  let _profileRefreshAt
L1631  let _profileRefreshInFlight
L1632  let _profileRefreshQueued
L1633  let _profileRefreshTimer
L1636  refreshProfile(force)  +35  — force = something just changed, so the 30s throttle doesn't apply.
L1674  _refreshProfileSoon()  — ~1s after a change: lets Psycle settle the count, and folds a burst…
L1708  authGateHTML()  — Hero for a tab that looks signed out although a token is still…
L1723  async retryAuth(btn)  — The Retry button on that hero.
L1737  const HISTORY_PROMPT_DISMISSED_KEY  — Skip / × / backdrop all mean "not now, and don't ask again".
L1738  let _syncPromptTimer
L1739  let _syncPromptDefers
L1741  _dismissSyncPrompt()
L1747  showHistorySyncPrompt()  +71
L1819  async startSyncFromPrompt()
L1835  openLoginPopup()
L1846  showTokenDialog()
L1857  closeTokenDialog()
L1861  validateToken()
L1866  async saveToken()
L1887  clearToken()
L1918  async confirmSignOut()  — Sign-out sits one stray tap away from a full email + password login,…
L1929  showSessionExpired()
L1961  const params
L1974  _healAuth()  — Re-check auth when there is no verified session.

### Offline indicator (L1997)
L2002  _setOfflineBanner(show)  — Nothing said "you're offline": requests just spun, then failed.
L2049  _laterModulesLoaded(doc, win)  {init-gate}  — Resolves once every module after this one has run.
L2061  const _modulesLoaded  — Armed NOW, while this script runs and DOMContentLoaded cannot have…
L2063  (iife) (anonymous)  +89
L2071  fetchJson(path)
L2101  let _sf  — Pre-select favourites — on a first run only, i.e. while no filter…
L2103  const _hasSavedFilters
L2125  const _ds  — The date row comes from the saved filters as well, not a blanket…

### Live filters: changes auto-run the search (debounced) (L2155)
L2159  let _autoSearchTimer  — Stays inert until signed in (avoids error spam pre-auth).
L2160  triggerAutoSearch()
L2195  _filterSummaryChips(state, maps)  +62  {filter-summary}  — The active filters as the collapsed Filters bar shows them: one…
L2260  _filtersBarName(count)  {filter-summary}  — What the Filters bar is called: the count is part of the NAME — the…
L2269  updateFiltersSummary()  +58  — The Filters bar's digest: a count on the bar and one removable chip…
L2333  let _summaryFollowsStars  — A star can change where no filter toggle runs (the instructor sheet,…
L2334  _followStarsInSummary()
L2345  _removeFilter(kind, id)  — × on a summary chip.
L2364  removeFilterChip(btn)
L2376  _addDaysStr(ds, n)  {filters}  — 'YYYY-MM-DD' plus n days.
L2384  _dateModeWindow(mode, todayStr)  {filters}  — What each date preset means today.
L2414  _windowEndDate(startDate, days, mode)  {filters}  — Last day of the fetch window.
L2422  _datePillState(mode, startDate, daysAhead, todayStr)  {filters}  — What the date row should show: the preset pill to light and, for a…
L2434  _restoredDateState(saved, todayStr)  {filters}  — Saved date filter → the state to restore.
L2450  _scrollLeftToReveal(scrollLeft, viewW, itemLeft, itemW, pad)  {filters}  — The scrollLeft that brings one item of a scroll row fully into view,…
L2470  _revealActiveDatePill()  — A reload with "Next week" (or any later pill) saved lit a pill that…
L2490  _mirrorDatePillAria()  — The date pills' selected state was a CSS class only — a screen…
L2501  let _pickDateBtnIdle  — Paint the date row from state (mode + the two inputs), never from…
L2502  _syncDatePills()  +30
L2533  setDateQuick(mode)
L2543  _applyDateQuick(mode)  — The preset itself.
L2564  _releaseDateRow()  — …nor may the member's NEXT tap change it: every wrapped toggle (a…
L2569  onDateInputChange()  — Clear active quick-btn highlight when date/days inputs are changed…

### The Filters bar (below 1024px) (L2578)
L2587  let _filtersCollapsed  — The date row is always on screen; Time / Location / Class Type /…
L2589  applyFiltersCollapsedState()
L2596  toggleFilters()
L2605  clearFilters()  +35
L2641  setStatus(html)
L2653  _nextEventsPageStart(batch)  {filters}  — `start` for the next /events page: ONE SECOND BEFORE the last class…
L2660  async fetchEventsForLocation(locId, startDate, endDateStr, seenIds, isStale)
L2690  let _searchSeq
L2695  let _loadableSearchStarted  — A search has started that could actually load a timetable: a token…

### Pre-loaded timetable window + stale-while-revalidate cache (L2697)
L2704  const WINDOW_CACHE_KEY  — On launch we hydrate the full all-studios window from localStorage…
L2705  const WINDOW_CACHE_TTL
L2711  const WINDOW_FRESH_MS  {window}  — How long a loaded window is re-filtered with no network call at all.
L2715  _parseWindowKey(key)  {window}  — 'start|end' → { start, end }; null for anything that is not two ISO…
L2721  _keyCovers(key, start, end)  {window}  — Does the window `key` hold every day of start..end?
L2730  _dayInRange(startAt, start, end)  {window}  — Is a class's day inside start..end?
L2741  _releaseFrom(now, dir)  {window}  — Psycle opens the next booking week on Mondays at 12:00 UK time (the…
L2753  _lastReleaseMs(now)  {window}
L2754  _nextReleaseMs(now)  {window}
L2770  const RELEASE_OPENS_FROM_DAYS  {window,horizon}  — HOW FAR AHEAD PSYCLE BOOKS — OBSERVED, NOT AN API CONTRACT.
L2771  const RELEASE_OPENS_TO_DAYS  {window,horizon}
L2772  const RELEASE_LISTED_EXTRA_DAYS  {window,horizon}
L2775  _horizonAddDays(dayStr, n)  {window,horizon}  — Calendar arithmetic on 'YYYY-MM-DD' through UTC, where every day has…
L2782  _horizonDayOf(ms)  {window,horizon}  — A release is Monday 12:00 in London — 11:00 or 12:00 UTC — so the…
L2788  _releaseReaching(dayStr, reachDays)  {window,horizon}  — The Monday-noon release that reaches `dayStr`: the latest Monday on…
L2800  _dayOpensMs(dayStr)  {window,horizon}  — When booking for a day opens, and when the day first appears on the…
L2801  _dayListedMs(dayStr)  {window,horizon}
L2808  _bookingHorizon(nowMs)  {window,horizon}  — What is open, what is listed, and what the next release brings.
L2833  _windowIsFresh(fetchedAt, now)  {window}  — A stamp from the future (the clock was moved back) is NOT fresh — it…
L2845  _cachePlan(cachedKey, start, end)  {window}  — What a persisted window can give a launch that asks for start..end:…
L2856  _revalRange(windowKey, selStart, selEnd, todayStr)  {window}  — The range a revalidate fetches.
L2872  _rollForwardMode(s)  {window}  — The preset to re-apply when the app is looked at on a different day…
L2883  _anyStartedBetween(events, fromMs, toMs)  {window}  — Did any class start in [fromMs, toMs)?
L2892  _windowCovers(startDate, endDateStr)  — Is the selected range inside the window already in memory?
L2897  currentWindowDates()
L2904  currentFilters()
L2921  _buildFacetClasses(events, relations)  — Lite per-event view that powers the cascading facet counts.
L2941  _setWindow(windowKey, events, relations, fetchedAt, partial)  — `partial`: the events do not fill the key's whole range (an older…
L2957  _persistWindow(windowKey, events, relations)
L2974  _readWindowCache()
L2982  let _windowRenderedAt
L2987  renderFromWindow(filters, quiet)  — `quiet`: nobody asked for this render (a background refresh landed,…
L3011  let _ownFullSeen  — "Available only" never hides the member's own class — but the launch…
L3012  _showOwnFullClasses()

### Pick-a-date calendar (redesign) (L3022)
L3023  let _calMonth
L3034  let _datePickerKeysWired  — Escape closes the calendar and hands focus back to its button.
L3035  _wireDatePickerKeys(el, btn)
L3049  toggleDatePicker()
L3069  calStep(dir)
L3082  pickCalDate(ds)
L3100  _classDays()  — Days (YYYY-MM-DD) that have classes in the cached window — drives…
L3105  renderCalendar()  +35
L3142  async fetchFullWindow(startDate, endDateStr, stale)  — Fetch the full window (every studio) for a date range.
L3159  _discoverBusy()  — True while a Discover card is mid-flow: a dialog is up, or a Book…
L3170  let _inPlaceTimer, _inPlaceDefers  — Re-render after a background refresh without yanking the page: the…
L3171  _renderWindowInPlace(isRetry)
L3191  let _fetchingSeq  — The sequence number of the search() that is fetching right now (0 =…
L3194  let _revalInFlight  — One revalidate per range at a time: renderLastUpdated() repaints on…
L3197  let _revalFailedAt  — A background refresh that failed is not retried for a minute — every…
L3198  const REVAL_RETRY_MS
L3201  let _handoverKey  — The provisional window a failed refresh was already handed to…
L3208  revalidateWindow(opts)  +35  — Refresh the window behind the current view: re-fetch, update the…
L3244  async _runRevalidate(run, range)  +67
L3313  _revalidateIfStale()  — Render from the window, then refresh it quietly when its numbers are…

### Discover rolls forward (L3318)
L3323  let _discoverDay  — The date row is derived once (launch, or a tap) and an iPhone keeps…
L3330  _rollDiscoverForward()  — Re-derive the date row if the day has changed.
L3352  let _resumeTimer, _resumeDefers  — Back in the foreground (the other visibility handlers reload…
L3353  _discoverResumed(isRetry)

### "Last updated" + manual refresh (Discover filters) (L3370)
L3371  _relativeTime(ts)
L3382  renderLastUpdated()
L3397  async refreshWindow()  — Manual refresh (the link above, and pull-to-refresh on Discover) —…

### Monday noon: the new booking week opens (L3402)
L3413  let _releaseTimer  — _windowIsFresh already calls anything fetched before the release…
L3414  _armReleaseTimer()
L3429  _refreshForRelease(again)
L3451  const OPENED_BATCH_FIRST_DAY  {week-opened}  — WHICH dates a Monday release opens, for the reminder tap below.
L3452  const OPENED_BATCH_LAST_DAY  {week-opened}
L3453  _isoDayPlus(ds, n)  {week-opened}
L3459  _openedBatch(horizon, lastReleaseMs)  {week-opened}  — → { from, to } ('YYYY-MM-DD', London days) or null when neither can…
L3470  _weekOpenedRouteFor(f)  {week-opened}  — Where the tap goes.
L3499  const WEEK_OPENED_GIVE_UP_MS  — The Monday "New Psycle dates are open" reminder was tapped (the iOS…
L3500  let _weekOpenedTimer, _weekOpenedTries, _weekOpenedAt, _weekOpenedRoute
L3502  _weekOpenedUsualWeek()
L3508  _weekOpenedReviewBlocked()  — Anything the review sheet would land on top of, or a Book button…
L3517  _showOpenedDay(day)  — The date row's own custom-date path — what pickCalDate writes —…
L3529  window._onBookingWeekOpened(isRetry)  +46
L3576  async search(opts)  +221
L3801  showSearchError(e)  +50  — Friendly search-failure handling.
L3855  _errorStatusHTML(message, label, onclick)  — An error line with its one way out — the old copy said "retry" with…
L3864  let _refDataFailed  — Reference data (/instructors, /locations, /event-types) failed at…
L3865  showInitError(err)
L3882  _discoverEmptyContext()  +50  — WHY the Discover list is empty, for theme.js's renderEmptyState (it…
L3933  mergeRelations(base, incoming)

### Booking (L3946)
L3947  const MAX_SEATS
L3952  let _usualPreselected  — _bookingContext and _selectedSlots managed by state.js The usual…
L3958  let _bookAuthRecheck  — A token but no profile means the launch /profile call failed or…
L3959  _recheckAuthForBooking()
L3973  _busyLabel(btn)  — The '…' busy label.
L4011  _clock24(hours, mins)  {clash,clock,class-type,bookings-card,day-pager,offline}  — _clock24(18, 30) → "18:30" · _clock24(6, '05') → "06:05".
L4025  _clockOf(startAt)  {clash,clock,class-type,bookings-card,day-pager,offline}  — The time of an API class time, cut from its DIGITS: '2026-09-21…
L4041  const CLASH_DEFAULT_MIN  {clash}  — Nothing stopped a member booking a 7:15 Ride at Bank while holding a…
L4042  const CLASH_TRAVEL_MIN  {clash}
L4043  _clashStartMin(startAt)  {clash}
L4061  _findClash(evt, bookings, cache, opts)  +35  {clash}  — The held SEAT `evt` collides with, or null.
L4100  _clashLabel(clash)  {clash}  — One member-facing sentence for a _findClash result, no trailing full…
L4119  _clashFor(eventId, fresh, opts)  — _findClash for a class about to be booked / joined / opened.
L4136  _noteFreshAvailability(eventId, evtData)  {book-fresh}  — bookClass has just read GET /events/{id}: keep what it says about…
L4160  _layoutFromEventDetail(detail, studioId)  {book-fresh}  — The studio's seat map out of a GET /events/{id} answer.
L4174  _studioFromEventDetail(detail, studioId)  {book-fresh}  — The studio's own record out of the same answer, for a class whose…
L4184  async bookClass(eventId, btn, studioId)  +286
L4479  var UI_ICONS  {sheets}  — Line marks for a sheet row or a menu option — the pictograms'…
L4491  _uiIcon(name, size)  {sheets}
L4511  _sheetPlanNote(f)  {sheets}  — What the sheet says beside its Book button: what this booking uses,…
L4535  _pickerConfirmLabel(slotWord, selected, verb)  {sheets}  — The picker's confirm button says what it will book: "Book bike 12",…
L4545  _seatList(list)  {sheets}  — "3 & 4", "3, 4 & 5" — as formatSlots lists the same seats everywhere…
L4551  _chooseSpotHint(slotWord, count, selected)  {sheets}  — The choose-only picker's hint: how many spots the sheet's row asks…
L4564  showBikePicker(eventId, btn, layout, availableSlotIds, mySlotIds, studioName, opts)  +229
L4797  _syncPickerConfirmLabel()  — The confirm button names what it books (_pickerConfirmLabel) — or,…
L4807  _tapReplacesUsual(selected, usualPreselected, tappedId, swapMode)  {booking}  — True when a tap should REPLACE the picker's auto-selected usual bike…
L4819  _bikeSlotA11y(classes, slotWord, label)  {a11y}  — What one seat on the picker map tells assistive tech, read off the…
L4841  _syncBikeSlotsA11y()  — The seats are SVG <g onclick> — no role, name, focus or keys, so a…
L4859  selectBike(slotId)  +57
L4917  closeBikePicker()
L4942  confirmSpotChoice()  — "Use bike 12": the choose-only picker hands the chosen spots back to…
L4952  async confirmBikeBooking()

### Waitlists (L4963)
L4982  const WAITLIST_MAX_PAGES  — Waitlist places are a SEPARATE server resource from bookings…
L4991  _normaliseWaitlistEntry(raw, fallbackEventId)  {waitlist}  — Normalise one API waitlist entry (GET /waitlists item, GET…
L5012  _isActiveWaitlistEntry(entry)  {waitlist}  — An entry still holds a place unless the server says it was…
L5021  _waitlistTimeMs(value)  {waitlist}  — Server timestamps on waitlist entries: ISO with Z/offset (added_at)…
L5035  _waitlistOfferPending(entry, nowMs)  {waitlist}  — A spot is being OFFERED (Psycle emails close to class time instead…
L5050  const WAITLIST_OFFER_WINDOW_MS  {waitlist}  — Close to class Psycle stops filling freed spots from the waitlist…
L5051  const WAITLIST_OFFERS_FROM_MS  {waitlist}
L5052  const WAITLIST_CLOSES_MS  {waitlist}
L5059  _waitlistEarlyOffersOpen(startAt, leftMs)  {waitlist}  — "After 10pm the evening before", for a 6–9am class with `leftMs` to…
L5067  _inWaitlistOfferWindow(startAt, nowMs)  {waitlist}  — Should the app be asking Psycle whether this class has a spot to…
L5076  _waitlistPhase(startAt, nowMs)  {waitlist}  — What a seatless place can still expect, for the copy: 'auto' (Psycle…
L5085  _waitlistEntryFromResponse(data, eventId)  {waitlist}  — Pull the created/affected entry out of any of the response envelopes…
L5094  _isAlreadyOnWaitlistResponse(status, message)  {waitlist}
L5101  _waitlistLeaveSucceeded(ok, status, message)  {waitlist}  — DELETE /waitlists/{id}: gone is gone — a 404, or the server's 500…
L5110  _eventCacheEntryFromWaitlist(ev)  +32  {waitlist}  — Build an _eventCache-compatible record from the event object…
L5147  _mergeWaitlistsIntoBookings(bookings, entries, nowMs)  {waitlist}  — Merge normalised, active entries into a bookings map (mutates +…
L5171  _waitlistOfferFromDetail(data, nowMs)  {waitlist}  — What GET /waitlist/{entryId} says about claiming right now.
L5196  _heldPlacesOf(bookings)  {waitlist}  — Compare the places we held last time (persisted {eventId: entryId})…
L5204  _diffWaitlistPlaces(prev, bookings, nowMs)  {waitlist}
L5226  const WAITLIST_PLACES_KEY  — Persisted between launches so an allocation that happened while the…
L5229  _readWaitlistPlaces()  — Owner-stamped: a memory written for another Psycle account (session…
L5242  _writeWaitlistPlaces(v)
L5253  _rememberPlace(eventId, entryId)
L5260  _markPlaceClaimed(eventId)  — The user claimed the spot themselves: badge it "From waitlist" but…
L5273  const _offerProbeAt  — Near class time Psycle stops auto-allocating and OFFERS spots…
L5277  _placeNeedsOfferCheck(b, id, now)  — A seatless place worth asking about right now: Psycle says an offer…
L5286  async _probeWaitlistOffers(bookings)  +38  — Resolves to { probed, changed, allocated } — `changed` when a card…
L5332  const WAITLIST_RECHECK_MS  — My Bookings left open close to class: the probe above only runs when…
L5333  let _waitlistRecheckTimer
L5334  let _waitlistRecheckBusy
L5335  let _waitlistRepaintOwed
L5342  _bookingsRepaintSafe()  — A repaint swaps every card button out: never under a dialog or the…
L5351  _placesShownSig(map)  — What the cards show for each seatless place: its offer state and…
L5359  let _waitlistShownSig
L5361  async _waitlistRecheckTick()
L5390  _syncWaitlistRecheck(repainted)  — `repainted`: the caller sits right next to a repaint of My Bookings…
L5410  _dropBookingKeepPlace(eventId)  — After a real booking is cancelled locally: keep any waitlist place…
L5424  _markSeatFreed(eventId)  — The user just gave a seat back, so the search-time "full" flag is…
L5431  _afterCardCancel(btn, eventId)  — After a Discover-card cancel: resync every card button for the event…
L5449  _bookingIdsFor(booking, explicitId)  — Booking ids to DELETE for an event's real seats: per-slot ids when…
L5466  const _shapeRecordedAt  — Field NAMES of one sample go to diagnostics (never values) so a…
L5467  _recordShape(kind, sample)
L5477  let _lastWaitlistEntries
L5483  async fetchMyWaitlists(startedAt)  +41  — Page through GET /waitlists.
L5524  let _lastWaitlistEntriesAt
L5527  _rememberLastEntry(entry)  — Keep the offline/outage fallback in step with what the user just did.
L5534  _forgetLastEntry(eventId, entryId)
L5542  async _fetchWaitlistEntryForEvent(eventId)  — My entry for one event straight from the server.
L5557  _syncCardButtonsForEvent(eventId)  +39  — Re-sync every rendered Discover card button for an event with…
L5600  _dropCardCounts(eventId)  — bookClass found no free seat in a class Psycle has not called full:…
L5604  _waitlistClassLine(eventId)
L5623  async confirmJoinWaitlist(eventId, btn, clashLine)  — Ask first, then join.
L5645  async joinWaitlist(eventId, btn, opts = {})  +113  — PUT /waitlists/{eventId}.
L5760  async leaveWaitlist(eventId, btn, opts = {})  +121  — DELETE /waitlists/{entryId}.
L5884  async claimWaitlistSpot(eventId, btn)  +168  — The emailed-offer flow, in-app: GET /waitlist/{id} tells us whether…

### Bike-preference memory (L6053)
L6057  const BIKE_HISTORY_KEY  — Remembers which slots the user books, keyed by studio + instructor,…
L6059  _getBikeHistory()
L6064  _recordBikeHistory(eventId, slots)
L6085  _usualSlotForEvent(eventId)  — Most-booked slot number for an event's studio+instructor, or null.
L6101  _isRealSeat(entry)  {booking}  — A real seat — as opposed to a seatless waitlist place, or nothing.
L6107  _knownBookingIds(entry)  {booking}  — Every booking-record id an entry knows of (deduped, order kept).
L6124  _mergeBookedSeats(prevEntry, newSlots, bookingId)  {booking}  — The entry for an event after booking `newSlots` (record id…
L6145  _withoutSeat(entry, slotId, recordId)  {booking}  — `entry` without seat `slotId` and its record `recordId` — what is…
L6165  _seatCancelId(booking, slotId)  {booking}  — The booking-record id that is provably seat `slotId`'s OWN, or null…
L6185  _recordIdsIncomplete(entry)  {booking}  — A booking entry that can't list all its records yet: fewer record…
L6199  _bookingOutcome(applied, entry, requested, idsBefore)  {booking}  — What a re-read of /bookings says about a POST whose own answer…
L6218  let _bookingsRefetchTimer  — One trailing refetch shared by every POST outcome.
L6219  _scheduleBookingsRefetch(delayMs)
L6229  const _unverifiedBookings  — Classes whose last POST /bookings ended with no verifiable outcome —…
L6230  const BOOKING_VERIFY_DEADLINE_MS
L6238  async _rereadBookingsForVerify()  — fetchMyBookings under a deadline.
L6253  _announceVerifiedSeats(eventId, btn, entry, landed)  — A seat /bookings has confirmed: button from the server's entry, then…
L6275  async _settleUnverifiedBooking(eventId, slots, btn, idsBefore, conflict, serverMsg)  +61  — A POST /bookings whose own answer can't be trusted: a timeout/5xx…
L6339  async _clearUnverifiedBooking(eventId, btn)  — Before another POST for a class whose last one is unverified: settle…
L6369  async submitBooking(eventId, slots, btn, opts = {})  +90  — Real seats only (POST /bookings).
L6461  let _confirmationTimer  — Feature 9: Post-booking confirmation overlay
L6462  showBookingConfirmation(eventId, slotsArr, opts = {})  +89
L6552  dismissBookingConfirmation()
L6569  scrollToUpcoming()
L6587  confirmModal(opts)  +111  — Promise-based confirmation modal matching the app's visual language.
L6709  _confirmTone(opts)  {sheets}  — How a confirm's action button is drawn: 'primary' | 'danger' |…
L6723  _visibleOpener(el)  {sheets}  — Where focus goes back to when a dialog closes.
L6730  window.confirmModal = confirmModal

### Sheets and panels: one focus stack, one key handler (L6732)
L6745  const _OVERLAYS  — Only confirmModal (above) and the first-run tour looked after the…
L6759  const _overlayStack
L6763  _overlayIsOpen(el)  — Seven are built on open and removed on close (present = showing);…
L6771  _ownKeysOverlayUp()  — confirmModal and the tour keep their own focus and keys, above…
L6778  _overlayFocusables(el)  — Same rule as confirmModal's trap.
L6784  _focusIntoOverlay(el)
L6807  _syncOverlayStack()  +40  — Bring the stack in line with what is actually open, and move focus…
L6858  _activateRoleButton(el)
L6862  let _spaceDownOn
L6932  describeCancelError(failedResponse, data, err, sentOnline)  +41  — Turn a failed cancel into a user-friendly message.
L6982  _friendlyError(e, fallback)  — Member wording for an error THROWN on a booking / waitlist / swap…
L7010  let _londonPartsFmt  {bookings-card}  — Psycle charges for cancelling inside 12 hours of class, and the API…
L7017  _londonParts(ms)  {bookings-card}  — What London's clocks read at an absolute instant: {dow, hour,…
L7044  _londonClock(ms)  {bookings-card}  — "17:45" — an instant that has a London meaning (a waitlist accept-by…
L7051  _cancelDeadline(startAt, nowMs)  {bookings-card}
L7085  _hoursMinsLeft(hoursUntil)  {bookings-card}  — Time left before class as whole hours + minutes, rounded ONCE on the…
L7094  _startsInPhrase(left)  {bookings-card}  — "starts in 1h 45m" / "starts in 30 min", from _hoursMinsLeft's result.
L7107  confirmCancelWithPolicy(eventId, base)  — Ask the user to confirm cancelling a booking.
L7141  async _readyForWholeCancel(eventId)  — Before a whole-booking cancel reads its record ids (call it AFTER…
L7148  async cancelBikeSlot(slotId, eventId)  +70
L7219  async confirmUnbook(bookingId, eventId, btn)  +84
L7305  applyBookedState(btn, eventId, booking)  +31
L7342  _spotsLeft(evt)  {discover}  — Places still open in a class, or null when the API did not say.
L7354  const SPOTS_MAX_AGE_MS  {discover}  — A count is only printed while the data behind it is recent: the list…
L7355  _countsFresh(dataAt, now)  {discover}
L7367  _spotsHtml(evt, held, fresh)  {discover}  — A card's availability line, in the redesign's wording ('' = no line).
L7381  const TIME_BANDS  {discover}  — Time-of-day bands (the Time filter row).
L7386  _timeBandOf(startAt)  {discover}
L7393  _inTimeBands(startAt, bands)  {discover}  — No band chosen = any time.
L7409  _mergeInsertPoints(existingStarts, newStarts)  {discover}  — Where each new card goes among a day's cards already on screen.
L7427  const PAGER_MAX_DAYS  {day-pager}  — A range of more than one day is shown ONE DAY AT A TIME: a strip of…
L7428  const _PAGER_WEEKDAYS  {day-pager}
L7429  const _PAGER_MONTHS  {day-pager}
L7431  _pagerIsDay(day)  {day-pager}
L7436  _pagerAddDays(day, n)  {day-pager}  — 'YYYY-MM-DD' n days on ('' for anything that is not a day).
L7444  _pagerDays(start, end)  {day-pager}  — Every day of start..end, in order.
L7455  _pagerDayList(start, end, eventDays)  {day-pager}  — The days to page over.
L7469  _pagerDayLabel(day, todayStr)  {day-pager}  — What a day is called.
L7484  _pagerPillParts(day, todayStr)  {day-pager}  — The strip pill as the Crisp Colour boards draw it: a small word over…
L7500  _pagerHeldDays(bookings, cache, days, o)  {day-pager}  — The days of the strip on which the member holds a SEAT → the…
L7528  _pagerHeldSpoken(held)  {day-pager}  — "…, 14 classes.
L7533  _pagerCountText(n)  {day-pager}
L7540  _pagerSpoken(day, todayStr, n, state)  {day-pager}  — "Saturday 20 September, 14 classes" — what a screen reader hears on…
L7557  _pagerEmptyState(day, n, o)  {day-pager}  — A day with nothing to show is not always a day with no classes.
L7572  _pagerOpensText(opensMs, todayStr)  {day-pager}  — "Booking usually opens Monday 12 October, 12:00" — the release that…
L7578  _pagerFirstWithClasses(days, counts)  {day-pager}
L7583  _pagerStep(days, current, dir)  {day-pager}  — The neighbouring day; null at either end of the range (the pager…
L7591  _pagerJump(days, counts, current)  {day-pager}  — Where an empty day sends the member: the next day with classes, else…
L7607  _pagerPickDay(s)  {day-pager}  — Which day is on screen.
L7619  _pagerHintWanted(f)  {day-pager}  — The one-time "Swipe to change day" line: a touch screen, the first…
L7627  let _cardCountsFresh  — Are the counts in this render() pass recent enough to print?
L7644  let _countsExpireTimer  — …and a page that simply STAYS visible (a desktop tab kept open for…
L7645  _armCountsExpiry(dataAt)
L7656  eventCard(evt, instrMap, studioMap, locationMap, typeMap)  +77
L7738  let _lastResultsTimer  — Feature 13's write, behind a trailing debounce: every filter tap…
L7739  _saveLastResultsSoon(events, relations, filters, dataAt)
L7759  render(events, relations, filters, done)  +175

### Discover: one day at a time (L7935)
L7957  let _pagerModel
L7958  let _pagerStripLeft
L7959  let _pagerStripFocused
L7960  let _pagerSwap
L7961  let _pagerSettleTimer
L7962  let _pagerHintLive
L7963  let _pagerSawWelcome
L7964  const PAGER_HINT_KEY
L7965  const PAGER_SLIDE_MS
L7967  _pagerModelFor(byDay, filters, done, dataAt, maps, heldEnd)  +32
L8003  _paintDays(container, m)  — Lay the model out in #results.
L8027  _paintDayGroup(host, day, m)  +93  — One day: its heading and its cards, built — or brought up to date in…
L8127  _heldDaysFor(m)  — The strip's class-colour dots: a day on which the member holds a…
L8142  _paintDayDot(pill, held)  — One pill's dot, in place: added, re-coloured or taken off.
L8156  _repaintDayDots()  — A booking made, cancelled or loaded changes which days are held…
L8174  _paintDayStrip(container, m)  +73  — The strip: one tab per day of the range, each with its…
L8250  _revealDayPill(strip)  — Bring the selected day into view inside the strip — the ROW's own…
L8266  _restoreDayStrip()  — switchTab('discover') pays what _paintDayStrip owed…
L8282  _revealPillsAfterThemeChange()  — A theme re-measures every pill without the strip being rebuilt:…
L8294  _ensureDayPager(container, m)  — The pager: the tab panel of whichever day is selected.
L8313  _paintDayHint(pager, m)  +32  — "Swipe to change day" — once, ever.
L8346  _dismissDayHint()
L8354  _wireDayPager(container)  — One click listener on #results (it outlives every wipe): day pills,…
L8369  _dayStripKeydown(e)  — Left / Right / Home / End with focus in the strip.
L8382  _pagerReducedMotion()
L8391  _pagerScrollToTop()  — A change of day puts the top of the day under the strip — and moves…
L8413  showDiscoverDay(day, how)  — Show another day of the loaded range.
L8434  stepDiscoverDay(dir, how)
L8444  _swapPagerDay(container, dir)  +48  — Old day out, new day in: a short slide the way the days run.
L8496  _flushPagerSwap()  — A day still sliding out has not been painted yet.
L8504  _resetDayTrack(track)
L8515  window._dayPagerSwipe = {…}  +43  — keys: canStart, edges, drag, release — The swipe, as js/interactions.js's _psycleSwipe reports it (that helper owns the gesture rules — horizontal intent, the 25% / flick release, resistance at an…

### Studio multi-select chips (L8559)
L8561  const selectedLocations  — selectedLocations: Set of location IDs (strings).

### Faceted filter counts (L8563)
L8570  discoverFacets()  — The filter controls show LIVE match-counts from the fetched…
L8592  refreshFacetCounts()
L8603  _facetCounts(dim)  — value -> count map for one dimension, or null before the first search.
L8611  renderLocationChips()
L8629  toggleLocation(id)
L8642  updateLocationHint()

### Feature 13: Restore persisted search results (L8651)
L8652  restoreLastResults()  +30

### Instructor multi-select widget (L8683)
L8697  _cleanStoredId(v)  {stored-data}  — An id, as it may safely sit in an attribute or a quoted handler…
L8703  _cleanStoredIdList(v, max)  {stored-data}
L8715  _cleanStoredNumbers(v)  {stored-data}  — Seat numbers: numbers, or numeric text — nothing else becomes one…
L8729  const STORED_HISTORY_TEXT_KEYS  {stored-data}  — psycle_class_history: plain entries of primitives plus `slots`, a…
L8730  _cleanStoredHistory(v)  {stored-data}
L8751  _cleanStoredTiers(v)  {stored-data}  — psycle_instructor_tiers: { id: 'S'…'F' } and nothing else.
L8763  _cleanStoredBikePrefs(v)  {stored-data}  — psycle_bike_prefs: { studioId: { avoid: [n…], prefer: [n…] } } —…

### Favourite instructors (persisted to localStorage) (L8777)
L8778  const FAV_KEY
L8780  loadFavourites()
L8789  _saveSetting(key, json)  — Small hand-entered settings (stars, rankings, bike prefs).
L8799  saveFavourites(favs)
L8819  setFavourite(id, on)  — One star on or off.
L8826  toggleFavourite(id, e)
L8835  applyFavouritesAsFilter()
L8850  _topTierInstructorIds()  {tier-filter}  — Ids of the LOADED instructors the member ranked S or A. The tier map…
L8862  applyTierFilter()  {tier-filter}  — "S/A" beside "★ Favs": the same one-tap filter, from the tier…
L8876  _syncTierFilterBtn(ids)  {tier-filter}  — Shown only while at least one loaded instructor is ranked S or A —…
L8882  getFilteredInstructors()
L8887  renderInstrChips()
L8905  renderInstrDropdown()  +39
L8945  toggleInstructor(id)
L8955  removeInstructor(id)
L8966  focusInstrSearch(e)
L8970  filterInstrDropdown()
L8975  showInstrDropdown()
L8980  hideInstrDropdown()
L8986  instrKeydown(e)

### Category pill filter (L9010)
L9011  renderCategoryPills()
L9033  toggleCategory(key)

### Time row: time-of-day bands + "Available only" (L9043)
L9048  const selectedTimeBands  — Onboarding promised filtering "by time" and there was none: an…
L9049  let _availableOnly
L9053  renderTimePills()  — The row reuses .location-chips + .cat-pill (css/redesign.css styles…
L9064  toggleTimeBand(key)
L9075  toggleAvailableOnly()  — triggerAutoSearch re-filters at once and, when the window's numbers…
L9086  clearTimeFilters()  — Lift the Time row only (the empty list's button): the instructor,…
L9099  setTimeFilters(bands, availableOnly)  — Saved Time row → state.
L9114  _admitTimeBands(startAts)  — A flow that searches FOR the member around one time of day ("Same…

### Upcoming bookings panel (L9137)
L9139  refreshUpcomingPanel()  — Keep backward compat — other modules call refreshUpcomingPanel
L9141  let _showPastBookings
L9143  togglePastBookings()

### Skeleton loading for My Bookings (L9148)
L9149  showBookingSkeleton(count)

### Countdown helper for imminent classes (L9172)
L9173  let _gymDayFmt
L9177  _gymDayKey(ms, addDays)  — 'YYYY-MM-DD' on the gym's calendar for an instant, `addDays` later.
L9191  getCountdownText(eventDate, now, startAt)  +40  — `startAt` is the API's raw (naive UK wall-clock) string.

### Saved copy of My Bookings (L9232)
L9242  const BOOKINGS_SNAPSHOT_MAX_ITEMS  {offline}
L9243  const BOOKINGS_SNAPSHOT_MAX_AGE_MS  {offline}
L9245  _snapshotText(v, max)  {offline}
L9251  _coerceSnapshotItem(it)  {offline}  — One item → exactly the shape the painter relies on, or null.
L9274  _coerceBookingsSnapshot(raw, nowMs)  {offline}  — What is in storage → { owner, savedAt, items }, or null:…
L9289  _buildBookingsSnapshot(bookings, eventCache, prev, owner, nowMs)  {offline}  — The record of what is held right now.
L9315  _snapshotItemsToShow(items, nowMs, startMsOf)  {offline}  — Saved classes still worth showing: until the class has ENDED — a…
L9323  _snapshotLabel(savedAt, nowMs)  {offline}  — "Saved copy · 14:05" — with the day once it is no longer today's.
L9334  _savedBookingsHTML(items, label, waiting)  +47  {offline}  — Read-only cards in the live list's markup (same day groups and card…
L9383  const BOOKINGS_SNAPSHOT_KEY
L9385  _readBookingsSnapshot()
L9389  _clearBookingsSnapshot()
L9396  _saveBookingsSnapshot()  — Only from a map Psycle has confirmed this session ('loaded'): before…
L9409  _paintSavedBookings(panel, list, countEl, emptyEl)  — Called from renderMyBookings' empty branch.
L9436  retrySavedBookings(btn)  — Retry on the saved copy: whichever step never got an answer.
L9479  _classHasStarted(startAt, nowMs, startMsOf)  {bookings-started}  — Has this held class started?
L9493  _mbShortDay(dayKey)  {bookings-card,bookings-crisp}  — 'YYYY-MM-DD…' (a London date, straight off start_at) → "Thu 24".
L9507  _mbActionPlan(f)  {bookings-card,bookings-crisp}  — Which actions a held class shows, and which sit behind "More".
L9526  _mbMoreName(typeName, keys)  {bookings-card,bookings-crisp}  — The More button's name says what is behind it: "More for Ride 45:…
L9534  _mbMoreKeyStep(key, index, count)  {bookings-card,bookings-crisp}  — Arrow keys inside the open menu: the item to focus next, or null…
L9545  _mbPeriodWord(startMs, endMs)  {bookings-card,bookings-crisp}  — What a billing period is called: Psycle bills by the month, some…
L9559  _mbUsageModel(o)  {bookings-card,bookings-crisp}  — "8 of 12 this month · Resets 12 Oct" — the plan-usage line over the…
L9591  _mbUsageHtml(model, esc)  {bookings-card,bookings-crisp}  — …and its markup.
L9610  renderMyBookings()  +446
L10066  _bookingsHtmlKey(html)  {render-perf}  — The list minus the one thing the minute tick rewrites in place: two…
L10072  _countdownChipText(startAt, nowMs)  {render-perf}  — What the chip of a class starting at `startAt` (the API's raw…
L10081  _tickCountdownChips(root, nowMs)  {render-perf}  — null = started (or no longer today / tomorrow): that card is about…
L10095  _mbInFlight(b)  {render-perf}  — Is this button waiting on Psycle?
L10110  _bookingButtonsIntact(list, nowMs)  {render-perf}  — A list that stays up keeps its buttons as they are.
L10132  _commitBookingsHtml(list, html, nowMs)  +31  {render-perf}  — renderMyBookings' last step: put `html` on screen unless it is there…
L10165  _anyInstantBetween(times, sinceMs, nowMs)  {render-perf}  — True when one of `times` (ms; NaN / null are skipped) fell in…

### The minute tick (L10170)
L10179  let _minuteTimer  — Nothing on the page followed the clock except the floating pill…
L10185  _bookingsFlipDue(sinceMs, nowMs)  — Has a card changed by itself since the list was last known right?
L10205  _bookingsListBusy(list)  — A timed rebuild must not land under the member's hands: a dialog or…
L10215  _tickLastUpdated()  — "Updated 3m ago": only the words.
L10222  _minuteTick()
L10234  _armMinuteTick()

### My Bookings: the card's "More" menu (L10251)
L10263  let _mbMoreOpen  — Add spot · Find similar · Map · Share sit behind one button on a…
L10265  _mbMoreItems(menu)
L10269  closeBookingMore(focusBack)
L10279  toggleBookingMore(btn)
L10294  window.toggleBookingMore = toggleBookingMore
L10295  window.closeBookingMore = closeBookingMore
L10298  _mbMoreState()  — A rebuild can take the open menu's nodes away: that is a closed menu.

### Open a booking's studio in the maps app (L10354)
L10355  window.openMapForBooking(eventId)
L10374  _eventCacheEntry(e, relations, existing)  {bookings-card}  — _eventCache entry for one /events row, carrying the display names…
L10395  async rebookNextWeek(eventId)  +140

### Change Spot (L10536)
L10538  async window.changeSpot(eventId)  +103  — Opens bike picker.
L10645  renderChangeSpotHint()  — Render (or re-render) the modalHint for a Change-spot flow.
L10674  window.setChangeSpotTarget(slot)
L10693  _adjustBikeHistoryForSwap(eventId, oldSlot, newSlot)  — Keep the "usual spot" memory honest after a swap: the seat the user…
L10709  let _swapInFlight
L10710  async executeSpotSwap()  +168

### Find Similar popup (L10879)
L10880  window.findSimilar(eventId)  +177

### Share a class (L11058)
L11059  window.shareClass(eventId)  +48
L11108  async upcomingCancel(eventId, btn)  +89
L11198  async upcomingSeatCancel(eventId, slotId, btn)  +62

### A held class whose studio isn't known yet (L11263)
L11271  _studioNeedsReread(eventId)  — A booking painted from last launch's saved details (_fromSnapshot)…
L11281  const _studioRereads  — Re-read THAT class, under the deadline a booking tap waits for…
L11282  _ensureStudioKnown(eventId)
L11303  _pickerTakenSince(seqAtTap)  — Latest tap wins (see bookClass) — for the taps that wait BEFORE a…

### Class Detail Sheet (L11309)
L11322  const _sheetActionBusy  — The sheet's Book / booked button.
L11323  async _classDetailBookAction(eventId)  +45
L11375  async _classDetailClaimAction(eventId)  — The sheet's "Claim spot" (a waitlist place with an offer showing).
L11384  window.openClassDetail(eventId)  +225

## Feature: Weekly Template Booking Engine ("Your usual week") (L11627)
L11640  const WEEKLY_TEMPLATE_KEY  — localStorage 'psycle_weekly_template' = array of { dayOfWeek:0-6…
L11642  loadWeeklyTemplate()
L11649  saveWeeklyTemplate(arr)
L11655  clearWeeklyTemplate()
L11666  const TEMPLATE_TOLERANCE_MIN  {template}  — A template entry is a weekday plus a WALL-CLOCK time, and every…
L11670  _templateWall(startAt)  {template}  — { date: 'YYYY-MM-DD', min: minutes past midnight } of a naive API…
L11677  _templateAddDays(dateStr, n)  {template}  — Calendar arithmetic on 'YYYY-MM-DD' through UTC, where every day has…
L11683  _templateDow(dateStr)  {template}  — 0=Sun..6=Sat of a 'YYYY-MM-DD'.
L11691  _templateLondonNow(nowMs)  {template}  — London's calendar date and minutes past midnight at an instant — the…
L11707  const TEMPLATE_MAX_SEATS  {template}  — Seats per class.
L11715  _templateSeats(v, max)  {template}  — A saved entry's seat count: an integer 1–4, anything else 1 —…
L11724  _templateSeatsHeld(held)  {template}  — How many seats a held entry is: its seats, or — a studio with no…
L11733  _templatePickSlots(v)  {template}  — The slot ids a pick may send: whole positive numbers, each once, at…
L11748  _templateRanges(nowMs, horizon, wantNewest)  {template}  — The date ranges the sheet offers, each 7 days, in the order shown:…
L11767  _templateEntryHeld(entry, date, bookings, cache, tol)  {template}  — Is this entry's class already held on `date`?
L11788  _templateDefaultStart(nowMs, ctx)  {template}  — Which range the sheet opens on: the newly opened batch when it leads…
L11803  _templateDateFor(entry, start, now)  {template}  — The date a template entry falls on within the 7 days from `start`.
L11832  _templatePlanRow(entry, events, ctx)  +43  {template}  — One template entry × one day's /events rows → the class it means and…
L11882  _templateFromSeats(bookings, cache, now, resolveLocationId)  +31  {template}  — "Save my usual week": the REAL seats (never waitlist places) held…
L11922  _templatePlanCaution(f)  {template}  — ONE advisory line when the ticked seats are more than Psycle's own…
L11965  _spotSuggestion(o)  +66  {template-spots}  — AN OPINION ON THE SPOTS — SHOWN, NEVER SILENT.
L12033  _spotWhyText(s, labelOf)  {template-spots}  — …and WHY, in a few words.
L12054  _resolveTemplateLocationId(id)  — Resolve a template's stored id (which may be a real location id OR a…
L12070  _upcomingWeekdayDate(dayOfWeek, fromDate = new Date(), timeMinutes = null)  {core}  — Date of the given weekday (0=Sun..6=Sat) within the upcoming 7 days…
L12089  _templateLocationIdFor(evt)  — A cached class → the real location id a template entry stores:…
L12102  async _bookEventHeadless(eventId, studioId)  +73  — Headlessly book a single resolved event the way rebookNextWeek does…
L12179  _templateRefusalText()  — Psycle's own words for a booking it refused — what submitBooking has…
L12221  async _bookTemplateSeat(eventId, studioId, want)  +121  — ONE class the member ticked in the "usual week" sheet — booked…
L12369  let _templateBookingInFlight  — Executes what the member confirmed in the "usual week" sheet — and…
L12370  async bookWeeklyTemplate(picks, hooks)
L12384  async _bookWeeklyTemplateInner(counts, picks, hooks)  +79
L12468  _fetchTemplateDay(dayStr, locId, dayCache)  +40  — One day's /events at one location, shared between entries through…
L12533  async planWeeklyTemplate(weekStart, opts)  +77  — READ-ONLY plan for "Book my usual week": every template entry…
L12614  detectRecurringSlots()  +63  — Analyse psycle_class_history for day-of-week + time + type slots…
L12690  async templateSpotsFor(eventId, studioId)  +39  — READ-ONLY: what the sheet needs to show its opinion on the spots of…
L12733  templatePlanCaution(items)  — The sheet's ONE advisory line about the plan (pure:…
L12748  window.loadWeeklyTemplate = loadWeeklyTemplate
L12749  window.saveWeeklyTemplate = saveWeeklyTemplate
L12750  window.clearWeeklyTemplate = clearWeeklyTemplate
L12751  window.planWeeklyTemplate = planWeeklyTemplate
L12752  window.bookWeeklyTemplate = bookWeeklyTemplate
L12753  window.templateSpotsFor = templateSpotsFor
L12754  window.templatePlanCaution = templatePlanCaution
L12755  window._bookingHorizon = _bookingHorizon
L12756  window.detectRecurringSlots = detectRecurringSlots

## Feature: Saved / Recent searches + presets (L12758)
L12763  const RECENT_SEARCHES_KEY  — 'psycle_recent_searches' = array (cap 5), deduped by a signature of…
L12764  const RECENT_SEARCHES_CAP
L12767  _currentSearchState()  — Snapshot the live filter globals into a serialisable object.
L12787  _searchSignature(s)  {core}  — Order-independent signature for dedup: sorted ids + sorted…
L12799  _searchLabel(s)  — Human label for a saved/recent search pill.
L12829  getRecentSearches()
L12841  _recordRecentSearch()
L12853  getSearchPresets()  +50  — Built-in presets.
L12905  _syncFilterUI()  — Re-sync the filter UI to the current globals after a programmatic…

### The member's own filters, while a shortcut's are on screen (L12917)
L12928  const FOCUS_STASH_DIMS  — _focusSearch (below) replaces the studio / class-type / Time-row…
L12933  _dropFocusStash(dim)
L12939  _restoreFocusStash()
L12975  _focusSearch(o)  +56  — The "find it" shortcuts (Find similar, Book again, View schedule /…
L13034  applySavedSearch(obj)  +34  — Apply a saved search object: set the filter globals + inputs,…
L13070  applySearchPreset(key)  — Apply a preset by key, then search.
L13081  renderDiscoverPresets()  +33  — Render preset + recent-search pills inside the Discover empty state.
L13132  window.getRecentSearches = getRecentSearches
L13133  window.applySavedSearch = applySavedSearch
L13134  window.getSearchPresets = getSearchPresets
L13135  window.applySearchPreset = applySearchPreset
L13136  window.renderDiscoverPresets = renderDiscoverPresets

## Feature: First-run welcome (full screen, four swipeable pages) (L13138)
L13141  const ONBOARDING_KEY
L13151  _welcomeDecision(s)  {welcome}  — Should the welcome open at launch?
L13178  _welcomePages(native, touch, platform)  {welcome}  — The four pages: a title and ONE sentence each — except the first,…
L13190  _welcomeClamp(idx, count)  {welcome}
L13197  _welcomeSwipeAxis(dx, dy)  {welcome}  — One finger, two possible owners: the page under it (vertical) or the…
L13206  _welcomeDragOffset(idx, count, dx)  {welcome}  — How far the pages follow the finger: all the way between pages, a…
L13213  _welcomeSwipeTarget(idx, count, dx, width, ms)  {welcome}  — Where a released drag lands: past a quarter of the width, or a quick…
L13222  _welcomeDayLabels(y, m, d, n)  {welcome}  — Labels for the miniature day strip, from a calendar date the caller…
L13233  let _onboardIdx
L13234  let _onboardPages
L13235  let _onboardOpener
L13236  let _onboardTurnedAt
L13237  let _onboardLaunchSettled
L13239  _onboardNative()
L13244  _onboardPlatform()  — Which native app, in Capacitor's words ('ios' | 'android'); '' when…
L13250  _onboardCleanup(only)  — `only`: the overlay a fade-out was started on.
L13262  _onboardFinish(thenSignIn)
L13273  _onboardButtons(ov)  — The buttons Tab may land on right now (Back is `hidden` on the first…
L13281  _onboardKey(e)  — Own keys, like confirmModal (_ownKeysOverlayUp makes the shared…
L13301  _onboardSignedOut()  — Signed out = no session at all.
L13305  _onboardGo(idx)
L13315  _onboardRender(quiet)  — Updated in place, never rebuilt: the dialog keeps its focus and the…
L13348  _onboardPic(ct, size)  — Miniatures of the app's own components, built from the Crisp Colour…
L13352  _onboardMiniCard(cls, ct, time, name, sub, extra, action)
L13360  _onboardArt(id)  +45
L13408  const _BRAND_MARK_SVG  — The mark, as the header draws it (psycle-finder.html) — its own mask…
L13410  _onboardPageHTML(page, i)
L13429  _onboardBindSwipe(ov)  +32  — Pages follow the finger.
L13464  _onboardReveal()  — The last step of opening: keys and focus.
L13485  startOnboarding(opts)  +53  — opts.atLaunch — opaque from its first frame (a fade-in would show…
L13541  replayOnboarding()  — Settings → "Show the welcome again".
L13544  window.replayOnboarding = replayOnboarding
L13548  _onboardLaunchFacts(early)  — What a launch can know about the person in front of it.
L13584  _maybeStartOnboarding(early)  +35  — First-run trigger: only for genuinely new users.

## Feature: Timezone-aware travel notice (L13622)
L13627  const TRAVEL_NOTICE_DISMISS_KEY  — Classes are London time.
L13630  londonOffsetDeltaMinutes(at = new Date())  — Minutes that local time is ahead of London right now (London ahead →…
L13649  _isAwayFromLondon()
L13657  renderTravelNotice()
L13694  window.renderTravelNotice = renderTravelNotice
L13695  window.londonOffsetDeltaMinutes = londonOffsetDeltaMinutes

## Feature: Rebook prediction (L13697)
L13710  _predictFrom(history, heldEvents, nowMs, typeByName, instrByName)  +87  {predict}  — The scoring behind predictNextClass().
L13801  predictNextClass()  — Returns the single best-predicted recurring class, or null.
L13827  bookPrediction(pred)  — One-tap path into search for a predicted class: set type/instructor…
L13845  renderRebookHint()  +39  — Render the gentle "Book again?" hint on the My Bookings tab.
L13892  window.predictNextClass = predictNextClass
L13893  window.bookPrediction = bookPrediction
L13894  window.renderRebookHint = renderRebookHint

### Extend findSimilar with the predicted "book again" suggestion (L13903)
L13906  (iife) (anonymous)  +34  — Monkey-patch (the original is defined above) so the popup gains one…
L13907  const _origFindSimilar
L13909  window.findSimilar(eventId)  +30

### Wire the new Discover-tab surfaces into auth + first run (L13941)
L13943  (iife) (anonymous)  — Re-render presets/notice when the discover empty state updates (sign…
L13944  const _origUpdateDiscover
L13957  (iife) (anonymous)  — Record the search into recents once it completes (signed-in only).
L13958  const _origSearch
L13960  async wrapped()
L13970  window.search = wrapped

## Android: the hardware / gesture Back (L13984)
L14016  _androidBackDecision(s)  {android-back}  — What ONE Back press does, given what is on screen.
L14032  _androidBackDialog(s)  {android-back}  — Which of the dialogs with their own keys a 'close-dialog' answers —…
L14046  _androidBackWelcomeControl(pageIdx)  {android-back}  — Which control of the welcome a Back press is.
L14057  _androidBackAsksMember(s)  {android-back}  — A dialog that waits on the MEMBER — the welcome, a confirm — is…
L14066  var ANDROID_BACK_WAIT_MAX_MS  {android-back}  — A request is in flight, and this is still the same stretch of…
L14067  _androidBackWaits(busy, sinceMs)  {android-back}
L14074  _androidBackLive(id)  — An overlay that is fading out is already answered (confirmModal, the…
L14091  _androidBackBusy()  — A booking, a waitlist join / leave / claim or a cancel waiting on…
L14100  _androidBackFacts()  +34
L14139  _androidBackEscape(target)  — A real Escape keydown: on `document` it is served by whichever…
L14148  _androidBackFocusWithin(el)  — Keyboard, switch-access and TalkBack focus.
L14152  _androidBackFocus(el)
L14156  let _androidBackWaitingSince
L14157  let _androidBackWatch
L14165  _androidBackWatchWaiting()  — One stretch of waiting ends when the WAITING ends — not at the next…
L14177  window._psycleAndroidBack()  +79

### PWA Service Worker (L14257)
L14272  _swSkippedHere()  — What happens once a NEW worker has taken over (reload, or a "new…
