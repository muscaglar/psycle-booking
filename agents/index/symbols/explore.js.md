# js/explore.js — symbols
Purpose: the shape of `js/explore.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

1345 lines · 51 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.

L19  escapeHtml(s)
L21  var HISTORY_KEY
L22  var TIER_KEY
L23  var SYNC_KEY
L24  var _exploreDirty
L25  var _syncing
L26  var _syncLabel
L29  getHistory()  — Parse class history from localStorage.
L45  setHtml(el, html)  — The one door for every write to the four explore sections.
L61  timeWindow(hour)  — Bucket an hour-of-day into a coarse time window.
L68  var DAY_NAMES
L69  var WINDOW_LABELS
L75  slotOf(iso)  — Safely derive { day, hour, window } from an ISO datetime string.

## DATA LAYER (L83)
L91  buildNameIndex()  — Build a name → id reverse lookup from the instructors array.
L110  gatherInstructorProfiles()  +83  — Gather a comprehensive profile for every known instructor.
L200  getUserScheduleProfile()  — Derive the user's habitual booking schedule from their class history.
L232  getBookedInstructorIds(profiles)  — Build the set of instructor IDs the user has booked with.

## SECTION 1: "New to you" (L257)
L261  computeNewToYou(profiles)
L287  signedOutBlank()  — Signed out with nothing on record — the state renderInsights shows its one sign-in hero for.
L292  renderNewToYou(container, profiles)

## SECTION 2: "You might like" (L321)
L327  var YML_WEIGHTS  — Blend weights for the three recommendation dimensions.
L334  var FRESHNESS_DAYS  — 30 days un-booked before the lightweight "haven't tried recently" boost kicks in.
L341  scoreAffinity(p, refClassTypes, refLocations, maxTypeMatch, maxLocMatch)  — (a) TALENT/AFFINITY — how similar a candidate is to the reference set (favourites + highly-tiered + frequently-booked…
L358  scoreTiming(p, sched)  +39  — (b) TIMING FIT — fraction of the candidate's upcoming events that land in the user's habitual day/time windows,…
L403  scoreAvailability(p, maxUpcoming, now)  — (c) AVAILABILITY/FRESHNESS — more upcoming classes = more actionable, with a light boost for instructors the user…
L416  computeYouMightLike(profiles)  +82
L499  renderYouMightLike(container, profiles)
L535  buildWhyLine(c)  — Build the short "why" line for a recommendation, e.g. "Teaches Tue & Thu mornings · 4 classes this week" Falls back…
L564  getShortCategoryName(typeName)

## SECTION 3: "Your instructor map" (L572)
L576  computeInstructorMap(profiles)  +78
L655  renderInstructorMap(container, profiles)  +113

## SHARED: Instructor card renderer (L769)
L773  instrCard(profile, whyLabel)  +53

## HISTORY SYNC — Fetch full booking history from Psycle API (L827)
L831  hasSynced()
L835  renderSyncBanner(container)  +50
L886  window._explore_openSettingsForInstructor(name)
L902  window._explore_resetSync()
L915  var TOPUP_MAX_NEW  — opts.silent — the weekly top-up below: the same sync, with nothing said and nothing repainted unless it actually found…
L920  async window._explore_syncHistory(opts)  +269  — Probe the API for past bookings using common codexfit patterns.

### Silent weekly top-up (L1190)
L1198  var TOPUP_AFTER_MS  — features.js adds every UPCOMING seat to history on each bookings fetch, but a class booked elsewhere AND attended…
L1199  var TOPUP_RETRY_MS
L1200  var TOPUP_DELAY_MS
L1205  var TOPUP_AT_KEY  — When a top-up last STARTED (device-local; not exported or mirrored).
L1206  var _topUpTried
L1213  var TOPUP_SKIPPED_KEY  — {at: ISO, count} — a top-up found more unknown classes than it may fetch (TOPUP_MAX_NEW).
L1214  _noteTopUpTooBig(count)
L1217  _clearTopUpTooBig()
L1221  _topUpTooBig()  — → {at: ms | NaN, count: whole number, 0 when there is no readable note}
L1236  _topUpMayStart(now, lastSync, lastTried, lastTooBig, afterMs, retryMs)  {history-topup}  — May a silent top-up start now?
L1244  maybeTopUpHistory()

## MASTER RENDER (L1276)
L1280  window.renderExplore()  +36

## REACTIVITY: Mark dirty on data changes, re-render if tab active (L1317)
L1321  markDirtyAndMaybeRender()
