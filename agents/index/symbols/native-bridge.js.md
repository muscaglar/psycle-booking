# ios-app/www/native-bridge.js — symbols
Purpose: the shape of `ios-app/www/native-bridge.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `9573454`). Do not edit; rebuild with `npm run agents:index`.

2633 lines · 170 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## Native Bridge — Capacitor integration layer (L1)

### Gym Time (Europe/London) (L16)
L37  var GYM_TZ
L38  var _gymWallFmt
L48  _gymWallToUtcMs(y, mo, d, h, mi, s)  — Absolute UTC ms for a wall-clock time in the gym's zone (DST-correct, independent of the device timezone).
L79  _classStartMs(startAt)  — Absolute UTC ms of an API class time.
L95  window._psycleClassStartMs = _classStartMs

### Calendar ownership decisions (pure) (L97)
L107  _calForeignEvents(events, calId, markerOf)  — Unmarked events in calendar `calId` — what a full-ownership reconcile would delete that isn't ours.
L114  _calSweepVictimIds(events, calId, ownedAck, markerOf)  — Native ids to clear out of `calId` when it stops being the sync target.
L137  _calAckCovers(stored, calId)  — Does the stored ownership ack hand over calendar `calId`?
L140  window._psycleCalForeignEvents = _calForeignEvents
L141  window._psycleCalSweepVictimIds = _calSweepVictimIds
L142  window._psycleCalAckCovers = _calAckCovers

### Platform (L152)
L170  var PLATFORM  — Both native apps run this file.
L174  var IS_ANDROID
L176  var Preferences
L177  var Haptics
L178  var Share

### Fix Login Flow (L193)
L196  window.openLoginPopup()  — window.open() in WKWebView opens Safari, breaking postMessage.

### Fix window.open calls (L200)
L204  var Browser  — calendar.js uses window.open for Google Calendar and ICS blob.
L205  var _origWindowOpen
L206  window.open(url, target)

### Persistent Storage (L221)
L226  var SYNC_KEYS  +45
L277  async restoreFromNative()  — On startup: restore from native storage to localStorage.
L297  syncToNative(key, value)  — Sync localStorage changes to native storage
L307  var _origSetItem  — Intercept localStorage.setItem and removeItem
L308  var _origRemoveItem

### Native Share (L357)
L360  async window.nativeShare(title, text, url)

### Native Calendar Integration (L371)
L408  var Calendar
L409  var CAL_EVENT_MAP_KEY
L410  var CAL_ID_KEY
L411  var CAL_TARGET_KEY
L412  var CAL_MODE_KEY
L413  var CAL_ENABLED_KEY
L421  var CAL_OWNED_ACK_KEY  — '2:<calendarId>' once the user has confirmed that target under the full-ownership contract — only then may the…
L422  _calIsOwned(calId)
L425  var PSYCLE_CAL_TITLE
L426  var PSYCLE_CAL_COLOR
L427  var PSYCLE_EVENT_MARKER
L429  _loadCalMap()
L432  _saveCalMap(map)
L441  _nativeSlotLabel(typeName)  — Slot label for a class type.

### Permissions (L446)
L448  var _calPermissionGranted
L450  async _ensureCalendarPermission()

### Dedicated Psycle Calendar (L470)
L472  async _listNativeCalendars()
L487  hasChosenCalendar()  — Has the user explicitly chosen a calendar to write into?
L491  async _resolveTargetCalendarId()

### Add / Update / Remove Events (L502)
L504  _buildCalEventData(eventId)  +55

### v6-plugin-correct primitives (L560)
L565  _markerEventId(ev)  — Extract the Psycle event id from a native event's notes, or null.
L572  _nativeStartMs(ev)  — Millisecond start time of a native event, across plugin shapes.
L580  _createdEventId(result)  — The created-event id from a createEvent() result, across versions.
L592  async _deleteNativeEvents(ids)  — Delete native events by id.
L624  var _androidDeletedEventIds  — ANDROID ONLY.
L625  _androidRememberDeleted(ids)
L632  async _listNativeEvents(startMs, endMs)  — Normalized listEventsInRange (ms timestamps in, array out).
L644  calendarSyncEnabled()

### Hook into booking / cancel / seat-cancel flows (L652)
L659  var _origSubmitBookingNative
L670  var _origConfirmUnbookNative
L678  var _origUpcomingCancelNative
L686  var _origCancelBikeSlotNative
L694  var _origUpcomingSeatCancelNative

### Full Calendar Reconcile (L702)
L719  async syncAllBookingsToCalendar()  +118
L839  var _origRenderNative  — Run sync after bookings load
L840  var _calSynced
L856  async window.psycleResyncCalendar(opts)  — Force a fresh sync.
L887  async window.psycleCleanupDuplicates()  — Settings → "Remove duplicates".
L896  window.syncAllBookingsToCalendar = syncAllBookingsToCalendar

### Public API for Settings UI (L898)
L901  async window.psycleListCalendars()  — List iOS calendars the user can choose from.
L919  window.psycleGetCalendarConfig()  — Get the current calendar sync config for the Settings UI.
L939  async window.psycleCountForeignEvents(calId)  — READ-ONLY: how many upcoming events in calendar `calId` are NOT Psycle bookings — what handing that calendar over…
L964  async window.psycleSetCalendarConfig(cfg)  +62  — Update the calendar target.

### Weekly Booking Reminder (L1028)
L1033  var LocalNotifications

### Android: notification channels, the small icon and its tint (L1035)
L1053  var ANDROID_CHANNEL_CLASSES  — Android 8+ files every notification under a CHANNEL, which is what the member sees (and can silence) in the system's…
L1054  var ANDROID_CHANNEL_NEW_DATES
L1055  var ANDROID_NOTIF_ICON
L1059  var ANDROID_NOTIF_TINT  — The mark's ink.
L1060  var _androidChannels
L1062  _ensureAndroidChannels()
L1086  _forAndroid(notification, channelId)  — The Android-only fields of one notification.
L1100  var REMINDER_IDS  — Eight rolling one-shot notifications (the next 8 Mondays).
L1105  var WEEKLY_REMINDER_HOUR, WEEKLY_REMINDER_MINUTE  {weekly-reminder}  — WHEN it fires and WHAT it says.
L1106  var WEEKLY_TEMPLATE_KEY  {weekly-reminder}
L1115  _nextMondaysNoonLondon(count, now, wallToUtcMs)  {weekly-reminder}  — The next `count` occurrences of Monday 12:00 Europe/London as absolute Date instants, DST-correct for any device…
L1133  _hasUsualWeek(raw)  {weekly-reminder}  — Is a usual week saved?
L1143  _weeklyReminderCopy(hasUsualWeek)  {weekly-reminder}  — It fires AT the release, so it says the dates ARE open (and still reads true when the banner is seen later).
L1153  _weeklyTapTab(hasUsualWeek)  {weekly-reminder}  — Where the tap lands at once (the web layer's _onBookingWeekOpened does the rest when it can): the usual-week review…
L1162  _weeklyOfferDecision(f)  {weekly-reminder}  — Put the in-app "remind you on Mondays?" question (_offerWeeklyReminder)?
L1171  _usualWeekSaved()
L1175  var REMINDER_PREF
L1179  var _weeklyArmedWithWeek  — Which body the armed reminders carry (true = "Book your usual week…"), null while nothing is known to be armed — so a…
L1181  async _cancelReminders()
L1195  async scheduleWeeklyReminder(interactive)  +42
L1240  var _weeklyChain  — One pass at a time: each cancels the eight ids and schedules them again, and two interleaved passes could leave the…
L1241  _scheduleWeeklySerial(interactive)
L1250  window._nativeReminder = {…}  — keys: isOn, enable, disable — Exposed to the web layer (Membership tab toggle).
L1275  _rearmWeeklyReminder()  — The body names what the tap opens, so it follows the usual week.

### Notification Action Buttons (Book / Cancel / Snooze) (L1296)
L1308  registerNotificationActions()
L1330  var TAP_ROUTE_WAIT_MS  — Land a tapped notification on what it was about.
L1331  var _cancelTapRoute
L1342  _askDiscoverToRefresh()  — The Monday reminder exists for one moment: the release of new dates.
L1351  _routeNotificationTap(eventId)  +53
L1424  handleNotificationAction(notification)  +36  — Route a tapped notification / action button.
L1479  _parseWidgetLink(url)  {widget-link}  — A Home/Lock Screen widget tap opens psync://bookings?event=<id> (minted in PsycleWidget.swift).
L1498  handleWidgetURL(info)  {widget-link}  — Same landing as a class-reminder tap: My Bookings, then that class's sheet (only while the seat is still held).
L1515  var PsycleDeepLink  — The in-app PsycleDeepLink plugin RETAINS the URL until this listener attaches — a widget tap usually cold-launches the…

### Widget / Live Activity / Siri Snapshot (L1523)
L1558  var WIDGET_APP_GROUP  — App Group container id.
L1560  var WIDGET_NEXT_KEY
L1561  var WIDGET_WEEK_KEY
L1566  var WIDGET_UPCOMING_KEY  — Next few classes (same shape as widget_next_class, array of up to 5) so the widget can build a MULTI-ENTRY timeline…
L1571  _prefSet(key, value)  — Standard Preferences plugin: lands in UserDefaults.standard under "<group>.<key>" (NOT extension-readable — kept for…
L1584  _appGroupSet(key, value)  — App Group mirror: writes the SAME logical value under the BARE key into the shared App Group suite, so a native…
L1593  _writeSnapshotKey(key, value)
L1608  var WIDGET_UPCOMING_MAX  {ios-polish}  — The widget timeline shows the next few classes; reminders cover every class held.
L1609  var CLASS_REMINDER_MAX  {ios-polish}
L1613  _isHeldSeat(booking)  {ios-polish}  — A real seat, as opposed to a waitlist place — true even when the class's details failed to load this pass (that makes…
L1622  _evtFromSavedItem(item)  {ios-polish}  — Stand-in for an _eventCache entry, built from app.js's saved copy of My Bookings (psycle_bookings_snapshot), which…
L1640  _staleReminderIds(map, wanted, keep)  {ios-polish}  — Event ids whose armed reminder should go: the class is no longer wanted, or its time moved (it is re-armed for the new…
L1662  var SNAPSHOT_NEUTRAL_TINT  {native-snapshot}  — The card ground at intensity "off": the app's own neutral surface (Cloud / Graphite, css/theme.css) — the widget's…
L1666  _snapHex(value)  {native-snapshot}  — '#2d5fd6' → '#2D5FD6'; anything that is not six hex digits → null.
L1676  _snapClassTypeKey(typeName, keyFn)  {native-snapshot}  — "RIDE: 45" → 'ride', through the app's OWN classTypeKey (js/app.js) — the same answer data-ct gets.
L1696  _snapClassColourFields(key, engine)  {native-snapshot}  — The colour fields for one class type, read from the app's colour engine (`engine` = window.PsycleClassColours: get() →…
L1729  _savedItemsById()  — app.js's saved copy of My Bookings, by event id ({} when there is none).
L1744  _snapshotEventFor(eventId, known)  +35  — Resolve a display-ready event object from the cache, or null.
L1791  var _snapServerConfirmed  — Only trust an EMPTY bookings map after the server confirmed it (bookings:loaded fired this session).
L1793  var _signOutPass  — True only while the clearToken wrapper (a deliberate sign-out) runs its pass.
L1796  var _snapColourSig  — The class colours the last WRITTEN snapshot carries (see the 'classcolours:changed' listener below), and that…
L1797  var _colourSnapTimer
L1798  _classColourSig()
L1807  updateWidgetSnapshot()  +128
L1935  window.updateWidgetSnapshot = updateWidgetSnapshot

### Class-start reminders (T-90 minutes) (L1937)
L1949  var CLASS_REMINDER_PREF
L1950  var CLASS_REMINDER_MAP
L1957  _classReminderTail()  — How the reminder's body ends.
L1961  _classRemindersEnabled()

### The Android countdown's switch (L1965)
L1980  var COUNTDOWN_ENABLED_KEY  — The Android app has no Live Activity.
L1981  var _countdownSwitchSends
L1982  _androidCountdownSwitch()
L1996  _androidCountdownFlipped(sendsBefore)  — The member flipped Class reminders.
L2007  _loadReminderMap()
L2010  _saveReminderMap(map)
L2014  _reminderIdFor(eventId, map)
L2028  var _reminderChain  — Serialized: overlapping snapshot passes (booking events fire in quick succession) must not interleave the…
L2032  _scheduleClassReminders(upcomingList, unknownSeats)  — `unknownSeats` = {eventId: true} for held seats this pass knew nothing about (see _staleReminderIds) — captured with…
L2039  async _scheduleClassRemindersInner(upcomingList, unknownSeats)  +52
L2093  window._nativeClassReminders = {…}  +33  — keys: isOn, hasPermission, enable, disable — Settings toggle API (rendered by tabs.js next to the weekly reminder).

### First-booking ask (L2127)
L2133  var CLASS_REMINDER_ASKED  — The pref defaults ON, but only the Settings switch ever requested notification permission — and it looked on already,…
L2134  var ASK_POLL_MS
L2135  var ASK_GIVE_UP_MS
L2136  var _reminderAskBusy
L2137  var _reminderAskVoid
L2146  var ASK_BLOCKING_IDS  — Anything the ask would land on top of.
L2151  _askBlocked()
L2160  _askStillWanted()
L2164  async _maybeAskClassReminders()  +56

### Usual-week ask: the Monday reminder, offered in context (L2229)
L2240  var WEEKLY_REMINDER_ASKED  — The weekly reminder defaults OFF and its switch sits in Settings, where nobody looks for it.
L2241  var _weeklyAskBusy
L2242  var _weeklyAskSince
L2244  _weeklyOfferFacts(permission)
L2253  async window._offerWeeklyReminder(reason)  +56
L2319  var _origClearTokenNative  — Deliberate sign-out must blank the snapshot and cancel pending class reminders — otherwise the previous account's…
L2361  var _calReconcileTimer, _calReconcileInFlight, _calReconcilePending  — Auto-reconcile the calendar whenever bookings change — covers classes cancelled on the web / another device and…
L2364  async _runCalSync()  — Single-flight calendar sync.
L2373  _scheduleCalReconcile()
L2381  var _calServerConfirmedEmpty  — Tracks whether the latest bookings state came from a successful server fetch.

### Status Bar (L2404)
L2415  _themeGround(registry, themeId, base)  {android-bridge}  — The ground of the theme in force, for the Android status bar: the `bg` of its APP_THEMES entry (js/theme.js) — the…
L2429  updateStatusBar()  +36
L2467  var observer  — Watch for theme changes

## Analytics / Diagnostics (developer-only, no third-party services) (L2472)
L2476  var ACTION_LOG_NATIVE_KEY
L2477  var MAX_NATIVE_ACTION_LOG
L2481  var _origPushAction  — Persist action log to Capacitor Preferences (survives iOS storage purges).
L2490  _persistActionLogToNative()
L2509  async window.getDiagnosticReport()  +121  — getDiagnosticReport — comprehensive diagnostics string for developer use.
