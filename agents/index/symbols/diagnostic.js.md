# js/diagnostic.js — symbols
Purpose: the shape of `js/diagnostic.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `9836e6a`). Do not edit; rebuild with `npm run agents:index`.

519 lines · 31 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## Storage keys (we introduce two; both store field NAMES only, no PII) (L23)
L30  var SCHEMA_LOG_KEY  — psycle_api_schema_log: live, rolling record of observed shapes.
L35  var CONTRACT_KEY  — psycle_api_contract: a frozen snapshot of the schema log taken once after a healthy authenticated load, used as the…
L38  var LAST_DRIFT_KEY  — Where the last drift was observed (ISO) — surfaced in getDiagnostics().
L40  var MAX_FIELDS_PER_KIND
L41  var MAX_KINDS
L42  var CONTRACT_DELAY_MS
L43  var INIT_CHECK_DELAY_MS

### Session-scoped state (reset on reload; not persisted) (L45)
L46  var _recordedAny
L47  var _contractTimer
L48  var _safeModeActive

## Small storage helpers — never throw (L50)
L54  readJSON(key, fallback)
L63  writeJSON(key, value)
L67  nowISO()
L72  softError(msg)  — Safely report an internal problem without ever throwing.

## Shape extraction — field NAMES only, never values (L79)
L90  shapeOf(sample)  — Derive the set of top-level field names that describe `sample`.
L113  getSchemaLog()

## Access to PsycleAPI.SCHEMAS — fully guarded (L118)
L123  getSchemas()  — Return PsycleAPI.SCHEMAS or null if unavailable.
L137  requiredFieldsFor(kind)  — Return the array of required field names declared for a kind, or [].

## 1. record(kind, sampleData) (L149)
L156  record(kind, sampleData)  +30

## 2. captureContract() (L187)
L195  captureContract()
L216  getContract()
L222  scheduleContractCapture()  — Arm a one-shot delayed self-capture if no contract exists yet.

## 3. checkContract() (L232)
L241  checkContract()  +53

## 4. enterSafeMode(reason) (L295)
L302  var SAFE_BANNER_ID
L304  enterSafeMode(reason)  +74
L379  removeBanner()
L392  openDiagnostics()  — Open the diagnostics view.

## 5. getDiagnostics() (L408)
L413  getDiagnostics()  +48

## 6. Self-init — run one contract check after the app's initial calls (L462)
L468  var _initialCheckDone
L470  runInitialCheck()

## Public surface (L507)
L511  window.PsycleDiag = {…}  — keys: record, captureContract, checkContract, enterSafeMode, getDiagnostics
