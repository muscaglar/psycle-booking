# ios-app/www/native-bridge.js — symbols
Purpose: the shape of `ios-app/www/native-bridge.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `754fbb0`). Do not edit; rebuild with `npm run agents:index`.

2547 lines · 166 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## Native Bridge — Capacitor integration layer (L1)

### Gym Time (Europe/London) (L16)
L37  var GYM_TZ
L38  var _gymWallFmt
L48  _gymWallToUtcMs(y, mo, d, h, mi, s)  — Absolute UTC ms for a wall-clock time in the gym's zone (DST-correct, independent of the device timezone).
L79  _classStartMs(startAt)  — Absolute UTC ms of an API class time.
L95  window._psycleClassStartMs = _classStartMs

### Calendar ownership decisions (pure) (L97)
L107  _calForeignEvents(events, calId, markerOf)  — Unmarked events in calendar `calId` — what a full-ownership reconcile would delete that isn't ours.
L114  _calSweepVictimIds(events, calId, ownedAck, markerOf)  — Native ids to clear out of `calId` when it stops being the sync target.
L137  _calAckCovers(stored, calId)  — Does the stored ownership ack hand over calendar `calId`?
L140  window._psycleCalForeignEvents = _calForeignEvents
L141  window._psycleCalSweepVictimIds = _calSweepVictimIds
L142  window._psycleCalAckCovers = _calAckCovers

### Platform (L152)
L162  var PLATFORM  — Both native apps run this file.
L166  var IS_ANDROID
L168  var Preferences
L169  var Haptics
L170  var Share

### Fix Login Flow (L185)
L188  window.openLoginPopup()  — window.open() in WKWebView opens Safari, breaking postMessage.

### Fix window.open calls (L192)
L196  var Browser  — calendar.js uses window.open for Google Calendar and ICS blob.
L197  var _origWindowOpen
L198  window.open(url, target)

### Persistent Storage (L213)
L218  var SYNC_KEYS  +45
L269  async restoreFromNative()  — On startup: restore from native storage to localStorage.
L289  syncToNative(key, value)  — Sync localStorage changes to native storage
L299  var _origSetItem  — Intercept localStorage.setItem and removeItem
L300  var _origRemoveItem

### Native Share (L349)
L352  async window.nativeShare(title, text, url)

### Native Calendar Integration (L363)
L400  var Calendar
L401  var CAL_EVENT_MAP_KEY
L402  var CAL_ID_KEY
L403  var CAL_TARGET_KEY
L404  var CAL_MODE_KEY
L405  var CAL_ENABLED_KEY
L413  var CAL_OWNED_ACK_KEY  — '2:<calendarId>' once the user has confirmed that target under the full-ownership contract — only then may the…
L414  _calIsOwned(calId)
L417  var PSYCLE_CAL_TITLE
L418  var PSYCLE_CAL_COLOR
L419  var PSYCLE_EVENT_MARKER
L421  _loadCalMap()
L424  _saveCalMap(map)
L433  _nativeSlotLabel(typeName)  — Slot label for a class type.

### Permissions (L438)
L440  var _calPermissionGranted
L442  async _ensureCalendarPermission()

### Dedicated Psycle Calendar (L462)
L464  async _listNativeCalendars()
L479  hasChosenCalendar()  — Has the user explicitly chosen a calendar to write into?
L483  async _resolveTargetCalendarId()

### Add / Update / Remove Events (L494)
L496  _buildCalEventData(eventId)  +55

### v6-plugin-correct primitives (L552)
L557  _markerEventId(ev)  — Extract the Psycle event id from a native event's notes, or null.
L564  _nativeStartMs(ev)  — Millisecond start time of a native event, across plugin shapes.
L572  _createdEventId(result)  — The created-event id from a createEvent() result, across versions.
L584  async _deleteNativeEvents(ids)  — Delete native events by id.
L616  var _androidDeletedEventIds  — ANDROID ONLY.
L617  _androidRememberDeleted(ids)
L624  async _listNativeEvents(startMs, endMs)  — Normalized listEventsInRange (ms timestamps in, array out).
L636  calendarSyncEnabled()

### Hook into booking / cancel / seat-cancel flows (L644)
L651  var _origSubmitBookingNative
L662  var _origConfirmUnbookNative
L670  var _origUpcomingCancelNative
L678  var _origCancelBikeSlotNative
L686  var _origUpcomingSeatCancelNative

### Full Calendar Reconcile (L694)
L711  async syncAllBookingsToCalendar()  +118
L831  var _origRenderNative  — Run sync after bookings load
L832  var _calSynced
L848  async window.psycleResyncCalendar(opts)  — Force a fresh sync.
L879  async window.psycleCleanupDuplicates()  — Settings → "Remove duplicates".
L888  window.syncAllBookingsToCalendar = syncAllBookingsToCalendar

### Public API for Settings UI (L890)
L893  async window.psycleListCalendars()  — List iOS calendars the user can choose from.
L911  window.psycleGetCalendarConfig()  — Get the current calendar sync config for the Settings UI.
L931  async window.psycleCountForeignEvents(calId)  — READ-ONLY: how many upcoming events in calendar `calId` are NOT Psycle bookings — what handing that calendar over…
L956  async window.psycleSetCalendarConfig(cfg)  +62  — Update the calendar target.

### Weekly Booking Reminder (L1020)
L1025  var LocalNotifications

### Android: notification channels, the small icon and its tint (L1027)
L1045  var ANDROID_CHANNEL_CLASSES  — Android 8+ files every notification under a CHANNEL, which is what the member sees (and can silence) in the system's…
L1046  var ANDROID_CHANNEL_NEW_DATES
L1047  var ANDROID_NOTIF_ICON
L1051  var ANDROID_NOTIF_TINT  — The mark's ink.
L1052  var _androidChannels
L1054  _ensureAndroidChannels()
L1078  _forAndroid(notification, channelId)  — The Android-only fields of one notification.
L1092  var REMINDER_IDS  — Eight rolling one-shot notifications (the next 8 Mondays).
L1097  var WEEKLY_REMINDER_HOUR, WEEKLY_REMINDER_MINUTE  {weekly-reminder}  — WHEN it fires and WHAT it says.
L1098  var WEEKLY_TEMPLATE_KEY  {weekly-reminder}
L1107  _nextMondaysNoonLondon(count, now, wallToUtcMs)  {weekly-reminder}  — The next `count` occurrences of Monday 12:00 Europe/London as absolute Date instants, DST-correct for any device…
L1125  _hasUsualWeek(raw)  {weekly-reminder}  — Is a usual week saved?
L1135  _weeklyReminderCopy(hasUsualWeek)  {weekly-reminder}  — It fires AT the release, so it says the dates ARE open (and still reads true when the banner is seen later).
L1145  _weeklyTapTab(hasUsualWeek)  {weekly-reminder}  — Where the tap lands at once (the web layer's _onBookingWeekOpened does the rest when it can): the usual-week review…
L1154  _weeklyOfferDecision(f)  {weekly-reminder}  — Put the in-app "remind you on Mondays?" question (_offerWeeklyReminder)?
L1163  _usualWeekSaved()
L1167  var REMINDER_PREF
L1171  var _weeklyArmedWithWeek  — Which body the armed reminders carry (true = "Book your usual week…"), null while nothing is known to be armed — so a…
L1173  async _cancelReminders()
L1187  async scheduleWeeklyReminder(interactive)  +42
L1232  var _weeklyChain  — One pass at a time: each cancels the eight ids and schedules them again, and two interleaved passes could leave the…
L1233  _scheduleWeeklySerial(interactive)
L1242  window._nativeReminder = {…}  — keys: isOn, enable, disable — Exposed to the web layer (Membership tab toggle).
L1267  _rearmWeeklyReminder()  — The body names what the tap opens, so it follows the usual week.

### Notification Action Buttons (Book / Cancel / Snooze) (L1288)
L1300  registerNotificationActions()
L1322  var TAP_ROUTE_WAIT_MS  — Land a tapped notification on what it was about.
L1323  var _cancelTapRoute
L1334  _askDiscoverToRefresh()  — The Monday reminder exists for one moment: the release of new dates.
L1343  _routeNotificationTap(eventId)  +53
L1416  handleNotificationAction(notification)  +36  — Route a tapped notification / action button.
L1466  _parseWidgetLink(url)  {widget-link}  — A Home/Lock Screen widget tap opens psync://bookings?event=<id> (minted in PsycleWidget.swift).
L1485  handleWidgetURL(info)  {widget-link}  — Same landing as a class-reminder tap: My Bookings, then that class's sheet (only while the seat is still held).
L1501  var PsycleDeepLink  — The in-app PsycleDeepLink plugin RETAINS the URL until this listener attaches — a widget tap usually cold-launches the…

### Widget / Live Activity / Siri Snapshot (L1509)
L1532  var WIDGET_APP_GROUP  — App Group container id.
L1534  var WIDGET_NEXT_KEY
L1535  var WIDGET_WEEK_KEY
L1540  var WIDGET_UPCOMING_KEY  — Next few classes (same shape as widget_next_class, array of up to 5) so the widget can build a MULTI-ENTRY timeline…
L1545  _prefSet(key, value)  — Standard Preferences plugin: lands in UserDefaults.standard under "<group>.<key>" (NOT extension-readable — kept for…
L1558  _appGroupSet(key, value)  — App Group mirror: writes the SAME logical value under the BARE key into the shared App Group suite, so a native…
L1567  _writeSnapshotKey(key, value)
L1582  var WIDGET_UPCOMING_MAX  {ios-polish}  — The widget timeline shows the next few classes; reminders cover every class held.
L1583  var CLASS_REMINDER_MAX  {ios-polish}
L1587  _isHeldSeat(booking)  {ios-polish}  — A real seat, as opposed to a waitlist place — true even when the class's details failed to load this pass (that makes…
L1596  _evtFromSavedItem(item)  {ios-polish}  — Stand-in for an _eventCache entry, built from app.js's saved copy of My Bookings (psycle_bookings_snapshot), which…
L1614  _staleReminderIds(map, wanted, keep)  {ios-polish}  — Event ids whose armed reminder should go: the class is no longer wanted, or its time moved (it is re-armed for the new…
L1636  var SNAPSHOT_NEUTRAL_TINT  {native-snapshot}  — The card ground at intensity "off": the app's own neutral surface (Cloud / Graphite, css/theme.css) — the widget's…
L1640  _snapHex(value)  {native-snapshot}  — '#2d5fd6' → '#2D5FD6'; anything that is not six hex digits → null.
L1650  _snapClassTypeKey(typeName, keyFn)  {native-snapshot}  — "RIDE: 45" → 'ride', through the app's OWN classTypeKey (js/app.js) — the same answer data-ct gets.
L1670  _snapClassColourFields(key, engine)  {native-snapshot}  — The colour fields for one class type, read from the app's colour engine (`engine` = window.PsycleClassColours: get() →…
L1703  _savedItemsById()  — app.js's saved copy of My Bookings, by event id ({} when there is none).
L1718  _snapshotEventFor(eventId, known)  +35  — Resolve a display-ready event object from the cache, or null.
L1765  var _snapServerConfirmed  — Only trust an EMPTY bookings map after the server confirmed it (bookings:loaded fired this session).
L1767  var _signOutPass  — True only while the clearToken wrapper (a deliberate sign-out) runs its pass.
L1770  var _snapColourSig  — The class colours the last WRITTEN snapshot carries (see the 'classcolours:changed' listener below), and that…
L1771  var _colourSnapTimer
L1772  _classColourSig()
L1781  updateWidgetSnapshot()  +119
L1900  window.updateWidgetSnapshot = updateWidgetSnapshot

### Class-start reminders (T-90 minutes) (L1902)
L1914  var CLASS_REMINDER_PREF
L1915  var CLASS_REMINDER_MAP
L1920  _classReminderTail()  — How the reminder's body ends.
L1924  _classRemindersEnabled()
L1928  _loadReminderMap()
L1931  _saveReminderMap(map)
L1935  _reminderIdFor(eventId, map)
L1949  var _reminderChain  — Serialized: overlapping snapshot passes (booking events fire in quick succession) must not interleave the…
L1953  _scheduleClassReminders(upcomingList, unknownSeats)  — `unknownSeats` = {eventId: true} for held seats this pass knew nothing about (see _staleReminderIds) — captured with…
L1960  async _scheduleClassRemindersInner(upcomingList, unknownSeats)  +52
L2014  window._nativeClassReminders = {…}  +30  — keys: isOn, hasPermission, enable, disable — Settings toggle API (rendered by tabs.js next to the weekly reminder).

### First-booking ask (L2045)
L2051  var CLASS_REMINDER_ASKED  — The pref defaults ON, but only the Settings switch ever requested notification permission — and it looked on already,…
L2052  var ASK_POLL_MS
L2053  var ASK_GIVE_UP_MS
L2054  var _reminderAskBusy
L2055  var _reminderAskVoid
L2064  var ASK_BLOCKING_IDS  — Anything the ask would land on top of.
L2069  _askBlocked()
L2078  _askStillWanted()
L2082  async _maybeAskClassReminders()  +55

### Usual-week ask: the Monday reminder, offered in context (L2146)
L2157  var WEEKLY_REMINDER_ASKED  — The weekly reminder defaults OFF and its switch sits in Settings, where nobody looks for it.
L2158  var _weeklyAskBusy
L2159  var _weeklyAskSince
L2161  _weeklyOfferFacts(permission)
L2170  async window._offerWeeklyReminder(reason)  +56
L2233  var _origClearTokenNative  — Deliberate sign-out must blank the snapshot and cancel pending class reminders — otherwise the previous account's…
L2275  var _calReconcileTimer, _calReconcileInFlight, _calReconcilePending  — Auto-reconcile the calendar whenever bookings change — covers classes cancelled on the web / another device and…
L2278  async _runCalSync()  — Single-flight calendar sync.
L2287  _scheduleCalReconcile()
L2295  var _calServerConfirmedEmpty  — Tracks whether the latest bookings state came from a successful server fetch.

### Status Bar (L2318)
L2329  _themeGround(registry, themeId, base)  {android-bridge}  — The ground of the theme in force, for the Android status bar: the `bg` of its APP_THEMES entry (js/theme.js) — the…
L2343  updateStatusBar()  +36
L2381  var observer  — Watch for theme changes

## Analytics / Diagnostics (developer-only, no third-party services) (L2386)
L2390  var ACTION_LOG_NATIVE_KEY
L2391  var MAX_NATIVE_ACTION_LOG
L2395  var _origPushAction  — Persist action log to Capacitor Preferences (survives iOS storage purges).
L2404  _persistActionLogToNative()
L2423  async window.getDiagnosticReport()  +121  — getDiagnosticReport — comprehensive diagnostics string for developer use.
