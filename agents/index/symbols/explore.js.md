# js/explore.js — symbols
Purpose: the shape of `js/explore.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `ebc2926`). Do not edit; rebuild with `npm run agents:index`.

1243 lines · 49 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.

L18  escapeHtml(s)
L20  var HISTORY_KEY
L21  var SYNC_KEY
L22  var _exploreDirty
L23  var _syncing
L24  var _syncLabel
L27  getHistory()  — Parse class history from localStorage.
L43  setHtml(el, html)  — The one door for every write to the four explore sections.
L59  timeWindow(hour)  — Bucket an hour-of-day into a coarse time window.
L66  var DAY_NAMES
L67  var WINDOW_LABELS
L73  slotOf(iso)  — Safely derive { day, hour, window } from an ISO datetime string.

## DATA LAYER (L81)
L89  buildNameIndex()  — Build a name → id reverse lookup from the instructors array.
L108  gatherInstructorProfiles()  +83  — Gather a comprehensive profile for every known instructor.
L198  getUserScheduleProfile()  — Derive the user's habitual booking schedule from their class history.
L230  getBookedInstructorIds(profiles)  — Build the set of instructor IDs the user has booked with.

## SECTION 1: "New to you" (L255)
L259  computeNewToYou(profiles)
L283  signedOutBlank()  — Signed out with nothing on record — the state renderInsights shows its one sign-in hero for.
L288  renderNewToYou(container, profiles)

## SECTION 2: "You might like" (L317)
L323  var YML_WEIGHTS  — Blend weights for the three recommendation dimensions.
L330  var FRESHNESS_DAYS  — 30 days un-booked before the lightweight "haven't tried recently" boost kicks in.
L337  scoreAffinity(p, refClassTypes, refLocations, maxTypeMatch, maxLocMatch)  — (a) TALENT/AFFINITY — how similar a candidate is to the reference set (favourites + frequently-booked instructors) by…
L354  scoreTiming(p, sched)  +39  — (b) TIMING FIT — fraction of the candidate's upcoming events that land in the user's habitual day/time windows,…
L399  scoreAvailability(p, maxUpcoming, now)  — (c) AVAILABILITY/FRESHNESS — more upcoming classes = more actionable, with a light boost for instructors the user…
L412  computeYouMightLike(profiles)  +74
L487  renderYouMightLike(container, profiles)
L523  buildWhyLine(c)  — Build the short "why" line for a recommendation, e.g. "Teaches Tue & Thu mornings · 4 classes this week" Falls back…
L552  getShortCategoryName(typeName)

## SECTION 3: "Your instructor map" (L560)
L564  computeInstructorMap(profiles)  +59
L624  renderInstructorMap(container, profiles)  +61

## SHARED: Instructor card renderer (L686)
L690  instrCard(profile, whyLabel)  +50

## HISTORY SYNC — Fetch full booking history from Psycle API (L741)
L745  hasSynced()
L749  renderSyncBanner(container)  +50
L800  window._explore_resetSync()
L813  var TOPUP_MAX_NEW  — opts.silent — the weekly top-up below: the same sync, with nothing said and nothing repainted unless it actually found…
L818  async window._explore_syncHistory(opts)  +269  — Probe the API for past bookings using common codexfit patterns.

### Silent weekly top-up (L1088)
L1096  var TOPUP_AFTER_MS  — features.js adds every UPCOMING seat to history on each bookings fetch, but a class booked elsewhere AND attended…
L1097  var TOPUP_RETRY_MS
L1098  var TOPUP_DELAY_MS
L1103  var TOPUP_AT_KEY  — When a top-up last STARTED (device-local; not exported or mirrored).
L1104  var _topUpTried
L1111  var TOPUP_SKIPPED_KEY  — {at: ISO, count} — a top-up found more unknown classes than it may fetch (TOPUP_MAX_NEW).
L1112  _noteTopUpTooBig(count)
L1115  _clearTopUpTooBig()
L1119  _topUpTooBig()  — → {at: ms | NaN, count: whole number, 0 when there is no readable note}
L1134  _topUpMayStart(now, lastSync, lastTried, lastTooBig, afterMs, retryMs)  {history-topup}  — May a silent top-up start now?
L1142  maybeTopUpHistory()

## MASTER RENDER (L1174)
L1178  window.renderExplore()  +36

## REACTIVITY: Mark dirty on data changes, re-render if tab active (L1215)
L1219  markDirtyAndMaybeRender()
