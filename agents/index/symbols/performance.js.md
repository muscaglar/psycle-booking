# js/performance.js — symbols
Purpose: the shape of `js/performance.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

251 lines · 20 symbols. Top level, plus the declarations one level inside its top-level IIFEs. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


### A. Debounce Utility (L14)
L20  debounce(fn, ms)  — Returns a debounced version of `fn` that delays invocation until `ms` milliseconds have elapsed since the last call.
L31  (iife) patchInstrSearchDebounce()  — Wrap the instructor search input with a debounced handler.
L37  const _origFilterInstrDropdown
L38  const debouncedFilter
L41  window.filterInstrDropdown = debouncedFilter
L44  patchInputElement()  — Also patch the input element directly in case the browser cached the inline handler reference.

### C. API Response Caching (Stale-While-Revalidate) (L59)
L61  const CACHE_PREFIX
L62  const TTL_24H
L69  _staticCacheHasList(cached)  {static-cache}  — Does the entry hold a list worth serving at all?
L77  _staticCacheIsFresh(cached, ttlMs, nowMs)  {static-cache}  — Is a cached static list still good enough to skip the network altogether?
L88  _staticCacheDecision(cached, ttlMs, nowMs)  {static-cache}  — What the patched apiFetch does with the entry it found: 'cache' a usable list inside its TTL — the whole answer, no…
L112  (iife) patchApiFetchForCaching()  +85  — Patch apiFetch so that GET requests to cacheable endpoints are served from localStorage when cache data is available.
L113  const CACHEABLE_PATHS
L124  const _origApiFetch
L126  window.apiFetch(path, opts)  +70
L208  (iife) eagerCachePrePopulate()  +43  — Eager cache pre-population: app.js's init IIFE is still waiting on securityReady (slow on iOS) when this script loads.
L209  readCache(key)
L221  const cachedInstructors
L222  const cachedLocations
L223  const cachedEventTypes
