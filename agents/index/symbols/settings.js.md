# js/settings.js — symbols
Purpose: the shape of `js/settings.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `bdd5ef5`). Do not edit; rebuild with `npm run agents:index`.

2189 lines · 103 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.

L20  var TIER_KEY
L21  var BIKE_PREF_KEY
L22  var TIERS

### Data Persistence (L24)
L30  _store(key, value)  — Reads are coerced (app.js, pure:stored-data — both keys can come out of an imported file); writes go through…
L35  loadTiers()
L41  saveTiers(t)
L43  loadBikePrefs()
L49  saveBikePrefs(p)

### Public API (L51)
L53  window.getInstructorTier(instrId)
L57  window.getBikePrefs(studioId)
L62  window.tierBadgeHTML(instrId)

## Floating Next Class Pill (L71)
L75  var _pillEl
L76  var _pillTimer
L78  createPill()
L110  _pillA11y(label)  — The pill is a div that acts as a button (it holds div children, so it stays a div).
L130  updatePill()  +88
L219  startPillTimer()

### The pill steps aside (L258)
L269  var PILL_TUCK_AFTER  {pill-scroll}
L270  var PILL_SHOW_AFTER  {pill-scroll}
L271  var PILL_EDGE  {pill-scroll}
L276  _pillScrollStep(prev, f)  {pill-scroll}  — One scroll position in, the pill's next state out.
L306  _pillCalledBack(s)  {pill-scroll}  — A state in, "did the member just call it back?" out: mid-list, a run UP of PILL_SHOW_AFTER — the run that un-tucks it,…

### At rest it never sits on a Book button (L311)
L326  var PILL_REST_MS  {pill-rest}
L327  var PILL_REST_AGAIN_MS  {pill-rest}
L329  var PILL_OVERLAP_MIN  {pill-rest}
L337  _pillCoversBook(box, rects)  {pill-rest}  — box: where the pill rests; rects: the Book buttons' boxes — { left, top, right, bottom }, same space.
L354  _pillRestApplies(tuck, f)  {pill-rest}  — Is the question asked at all?
L365  _pillRestStep(s, covers)  {pill-rest}  — One look in, the next state out.
L376  _pillBox()  — Where the pill RESTS, whatever is drawn right now (tucked it is slid down, hovered it is scaled): its layout size…
L386  _pillBookRects()  — The card's ONE pill, on both tabs (Book / Join waitlist, Cancel booking / Leave waitlist).
L396  _pillKeyFocused()  — Focus a KEYBOARD put there (a tap focuses the pill too, and must not pin it over a button for as long as nothing else…
L401  _pillRestCheck()
L414  var _pillTuck
L415  var _pillRest
L416  var _pillRestClear
L417  var _pillRestTimer
L418  var _pillScrollSrc
L419  var _pillScrollQueued
L426  _pillDialogUp()  — A dialog, sheet or panel is up.
L442  _pillDraw()  — Out of the way for either reason: on the way down a list, or at rest on a Book button.
L450  _pillRestQueue(ms)  — "Ask again once things have settled" — every caller re-arms the one timer, so a run of scroll frames or of DOM writes…
L458  _pillRendered()  — The page was written to (a search streams its studios in, one render after another): what the last look saw is gone,…
L466  _pillOnScroll()  — Once per frame at most.
L490  _pillScrollEvent(e)  — Capture: `scroll` does not bubble, and .tab-content (the scroller at <=640px) is built by tabs.js.
L502  _pillUntuck()  — Focus landed on it (Tab, a screen reader): it has to be on screen.

## Settings Panel (L509)
L515  var SETTINGS_SECTION_IDS  — The Membership rows each open the panel AT their own section; all four used to land at the top of the same sheet.
L526  scrollSettingsTo(section)  — Bring one section to the top of the panel's own scroller.
L538  window.openSettings(section)  +89  — `section` is optional ('reminders' | 'bike' | 'calendar' | 'data').
L628  window.closeSettings()

## Instructor Tier UI (L636)
L640  window.filterTierList()
L642  tierRowHTML(instr, tiers, favs)
L661  renderTierList()  +83
L745  window.toggleFavFromSettings(instrId)
L760  window.setInstructorTier(instrId, tier)

## Calendar Sync UI (iOS only) (L776)
L781  async renderCalendarSync()  +48  {calendar-sync}
L830  async window.onCalendarSyncToggle(checkbox)  {calendar-sync}
L844  async _confirmCalendarOwnership(calId, name, cancelText, lead)  +30  {calendar-sync}  — The ownership dialog.
L878  _calSyncProblem(r)  {calendar-sync}  — Why a sync did NOT happen, in the member's words — '' when it really ran.
L886  async window.onCalendarTargetChange(select)  +39  {calendar-sync}
L926  async window.onCalendarCleanupDupes(btn)  +42  {calendar-sync}
L969  async window.onCalendarResync(btn)  +41  {calendar-sync}

## Bike Preference UI (L1013)
L1017  async populateStudioSelect()  +61
L1079  window.renderBikePrefGrid()  +62
L1142  window.toggleBikePref(studioId, slotId)

## Integration: Bike Picker Highlights (L1171)
L1175  var _origShowBikePicker

## Integration: Tier Badges on Class Cards (L1239)
L1247  window.tierBadgeHTML(instructorId)  — app.js (eventCard / renderMyBookings) calls this hook directly next to the instructor name.

## Export / Import Settings (L1258)
L1262  var EXPORT_KEYS
L1274  window.exportSettings()  +35
L1316  _shareSettingsExport(json, fileName)  +33  {settings-export}  — Native export.
L1366  var IMPORT_MAX_FILE  {import-validate}  — What an import file may do.
L1367  var IMPORT_MAX_VALUE  {import-validate}
L1373  _planSettingsImport(data, deviceGet, opts)  +158  {import-validate}  — data: the parsed file.
L1533  _importSummary(added)  {import-validate}  — "120 classes, 4 rankings and 2 favourites" — what the plan adds, in words.
L1552  _applySettingsImport(plan)  — Writes an accepted plan, says what changed, reloads (the app reads most of these keys once, at launch).
L1570  window.importSettings(input)  +53

## Bug Report (L1625)

### Build id (window.APP_VERSION) (L1630)
L1638  var BUILD_ID_RE  {build-id}  — Nothing stamps a version into the page, so every report said "App version: unknown".
L1639  var _appVersionPending  {build-id}
L1646  var _swSwappedSinceLoad  {build-id}  — A worker that takes over mid-session deletes the old cache while the page keeps running the previous build's scripts,…
L1654  _buildIdsFromCaches()  {build-id}
L1664  _buildIdFromSwFile()  {build-id}  — Same-origin file (the app bundle on iOS).
L1677  resolveAppVersion()  {build-id}  — Resolves to the build id (also left on window.APP_VERSION), or null.
L1700  window.getAppVersion = resolveAppVersion  {build-id}
L1703  buildBugReport()  +51
L1755  async window.downloadBugReport()  +38
L1794  window.copyBugReport()
L1816  _fallbackCopy(text, status, what)  +30  — Returns whether the text really reached the clipboard, so a caller with its own status line (copyDiagnostics) never…

## Diagnostics Panel (L1848)
L1862  var DIAG_MAX_LOG_ROWS
L1864  esc(v)
L1871  diagHasToken()  — Boolean-only check for whether an auth token is present.
L1881  diagGetDiagnostics()  — Pull the diagnostics object from PsycleDiag, fully guarded.
L1892  diagRecentLog(getterName, key)  — Last N entries (newest first) from a log getter that returns [{timestamp, <key>}].
L1916  diagApiHealthHTML(diag)  +52  — Build the API-health section HTML from a diagnostics object (may be null).
L1970  diagEnvironmentHTML(diag)  — Build the environment section HTML.
L2002  var _diagReportText  — The iOS bridge's getDiagnosticReport() is async, but "Copy diagnostics" has to stay synchronous: awaiting inside the…
L2003  var _diagReportSeq
L2005  diagPrefetchReport()
L2027  diagBuildCopyBlob(diag)  +47  — Assemble the JSON blob copied by "Copy diagnostics".
L2075  window.openDiagnostics()  +60
L2136  window.closeDiagnostics()
L2141  diagStatus(msg, ok)
L2150  copyDiagnostics()
L2165  copyDiagnosticsFallback(blob)  — The status line used to say "copied" whatever the fallback did.
L2170  clearDiagnosticLogs()
