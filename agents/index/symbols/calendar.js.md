# js/calendar.js — symbols
Purpose: the shape of `js/calendar.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

384 lines · 16 symbols. Top level, plus the declarations one level inside its top-level IIFEs. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.

L13  const CALENDAR_DATA_KEY

### Psycle studio geo coordinates (L15)
L19  const STUDIO_GEO  — Addresses come from the API (relations.locations.address).
L29  _lookupGeo(locName)
L39  syncCalendarData()  +41  — Persist current bookings + event metadata to localStorage: generateICS() and addToGoogleCalendar() build from this…
L85  _icsTimestamp(dateInput)  — Format a JS Date (or ISO string) into ICS DTSTART/DTEND format: YYYYMMDDTHHmmssZ
L95  _icsEscapeText(s)  — Escape TEXT property values per RFC 5545 §3.3.11: backslash, semicolon, comma and newline.
L106  _icsFold(line)  — Fold long ICS lines at 75 octets per RFC 5545 Section 3.1.
L123  _slotLabel(slots, typeName)  — Build a slot label for the description, using the right noun for the class type (Bike / Bed / Bench / Spot).
L133  generateICS(entriesArg)  +66  — Generate a full VCALENDAR string from the provided entries array.
L203  downloadICS()  — Trigger a browser download of the .ics file.
L233  _shareICS(ics, fileName)  +32  {ics-share}  — Native .ics export — the same route as _shareSettingsExport in settings.js.
L271  openICSInCalendar()  — Open the .ics file directly in the browser so the OS offers to add events to the default calendar app.
L285  addToGoogleCalendar()  +38  — Add events to Google Calendar via URL scheme (one at a time for the next upcoming event, since Google doesn't support…
L328  renderCalendarActions()  — Render calendar sync actions inside the My Bookings panel.
L346  (iife) (anonymous)  +31  — Inject styles
L348  const style
