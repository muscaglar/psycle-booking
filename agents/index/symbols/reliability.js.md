# js/reliability.js — symbols
Purpose: the shape of `js/reliability.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

1135 lines · 58 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## E. Global Error Tracking (register first so it catches everything) (L13)
L17  const ERROR_LOG_KEY
L18  const ACTION_LOG_KEY
L19  const MAX_LOG_ENTRIES
L21  pushError(msg)
L31  window.pushError = pushError
L34  pushAction(action)  — Log a user action (action name + timestamp only, no sensitive data).
L41  window.pushAction = pushAction
L44  logNetworkError(path, status, method)  — Log a network error (status + path only, no auth tokens).
L48  window.onerror(message, source, lineno, colno, error)
L55  window.onunhandledrejection(event)
L64  window.getErrorLog()  — Retrieve the error log for debugging.
L69  window.getActionLog()  — Retrieve the action log for debugging.
L74  window.getFullLog()  — Get combined error + action log as a formatted string for bug reports.

## E2. Action Logging Hooks (L93)
L133  var _origToggleTheme  — Hook into theme toggle (theme.js loads before this file, so this one does attach)

## A. Retry with Exponential Backoff (L142)
L157  const FETCH_TIMEOUT_MS  — Per-attempt timeout — hung requests should fail fast so callers can retry or show an error, not leave buttons stuck on…
L159  async window.fetchWithRetry(url, opts, maxRetries = 3, stillWanted)  +48
L209  _backoff(attempt)  — Exponential delay with jitter: base * 2^attempt + random 0-500ms
L216  _retryAbandoned()  — What a retry that is no longer wanted rejects with (see stillWanted).
L229  _retryTokenStillValid(startedWith, current)  {retry-auth}  — May a request that was built with the bearer token `startedWith` be sent AGAIN now that the stored token reads…

## D. Offline Booking Queue (L402)
L406  var OFFLINE_QUEUE_KEY
L408  getOfflineQueue()
L415  saveOfflineQueue(queue)
L427  var _queueSessionIds  — Items queued during THIS page session.
L429  _queueOwnerId()
L437  _stampQueueItem(item)  — Every item says who queued it (it never runs under another account), has an id of its own (session membership,…
L453  queueOfflineCancel(eventId, bookingIds)  — Enqueue a booking cancel for offline replay.
L463  window.queueOfflineCancel = queueOfflineCancel
L468  _queueHoldsSeat(entry)  {offline-queue}  — A _myBookings entry that is a real seat (a waitlist place alone is not).
L478  _queueCancelStillHeld(item, myBookings)  {offline-queue}  — Is the booking a queued cancel was for still held?
L492  _queueStartMs(startAt)  {offline-queue}  — Absolute start of a queued class.
L502  _offlineQueueIsLive(item, queuedThisSession, trigger)  {offline-queue}  — Is this replay still the member's live intent?
L518  _offlineQueueDecision(item, now, sameSession, myBookings, ownerId)  +31  {offline-queue}  — What may happen to ONE queued item right now.
L552  _queueFallbackLabel(startAt)  {offline-queue}  — Names a class when neither the item nor the event cache can (legacy items).
L570  var _queueProcessing  — Process all queued offline operations (bookings + cancels) when connectivity is restored.
L571  var _queueRerunWanted
L575  var _queueTrigger  — What started the running drain: 'online' (the reconnect replay), 'loaded' (a /bookings answer landed), 'approved' (the…
L579  var _queueFreshMap  — The map the last 'bookings:loaded' delivered.
L582  var _queueMapAtReconnect  — _queueFreshMap as the last reconnect found it.
L583  var _queueApproved
L584  var _queueAskWanted
L586  async processOfflineQueue(trigger)
L615  _stampLegacyQueueItems()  — Items from before queue ids existed: give them one so they can be asked about and removed.
L628  var _queueRemoveWanted  — Taking items OUT (a discard, another account's, sign-out) must not race a running drain: its closing save is built…
L629  var _queueClearWanted
L630  _applyQueueRemovals()
L640  _removeQueuedItems(qids)
L644  _clearOfflineQueue()
L654  _releaseQueuedButton(eventId)  — A "Queued" card button whose item left the queue unsent goes back to what state says (Book / the seat already held).
L660  _offlineQueueVerdict(item)  +30  — What the running drain may do with one item.
L691  async _processOfflineQueueInner()  +200
L891  window.processOfflineQueue = processOfflineQueue
L895  _queueScreenBusy()  — Never stack on another dialog, the class sheet, the "Booked!" sheet, the first-run tour or the history prompt — nor…
L906  var _queueAsking  — A booking nobody is watching any more (left by an earlier launch, or not sent by the reconnect replay) spends a credit…
L907  async _askAboutQueuedBookings()
L926  async _askAboutQueuedBooking(item)  +74  — → 'next' (settled, or no longer ours to ask) | 'stop' (can't ask right now).
L1048  _renderQueueStatus()  — "N change(s) waiting to sync" at the top of the My Bookings tab.

### When the queue drains (L1069)
L1077  var _queueDrainArmed  — It used to drain on 'online' alone, so a queue left by a closed app sat there until some later reconnect fired it,…
L1126  var _clearTokenBeforeQueue  — Deliberate sign-out: whoever signs in next must not inherit this queue.
