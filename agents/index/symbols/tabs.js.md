# js/tabs.js — symbols
Purpose: the shape of `js/tabs.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `ebc2926`). Do not edit; rebuild with `npm run agents:index`.

4588 lines · 183 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


### Tab Navigation (L20)
L22  var TABS
L23  var _currentTab
L25  initTabs()  +252
L278  window.switchTab(tab, noHash)  +73
L365  _stillToCome(startAt, now)  — Is this held class still to come?
L371  updateTabBadge()
L433  var _origRender  — Also update badge when renderMyBookings is called directly
L434  _patchRender()

### Stats sub-pages (L446)
L460  var STATS_PAGES  {stats-pages}  — THE grouping.
L468  var STATS_PAGE_DEFAULT  {stats-pages}
L469  var STATS_PAGE_KEY  {stats-pages}
L471  _statsPageById(id)  {stats-pages}
L477  _statsPageId(raw)  {stats-pages}  — Whatever was stored or passed in → a real page id.
L480  _statsPageOfSection(sectionId)  {stats-pages}
L488  _statsPageForKey(currentId, key)  {stats-pages}  — A key pressed in the tab list → the page to move to, or null when the key is not the tab list's.
L499  _statsPageStep(currentId, dir)  {stats-pages}  — The neighbour a horizontal swipe leads to (dir: +1 = next, -1 = previous).
L505  _statsPageIsEmpty(id, shown)  {stats-pages}  — Is there nothing on this page?
L514  _statsEmptyState(id, shown, o)  {stats-pages}  — What a page with nothing on it says: ONE line — never a blank page, never a paragraph.
L525  _statsScrollTarget(current, pageTop, barHeight, pinTop)  {stats-pages}  — Where the scroller goes when a page opens.
L533  var _statsPageNow  — The page on screen.
L538  var _statsPainted  — Pages built since the last renderInsights(): a page is built when it is first opened, and not again until the data is…
L540  var _statsHeroOnly  — Signed out / can't-reach-Psycle with nothing on record: the hero has the tab.
L545  var _STATS_SECTION_PAINT  — Which function fills which section.
L559  _statsPagesHtml(sectionHtml, current)  — The switcher and the three page wrappers, from the table above.
L579  _wireStatsPages(panel)  — Once, from initTabs.
L610  _paintStatsPage(id)  — Build the sections of one page, unless they were built since the last renderInsights().
L622  _applyStatsPage()  — Selected tab, roving tabindex, and which page is displayed.
L638  _syncStatsEmpty()  — The one quiet line of a page with nothing on it (pure:stats-pages decides).
L666  _syncStatsPin()  — Wider than a phone the document scrolls and the tab bar is what sticks to the top: the switcher pins beneath it,…
L677  _scrollToStatsPageTop(page)  — The DOM half of _statsScrollTarget.
L700  _syncStatsPages(heroOnly)  — renderInsights' half of the pages: every call is a fresh look at the data, so nothing built before it is trusted — and…
L714  window.showStatsPage(id)  — Open one of the Stats sub-pages ('overview' | 'habits' | 'instructors').
L731  window._statsPageStep(dir)  — For a swipe helper: the page a swipe in `dir` (+1 / -1) would open, or null.
L744  _statsSwipeMayStart(target)  — Swipe left / right between the pages.
L757  _wireStatsSwipe(panel)  — Once, from initTabs — bound to the panel, which outlives every repaint of its sections.

### Render insights tab content (L771)
L779  var _historyRaw, _historyParsed  — One Stats paint asks getFullHistory() ~11 times (every section below) and the string runs to 400 KB.
L785  getFullHistory()  — Get the full class history from localStorage (synced + locally tracked).
L802  var _tokenReadable  — The stored token is only readable once security.js has decrypted it (on iOS: after the Preferences restore).
L808  window.renderInsights()  +41

### Quick Stats (L850)
L852  renderQuickStats()  +128

### Weekly Calendar View (L981)
L983  var _weekOffset
L985  window.weekNav(dir)
L992  window.planDay(dateStr)  — Interactive weekly planner: tap an empty day to search for classes on that date
L1005  renderWeekView()  +112

### Your usual week (weekly template — UI only) (L1118)
L1125  var UW_DAYS
L1129  _uwDateLabel(dateStr)  — 'YYYY-MM-DD' → "Mon 21 Sep".
L1136  _uwPlural(n, one, many)
L1144  _uwTypeOf(label)  {usual-week-crisp}  — A template entry's label is "Type · Instructor" (app.js _templateFromSeats).
L1150  _uwTime(totalMin)  {usual-week-crisp}  — Minutes after midnight → "18:30", through app.js's one formatter (_clock24, pure:clock).
L1158  _uwClassMark(typeName, keyOf, pictogram)  {usual-week-crisp}  — The tile + data-ct of a class type.
L1169  _uwTimeHtml(totalMin)
L1173  _uwMark(typeName)
L1179  _usualWeekTemplate()
L1190  _collectDisplayedWeekTemplate()  — Template entries from the REAL seats held over the next 7 days (it used to read the week shown in the Discover…
L1199  _usualWeekFromHistory()  — Nothing booked this week (a holiday, or the first Monday with the app): fall back to the slots history shows 2+ times.
L1220  _uwLog(action)
L1224  async window.saveWeekAsTemplate()  +42
L1268  async window.clearUsualWeek()  — The card's own handlers (window.clearWeeklyTemplate is app.js's storage call).
L1286  window.removeUsualWeekEntry(idx)
L1300  _placeUsualWeekCard(card)  — Right after #rebookHint — a node app.js removes and re-creates in front of #upcomingPanel whenever it likes.
L1310  var _usualWeekWatch  — The hint is re-made outside any event this module hears (first paint), so watch the tab's own children — not its…
L1311  _watchUsualWeekPlacement(card)
L1324  var UW_COLLAPSED_KEY  {usual-week-card}
L1330  _uwCollapsedFrom(stored, mem)  {usual-week-card}  — Stored '1' = collapsed; anything else there (absent, junk, an import's "true") = expanded.
L1338  _uwCardSeats(value)  {usual-week-card}  — How many seats an entry books: an integer 1–4 (Psycle's own limit per booking); anything else stored there reads as 1.…
L1343  _uwToggleWord(collapsed)  {usual-week-card}  — The word that shows the head is a control (no chevron: the Filters bar's rule).
L1349  _uwCardHtml(template, collapsed)  +49  {usual-week-card}  — The whole card for a saved week.
L1404  var _uwCollapsedMem  — Collapsed or not: localStorage psycle_usual_week_collapsed ('1' | absent), read at every paint — never cached: in the…
L1405  _uwIsCollapsed()
L1410  _uwRememberCollapsed(collapsed)
L1423  window.toggleUsualWeek()  — The head's button.
L1450  _uwFocusedButton(card, doc)  — Which of the card's buttons holds focus (-1: none), and putting it back on the button in that place once the card has…
L1453  _uwRefocusButton(card, idx)
L1461  renderUsualWeekCard()  +38
L1499  window.renderUsualWeekCard = renderUsualWeekCard
L1516  var UW_NOT_OPEN  {usual-week-sheet}  — What the sheet says about a planned row (state → copy).
L1517  var UW_NOT_LISTED  {usual-week-sheet}
L1520  var UW_WAITLIST_NOTE  {usual-week-sheet}  — What joining a waitlist from here means (it leads with a space: it follows the sheet's money sentence).
L1521  _uwPlanNote(row)  +30  {usual-week-sheet}
L1553  _uwTopUpNeed(row)  {usual-week-sheet}  — "1 of 2 seats held": the seat(s) still missing — all a top-up may ask for.
L1560  _uwTickOf(row, need)  {usual-week-sheet}  — "Choose again" lists the same dates afresh.
L1573  _uwKeepTick(kept, row)  {usual-week-sheet}  — …and a kept tick stands only on a row that is still THAT: by template index alone, a seat that went while the class…
L1590  _uwResultNote(result, info)  {usual-week-sheet}  — …and about what happened to a ticked row once the run reached it.
L1613  _uwSeatOptions(saved, max)  {usual-week-sheet}  — The seat counts a row offers: 1 · 2, and 3 · 4 only when the saved entry asked for them — never above the class's own…
L1624  _uwConfirmLabel(t)  {usual-week-sheet}  — The confirm button counts what it will SPEND: "Book 3 classes · 4 seats".
L1636  _uwRangeLabel(range, fmt)  {usual-week-sheet}  — A date range of the sheet's control, as it is labelled.
L1649  _uwLabelHalves(label)  {usual-week-sheet}  — "Newly opened · Fri 25 Sept – Thu 1 Oct" as two halves that each stay whole.
L1663  _uwScrollTopToReveal(scrollTop, viewH, row, used)  {usual-week-sheet}  — The list's scrollTop once the sheet has re-flowed around the row the member is on.
L1685  _uwRowHtml(row, note, checkbox, spots)  — One row of the sheet.
L1716  _uwNoteInner(note)  {usual-week-note}  — What a row's note holds.
L1725  _uwFocusBack(prev, doc)  — Where focus goes when the sheet closes: what held it on open, if that is a real element still in the page — else the…
L1745  _runUsualWeekSheet(opts)  +620  — The sheet between "Book my usual week" and any booking.
L2370  var _templateWeekRunning  — Opens the REVIEW sheet — it books nothing by itself.
L2371  async window.bookTemplateWeek(opts)  +42
L2416  _statsTile(ct)  — The small pictogram tile that leads a class name on a Stats card (app.js classPictogram; css/crisp.css .ct-tile).

### Recommendations (L2421)
L2423  renderRecommendations()  +58

### Habit-slot alerts (Feature 1) (L2482)
L2487  var DAY_NAMES_FULL
L2491  _nextWeekdayDateStr(dow)  — Local-date "YYYY-MM-DD" for the next occurrence of a weekday (0=Sun..6=Sat).
L2501  renderHabitSlots()  +66

### Streaks & milestones (Feature 3) (L2597)
L2606  _weekIndex(date)  — Monday-anchored week index: whole weeks since a fixed epoch Monday.
L2613  renderStreaks()  +82
L2706  var HEAT_BANDS  {stats-charts}  — Five bands of the day, as the board draws them.
L2713  var HEAT_DAYS  {stats-charts}
L2716  _wallParts(s)  {stats-charts}  — 'YYYY-MM-DD HH:MM…' (space or T) → { day: 0–6 Monday first, hour } | null.
L2725  _heatBandIndex(hour)  {stats-charts}
L2732  _heatLevel(count, max)  {stats-charts}  — 0 = never; 1–4 = quarters of the busiest cell (the old grid's own steps).
L2740  _heatmapModel(starts)  {stats-charts}  — Class start times → { total, max, counts[band][day], levels[band][day], top: the (up to three) busiest cells, busiest…
L2763  _heatmapLabel(model)  {stats-charts}  — What the grid says to someone who cannot see it.
L2772  _streakWeeks(weeks, thisWeek, n)  {stats-charts}  — The streak's bars: the last `n` weeks, oldest first.
L2783  _streakWeeksLabel(bars)  {stats-charts}
L2793  _classTypeBreakdown(counts, cats)  {stats-charts}  — counts by CATEGORY_MAP key + the category list → what the class-type bar draws: rows with a class, biggest first (ct =…

### Activity Heatmap (L2807)
L2811  renderHeatmap()  +55

### Support Psync: optional tips (L2871)
L2885  var TIP_PRODUCTS  {tips}  — The allow-list, cheapest first: the same three ids as TipJarPlugin.swift and PsyncTips.java.
L2894  _tipRows(answer)  {tips}  — What the store answered → the rows to draw, in OUR order.
L2908  _tipOutcome(status)  {tips}  — The plugin's status → what to say.
L2915  _isTipId(id)  {tips}
L2920  _tipJar()
L2927  var _tipAnswer
L2928  var _tipAsking
L2929  var _tipBusy
L2931  renderSupportPsync()
L2956  async _leaveTip(id)
L2978  renderMembershipInfo()  +167

### Cost Per Class Tracker (L3146)
L3148  renderCostTracker()  +94
L3244  var FORECAST_TARGET_GBP  {cost-forecast}
L3245  var FORECAST_MAX_PER_DAY  {cost-forecast}
L3249  _bookableMore(made, max, daysLeft)  {cost-forecast}  — The most classes a "Book N more" line may ask for: what the plan still holds (max 0 = unlimited) and what fits in the…
L3259  _forecastVerdict(made, max, priceGbp, costAtMax, projected, projectedCost, daysLeft)  {cost-forecast}  — The forecast's one-line verdict.
L3282  _forecastHtml(sub, made, max, priceGbp, costAtMax)  +47  — Returns the forecast block HTML (or '' if there isn't enough to forecast).

### Theme picker (Membership → App) (L3330)
L3332  renderThemePicker()
L3357  window._pickTheme(id)

### Class colours (Membership → Appearance) (L3362)
L3371  var CC_INTENSITY_LABELS  {class-colour-control}
L3377  var CC_SWATCH_INK  {class-colour-control}  — What the preview card says for each class type (decoration — never timetable data).
L3378  var CC_PREVIEW  {class-colour-control}
L3383  _ccHas(obj, k)  {class-colour-control}
L3396  _ccControlModel(api, cats, theme, open)  +40  {class-colour-control}  — Everything the control draws, from what the engine says.
L3440  _ccRadioTarget(count, index, key)  {class-colour-control}  — A key pressed on radio `index` of `count` → the radio to move to (and check: in a radio group selection follows…
L3450  var _ccOpenKey
L3452  _ccThemeNow()
L3459  var CC_TICK
L3461  _ccControlHtml(m)  +52
L3517  renderClassColours()  — Rebuilt whole on every change (it is small), with the focus put back where it was: a radio group rebuilt under the…
L3539  window.renderClassColours = renderClassColours
L3541  _ccSet(partial, said)
L3552  _wireClassColours(box)  +42  — Once per container (it outlives every repaint of its contents).
L3605  window.renderThemePicker = renderThemePicker
L3606  window.renderReminderRow = renderReminderRow

### Weekly reminder row (the native apps only — needs the native bridge) (L3608)
L3613  var _classReminderGranted  — Last known answer to "may Psync send notifications?" for the class switch.
L3625  _classReminderSwitch(prefOn, granted, android)  {reminder-row}  — What the class-reminder switch shows.
L3641  _reminderAndroid()  — These rows exist in both native apps.
L3646  _osSettingsWords(text)
L3650  renderReminderRow()
L3675  _paintReminderRow(row)
L3698  async window._toggleClassReminders()
L3725  async window._toggleReminder()
L3737  _formatGbp(amount)
L3741  _daysLeftInBillingPeriod(sub)

### Class Type Distribution (L3758)
L3760  renderClassTypeDistribution()  +50

### Lapsed Favourites (L3811)
L3813  renderLapsedFavourites()  +53

### Instructor Variety Trend (L3867)
L3869  renderVarietyTrend()  +52

### Year in Review (Feature 4) (L3923)
L3935  _yearReviewRows(history, year, nowMs, startMs)  {year-review}  — The rows a year's wrap may count: this year's, not cancelled — and not still to come.
L3944  _takenRows(history, nowMs, startMs)  {year-review}  — The same test over every year: what the Stats tiles ("This month", "All time") and the "Share my stats" image may…
L3951  _historyStartMs()  — app.js's London resolver (h.date is gym wall clock); the device-local parse only when app.js is absent — the same pair…
L3958  _hasTakenHistory()  — Is there a class TAKEN to show?
L3963  _computeYearReview(year)  +62  — Aggregate this year's attended history into a tidy summary object.
L4026  renderYearReview()
L4047  window.openYearReview()  +47

### Share images (year wrap + stats card): shared helpers (L4095)
L4106  async _shareAsTextOnAndroid(title, text)  — The ANDROID app's web view has no Web Share API and no download manager: navigator.share is missing, and the <a…
L4122  var SHARE_FALLBACK  {share}  — Cloud, whole.
L4125  var SHARE_TOKENS  {share}  — Small grey labels read `muted` (--text-muted), never --text-faint: faint is under 3:1 in Handheld, and these labels…
L4128  var SHARE_FOOTER  {share}  — One credit line for both images (they were signed "Psycle Companion" and "Psycle Class Finder" — neither is the app's…
L4132  var SHARE_SCALE  {share}  — Export scale: a fixed integer, not devicePixelRatio — the PNG is looked at on OTHER devices, and a fractional ratio…
L4135  _sharePaletteFrom(read)  {share}  — read(tokenName) → the custom property's text, e.g. ' #efeee9'.
L4146  _sharePalette()
L4156  _shareCanvas(W, H)  — A W x H surface exported at SHARE_SCALE: every coordinate below stays in 640-wide layout units, the pixels are 3x (a…
L4171  var SHARE_WORDMARK_FONT  — Wait for the display face so the wordmark is not drawn in a fallback — but never for long: navigator.share() spends…
L4172  var SHARE_DISPLAY
L4173  var SHARE_BODY
L4174  _shareFontReady()
L4188  _shareWordmark(ctx, pal, y, label)  — "Psync" + the image's section label, on one baseline.
L4202  _shareClassColour(cat, pal)  — The class-type colour in effect, as a literal a canvas can paint: the member's own swatch through the engine…
L4211  async window.shareYearReview()  +114

### Share Insights Card (L4327)
L4348  async window.shareInsights()  +229
