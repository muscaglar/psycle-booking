# API calls — every request the shipped code can make
Purpose: each `apiFetch` / `fetch` / request-helper call with a path literal, as `METHOD path` → `file:line enclosingFunction`, writes first. Read this when you touch booking, cancel, waitlist, sign-in or retry code, or need to stub the API. Skip it otherwise. HARD RULE: https://psycle.codexfit.com is a real booking system — tests, scripts and browser checks must never send it a POST / PUT / DELETE, and the unit tests never touch the network (tests/README.md). What each endpoint answers: agents/architecture/api.md.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `0fc5967`). Do not edit; rebuild with `npm run agents:index`.

Paths are relative to the customer API base (`DIRECT_API` in js/app.js). `{id}` stands for any interpolated part; the literal as written follows when it says more (a query string, or the expression a path is joined to: `/bookings/{ctx.bookingId}`). `retries:` is shown only where the call sets it — the default for a POST is 0 and for other verbs 3 (js/reliability.js wraps `apiFetch`). A method in brackets is an expression, not a literal. `via` = the helper called, or the variable the path was read from (resolved to the nearest assignment above the call).
Not every request goes straight to that base: opened from `file://`, `apiUrl()` (js/app.js, `IS_FILE` / `PROXY`) routes EVERY call — the bearer token with it — through a public CORS proxy, so a stub that matches on the host psycle.codexfit.com does not cover that path. Always serve the app over http (`python3 -m http.server 8080`).

## Writes (POST / PUT / PATCH / DELETE)

### POST /auth/login
- login.html:341 · via `fetch(LOGIN_URL)`

### POST /bookings
- js/app.js:6351 submitBooking
- js/app.js:10710 executeSpotSwap · retries: 0
- js/reliability.js:776 _processOfflineQueueInner · retries: bySlot ? 3 : 0

### DELETE /bookings
- js/app.js:7217 confirmUnbook · via `path` · `/bookings?event_id={eventId}`
- js/app.js:11083 upcomingCancel · via `path` · `/bookings?event_id={eventId}`
- js/reliability.js:726 _processOfflineQueueInner · via `path` · `/bookings?event_id={item.eventId}`

### DELETE /bookings/{id}
- js/app.js:7136 cancelBikeSlot · `/bookings/{resolvedId}`
- js/app.js:7217 confirmUnbook · via `path` · `/bookings/{bid}`
- js/app.js:10677 executeSpotSwap · `/bookings/{ctx.bookingId}`
- js/app.js:11083 upcomingCancel · via `path` · `/bookings/{bid}`
- js/app.js:11154 upcomingSeatCancel · `/bookings/{resolvedId}`
- js/reliability.js:726 _processOfflineQueueInner · via `path` · `/bookings/{bid}`

### POST /waitlist/{id}
- js/app.js:5942 claimWaitlistSpot · `/waitlist/{Number(entryId)}`

### PUT /waitlists/{id}
- js/app.js:5632 joinWaitlist · retries: 0 · `/waitlists/{eventId}`

### DELETE /waitlists/{id}
- js/app.js:5772 leaveWaitlist · retries: 1 · `/waitlists/{Number(entryId)}`

## Reads (GET)

### GET /bookings
- js/app.js:873 fetchMyBookings · `/bookings?limit=200`
- js/explore.js:860 _explore_syncHistory · via `strategies` · `/bookings?type=previous&limit=100`
- js/explore.js:901 _explore_syncHistory · via `rangeUrl` · `/bookings?start={startStr + '&end=' + endStr + '&limit=200'}`
- js/reliability.js:790 _processOfflineQueueInner · `/bookings?limit=200`

### GET /bookings/{id}
- js/app.js:4383 bookClass · `/bookings/{bId}`

### GET /event-types
- js/app.js:2044 · via `fetchJson()`

### GET /events
- js/app.js:2632 fetchEventsForLocation · `/events?{params}`
- js/app.js:3662 search · `/events?{params}`
- js/app.js:10377 rebookNextWeek · `/events?{params}`
- js/app.js:12309 _fetchTemplateDay · `/events?{params}`
- js/settings.js:958 populateStudioSelect · `/events?start={today + '+00:00:00&end=' + today + '+23:59:59&location=' + loc.id +…`

### GET /events/{id}
- js/app.js:492 _hydrateEventDetails · retries: 1 · `/events/{evtId}`
- js/app.js:4263 bookClass · retries: 1 · `/events/{eventId}`
- js/app.js:10514 changeSpot · `/events/{eventId}`
- js/app.js:12061 _bookTemplateSeat · `/events/{eventId}`
- js/app.js:12522 templateSpotsFor · `/events/{eventId}`
- js/explore.js:969 _explore_syncHistory · `/events/{evtId}`
- js/features.js:936 checkWatchedEvents · via `window.apiFetch()` · retries: 0 · `/events/{eid}`
- js/reliability.js:749 _processOfflineQueueInner · `/events/{item.eventId}`
- js/reliability.js:945 _askAboutQueuedBooking · `/events/{item.eventId}`

### GET /instructors
- js/app.js:2042 · via `fetchJson()`

### GET /locations
- js/app.js:2043 · via `fetchJson()`

### GET /profile
- js/app.js:1576 _checkAuthOnce · via `fetch()`
- js/app.js:1656 refreshProfile

### GET /waitlist/{id}
- js/app.js:5262 _probeWaitlistOffers · retries: 0 · `/waitlist/{Number(entryId)}`
- js/app.js:5870 claimWaitlistSpot · retries: 1 · `/waitlist/{Number(entryId)}`

### GET /waitlists
- js/app.js:5454 fetchMyWaitlists · retries: 1 · `/waitlists?page={page}`

### GET /waitlists/{id}
- js/app.js:5507 _fetchWaitlistEntryForEvent · retries: 1 · `/waitlists/{Number(eventId)}`

## Calls with no path literal (wrappers, pass-throughs, non-API fetches)
- js/app.js:130 apiFetch · `fetch(apiUrl(path))`
- js/app.js:2036 fetchJson · `apiFetch(path)`
- js/explore.js:876 _explore_syncHistory · `apiFetch(baseUrl + sep + 'page=' + pg)`
- js/performance.js:130 apiFetch · `_origApiFetch(path)`
- js/performance.js:136 apiFetch · `_origApiFetch(path)`
- js/performance.js:159 apiFetch · `_origApiFetch(path)`
- js/performance.js:181 apiFetch · `_origApiFetch(path)`
- js/reliability.js:183 fetchWithRetry · `fetch(url)`
- js/reliability.js:260 · `fetchWithRetry(apiUrl(path))`
- js/settings.js:1575 _buildIdFromSwFile · `fetch('sw.js')`
- sw.js:88 · `fetch(e.request)`
- sw.js:113 · `fetch(e.request)`
- sw.js:134 · `fetch(e.request)`
