# js/state.js — symbols
Purpose: the shape of `js/state.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

228 lines · 8 symbols. Top level, plus the declarations one level inside its top-level IIFEs. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


### Centralized State Management (L1)
L7  const PsycleState  +99  — keys: instructors, locations, eventTypes, currentUser, _studioMap, _myBookings, _eventCache, _activeSubscription, selectedInstructors, selectedCategories, selectedStrengthSubs, selectedReformerSubs,…

### Reactive property wiring (L107)
L114  (iife) wireReactiveProperties()  +36
L116  const reassignable  — Properties that get reassigned (let in app.js)

### Backward-compatible window accessors (L151)
L157  (iife) defineWindowAccessors()  +33
L158  const globals

### Event Emitter (L191)
L199  const PsycleEvents  — keys: _handlers, on, off, emit
L225  window.PsycleEvents = PsycleEvents

### Expose PsycleState globally (L227)
L228  window.PsycleState = PsycleState
