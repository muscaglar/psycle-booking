# Java — types, methods and constants of the Android app
Purpose: per Java file of the Android app's own source sets (ios-app/android/app/src/main, /debug and /test): its types, and the methods, constructors and `static final` constants declared directly inside a type, with line numbers. Read this when you touch the plugin twins, the home-screen widget, the class countdown or a JVM test — a `@Test` method's name says what it holds, so `grep -n -i "<word>" agents/index/java.md` also finds the JVM test for a rule. Skip it for web-only or iPhone-only work. What each class does: agents/architecture/android.md.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `5f108f8`). Do not edit; rebuild with `npm run agents:index`.

22 files · 27 types · 277 methods and constructors · 90 constants. Format: `L<line>  <declaration as written, without its body>  — first sentence of the comment directly above it`; indentation = nesting inside a type. The line is the one that holds the declared NAME: an annotation on a line of its own (`@Test`, `@PluginMethod`) sits above it and is printed in front of the declaration (`@Override` is left out). NOT listed: fields that are not `static final`, enum constants, the members of an anonymous or local class, lambdas, and anything Capacitor or Gradle generates (it is outside app/src). No compiler runs here: a line number is read from the text, so rebuild after an edit, and prove a Java edit with CI's `android-build` job.

## ios-app/android/app/src/main/java/com/psyclefinder/app/AppGroupPreferencesPlugin.java — 119 lines
L38  @CapacitorPlugin(name = "AppGroupPreferences") public class AppGroupPreferencesPlugin extends Plugin  — The Android twin of the iPhone app's AppGroupPreferences plugin (ios-app/ios/App/App/AppGroupPreferences.swift): the SAME JavaScript name and the same three…
  L41  @PluginMethod public void set(PluginCall call)
  L69  @PluginMethod public void get(PluginCall call)
  L81  @PluginMethod public void remove(PluginCall call)
  L99  private void planIfTheSwitchFlipped(String key, boolean wasOn)  — Plans the countdown again when a write to its switch changed what the switch MEANS - on to off, or back.
  L107  private static String keyOf(PluginCall call)  — The call's key when the call is well-formed; otherwise the call is rejected and this is null.

## ios-app/android/app/src/main/java/com/psyclefinder/app/MainActivity.java — 206 lines
L22  public class MainActivity extends BridgeActivity
  L44  private static final String ASK_PAGE_TO_GO_BACK = "(window._psycleAndroidBack ? window._psycleAndroidBack() : false)"  — What the hardware / gesture BACK asks the page.
  L48  protected void onCreate(Bundle savedInstanceState)
  L124  protected void onNewIntent(Intent intent)  — A tap on the home-screen widget while the app is alive: the activity is singleTask.
  L146  private void handWidgetTap(Intent intent)  — Hands a widget tap to the page - and a tap on the class countdown's notification, which is the same intent from the same builder.
  L181  public void onConfigurationChanged(Configuration newConfig)  — The system switched between light and dark while the activity was alive.
  L202  private WebView currentWebView()  — The bridge's web view, or null when the bridge (or its web view) is not there.

## ios-app/android/app/src/main/java/com/psyclefinder/app/PsycleDeepLinkPlugin.java — 39 lines
L28  @CapacitorPlugin(name = "PsycleDeepLink") public class PsycleDeepLinkPlugin extends Plugin  — The Android twin of the iPhone app's PsycleDeepLink plugin (AppGroupPreferences.swift): the same JavaScript name and the same one event, 'openURL' { url }, so…
  L31  static final String NAME = "PsycleDeepLink"  — The name MainActivity looks this plugin up by: the @CapacitorPlugin name above.
  L34  void openFromWidget(String rawEventId)  — A widget tap: `rawEventId` is what the intent carried, or null.

## ios-app/android/app/src/main/java/com/psyclefinder/app/PsycleTipJarPlugin.java — 250 lines
L47  @CapacitorPlugin(name = "PsycleTipJar") public class PsycleTipJarPlugin extends Plugin implements PurchasesUpdatedListener  — "Support Psync": optional tips through Google Play Billing.
  L55  public void load()
  L64  protected void handleOnDestroy()
  L71  @PluginMethod public void products(final PluginCall call)  — { products: [{ id, displayPrice }] } in the list's own order; any failure is an empty list.
  L119  @PluginMethod public void purchase(final PluginCall call)  — { status: 'purchased' | 'cancelled' | 'pending' | 'failed' | 'unavailable' }. Play's own sheet is the confirmation.
  L164  public void onPurchasesUpdated(@NonNull BillingResult result, @Nullable List<Purchase> purchases)
  L187  private void consume(Purchase purchase, final boolean answers)  — Consuming also acknowledges.
  L196  private void consumeLeftovers()  — A tip bought earlier and never consumed - the app was closed, or a slow payment came through since.
  L208  private void whenConnected(final Runnable then, final Runnable otherwise)
  L236  private void settle(String status)  — Answers the waiting purchase() call, once, whatever path got here.
  L245  private static void resolve(PluginCall call, String status)

## ios-app/android/app/src/main/java/com/psyclefinder/app/WidgetCenterPlugin.java — 37 lines
L27  @CapacitorPlugin(name = "WidgetCenter") public class WidgetCenterPlugin extends Plugin  — The Android twin of the iPhone app's WidgetCenter plugin (AppGroupPreferences.swift): the same JavaScript name and the same one method.
  L30  @PluginMethod public void reloadAllTimelines(PluginCall call)

## ios-app/android/app/src/main/java/com/psyclefinder/app/countdown/PsyncCountdownDismissReceiver.java — 70 lines
L34  public class PsyncCountdownDismissReceiver extends BroadcastReceiver  — Hears that the class countdown was DISMISSED, and remembers it for that class.
  L37  public static final String ACTION_DISMISSED = "com.psyclefinder.app.countdown.DISMISSED"  — The delete intent's action.
  L39  static final String EXTRA_KEY = "com.psyclefinder.app.countdown.KEY"  — The key of the countdown that was up (PsyncCountdownPlan.keyFor).
  L46  static PendingIntent intent(Context context, String key)  — The delete intent for the countdown `key` names.
  L53  public void onReceive(Context context, Intent intent)

## ios-app/android/app/src/main/java/com/psyclefinder/app/countdown/PsyncCountdownNotifier.java — 246 lines
L45  public final class PsyncCountdownNotifier  — The class countdown as the member sees it: ONE silent, ongoing notification that counts down to the next held class - the system's own chronometer, so nothing…
  L52  public static final String CHANNEL_ID = "class-countdown"  — The channel's id.
  L60  public static final String TAG = "psync-countdown"  — ONE notification: this tag and this id, always.
  L61  public static final int NOTIFICATION_ID = 7090
  L63  private PsyncCountdownNotifier()
  L71  public static boolean canPost(Context context)  — True when a notification posted now would be shown: notifications are on for the app and, from Android 13, the member has granted the permission.
  L101  public static boolean apply(Context context, PsyncCountdownPlan plan, long nowMillis, boolean alarmArmed)  — Makes what is on screen match the plan: the one notification posted or updated in place for the class it shows, or taken down.
  L129  public static void cancel(Context context)  — Takes the notification down.
  L152  private static void ensureChannel(Context context, NotificationManager manager)  — The countdown's own channel, from Android 8 (before that there are no channels, and the builder's priority below says the same).
  L169  private static Notification build(Context context, PsyncCountdownPlan plan, long timeoutMillis)
  L199  private static Notification publicVersion(Context context, PsyncCountdownPlan plan, long timeoutMillis)  — What a locked phone may show to anyone: no class name, no place, no seat.
  L217  @SuppressWarnings("deprecation") private static Notification.Builder builder(Context context)  — A builder on the countdown's channel.
  L231  private static void clock(Notification.Builder builder, long untilMillis, long timeoutMillis)  — The time: `when` is the class's start.
  L243  private static NotificationManager manager(Context context)

## ios-app/android/app/src/main/java/com/psyclefinder/app/countdown/PsyncCountdownPlan.java — 275 lines
L43  public final class PsyncCountdownPlan  — What the class countdown should be doing at one instant: which class it counts down to (or none), until when, and when to look again.
  L46  public static final long LEAD_MILLIS = 90L * 60L * 1000L  — How long before a class the countdown starts: the iPhone's leadWindow, 90 * 60 seconds.
  L48  public static final long NONE = -1L  — "No instant": for until and nextWake with nothing to show, or nothing to wake for.
  L51  static final int MAX_KEY = PsyncSnapshot.MAX_TEXT + 1 + 20  — The longest dismissal key read back: an id (PsyncSnapshot.MAX_TEXT), '@', a long's digits.
  L53  private static final PsyncCountdownPlan NOTHING = new PsyncCountdownPlan(null, NONE, NONE, null)
  L73  private PsyncCountdownPlan(PsyncSnapshot.Entry show, long until, long nextWake, String key)
  L81  public static PsyncCountdownPlan none()  — Nothing to show, nothing to wake for.
  L86  public static PsyncCountdownPlan of(PsyncSnapshot snapshot, long nowMillis, TimeZone zone, boolean enabled)  — The plan for nowMillis, with no countdown swiped away.
  L95  public static PsyncCountdownPlan of(PsyncSnapshot snapshot, long nowMillis, TimeZone zone, boolean enabled, String dismissedKey)  — The plan for nowMillis.
  L147  private static long after(long nowMillis, long at)  — `at` when it is strictly after now, else NONE: an alarm is never armed for the past.
  L158  public static String keyFor(String eventId, long startMillis)  — What names ONE countdown: the class's id, an "@", and the instant it starts in milliseconds - "9001@1790271000000".
  L167  public static boolean isKey(String key)  — True for a string keyFor could have made: what the dismissal's receiver may store.
  L193  public boolean mayPost(boolean alarmArmed, boolean systemEndsIt, boolean canPost, long nowMillis)  — Whether the notification may be UP after this plan run; false = take it down.
  L206  public boolean shows()  — True when there is a class to count down to.
  L211  public String title()  — The notification's title: the class name, as the widget prints it.
  L216  public String startLabel()  — "18:30": the start as the snapshot writes it, 24-hour.
  L229  public String text(boolean withStartTime)  — The notification's text: the place and the seat in the widget's own words, joined the way the widget joins who and where - "Shoreditch (middle dot) Bike 9".
  L242  private static void join(StringBuilder out, String part)
  L258  public int colour()  — The notification's accent: the class's own base colour when the snapshot's colours are colours, else the neutral "Other" colour - PsyncSnapshot settles which,…
  L267  public long timeoutMillis(long nowMillis)  — How long from nowMillis until the class starts - what the system is asked to remove the notification after, so that it goes at the start even if nothing in…

## ios-app/android/app/src/main/java/com/psyclefinder/app/countdown/PsyncCountdownReceiver.java — 137 lines
L56  public class PsyncCountdownReceiver extends BroadcastReceiver  — Keeps the class countdown true with the app not running.
  L59  public static final String ACTION_PLAN = "com.psyclefinder.app.countdown.PLAN"  — Plan again: the action of this receiver's own alarm.
  L65  private static final long WAKE_WINDOW_MILLIS = 10L * 60L * 1000L  — How late the alarm may be delivered on a phone that is awake: ten minutes, the shortest window Android 12+ honours for an alarm that is not exact (setWindow).
  L68  public void onReceive(Context context, Intent intent)
  L77  public static synchronized void plan(Context context)  — Makes the countdown match what is stored, now.
  L110  private static PendingIntent wake(Context context)
  L121  private static boolean schedule(Context context, long atMillis)  — ONE alarm, replaced each time (the same PendingIntent), for the instant the plan names; PsyncCountdownPlan.NONE cancels it.

## ios-app/android/app/src/main/java/com/psyclefinder/app/tips/PsyncTips.java — 39 lines
L15  public final class PsyncTips  — The ONLY things this app sells: three consumable tips, cheapest first.
  L16  private PsyncTips()
  L19  public static final List<String> IDS = Collections.unmodifiableList(Arrays.asList( "com.psyclefinder.app.tip.small",…
  L27  public static boolean isTip(String productId)  — One of the three, exactly: no trimming, no case folding, never null.
  L32  public static boolean allTips(List<String> productIds)  — True only for a non-empty list whose EVERY product is a tip: a mixed or empty purchase is never ours to consume.

## ios-app/android/app/src/main/java/com/psyclefinder/app/widget/NextClassWidgetProvider.java — 242 lines
L49  public class NextClassWidgetProvider extends AppWidgetProvider  — The home-screen widget "Next class": a classic App Widget (AppWidgetProvider + RemoteViews + XML layouts; no Compose, no Glance, no new dependency).
  L52  public static final String ACTION_REFRESH = "com.psyclefinder.app.widget.REFRESH"  — Repaint now: sent by the app itself, and by the repaint alarm.
  L55  private static final long KEEP_ALARM = Long.MIN_VALUE  — For scheduleRepaint: the alarm that is armed stays as it is.
  L57  private static final long NO_ALARM = -1L  — For scheduleRepaint: no alarm - what PsyncSnapshot.nextRepaintMillis says with nothing upcoming.
  L62  private static final long REPAINT_WINDOW_MILLIS = 10L * 60L * 1000L  — How late the repaint alarm may be delivered on a phone that is awake: ten minutes, the shortest window Android 12+ honours for an alarm that is not exact…
  L65  public static void requestRefresh(Context context)  — Asks the provider to repaint every widget.
  L79  public void onReceive(Context context, Intent intent)
  L97  public void onUpdate(Context context, AppWidgetManager manager, int[] appWidgetIds)
  L106  public void onAppWidgetOptionsChanged(Context context, AppWidgetManager manager, int appWidgetId, Bundle newOptions)
  L112  public void onEnabled(Context context)
  L117  public void onDisabled(Context context)
  L124  private static void refreshAll(Context context)
  L161  private static RemoteViews viewsFor(Context context, AppWidgetManager manager, int id, PsyncSnapshot snapshot, String shownId, boolean…  — One widget's views.
  L178  private static RemoteViews build(Context context, PsyncSnapshot snapshot, PsyncWidgetPlan plan, String shownId, boolean night, long now)
  L195  private static int size(Bundle options, String key)
  L199  private static boolean isNight(Context context)
  L206  private static PendingIntent repaint(Context context)
  L224  private static void scheduleRepaint(Context context, long atMillis)  — ONE alarm, replaced each time (the same PendingIntent), for the instant PsyncSnapshot.nextRepaintMillis worked out - the roll-over to the next class, or the…

## ios-app/android/app/src/main/java/com/psyclefinder/app/widget/PsyncSnapshot.java — 901 lines
L40  public final class PsyncSnapshot  — What the home-screen widget knows: the snapshot the web layer's bridge writes (ios-app/www/native-bridge.js, updateWidgetSnapshot) as three JSON strings, read…
  L43  public static final String KEY_NEXT = "widget_next_class"  — The three SNAPSHOT keys the bridge writes through the AppGroupPreferences plugin (isWidgetKey).
  L44  public static final String KEY_UPCOMING = "widget_upcoming"
  L45  public static final String KEY_WEEK = "widget_week"
  L51  public static final String KEY_COUNTDOWN_ENABLED = "countdown_enabled"  — The one stored key that is NOT part of the snapshot: whether the class countdown (countdown/PsyncCountdownPlan) may show, "1" or "0".
  L54  public static final int MAX_VALUE_BYTES = 64 * 1024  — A stored value is at most 64 KB of UTF-8.
  L56  public static final int MAX_TEXT = 120  — Every printed string is trimmed and cut to this many characters.
  L58  public static final int MAX_SLOTS = 8  — Seats kept per class (the app books at most four at a time).
  L60  public static final int MAX_UPCOMING = 5  — Classes kept: what the bridge writes (WIDGET_UPCOMING_MAX in native-bridge.js).
  L62  static final int MAX_SCAN = 50  — How far into a stored array the reader looks; the rest is ignored unread.
  L64  static final int MAX_DEPTH = 6  — Deeper JSON than this is not handed to the parser at all (an entry is three levels deep).
  L66  static final int MAX_ID_DIGITS = 12  — Event ids are numeric; a tap carries one only when it is 1 to 12 digits.
  L68  public static final long ROLL_OVER_AFTER_MS = 60L * 1000L  — How long after a class starts the widget moves on to the next: the iPhone timeline's minute.
  L70  static final long AFTER_MIDNIGHT_MS = 1000L  — How long after local midnight the day words are printed again.
  L72  private static final String[] WEEKDAYS = {…}
  L75  private static final Pattern WALL_CLOCK = Pattern.compile( "^([0-9]{4})-([0-9]{2})-([0-9]{2})[T…  — [0-9], not \d: Android's \d also matches digits of other scripts.
  L77  private static final Pattern HEX_COLOUR = Pattern.compile("^#([0-9a-fA-F]{6})$")
  L78  private static final Pattern DAY_KEY = Pattern.compile("^[0-9]{4}-[0-9]{2}-[0-9]{2}$")
  L82  static final int SURFACE_LIGHT = 0xFFFCFDFE  — The app's chrome (css/theme.css: Cloud by day, Graphite by night) - the same six values as PsycleChrome in PsycleClassType.swift and as res/values*/colors.xml…
  L83  static final int SURFACE_DARK = 0xFF1B2130
  L84  static final int INK_ON_LIGHT = 0xFF1B2130
  L85  static final int INK_2_ON_LIGHT = 0xFF3A4252
  L86  static final int INK_ON_DARK = 0xFFEEF1F5
  L87  static final int INK_2_ON_DARK = 0xFFB7BFCC
  L88  static final int WHITE = 0xFFFFFFFF
  L93  private static final Side NEUTRAL_LIGHT = new Side(SURFACE_LIGHT, null, 0xFF57647B, 0xFF313B4D)  — The neutral fallback, for an entry whose colours are missing or are not colours: the neutral surface, with the app's "Other" class colour (js/theme.js) in the…
  L94  private static final Side NEUTRAL_DARK = new Side(SURFACE_DARK, null, 0xFF68758D, 0xFFDBE2EE)
  L99  private PsyncSnapshot(List<Entry> stored, List<WeekDay> week)
  L105  public static PsyncSnapshot empty()  — Nothing stored: what a phone that never opened the app, or a signed-out one, has.
  L113  public static PsyncSnapshot parse(String nextJson, String upcomingJson, String weekJson)  — Reads the three stored strings.
  L149  private static void addEntry(List<Entry> entries, Set<String> seen, Entry entry)
  L160  public List<Entry> upcoming(long nowMillis, TimeZone zone)  — The classes that have NOT started at nowMillis, soonest first, at most MAX_UPCOMING.
  L194  public long nextRepaintMillis(long nowMillis, TimeZone zone)  — When the widget must next be painted WITHOUT the app asking, as an instant - or -1 with nothing upcoming (the empty state holds no word that time can change).
  L218  public int weekCount(long nowMillis, TimeZone zone)  — Bookings in the stored week, counting only days from today on (today in the given zone).
  L235  public static boolean isWidgetKey(String key)  — True for the three keys the bridge writes through AppGroupPreferences, and nothing else.
  L240  public static boolean fitsStore(String value)  — True when the value may be stored: a string of at most MAX_VALUE_BYTES of UTF-8.
  L252  public static boolean isStoreKey(String key)  — True for every key the store takes: the three snapshot keys, and the countdown's one switch.
  L261  public static boolean fitsKey(String key, String value)  — True when THIS value may be stored under THIS key.
  L277  public static boolean countdownEnabled(String stored)  — What the stored switch says.
  L287  public static String tapId(String raw)  — The class id a widget tap may carry, or null.
  L309  public static String deepLink(String rawId)  — The link the page is handed for a tap - the iPhone widget's own (PsycleWidget.swift): psync://bookings?event=ID, or psync://bookings alone when there is no…
  L317  public static final class Entry  — One booked class, as the bridge's _snapshotEventFor writes it.
    L344  private Entry(String eventId, int[] when, String typeName, String instrName, String studioName, String locName, List<Integer> slots, int…
    L367  static Entry from(Object value)  — An entry from one parsed JSON value, or null when it is not a usable class.
    L395  public long startMillis(TimeZone zone)  — The start as an instant: the stored wall clock, read in the given zone.
    L403  public String timeLabel()  — "18:30" - the digits as written, 24-hour, whatever the phone's own clock setting.
    L408  public String dayWord(long nowMillis, TimeZone zone)  — "Today", "Tomorrow", else "Thu 24" - against today in the given zone.
    L426  public String whenLabel(long nowMillis, TimeZone zone)  — "Tomorrow 07:30" - one of the wide widget's following rows.
    L430  private boolean isDate(Calendar calendar)
    L437  public String title()  — The class name.
    L445  public String shortTitle()  — A long name's head, for a one-line row: "REFORMER PILATES: SCULPT 50" -> "REFORMER PILATES" (PsycleInlineAccessory.shortTitle in PsycleWidgetLayouts.swift).
    L458  public String place()  — The building, else the room - as every native "Instructor (middle dot) Studio" line has it.
    L463  public String whoWhere()  — "Alex Hart (middle dot) Shoreditch"; either part may be missing, and then so is the dot.
    L478  public String seatLabel()  — "Bike 9", "Bikes 5 & 6", "Bench 3" - the noun by class name, as PsycleSlotLabel (PsycleSnapshot.swift) picks it and as the app pluralises it…
    L499  public String seatBadge(int maxChars)  — The seat badge's words for a badge with room for maxChars characters (PsyncWidgetPlan.seatChars): seatLabel() when it fits, else the seats COUNTED - "4…
    L508  public Look look(boolean night)  — The inks and fills for one appearance: the entry's own colours, else the neutral fallback.
  L520  static final class Side  — One appearance of a class's colours, as the snapshot carries them (ARGB ints).
    L526  Side(int tint, Integer wash, int base, int deep)
    L537  static Side from(JSONObject json, String suffix)  — All or nothing, as PsycleClassStyle.palette has it: base, tint AND deep must each be #rrggbb, or the side is null and the neutral fallback is drawn.
  L553  public static final class Look  — What a layout paints with, settled once - PsycleSurface.resolve in PsycleWidgetStyle.swift, which follows css/crisp.css: copy on a tint is ink, ink 2 or the…
    L565  private Look(int card, int ink, int ink2, int accent, int tileFill, int tileInk, int chipFill, int chipInk)
    L576  static Look of(Side side, String intensity)
  L593  static double luminance(int argb)  — WCAG relative luminance of an ARGB int (the alpha is ignored).
  L599  private static double channel(int value)
  L605  static double contrast(int a, int b)  — WCAG contrast ratio, 1 to 21.
  L613  static final class WeekDay
    L617  private WeekDay(String day, int count)
    L622  static WeekDay from(Object value)
  L640  private static Object parseJson(String raw)  — The parsed JSON value, or null for anything that is not there or cannot be read.
  L661  private static boolean tooDeep(String json)  — True when brackets nest deeper than MAX_DEPTH (the parser recurses once per level).
  L687  static String text(Object value)  — A printable string: whitespace runs collapsed, trimmed, cut to MAX_TEXT.
  L719  private static String idText(Object value)  — The event id as text: a non-empty string, or a whole number.
  L735  public static long wallMillis(String wall, TimeZone zone)  — 'YYYY-MM-DDTHH:MM[:SS]' read as a wall clock in the given zone, as a class's startAt is; -1 when it cannot be read.
  L747  static int[] wallClock(Object value)  — { year, month, day, hour, minute, second } of 'YYYY-MM-DDTHH:MM[:SS]', or null.
  L772  private static int daysIn(int year, int month)
  L781  private static List<Integer> slots(Object value)  — Seat numbers: whole numbers from 1 to 9999, no repeats, at most MAX_SLOTS.
  L798  static int wholeNumber(Object value, int min, int max)  — A JSON whole number within [min, max], else 0. A numeric STRING is not a number.
  L810  static Integer colour(Object value)  — '#rrggbb' -> an opaque ARGB int; anything else -> null, never a wrong colour.
  L821  private static String intensity(Object value)
  L835  static String classType(Object ct, String typeName)  — The snapshot's own key when it has one (a key this build does not know -> "other"), else worked out from the class name the way the app does (getCategory in…
  L871  private static boolean isClassType(String key)
  L877  static String plural(String noun)  — Bench -> Benches, else an s: pluralizeSlotLabel in js/app.js.
  L882  static String slotNoun(String typeName)  — Machine, Bed, Bike, Bench or Spot - PsycleSlotLabel in PsycleSnapshot.swift, line for line.

## ios-app/android/app/src/main/java/com/psyclefinder/app/widget/PsyncTapIntent.java — 73 lines
L25  public final class PsyncTapIntent  — The ONE way a tap outside the app opens it on a class: the home-screen widget's card and the class countdown's notification (countdown/PsyncCountdownNotifier)…
  L33  public static final String ACTION_OPEN = "com.psyclefinder.app.widget.OPEN"  — The action of the intent a tap sends to MainActivity.
  L35  public static final String EXTRA_EVENT_ID = "com.psyclefinder.app.widget.EVENT_ID"  — The shown class's id, as a string of digits.
  L43  public static final int FROM_WIDGET = 0  — Who the tap is for.
  L44  public static final int FROM_COUNTDOWN = 1
  L46  private PsyncTapIntent()
  L49  public static PendingIntent open(Context context, String eventId, int from)  — The tap: MainActivity, by class, with the class's id when it is a usable one.
  L66  public static int immutableFlags()  — The flags of EVERY PendingIntent the app builds - the tap above, the widget's repaint alarm, the countdown's wake: the newest extras win, and nothing can be…

## ios-app/android/app/src/main/java/com/psyclefinder/app/widget/PsyncWidgetPlan.java — 192 lines
L17  public final class PsyncWidgetPlan  — Which layout a widget of a given size gets, and what fits in it.
  L20  public enum Bucket  — COMPACT: day word, TIME, class name, seat.
  L23  static final int WIDE_FROM_DP = 250  — From this width on the wide layout is used: four cells on most launchers (70 x 4 - 30 = 250dp).
  L25  static final int COMPACT_TYPICAL_DP = 156  — The width assumed for a COMPACT card whose launcher reports none: two cells on most.
  L31  static final int DAY_SP = 13  — res/values/widget_styles.xml (the sp sizes, the badge's padding) and res/layout/widget_next_class_compact.xml / _wide.xml (the tiles, the margins), copied by…
  L32  static final int TITLE_SP = 14
  L33  static final int TITLE_WIDE_SP = 15
  L34  static final int BODY_SP = 12
  L35  static final int SEAT_SP = 12
  L36  static final int TILE_DP = 24
  L37  static final int TILE_WIDE_DP = 28
  L39  static final int SEAT_PAD_TALL_DP = 8  — The seat badge's padding: 4 above its words and 4 below, 9 before and 9 after.
  L40  static final int SEAT_PAD_WIDE_DP = 18
  L42  static final int SEAT_GAP_DP = 6  — The margin above the seat badge, COMPACT and WIDE.
  L43  static final int SEAT_GAP_WIDE_DP = 5
  L45  static final int WHO_GAP_DP = 3  — The margin above "Instructor (middle dot) Place".
  L47  static final int RULE_DP = 15  — The hairline over the following rows: 1dp, with 8 above it and 6 below.
  L49  static final int ROW_GAP_DP = 3  — The margin above the second following row.
  L51  static final int COLUMN_GAP_DP = 14  — WIDE: the margin between the time column and the class beside it.
  L55  static final double LINE_PER_SIZE = 1.18  — A line of text is this many times its size tall (Roboto without font padding: 1.17).
  L57  static final double TIME_WIDTH_PER_SIZE = 2.3  — "18:30" in the condensed bold face is about this many times its size wide.
  L59  static final double DAY_WIDTH_PER_SIZE = 4.3  — "Tomorrow", the longest day word, in the condensed bold face.
  L61  static final double SEAT_CHAR_PER_SIZE = 0.5  — One character of the seat badge at 1sp: half an em, on the generous side for a condensed face.
  L63  static final int MIN_SEAT_CHARS = 7  — "4 bikes": the badge is never planned narrower than its counted form.
  L79  private PsyncWidgetPlan(Bucket bucket, int timeSizeDp, int titleLines, int rows, int paddingDp, int widthDp, float fontScale)
  L91  public static Bucket bucketFor(int widthDp)  — The bucket for a reported width; an unknown width (0 or less) is COMPACT, which fits everywhere.
  L96  public static PsyncWidgetPlan forSize(int widthDp, int heightDp)  — The plan for the size a launcher reports, at the default font scale.
  L104  public static PsyncWidgetPlan forSize(int widthDp, int heightDp, float fontScale)  — The plan for the size a launcher reports and the member's font scale (Configuration.fontScale).
  L109  public static PsyncWidgetPlan forBucket(Bucket bucket, int widthDp, int heightDp, float fontScale)  — The plan for a bucket the caller has chosen (the debug preview), sized like forSize.
  L125  static PsyncWidgetPlan[] ladder(Bucket bucket, int widthDp, float fontScale)  — The steps a bucket gives way by, roomiest first.
  L152  public int neededHeightDp()  — The height this plan's rows add up to, in dp, at its font scale - the FULL card: with a seat badge, and (WIDE) with "Instructor (middle dot) Place".
  L169  static int line(double sizeDp)  — The height of one line of text of the given size, in dp, rounded up.
  L174  static float scale(float fontScale)  — A usable font scale: Android offers 0.85 to 2; anything that is not a number is 1.
  L181  private static int seatChars(Bucket bucket, int widthDp, int paddingDp, int timeSizeDp, float fontScale)

## ios-app/android/app/src/main/java/com/psyclefinder/app/widget/PsyncWidgetStore.java — 120 lines
L20  public final class PsyncWidgetStore  — Where the widget snapshot is kept: ONE private SharedPreferences file of the app, written by AppGroupPreferencesPlugin and read by the widget provider and by…
  L23  public static final String FILE = "psync_widget"  — The file's name.
  L30  private static final String KEY_COUNTDOWN_DISMISSED = "countdown_dismissed"  — Native only, and NOT one of PsyncSnapshot's keys: which countdown the member swiped away (PsyncCountdownPlan.keyFor).
  L32  private PsyncWidgetStore()
  L34  public static SharedPreferences prefs(Context context)
  L39  public static String get(Context context, String key)  — The stored string, or null: for a key the store does not take, a missing one, or a value of another type.
  L55  public static boolean set(Context context, String key, String value)  — False when the store does not take the key, or the value is missing, over 64 KB or - for the countdown's switch - anything but "1" or "0": nothing is written.
  L63  public static boolean remove(Context context, String key)
  L72  public static PsyncSnapshot read(Context context)  — What is stored now, read defensively: never null, never throws.
  L88  public static boolean countdownEnabled(Context context)  — Whether the class countdown may show: the member's class-reminders preference, as the bridge last wrote it.
  L97  public static String countdownDismissed(Context context)  — The key of the countdown the member swiped away, or null.
  L107  public static void setCountdownDismissed(Context context, String key)  — Records the swiped-away countdown's key; null forgets it.

## ios-app/android/app/src/main/java/com/psyclefinder/app/widget/PsyncWidgetViews.java — 191 lines
L36  public final class PsyncWidgetViews  — Builds the widget's RemoteViews - and nothing else, so that the provider (NextClassWidgetProvider) and the debug-only preview (WidgetPreviewActivity,…
  L39  public static final int ROOT_ID = android.R.id.background  — The root of every widget layout, and the tap target.
  L41  private PsyncWidgetViews()
  L48  public static RemoteViews build(Context context, PsyncSnapshot snapshot, PsyncWidgetPlan plan, boolean night, long nowMillis)  — The widget for a snapshot at a moment, laid out to a plan.
  L127  public static RemoteViews empty(Context context)  — "Nothing booked": every colour is a night-aware resource named in the layout, nothing is set here.
  L133  private static void row(RemoteViews views, int rowId, int whenId, int whatId, PsyncSnapshot.Entry entry, PsyncSnapshot.Look light,…
  L145  private static void filter(RemoteViews views, int id, int light, int dark, boolean night)  — The colour of a WHITE shape or mark in an ImageView (drawable/widget_card.xml says why).
  L149  private static void ink(RemoteViews views, int id, int light, int dark, boolean night)
  L153  private static void colour(RemoteViews views, int id, String method, int light, int dark, boolean night)
  L161  private static int px(Context context, int dp)
  L167  private static int pictogram(String classType)  — res/drawable/ic_ct_KEY.xml: the app's own marks (js/app.js CLASS_PICTOGRAMS).

## ios-app/android/app/src/debug/java/com/psyclefinder/app/CountdownProofReceiver.java — 195 lines
L61  public class CountdownProofReceiver extends BroadcastReceiver  — DEBUG BUILDS ONLY (app/src/debug: a release build has neither this class nor its manifest entry).
  L64  private static final String TAG = "PsyncCountdownProof"  — The log tag of every line this receiver writes; CI's emulator job reads the log for it.
  L67  public void onReceive(Context context, Intent intent)
  L85  private static String prove(Context context, Intent intent)
  L128  private static String extra(Intent intent, String name)
  L136  private static int number(String text, int fallback)
  L151  private static String sample(String startAt)  — widget_upcoming holding ONE class, in the shape the bridge writes (_snapshotEventFor).
  L178  private static String first(String upcoming)  — widget_next_class for a list: its first class, or "null".
  L188  private static String wall(long millis, TimeZone zone)  — An instant as the snapshot writes a start: a zone-less wall clock, here to the second.

## ios-app/android/app/src/debug/java/com/psyclefinder/app/WidgetPreviewActivity.java — 302 lines
L67  public class WidgetPreviewActivity extends Activity  — DEBUG BUILDS ONLY (app/src/debug: a release build has neither this class nor its manifest entry).
  L70  private static final String TAG = "PsyncWidgetPreview"  — The log tag of a widget that could not be drawn; .github/workflows/ci.yml greps for it.
  L72  private static final int PAGE_GAP_DP = 24  — The page's padding before and after the frame, in dp.
  L75  protected void onCreate(Bundle savedInstanceState)
  L80  private View render(Intent intent)
  L164  private static String extra(Intent intent, String name)
  L172  private static int number(String text, int fallback)
  L184  private static String givenSnapshot(Intent intent)
  L196  private int px(int dp)
  L204  private static String sample(String name, long now, TimeZone zone)  — widget_upcoming for three classes after `now`: in 90 minutes, tomorrow 07:30, the day after 18:00.
  L242  private static JSONObject entry(String id, String startAt, String type, String instructor, String studio, String location, int[] seats)…  — One class, in the shape the bridge writes (_snapshotEventFor in native-bridge.js).
  L262  private static void paint(JSONObject entry, String ct, String intensity, String soft, String bold, String base, String deep, String…  — The colour fields, as _snapClassColourFields writes them: ctTint is the card's ground at the intensity (the neutral surface at "off"), ctWash is always the…
  L280  private static String soon(long now, TimeZone zone, int minutes)  — `minutes` after now, on the five minutes, as a wall clock in the zone.
  L288  private static String at(long now, TimeZone zone, int days, int hour, int minute)  — `days` after today, at hour:minute, as a wall clock in the zone.
  L297  private static String wall(Calendar calendar)

## ios-app/android/app/src/test/java/com/psyclefinder/app/countdown/PsyncCountdownPlanTest.java — 824 lines
L29  public class PsyncCountdownPlanTest  — PsyncCountdownPlan on the JVM (./gradlew testDebugUnitTest): what the class countdown shows, until when, and when it looks again - with no phone and no…
  L31  private static final TimeZone LONDON = TimeZone.getTimeZone("Europe/London")
  L32  private static final TimeZone NEW_YORK = TimeZone.getTimeZone("America/New_York")
  L33  private static final TimeZone TOKYO = TimeZone.getTimeZone("Asia/Tokyo")
  L34  private static final TimeZone UTC = TimeZone.getTimeZone("UTC")
  L36  private static final long MINUTE = 60L * 1000L
  L37  private static final long LEAD = PsyncCountdownPlan.LEAD_MILLIS
  L38  private static final long NONE = PsyncCountdownPlan.NONE
  L40  private static final String RIDE_SOFT = ",\"ct\":\"ride\",\"ctIntensity\":\"soft\"" +…
  L48  private static long at(String wall, TimeZone zone)  — A wall clock in a zone, as an instant.
  L55  private static long at(String wall)  — A London wall clock as an instant.
  L60  private static String entry(String id, String startAt, String type, String slots, String more)  — One class as the bridge writes it; `more` is spliced in as further fields (",\"x\":1").
  L66  private static String ride(String id, String startAt)
  L70  private static PsyncSnapshot snapshot(String... entries)
  L78  private static PsyncCountdownPlan plan(PsyncSnapshot snapshot, long now)
  L82  private static String shownId(PsyncCountdownPlan plan)
  L86  private static void assertNothing(String what, PsyncCountdownPlan plan)
  L92  private static void assertShows(String what, String id, long until, PsyncCountdownPlan plan)
  L101  @Test public void theLeadIsTheIPhonesNinetyMinutes()
  L107  @Test public void theWindowOpensAtExactlyNinetyMinutesToTheMillisecond()
  L132  @Test public void theWindowShutsAtTheStartToTheMillisecond()
  L153  @Test public void withTwoClassesInsideTheWindowTheFirstShowsAndTheSecondTakesOverAtItsStart()
  L177  @Test public void aSecondClassFurtherOutWaitsForItsOwnWindow()
  L195  @Test public void theWakeIsTheEarlierOfTheShownStartAndTheNextWindowOpening()
  L212  @Test public void theSoonestClassIsTheOneWhateverOrderTheSnapshotIsIn()
  L218  @Test public void twoClassesAtTheSameMinuteShowTheFirstStoredAndLeaveTogether()
  L228  @Test public void followingTheAlarmsWalksThroughEveryClassAndThenStops()
  L257  @Test public void everyPlanIsSaneMinuteByMinuteAcrossADay()
  L285  @Test public void nothingUpcomingIsNothingAndNoAlarm()
  L301  @Test public void theSnapshotASignOutWritesTakesTheCountdownDown()
  L311  @Test public void switchedOffThereIsNothingToShowAndNothingToWakeFor()
  L325  @Test public void aGarbageSnapshotIsNothingAndNothingThrows()
  L349  @Test public void aClockFarInThePastNeverArmsAnAlarmBehindItself()
  L372  @Test public void aWaitlistPlaceIsNeverCountedDownTo()
  L394  @Test public void theStartIsAWallClockInTheZoneHandedInEastAndWestOfLondon()
  L413  @Test public void oneInstantIsThreePlansInThreeZones()
  L432  @Test public void theWindowIsNinetyRealMinutesOnTheDayTheClocksGoBack()
  L455  @Test public void theWindowIsNinetyRealMinutesOnTheDayTheClocksGoForward()
  L473  @Test public void nothingFollowsThePhonesLocaleOrItsDefaultZone()
  L498  @Test public void theTextIsThePlaceAndTheSeatInTheWidgetsWords()
  L528  @Test public void withNothingToShowThereAreNoWords()
  L541  @Test public void theAccentIsTheClassColourOrTheNeutralOne()
  L555  @Test public void threeClassesInsideOneWindowAreWalkedThroughOneByOne()
  L585  @Test public void aStartWithSecondsIsCountedDownToTheSecondAndPrintedToTheMinute()
  L601  @Test public void aChangedBookingIsANewPlanAndNothingIsRememberedBetweenRuns()
  L629  @Test public void aLongListWhoseFirstFiveHaveStartedStillReachesTheSixth()
  L642  @Test public void theSingleKeyIsCountedDownToOnlyWhenTheListYieldsNoClass()
  L659  @Test public void aKeyNamesTheClassAndItsStart()
  L686  @Test public void aSwipedAwayCountdownStaysDownForThatClassAtThatStart()
  L710  @Test public void aMovedStartOrTheNextClassShowsAgainAfterASwipe()
  L727  @Test public void aSwipeStillHandsOverToTheClassAfterIt()
  L750  @Test public void itIsPostedOnlyWhenItShowsCanBePostedAndSomethingWillEndIt()
  L782  @Test public void theStoreTakesOneMoreKeyAndOnlyOneOrNoughtUnderIt()
  L811  @Test public void onlyOneSwitchesTheCountdownOnAndNeverWrittenIsOff()

## ios-app/android/app/src/test/java/com/psyclefinder/app/tips/PsyncTipsTest.java — 46 lines
L11  public class PsyncTipsTest
  L14  @Test public void threeTipsCheapestFirst()
  L22  @Test(expected = UnsupportedOperationException.class) public void theListCannotBeChanged()
  L27  @Test public void onlyTheThreeAreTips()
  L38  @Test public void aPurchaseIsOursOnlyWhenEveryProductIsATip()

## ios-app/android/app/src/test/java/com/psyclefinder/app/widget/PsyncSnapshotTest.java — 769 lines
L24  public class PsyncSnapshotTest  — PsyncSnapshot on the JVM (./gradlew testDebugUnitTest): the widget's reading of the snapshot the bridge writes, with no phone and no emulator.
  L26  private static final TimeZone LONDON = TimeZone.getTimeZone("Europe/London")
  L27  private static final TimeZone NEW_YORK = TimeZone.getTimeZone("America/New_York")
  L35  private static final String[][] PALETTE = {…}  — js/theme.js CLASS_COLOUR_PALETTE - every swatch a member can give a class type: name, then light tintSoft, tintBold, base, deep, then the same four for dark.
  L50  private static final String RIDE_SOFT = ",\"ct\":\"ride\",\"ctIntensity\":\"soft\"" +…
  L58  private static long at(String wall)  — A London wall clock as an instant.
  L65  private static String entry(String id, String startAt, String type, String slots, String more)  — One class as the bridge writes it; `more` is spliced in as further fields (",\"x\":1").
  L71  private static String list(String... entries)
  L80  private static PsyncSnapshot.Entry only(String json, long now)  — The one class of a snapshot that holds a single upcoming class.
  L86  private static List<String> ids(List<PsyncSnapshot.Entry> entries)
  L94  private static List<String> listOf(String... values)
  L102  private static void assertColour(String what, int expected, int actual)
  L106  private static String repeat(char c, int times)
  L117  @Test public void whatCannotBeReadIsSimplyNotThere()
  L134  @Test public void aTenThousandElementArrayIsReadOnlyAtItsHead()
  L151  @Test public void aValueOverSixtyFourKilobytesIsNotRead()
  L162  @Test public void onlyTheThreeWidgetKeysAndOnlySixtyFourKilobytes()
  L183  @Test public void aClassThatHasStartedIsDropped()
  L192  @Test public void startAtIsAWallClockInTheZoneHandedIn()
  L201  @Test public void classesAreDeDuplicatedByIdAndSortedByStart()
  L219  @Test public void theNextClassAloneIsReadOnlyWhenTheListYieldsNothing()  — The iPhone's rule (PsycleSnapshotStore.upcoming in PsycleSnapshot.swift): the single key is read ONLY when the list yields no class.
  L238  @Test public void anIdWrittenAsANumberAndAsTextIsOneClass()
  L248  @Test public void twoClassesAtTheSameMinuteKeepTheirStoredOrderAndLeaveTogether()
  L262  @Test public void theNextRepaintIsAMinuteAfterTheClassStartsOrJustAfterMidnight()
  L283  @Test public void thereIsNoNextRepaintWithNothingUpcoming()
  L292  @Test public void aSameMinutePairRollsOverOnce()
  L306  @Test public void theNextRepaintIsAlwaysLaterThanNow()
  L326  @Test public void atMostFiveClassesTheSoonest()
  L340  @Test public void aStartThatIsNotAWallClockDropsTheClass()
  L361  @Test public void theTimeIsTwentyFourHourAndZeroPadded()
  L369  @Test public void theDayWordIsTodayTomorrowOrTheWeekdayAndDate()
  L390  @Test public void theDayWordsHoldInEveryZoneAndOnTheDaysTheClocksChange()  — The day words are the device's, whatever the device's zone - and the clocks changing moves nothing.
  L435  @Test public void nothingFollowsThePhonesLocaleOrItsDefaultZone()  — Nothing printed or parsed may follow the phone's language, digits, calendar or DEFAULT zone: the iPhone widget was once empty on every phone set to a 12-hour…
  L470  @Test public void theSeatIsNamedForTheClass()
  L491  @Test public void seatsTheBadgeHasNoRoomForAreCounted()  — A badge with no room for every number COUNTS the seats: a number is never silently cut off the end.
  L509  @Test public void spacesAreCountedOnlyWhenThereIsMoreThanOne()
  L520  @Test public void seatNumbersAreWholePositiveUniqueAndAtMostEight()
  L529  @Test public void stringsAreTrimmedCollapsedAndCapped()
  L541  @Test public void whoAndWhere()
  L554  @Test public void aLongNameIsShortenedToItsHeadForARow()
  L564  @Test public void theClassTypeIsTheSnapshotsKeyElseWorkedOutFromTheName()
  L583  @Test public void aClassWearsItsOwnColoursLightAndDark()
  L609  @Test public void boldAndOffPutTheClassColourInTheTile()
  L633  @Test public void boldAndOffByNight()
  L660  @Test public void everySwatchAtEveryIntensityIsReadableByDayAndByNight()  — Every swatch a member can choose, at every intensity, by day and by night, written as the bridge writes it (_snapClassColourFields): what is printed on the…
  L688  private static String ground(String[] swatch, String intensity, int night)  — ctTint as the bridge writes it: the neutral surface at "off", else the pale or the full tint.
  L695  private static void assertReadable(String what, double floor, int ink, int ground)
  L701  @Test public void aColourThatIsNotHashRrggbbMeansTheNeutralFallback()
  L725  @Test public void inkFollowsTheGroundAndTheHueLineNeedsThreeToOne()
  L739  @Test public void theWeekCountsFromTodayOn()
  L752  @Test public void aTapCarriesOneToTwelveDigitsOrNothing()

## ios-app/android/app/src/test/java/com/psyclefinder/app/widget/PsyncWidgetPlanTest.java — 208 lines
L17  public class PsyncWidgetPlanTest  — PsyncWidgetPlan on the JVM: which layout a reported size gets, and what is left out when the card is short or the member's font is large.
  L19  private static final PsyncWidgetPlan.Bucket COMPACT = PsyncWidgetPlan.Bucket.COMPACT
  L20  private static final PsyncWidgetPlan.Bucket WIDE = PsyncWidgetPlan.Bucket.WIDE
  L23  private static int rowsAddedUp(PsyncWidgetPlan plan)  — The card's rows, added up HERE from the named constants: the second statement of neededHeightDp's sum.
  L43  private static String shape(PsyncWidgetPlan plan)
  L48  @Test public void theWideLayoutStartsAtFourCells()
  L63  @Test public void everyPlanFitsTheHeightThatChoseItAndIsTheRoomiestThatDoes()  — The point of the class: whatever the size and the font, the plan a height gets NEEDS no more than that height - so the seat badge, the last row, is not what a…
  L99  @Test public void theSumsAtTheDefaultFontScale()  — The sums at the default font, written out: a changed constant shows up here as a changed number.
  L119  @Test public void aCompactCardGivesWayFromTheNameDown()
  L140  @Test public void aWideCardShowsAsManyFollowingClassesAsItsHeightAllows()
  L155  @Test public void aLargerFontGivesWayEarlier()  — Every row but the time is sp: a larger font is a shorter card, as far as the plan goes.
  L172  @Test public void aFontScaleThatIsNotOneIsReadAsOne()
  L184  @Test public void theSeatBadgesRoomFollowsTheWidthAndTheFont()  — What the seat badge has room for: PsyncSnapshot.Entry.seatBadge counts the seats beyond it.
  L203  @Test public void anUnknownSizeIsTheRoomyPlan()
