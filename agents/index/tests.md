# Tests — what each suite covers and which source it depends on
Purpose: per `tests/suites/*.js`: its opening sentence, the pure blocks it loads (js/app.js unless a file is named), the shipped functions it slices out of source by anchor line, and the other source text it cuts by. Read this when you move, rename or re-indent a function (a sliced one breaks its suite), or look for the suite that covers an area. Skip it otherwise. How to run and write tests: tests/README.md.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `d568878`). Do not edit; rebuild with `npm run agents:index`.

77 suites, run in filename order by `node tests/unit.js` (`npm test`; it reads no arguments — there is no single-suite filter). "slices" = the suite holds a string such as `'function bookClass('` (or the whole opener, `'function _paintDayDot(pill, held) {'`) and cuts the real function out of the file from that opener to its closing bare `}` — keep the opener on one line, at its indentation, with its parameter list as written. "text anchors" = other strings the suite holds that are, verbatim, a piece of that source and shaped like the start of a declaration, a comment or a banner (`⏎` = a line break; a `pure:` marker is on the "pure" line instead): what `between(src, A, B)` and `src.indexOf(A)` cut by, so the code BETWEEN two of them is evaluated or read whole — do not move or reword them. "ok/eq call sites" counts calls in the file, not assertions run.
This list under-reports (an anchor built from parts is invisible): `grep -l "<name>" tests/suites/*.js` is the authority. Section titles are not repeated here — `grep -n "section(" tests/suites/<suite>.js` lists a suite's sections with their lines, and `grep -rn "section(" tests/suites | grep -i <word>` finds the section that pins a rule.

### 00-harness.js — 51 lines, 5 ok/eq call sites
Self-test for the suite loader and loadPure() in tests/unit.js — and the run's drain guard.

### 10a-time24.js — 299 lines, 58 ok/eq call sites
24-hour times everywhere ("18:30", never "6:30pm").
- pure: clock, bookings-card, class-type, gym-time, clash, day-pager, discover, filter-summary, usual-week-crisp (js/tabs.js), offline-queue (js/reliability.js)
- slices js/app.js: getCountdownText, showBikePicker, _waitlistClassLine, showBookingConfirmation, renderMyBookings, _londonClock, renderRebookHint, _snapshotLabel, eventCard, _onboardMiniCard, _onboardPageHTML, findSimilar, shareClass, openClassDetail
- slices js/tabs.js: renderHabitSlots, renderRecommendations, _computeYearReview
- text anchors in js/app.js: `window.findSimilar = function(eventId) {` · `window.shareClass = function(eventId) {` · `window.openClassDetail = function (eventId) {` · `// "After 10pm the evening before", for a 6–9am class…`

### 10b-themes.js — 404 lines, 85 ok/eq call sites
Two themes retired, and two small theme-level fixes.
- pure: theme-retired (js/theme.js), stored-data, import-validate (js/settings.js)
- slices js/tabs.js: renderThemePicker
- text anchors in js/theme.js: `// ── A. Themes` · `// ── B. Skeleton Loading Cards`
- text anchors in psycle-finder.html: `var RETIRED = { linen: 'cloud', synthwave: 'graphite' };`

### 10c-polish.js — 706 lines, 203 ok/eq call sites
The owner's follow-ups to the Crisp Colour build.
- pure: sheets, bookings-crisp, usual-week-note (js/tabs.js), pill-scroll (js/settings.js), pill-rest (js/settings.js), filter-summary, filters
- slices js/app.js: confirmModal, _repaintKeepingFocus, openClassDetail
- slices js/tabs.js: _uwRowHtml
- slices js/settings.js: createPill, _pillOnScroll, updatePill
- text anchors in js/app.js: `window.openClassDetail = function (eventId) {` · `const _swapOpen = !!window._changeSpotContext;` · `window.confirmModal = confirmModal;`
- text anchors in js/tabs.js: `// Where focus goes when the sheet closes` · `var setNote = function (i, result) {`
- text anchors in js/settings.js: `var _pillTuck = { y: 0, anchor: 0, dir: 0, tucked:…` · `// Where the pill RESTS`
- text anchors in js/interactions.js: `⏎⏎⏎  // ═══`

### 11-native-snapshot.js — 547 lines, 103 ok/eq call sites
The native surfaces in the Crisp Colour look: what the iOS bridge writes into the widget / Live Activity snapshot (class type + the member's colours), and the contracts that keep the Swift side in step with the web…
- pure: class-colours (js/theme.js), core, class-type, native-snapshot (ios-app/www/native-bridge.js)
- text anchors in PsycleShared/PsycleClassType.swift: `// ── fallback-palette:start` · `// ── fallback-palette:end` · `// ── class-type-words:start` · `// ── class-type-words:end` · `public struct PsycleClassStyle` · `public init(ct:` · `public enum PsycleFixedFormat` · `public enum PsycleClock`
- text anchors in App/AppDelegate.swift: `static let`
- text anchors in PsycleWidget/PsyclePictogram.swift: `// ── pictograms:start` · `// ── pictograms:end`
- text anchors in PsycleLiveActivity/PsycleLiveActivityAttributes.swift: `public struct ContentState` · `// Static attributes`
- text anchors in App/AppGroupPreferences.swift: `public let`
- text anchors in PsycleShared/PsycleSnapshot.swift: `public enum PsycleTimelinePlan` · `public enum PsycleSnapshotStore`
- text anchors in PsycleWidget/PsycleWidgetStyle.swift: `func psycleActivityGround(_ color: Color?)` · `func psycleCard(_ color: Color?)`
- text anchors in PsycleWidget/PsycleWidgetLayouts.swift: `private struct PsycleClassHead` · `private struct PsycleEmptyState` · `struct PsycleSmallLayout` · `struct PsycleMediumLayout` · `// MARK: - Lock Screen layouts`

### 12-brand-mark.js — 32 lines, 13 ok/eq call sites
The mark (the app icon's two engraved halves) is drawn in five places that cannot share code: the icon SVGs in assets/, the header (static HTML), the welcome (js/app.js, which can open before anything else has run) and…
- text anchors in js/app.js: `const _BRAND_MARK_SVG`

### 13-safe-area.js — 31 lines, 3 ok/eq call sites
css/crisp.css is linked LAST and wins every tie, so a `padding` shorthand there silently drops a safe-area inset an older sheet had set on the same selector (it did: the Settings header sat under the iOS status bar).

### 14a-usual-week-sheet.js — 806 lines, 241 ok/eq call sites
"Book my usual week": safe to press.
- pure: gym-time, window, template, template-spots, horizon, stored-data, usual-week-sheet (js/tabs.js), sheets, class-type, booking, clash
- slices js/app.js: _bookWeeklyTemplateInner, _fetchTemplateDay, planWeeklyTemplate, _layoutFromEventDetail, templateSpotsFor, _bookTemplateSeat, pluralizeSlotLabel, showBikePicker, _syncPickerConfirmLabel, closeBikePicker, confirmSpotChoice, confirmBikeBooking, selectBike, _syncBikeSlotsA11y, changeSpot, _busyLabel, formatSlots, _scheduleBookingsRefetch, _rereadBookingsForVerify, _announceVerifiedSeats, _settleUnverifiedBooking, _clearUnverifiedBooking, submitBooking, applyBookedState, _templateRefusalText
- slices js/facets.js: run
- slices (not a top-level symbol): paintPlan, showNewest, revealRow
- slices js/tabs.js: _runUsualWeekSheet
- text anchors in js/app.js: `// ── pure:`
- text anchors in js/reliability.js: `window.submitBooking = async function…`
- text anchors in js/tabs.js: `var _templateWeekRunning = false;` · `window._usualWeekSheetNewest = showNewest;`

### 14b-usual-week-card.js — 430 lines, 106 ok/eq call sites
The "Your usual week" CARD (js/tabs.js, css/crisp.css 9d §7).
- pure: clock, usual-week-crisp (js/tabs.js), core, usual-week-card (js/tabs.js), template, class-type
- slices js/tabs.js: _placeUsualWeekCard, renderUsualWeekCard, toggleUsualWeek
- text anchors in js/tabs.js: `window.renderUsualWeekCard = renderUsualWeekCard;` · `⏎  window.renderUsualWeekCard = renderUsualWeekCard;` · `var focusedIdx = _uwFocusedButton(card, document);` · `window.toggleUsualWeek = function () {` · `// Which of the card's buttons holds focus`

### 14c-weekly-reminder.js — 719 lines, 128 ok/eq call sites
The Monday reminder, pointed at the usual week.
- pure: gym-time, weekly-reminder (ios-app/www/native-bridge.js), week-opened, window
- slices js/tabs.js: _statsTile, saveWeekAsTemplate, clearUsualWeek, bookTemplateWeek
- slices js/app.js: search
- text anchors in js/tabs.js: `window.saveWeekAsTemplate = async function` · `window.clearUsualWeek = async function` · `window.bookTemplateWeek = async function`

### 15-agents-index.js — 73 lines, 8 ok/eq call sites
agents/index/ is a GENERATED map of the code (agents/tools/build-index.mjs → `npm run agents:index`): symbol → line, event → emitters, storage key → readers … so a reader can open a 14,000-line file at the right line.

### 16-agents-docs.js — 243 lines, 37 ok/eq call sites
The guide for agents: AGENTS.md is the lean entry, CLAUDE.md a pointer to it, the depth is under agents/.

### 17-leftovers.js — 258 lines, 47 ok/eq call sites
The "Known small leftovers" of IMPROVEMENTS-2026-09.md that were closed out.
- slices js/app.js: calStep, _wireDatePickerKeys, pickCalDate, rebookNextWeek
- slices js/settings.js: updatePill

### 18-android.js — 972 lines, 171 ok/eq call sites
The Android app ("level 2": everything the iPhone app does except the widgets, the Live Activity and Siri — then "level 3", a home-screen widget, which has its own suite: 20-android-widget.js, on this file's…
- pure: android-back, welcome, reminder-row (js/tabs.js), android-bridge (ios-app/www/native-bridge.js)
- slices js/app.js: _busyLabel, _friendlyError, bookClass, showTokenDialog, _announceAllocations
- slices js/tabs.js: _formatGbp, _shareAsTextOnAndroid
- slices js/diagnostic.js: openDiagnostics
- text anchors in js/app.js: `// ── PWA Service Worker`
- text anchors in js/tabs.js: `var _classReminderGranted = null;`
- text anchors in js/settings.js: `var _diagReportText = null;` · `window.openDiagnostics = function () {`

### 19-android-project.js — 1453 lines, 352 ok/eq call sites
The Android project (ios-app/android/ — the folder name ios-app/ is historical: both platforms wrap the same ios-app/www/).

### 1f-contrast-tokens.js — 333 lines, 38 ok/eq call sites
Colour-contrast guards for the design tokens (css/theme.css) and the stylesheets that consume them.

### 1g-layout-fixes.js — 175 lines, 47 ok/eq call sites
Layout fixes: sub-filter pills, date row order, theme picker grid, class-card action wrapping.
- pure: filters
- slices js/app.js: _busyLabel
- text anchors in js/app.js: `// ── Strength sub-filter` · `// Extract slot numbers from any API format`

### 20-android-widget.js — 424 lines, 84 ok/eq call sites
The Android home-screen widget ("level 3"), as far as the web layer and the bridge go.
- pure: class-colours (js/theme.js), core, class-type, widget-link, ios-polish
- slices ios-app/www/native-bridge.js: _prefSet, updateWidgetSnapshot
- text anchors in ios-app/www/native-bridge.js: `// ── Widget / Live Activity / Siri Snapshot` · `window.updateWidgetSnapshot = updateWidgetSnapshot;`

### 21-android-countdown.js — 421 lines, 73 ok/eq call sites
The Android class countdown, as far as the bridge goes.
- pure: reminder-row (js/tabs.js), welcome
- slices js/tabs.js: _formatGbp
- slices ios-app/www/native-bridge.js: updateWidgetSnapshot, _androidCountdownSwitch, _androidCountdownFlipped, _loadReminderMap
- text anchors in js/tabs.js: `var _classReminderGranted = null;`
- text anchors in ios-app/www/native-bridge.js: `window.updateWidgetSnapshot = updateWidgetSnapshot;`

### 22-play-release.js — 446 lines, 174 ok/eq call sites
Getting the Android app onto Google Play, as far as it can be READ.

### 23-release-polish.js — 112 lines, 16 ok/eq call sites
The release polish of September 2026, held so it cannot quietly come back.
- slices ios-app/www/native-bridge.js: _classReminderBody
- text anchors in js/diagnostic.js: `var msg = document.createElement('span');`
- text anchors in js/api-client.js: `var USER_MESSAGES = {`
- text anchors in js/settings.js: `var PRIVACY_POLICY_URL =` · `var _diagReportText = null;`

### 2e-css-layout.js — 296 lines, 67 ok/eq call sites
Tap targets, desktop Discover spacing, radius tokens, card entrance motion.
- pure: stats-pages (js/tabs.js)
- slices js/app.js: upcomingSeatCancel
- text anchors in js/tabs.js: `// ── Membership tab`
- text anchors in js/features.js: `// Bio⏎`

### 3d-leftovers.js — 611 lines, 108 ok/eq call sites
Leftovers, each seen in a browser first.
- pure: static-cache (js/performance.js), init-gate, cost-forecast (js/tabs.js), gym-time, bookings-started, clash, book-fresh, waitlist
- slices js/tabs.js: renderCostTracker, _forecastHtml, _formatGbp, _daysLeftInBillingPeriod, _historyStartMs, _stillToCome, updateTabBadge, renderQuickStats
- slices js/app.js: renderMyBookings, localDateStr, parsePsycleDate, fetchMyBookings, _busyLabel, _clashFor, bookClass, _markSeatFreed, _bookEventHeadless, renderTravelNotice
- slices js/settings.js: updatePill
- text anchors in js/app.js: `// ── pure:` · `const _modulesLoaded = _laterModulesLoaded(document,…` · `const fetchJson = path => apiFetch(path)`
- text anchors in js/performance.js: `const CACHE_PREFIX = 'psycle_cache_';⏎`

### 4f-cleanup-pwa.js — 430 lines, 99 ok/eq call sites
Clean-up + PWA shell guards.
- pure: ics-share (js/calendar.js)
- slices js/theme.js: injectThemeToggle
- slices js/calendar.js: downloadICS
- slices js/tabs.js: switchTab
- slices js/settings.js: exportSettings, importSettings
- text anchors in js/tabs.js: `window.switchTab = function (tab, noHash) {` · `// Deep links and back/forward`
- text anchors in js/settings.js: `window.exportSettings = function () {` · `window.importSettings = function (input) {` · `// Bug Report`

### 6e-design-tokens.js — 266 lines, 42 ok/eq call sites
Design-token guards: toast types, the late-cancel caution treatment, the bike-picker seat map, the display face, the pull-to-refresh pill.

### 7a-final-polish.js — 518 lines, 83 ok/eq call sites
Leftovers, each seen in a real browser.
- pure: history-topup (js/explore.js), booking, copy
- slices js/explore.js: renderSyncBanner, _explore_openSettingsForInstructor
- slices js/app.js: _studioNeedsReread, _ensureStudioKnown, _classDetailBookAction, _clock24, _cancelDeadline, _rereadBookingsForVerify, _pickerTakenSince, _wireDatePickerKeys, toggleDatePicker, changeSpot
- slices js/settings.js: _pillA11y
- text anchors in js/explore.js: `var TOPUP_MAX_NEW` · `window._explore_openSettingsForInstructor = function`
- text anchors in js/app.js: `// THE document keydown for overlays` · `// The other half of Space`

### 8a-filters.js — 433 lines, 103 ok/eq call sites
"Date range filter should be exposed, all other filters should be minimised as default" (a product designer's review of the iPhone app).
- pure: filter-summary, filters
- slices js/app.js: _repaintKeepingFocus, _removeFilter, clearFilters, _focusSearch, _syncFilterUI
- text anchors in js/app.js: `// ── The Filters bar (below 1024px)` · `// Allow pressing Enter to search` · `// ── Upcoming bookings panel`

### 8b-day-pager.js — 504 lines, 167 ok/eq call sites
Discover shows a range ONE DAY AT A TIME: a strip of days over a pager, changed by tap, arrow key or a sideways swipe.
- pure: day-pager, swipe-nav (js/interactions.js), filters, window, gym-time
- slices js/app.js: _pagerModelFor, render, _paintDayHint, showDiscoverDay, _swapPagerDay, _paintDays, _paintDayGroup, _wireDayPager, _paintDayStrip, _dayStripKeydown, _revealDayPill, _restoreDayStrip, _revealPillsAfterThemeChange, _focusSearch, applySavedSearch, applySearchPreset, toggleLocation, toggleCategory
- slices js/tabs.js: switchTab
- text anchors in js/app.js: `// ── Discover: one day at a time` · `window._dayPagerSwipe = {` · `// ── Studio multi-select chips`
- text anchors in js/interactions.js: `// D. Sideways swipe between pages` · `// C. Filter Persistence`
- text anchors in js/tabs.js: `window.switchTab = function (tab, noHash) {` · `// Deep links and back/forward`

### 8c-stats-pages.js — 447 lines, 121 ok/eq call sites
Stats in three sub-pages behind a content switcher.
- pure: stats-pages (js/tabs.js)
- slices js/tabs.js: renderInsights
- slices js/explore.js: renderExplore
- text anchors in js/tabs.js: `// ── Membership tab` · `// ── Render insights tab content` · `window.renderInsights = function () {` · `// ── Quick Stats`
- text anchors in js/explore.js: `var closed = function (el) {` · `// Loading state if data not ready` · `window.renderExplore = function () {`

### 8d-welcome.js — 594 lines, 170 ok/eq call sites
First-run welcome: the full-screen, swipeable overlay that replaced the four-card tour.
- pure: welcome
- slices js/app.js: _onboardLaunchFacts, _maybeStartOnboarding, _onboardFinish, replayOnboarding, _onboardBindSwipe
- text anchors in js/app.js: `⏎let _onboardIdx = 0;` · `⏎// What a launch can know about the person in front…` · `// Feature: First-run welcome` · `// Feature: Timezone-aware travel notice`

### 8e-declutter.js — 212 lines, 40 ok/eq call sites
"Remove bloat and pointless copy".
- pure: clock, stats-charts
- slices js/tabs.js: _weekIndex, renderStreaks, _nextWeekdayDateStr, renderHabitSlots

### 8f-wave8-seams.js — 207 lines, 49 ok/eq call sites
The seams BETWEEN the five lanes (filters bar, day pager, Stats sub-pages, first-run welcome, declutter).
- pure: stats-pages (js/tabs.js), welcome, swipe-nav (js/interactions.js), day-pager
- slices js/app.js: _ownKeysOverlayUp, _onboardReveal, startOnboarding, _paintDayHint, _discoverEmptyContext, mergeRelations
- slices js/tabs.js: _statsSwipeMayStart
- text anchors in js/tabs.js: `// ── Render insights tab content` · `var _statsHeroOnly =`
- text anchors in js/theme.js: `const EMPTY_STATE_ACTIONS` · `// ── D. Haptic Feedback`

### 9a-foundation.js — 580 lines, 168 ok/eq call sites
The Crisp Colour foundation: faces, tokens, the class-type colour engine (js/theme.js pure:class-colours + window.PsycleClassColours), the pictograms (js/app.js pure:class-type), css/crisp.css's mapping block and…
- pure: class-colours (js/theme.js), core, class-type, import-validate (js/settings.js)
- slices js/app.js: getCategory
- text anchors in js/theme.js: `// ── A. Themes` · `// ── B. Skeleton Loading Cards`
- text anchors in js/app.js: `const CATEGORY_MAP = [`

### 9b-discover.js — 446 lines, 133 ok/eq call sites
Crisp Colour on Discover: the app header and tab bar, the date row, the Filters bar and its chips, the day strip, and THE CLASS CARD.
- pure: day-pager, class-type, core, discover, class-colours (js/theme.js)
- slices js/app.js: _pagerHeldDays, _pagerHeldSpoken, _paintDayDot, _heldDaysFor, _repaintDayDots, _paintDayStrip, _paintDayGroup, eventCard, updateFiltersSummary, renderCategoryPills, renderLocationChips, renderTimePills
- slices js/theme.js: skeletonCardHTML, renderEmptyState
- text anchors in js/app.js: `// Feature 13's write, behind a trailing debounce` · `// A star can change where no filter toggle runs`
- text anchors in js/tabs.js: `var TAB_ICONS = {` · `var tabBar = document.createElement` · `var icon = function`

### 9c-sheets.js — 456 lines, 129 ok/eq call sites
Crisp Colour sheets: the class sheet, the seat picker, the Booked sheet, confirm dialogs, the Find-similar popup and the instructor / history modals.
- pure: sheets, bookings-crisp, class-type, core, clash
- slices js/app.js: confirmCancelWithPolicy, confirmModal, pluralizeSlotLabel, showBikePicker, _syncPickerConfirmLabel, selectBike, closeBikePicker, _clashFor, showBookingConfirmation, dismissBookingConfirmation, openClassDetail, findSimilar
- slices js/features.js: openHistoryModal, openInstructorModal, classStatusChip
- text anchors in js/app.js: `window.confirmModal = confirmModal;` · `window.openClassDetail = function (eventId) {` · `let _confirmationTimer = null;⏎` · `window.findSimilar = function(eventId) {` · `// ── Extend findSimilar with the predicted "book…` · `// ── Wire the new Discover-tab surfaces`
- text anchors in js/settings.js: `var _origShowBikePicker = window.showBikePicker;` · `// Integration: Tier Badges on Class Cards`
- text anchors in js/features.js: `// ── Monkey-patch submitBooking` · `// ★ + S–F under the name`

### 9d-bookings.js — 644 lines, 187 ok/eq call sites
My Bookings, "Your usual week" and the next-class pill in the Crisp Colour design.
- pure: core, bookings-crisp, bookings-card, offline, usual-week-crisp (js/tabs.js), clock, sheets, waitlist
- slices js/app.js: renderMyBookings, _bookingsListBusy, _bookingsRepaintSafe, _syncOverlayStack, confirmModal, findSimilar
- slices js/tabs.js: _uwCardHtml, renderUsualWeekCard, _uwRowHtml
- slices js/settings.js: updatePill
- slices js/calendar.js: renderCalendarActions
- text anchors in js/app.js: `// ── pure:` · `let _mbMoreOpen = null;` · `window.toggleBookingMore = toggleBookingMore;` · `// ── Open a booking's studio in the maps app` · `window.confirmModal = confirmModal;` · `window.findSimilar = function(eventId) {`
- text anchors in js/tabs.js: `window.renderUsualWeekCard = renderUsualWeekCard;` · `// Where focus goes when the sheet closes`
- text anchors in js/calendar.js: `// Inject styles`

### 9e-stats-membership.js — 332 lines, 107 ok/eq call sites
Crisp Colour on Stats, Membership, the Class colours control, the share images and the sign-in page.
- pure: stats-charts (js/tabs.js), class-colours (js/theme.js), class-colour-control (js/tabs.js), class-type, share (js/tabs.js)
- slices js/tabs.js: renderClassTypeDistribution, _ccSet, renderThemePicker, renderHeatmap, _shareClassColour
- slices js/explore.js: instrCard
- text anchors in js/tabs.js: `var CC_TICK =` · `// Rebuilt whole on every change` · `// The engine changed something` · `// ── Class colours (Membership` · `// ── Weekly reminder row`

### 9f-one-card.js — 235 lines, 62 ok/eq call sites
Integration: the class card is ONE component with two wearers.
- pure: class-type
- slices js/app.js: eventCard, renderMyBookings, _savedBookingsHTML

### a11y.js — 816 lines, 151 ok/eq call sites
Accessibility outside the Discover list: the toast / live-region helpers, what a picker seat tells a screen reader, and the ONE key handler + focus stack that look after the nine sheets and panels — run as the REAL…
- pure: a11y, sheets, session
- slices js/app.js: showSessionExpired, confirmModal, describeCancelError, _dialogOpen, showHistorySyncPrompt, startSyncFromPrompt, _overlayIsOpen
- text anchors in js/app.js: `// Say something to screen-reader users.` · `// ── Discover empty state` · `// Token from login is now received` · `window.confirmModal = confirmModal;` · `// ── Sheets and panels: one focus stack` · `⏎let _allocAnnounceTimer` · `const _OVERLAYS = [` · `const _overlayStack = [];` · `// confirmModal and the tour keep their own focus`

### add-spot-studio.js — 191 lines, 28 ok/eq call sites
"+ Add spot" on a My Bookings card painted from the saved class details (psycle_booked_event_details) calls bookClass(id, btn, evt.studio_id) before anything has put that studio on _studioMap.
- pure: book-fresh
- slices js/app.js: bookClass, _busyLabel

### booking-extras.js — 159 lines, 34 ok/eq call sites
Booking extras: the My Bookings countdown chip reading the same London-aware class start as the late-cancel warning (the runner's zone is New York — exactly the "member abroad" case), the shared class-history cap (#32)…
- pure: gym-time, bookings-card
- slices js/app.js: _gymDayKey, getCountdownText, selectBike
- slices js/theme.js: wrapSearch
- slices js/explore.js: _explore_openSettingsForInstructor
- text anchors in js/explore.js: `window._explore_openSettingsForInstructor = function…`

### booking-races.js — 590 lines, 88 ok/eq call sites
Booking races + failure wording.
- pure: booking
- slices js/app.js: _busyLabel, describeCancelError, _friendlyError, bookClass, _studioNeedsReread, _ensureStudioKnown, _pickerTakenSince, _classDetailBookAction, joinWaitlist, leaveWaitlist, claimWaitlistSpot, executeSpotSwap, rebookNextWeek, cancelBikeSlot, confirmUnbook, upcomingCancel, upcomingSeatCancel, _scheduleBookingsRefetch, _rereadBookingsForVerify, _dropBookingKeepPlace, _bookingIdsFor, _readyForWholeCancel, pluralizeSlotLabel, showBikePicker, changeSpot
- slices js/settings.js: renderBikePrefGrid
- text anchors in js/app.js: `window.changeSpot = async function(eventId) {`
- text anchors in js/settings.js: `window.renderBikePrefGrid = function () {`

### booking.js — 1088 lines, 257 ok/eq call sites
Booking integrity: the seat merge, the per-seat cancel id, the "what did that POST actually do?" decision table — and the wiring around them, run against the REAL submitBooking + the REAL reliability.js optimistic…
- pure: booking
- slices js/app.js: _busyLabel, pluralizeSlotLabel, formatSlots, _scheduleBookingsRefetch, _rereadBookingsForVerify, _announceVerifiedSeats, _settleUnverifiedBooking, _clearUnverifiedBooking, submitBooking, _bookEventHeadless, _dropBookingKeepPlace, _bookingIdsFor, _readyForWholeCancel, confirmUnbook, upcomingCancel, upcomingSeatCancel, _clock24, _cancelDeadline, _repaintKeepingFocus, renderChangeSpotHint, executeSpotSwap, _recheckAuthForBooking, bookClass, _bookWeeklyTemplateInner, _classDetailBookAction, changeSpot, setChangeSpotTarget
- slices js/reliability.js: _processOfflineQueueInner
- text anchors in js/reliability.js: `window.submitBooking = async function…`
- text anchors in js/app.js: `window.setChangeSpotTarget = function (slot) {`

### bookings-card.js — 679 lines, 170 ok/eq call sites
My Bookings card: the shared 12h late-cancel helper (_cancelDeadline), the cache-entry decorator rebookNextWeek seeds with (_eventCacheEntry), and the markup/selector pairs that silently went dead once before.
- pure: bookings-card, gym-time, swipe-cancel (js/interactions.js), filters
- slices js/app.js: getCountdownText, confirmCancelWithPolicy, getCategory, rebookNextWeek, _timeBandOf, _admitTimeBands, _focusSearch, changeSpot, findSimilar
- text anchors in js/app.js: `window.changeSpot = async function(eventId) {` · `const REFORMER_SUBS = [` · `const TIME_BANDS = [` · `window.findSimilar = function(eventId) {`
- text anchors in js/interactions.js: `// B. Swipe-to-Cancel` · `// C. Filter Persistence`

### bookings-load.js — 233 lines, 46 ok/eq call sites
My Bookings' load state (js/app.js): an empty _myBookings can't tell "nothing booked" from "couldn't ask".
- slices js/app.js: renderMyBookings, _checkAuthOnce
- text anchors in js/app.js: `let _bookingsSeq = 0;` · `// Refresh bookings when the page becomes visible…` · `const items = _showPastBookings` · `const PROFILE_REFRESH_MIN_GAP_MS`

### calendar-safety.js — 617 lines, 123 ok/eq call sites
Calendar safety: Psync may only destroy events in a calendar the member EXPLICITLY handed over (the ownership dialog → psycle_calendar_owned_ack).
- pure: calendar-sync (js/settings.js)

### clash.js — 246 lines, 65 ok/eq call sites
Clash detection (#63): the pure interval helper (pure:clash in js/app.js) and the places that speak it — the REAL bookClass confirms + picker hand-off, confirmJoinWaitlist and the headless "Book my week" path, sliced…
- pure: clash
- slices js/app.js: _busyLabel, _clashFor, bookClass, confirmJoinWaitlist, _bookEventHeadless

### copy.js — 307 lines, 72 ok/eq call sites
Member-facing wording + the two share images.
- pure: copy, share (js/tabs.js), year-review (js/tabs.js)
- text anchors in js/tabs.js: `// ── Share images (year wrap + stats card): shared…` · `// ── Init ─`

### data-owner.js — 417 lines, 93 ok/eq call sites
Whose data is on this device (js/app.js, pure:data-owner + _claimDataOwner): history, rankings, favourites, bike prefs, the usual bike, the weekly template… used to belong to the INSTALL.
- pure: data-owner, history (js/features.js), session
- slices js/app.js: _claimDataOwner, _applyProfile, clearToken, showSessionExpired
- slices js/tabs.js: renderInsights
- text anchors in js/app.js: `const HISTORY_PROMPT_DISMISSED_KEY` · `// Single-flight per token` · `// Token from login is now received`
- text anchors in js/tabs.js: `window.renderInsights = function () {` · `// ── Quick Stats`

### diag-wiring.js — 131 lines, 26 ok/eq call sites
API-drift diagnostics are actually wired (js/api-client.js, js/diagnostic.js, js/app.js): api-client called a PsycleDiag.noteSample that never existed, and app.js only ever recorded the waitlist resources — so the…
- pure: waitlist
- slices js/app.js: fetchMyBookings, fetchEventsForLocation, _checkAuthOnce
- text anchors in js/app.js: `const _shapeRecordedAt = {};` · `let _lastWaitlistEntries = null;` · `let _waitlistsUnavailable = false;` · `⏎let _searchSeq` · `const PROFILE_REFRESH_MIN_GAP_MS`

### discover-journeys.js — 533 lines, 130 ok/eq call sites
Discover journeys: the Monday-noon release (freshness, the release timer, the 'Next week' preset, the reminder-tap hook), the focused "find it" searches, and the instructor suggestions' move to Stats.
- pure: gym-time, filters, window
- slices js/app.js: _revalidateIfStale, _searchLabel, updateFiltersSummary, _discoverEmptyContext, mergeRelations, search, getCategory, _focusSearch, bookPrediction, _dropFocusStash, _restoreFocusStash, removeInstructor, toggleLocation, toggleCategory, toggleTimeBand, clearFilters, applySavedSearch, clearToken, toggleAvailableOnly, clearTimeFilters, _onBookingWeekOpened, findSimilar
- slices js/interactions.js: saveFilters, restoreFilters
- slices js/tabs.js: renderHabitSlots, switchTab
- slices js/explore.js: markDirtyAndMaybeRender, signedOutBlank
- slices js/features.js: _features_filterByInstructor
- text anchors in js/app.js: `// ── Discover rolls forward` · `// ── Monday noon: the new booking week opens` · `window._onBookingWeekOpened = function` · `window.findSimilar = function(eventId) {` · `const CATEGORY_MAP = [` · `const REFORMER_SUBS = [`
- text anchors in js/features.js: `window._features_filterByInstructor = function…` · `// Expose for onclick in patched HTML`
- text anchors in js/tabs.js: `// Delegated handler: habit "Find this week" buttons…` · `// ── Streaks & milestones` · `// ── My Bookings tab` · `window.switchTab = function (tab, noHash) {` · `// Deep links and back/forward`

### discover.js — 610 lines, 190 ok/eq call sites
Discover list: "spots left" (#19), the batched / linear-merge render (#39, #49) and the Time row — time-of-day bands + "Available only" (#4, #18).
- pure: discover, window, class-type, core
- slices js/app.js: render, _repaintKeepingFocus, renderReformerSubPills, clearFilters, setStatus, currentFilters, discoverFacets, refreshFacetCounts, _buildFacetClasses, _setWindow, restoreLastResults, eventCard, _armCountsExpiry, _syncCardButtonsForEvent, _dropCardCounts, _waitlistClassLine, bookClass, _mirrorDatePillAria, renderCalendar, openClassDetail
- slices js/interactions.js: restoreFilters, saveFilters
- text anchors in js/app.js: `// ── Studio multi-select chips` · `let _lastResultsTimer = null;` · `// The Time row, LAST` · `const container = document.getElementById('results');` · `const filtered = events.filter(e => {` · `window._discoverQuery` · `const locIds` · `let _ownFullSeen = '';` · `// ── Pick-a-date calendar` · `const selectedTimeBands = new Set();` · `// Allow pressing Enter to search` · `// Lite per-event view` · +11 more
- text anchors in js/interactions.js: `// Expose to global scope`

### event-details.js — 284 lines, 82 ok/eq call sites
Saved class details (js/app.js): My Bookings' first paint after a relaunch no longer waits on one GET /events/{id} per held class.
- pure: event-details
- slices js/app.js: fetchMyBookings
- text anchors in js/app.js: `let _bookingsSeq = 0;` · `// Refresh bookings when the page becomes visible…` · `let _waitlistsUnavailable = false;`

### facets-core.js — 205 lines, 81 ok/eq call sites
What decides which classes a member sees, and what they are told they booked.
- pure: core

### filters.js — 356 lines, 99 ok/eq call sites
Discover filters: date presets, saved-filter restore, the date row's state, /events paging and the Strength/Reformer sub-filter gate (js/app.js).
- pure: filters
- slices js/app.js: fetchEventsForLocation, search
- slices js/interactions.js: restoreFilters
- text anchors in js/app.js: `⏎let _searchSeq` · `// Strength sub-filter` · `const locIds` · `let _sf = null;` · `// Same rule as currentWindowDates()`
- text anchors in js/interactions.js: `// Expose to global scope`

### history-sync.js — 230 lines, 37 ok/eq call sites
The silent weekly history top-up (#76, js/explore.js): the REAL _explore_syncHistory and maybeTopUpHistory, sliced out of source and run against a scripted Psycle.
- pure: copy
- text anchors in js/explore.js: `var TOPUP_MAX_NEW`

### history.js — 291 lines, 64 ok/eq call sites
Class-history reconcile (#76): the pure diff (pure:history in js/features.js) and the REAL fetchMyBookings wrapper around it, sliced out of source and run against scripted fetches.
- pure: gym-time, history (js/features.js)
- slices js/explore.js: getHistory
- slices js/features.js: saveHistory, _hasToken, _classStartMs, _historyOwnerId, _historyIsMine, reconcileHistoryWithBookings, patchFetchMyBookings
- slices ios-app/www/native-bridge.js: restoreFromNative

### import-validate.js — 313 lines, 83 ok/eq call sites
What comes out of storage, and what an import file may put into it.
- pure: stored-data, import-validate (js/settings.js)
- slices js/app.js: instrLink, renderInstrChips, renderInstrDropdown, pluralizeSlotLabel, formatSlots, loadFavourites, _saveSetting, detectRecurringSlots, predictNextClass
- slices js/explore.js: getHistory
- slices js/features.js: saveHistory
- slices js/settings.js: importSettings
- text anchors in js/app.js: `const typeByName`
- text anchors in js/settings.js: `window.importSettings = function (input) {` · `// Bug Report`

### ios-bridge.js — 770 lines, 115 ok/eq call sites
iOS bridge (ios-app/www/native-bridge.js) + the two web-side pieces that only matter inside the app: the class-reminder switch (js/tabs.js) and the native settings export (js/settings.js).
- pure: reminder-row (js/tabs.js), settings-export (js/settings.js)
- slices js/tabs.js: _toggleReminder
- text anchors in js/tabs.js: `var _classReminderGranted = null;` · `window._toggleReminder = async function () {`

### ios-polish.js — 428 lines, 86 ok/eq call sites
iOS polish: which classes get a T-90 reminder, a held seat whose details failed to load, the Monday-reminder tap + copy, the pull pill's hidden state, and the Live Activity seat text.
- pure: ios-polish (ios-app/www/native-bridge.js)
- slices js/interactions.js: resetPullIndicator, showRefreshing, pullRefreshAction
- text anchors in PsycleLiveActivity/PsycleLiveActivityAttributes.swift: `public struct ContentState` · `// Static attributes`

### notify.js — 454 lines, 94 ok/eq call sites
"Notify me when a spot opens" (#100): the pure plan + the strict "opened" test (pure:notify in js/features.js), then the REAL checkWatchedEvents and its dialog, sliced out of source and run against a scripted Psycle.
- pure: gym-time, notify (js/features.js), book-fresh
- slices js/features.js: _hasToken
- text anchors in js/features.js: `const NOTIFY_KEY` · `// Request notification permission` · `let _watchLastPassAt` · `// ── Line class history up`

### offline-queue.js — 568 lines, 128 ok/eq call sites
The offline queue (js/reliability.js, section D).
- pure: gym-time, offline-queue (js/reliability.js), clock
- text anchors in js/reliability.js: `var OFFLINE_QUEUE_KEY = 'psycle_offline_queue';`

### offline.js — 663 lines, 166 ok/eq call sites
Offline / PWA plumbing.
- pure: offline, class-type, static-cache (js/performance.js)
- slices js/app.js: renderMyBookings, showBookingSkeleton
- text anchors in js/app.js: `// ── pure:` · `const BOOKINGS_SNAPSHOT_KEY =…` · `// Init — wait for security module to decrypt stored…` · `const fetchJson = path => apiFetch(path)` · `// ── Countdown helper` · `// ── PWA Service Worker`
- text anchors in js/reliability.js: `const FETCH_TIMEOUT_MS = 15000;` · `// Wrap the global apiFetch to use fetchWithRetry…`
- text anchors in js/performance.js: `const CACHE_PREFIX = 'psycle_cache_';`

### owner-tools.js — 337 lines, 72 ok/eq call sites
Owner tools: the build id in bug reports / diagnostics (js/settings.js + the iOS bridge's getDiagnosticReport) and Discover's "S/A" tier quick filter (js/app.js).
- pure: build-id (js/settings.js), tier-filter
- slices js/settings.js: buildBugReport, clearDiagnosticLogs, _fallbackCopy, copyDiagnostics, downloadBugReport, copyBugReport, closeDiagnostics
- slices js/app.js: renderInstrChips
- slices js/diagnostic.js: openDiagnostics
- slices ios-app/www/native-bridge.js: getDiagnosticReport
- text anchors in js/settings.js: `window.downloadBugReport = async function () {` · `window.copyBugReport = function () {` · `var _diagReportText = null;` · `window.openDiagnostics = function () {` · `window.closeDiagnostics = function () {`
- text anchors in ios-app/www/native-bridge.js: `window.getDiagnosticReport = async function () {` · `⏎⏎⏎  console.log('[native] Native bridge initialized');`

### predict.js — 114 lines, 27 ok/eq call sites
"Book again?" (js/app.js pure:predict + its two callers).
- pure: predict, clock
- slices js/app.js: predictNextClass, renderRebookHint

### reliability.js — 346 lines, 54 ok/eq call sites
The double-booking guards in js/reliability.js.
- pure: booking

### render-perf.js — 506 lines, 107 ok/eq call sites
Render-perf: work that used to be redone for nothing, outside the Discover list.
- pure: bookings-card, clock, history-chunks (js/features.js), render-perf, bookings-started, waitlist, copy
- slices js/app.js: _gymDayKey, getCountdownText, _minuteTick, _bookingsListBusy, _tickLastUpdated, renderMyBookings
- slices js/tabs.js: getFullHistory, renderHeatmap, renderRecommendations
- slices js/explore.js: getHistory, setHtml, _topUpTooBig, hasSynced, renderSyncBanner, _explore_syncHistory
- slices js/features.js: historyChunks, openHistoryModal
- text anchors in js/app.js: `// ── pure:`
- text anchors in js/tabs.js: `var _historyRaw = null, _historyParsed = null;`
- text anchors in js/explore.js: `var TOPUP_SKIPPED_KEY = 'psycle_history_topup_skipped';⏎` · `var escapeHtml =` · `window._explore_syncHistory = async function` · `// MASTER RENDER`

### retry-signout.js — 208 lines, 27 ok/eq call sites
js/reliability.js builds a request's Authorization header ONCE and the retry loop re-sends it after every backoff (1s, 2s, 4s…).
- pure: retry-auth (js/reliability.js)
- slices js/reliability.js: _renderQueueStatus
- text anchors in js/reliability.js: `// ── When the queue drains ──`

### session.js — 490 lines, 145 ok/eq call sites
Session lifecycle (js/app.js): what a /profile attempt means for the session, single-flight checkAuth, the token-change guard, refreshProfile, and the first-run history prompt's offer rule.
- pure: session
- slices js/app.js: clearToken, confirmSignOut, showSessionExpired
- text anchors in js/app.js: `const HISTORY_PROMPT_DISMISSED_KEY` · `// Token from login is now received` · `let _bookingsSeq = 0;` · `// Refresh bookings when the page becomes visible…`
- text anchors in js/explore.js: `// Update button with progress`

### shell.js — 131 lines, 42 ok/eq call sites
The page shell: which scripts load, and in what order.

### signin-storage.js — 370 lines, 63 ok/eq call sites
Storage around the sign-in path.
- pure: stored-data
- slices js/app.js: applyFavouritesAsFilter, _persistWindow, _readWindowCache
- slices js/theme.js: initTheme, setAppTheme
- text anchors in js/theme.js: `window.setAppTheme = function (id) {`

### waitlist-polish.js — 856 lines, 156 ok/eq call sites
Waitlist polish: the offer-window / phase helpers in the waitlist:pure block, what the My Bookings card, the class sheet and the two seat-creating dialogs say because of them, the clash helper's opt-in for places, the…
- pure: gym-time, bookings-card, clash, waitlist, bookings-started, copy, render-perf
- slices js/app.js: _placeNeedsOfferCheck, _probeWaitlistOffers, fetchMyBookings, leaveWaitlist, joinWaitlist, renderMyBookings, showBookingConfirmation, _clashFor, confirmJoinWaitlist, _bookEventHeadless, confirmCancelWithPolicy, _busyLabel, _friendlyError, claimWaitlistSpot, _dialogOpen, _announceAllocations, _classDetailClaimAction, rebookNextWeek, openClassDetail
- text anchors in js/app.js: `let _bookingsSeq = 0;` · `// Refresh bookings when the page becomes visible…` · `window.openClassDetail = function (eventId) {` · `const WAITLIST_RECHECK_MS = 120000;` · `// After a real booking is cancelled locally` · `// The waitlist list missed the deadline` · `// Places that arrived late are full classes too`

### wave7-leftovers.js — 331 lines, 50 ok/eq call sites
Leftovers, each seen in a real browser.
- pure: year-review (js/tabs.js), gym-time, filters, clock
- slices js/tabs.js: _historyStartMs, _stillToCome, renderQuickStats, _hasTakenHistory, renderVarietyTrend, shareInsights, renderInsights, switchTab
- slices js/app.js: _revealActiveDatePill, _syncDatePills, _waitlistClassLine, setDateQuick, _applyDateQuick, _showOpenedDay, search, _releaseDateRow, onDateInputChange, clearFilters, applySavedSearch, clearToken
- slices js/interactions.js: saveFilters, restoreFilters
- text anchors in js/app.js: `// ── pure:` · `window._dateRowHeld = true;`
- text anchors in js/tabs.js: `window.shareInsights = async function () {` · `window.renderInsights = function () {` · `// ── Quick Stats` · `window.switchTab = function (tab, noHash) {` · `// Deep links and back/forward`

### weekly-template.js — 689 lines, 163 ok/eq call sites
"Your usual week" (weekly template, #35/#91): the pure matching block (pure:template in js/app.js) and the three functions that speak it — planWeeklyTemplate (reads only), _bookWeeklyTemplateInner (executes the picks…
- pure: template, clash
- slices js/app.js: _bookWeeklyTemplateInner, _layoutFromEventDetail, _templateRefusalText, _bookTemplateSeat, _friendlyError, joinWaitlist, _fetchTemplateDay, planWeeklyTemplate, showBookingConfirmation, dismissBookingConfirmation, detectRecurringSlots
- slices js/tabs.js: _uwFocusBack, _uwFocusedButton, renderUsualWeekCard
- text anchors in js/app.js: `let _confirmationTimer = null;⏎`
- text anchors in js/tabs.js: `window.renderUsualWeekCard = renderUsualWeekCard;` · `var focusedIdx = _uwFocusedButton(card, document);`

### widget-link.js — 298 lines, 53 ok/eq call sites
Widget tap → My Bookings.
- pure: widget-link (ios-app/www/native-bridge.js)

### window.js — 741 lines, 222 ok/eq call sites
Discover's timetable window (js/app.js): which ranges the loaded window can serve, when its numbers count as fresh, what a revalidate fetches and — above all — that a revalidate can never commit a truncated window.
- pure: filters, window, filter-summary
- slices js/app.js: render, discoverFacets, refreshFacetCounts, renderLastUpdated, refreshWindow, search, _setWindow, _persistWindow, triggerAutoSearch, _discoverEmptyContext, mergeRelations, renderFromWindow, _renderWindowInPlace, showSearchError
- slices js/interactions.js: pullRefreshAction
- text anchors in js/app.js: `// ── Studio multi-select chips` · `// Fetch the full window (every studio) for a date…` · `// ── Discover rolls forward` · `const btn = document.getElementById('searchBtn');` · `// Friendly search-failure handling.` · `let _discoverDay = localDateStr();` · `// Back in the foreground` · `// ── "Last updated"` · `// "Available only" never hides the member's own class` · `// The sequence number of the search()` · `// Signal is back: heal an unverified session` · `// ── Offline indicator` · +2 more
- text anchors in js/interactions.js: `// At <=640px the body is overflow:hidden`
- text anchors in js/theme.js: `const EMPTY_STATE_ACTIONS` · `// ── D. Haptic Feedback`

### year-review.js — 104 lines, 17 ok/eq call sites
Year in review (Stats → "See your 2026 wrap" → the share image).
- pure: gym-time, year-review (js/tabs.js), clock
- slices js/tabs.js: _weekIndex, _computeYearReview

## tests/tools
- tests/tools/appstore-capture.html (78 lines)
- tests/tools/appstore-shots.mjs (105 lines) — Rebuild the six App Store screenshots (1290 x 2796) from the REAL app on the fake Psycle server.
- tests/tools/fake-psycle.js (245 lines) — A fake Psycle server for browser checks and App Store captures (tests/README.md → "Driving the app against a stubbed API").
