# js/settings.js — symbols
Purpose: the shape of `js/settings.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `ebc2926`). Do not edit; rebuild with `npm run agents:index`.

2141 lines · 100 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.

L20  var BIKE_PREF_KEY

### Data Persistence (L22)
L28  _store(key, value)  — Reads are coerced (app.js, pure:stored-data — both keys can come out of an imported file); writes go through…
L33  loadBikePrefs()
L39  saveBikePrefs(p)

### Public API (L41)
L43  window.getBikePrefs(studioId)

## Floating Next Class Pill (L49)
L53  var _pillEl
L54  var _pillTimer
L56  createPill()
L88  _pillA11y(label)  — The pill is a div that acts as a button (it holds div children, so it stays a div).
L108  updatePill()  +88
L197  startPillTimer()

### The pill steps aside (L236)
L247  var PILL_TUCK_AFTER  {pill-scroll}
L248  var PILL_SHOW_AFTER  {pill-scroll}
L249  var PILL_EDGE  {pill-scroll}
L254  _pillScrollStep(prev, f)  {pill-scroll}  — One scroll position in, the pill's next state out.
L284  _pillCalledBack(s)  {pill-scroll}  — A state in, "did the member just call it back?" out: mid-list, a run UP of PILL_SHOW_AFTER — the run that un-tucks it,…

### At rest it never sits on a Book button (L289)
L304  var PILL_REST_MS  {pill-rest}
L305  var PILL_REST_AGAIN_MS  {pill-rest}
L307  var PILL_OVERLAP_MIN  {pill-rest}
L315  _pillCoversBook(box, rects)  {pill-rest}  — box: where the pill rests; rects: the Book buttons' boxes — { left, top, right, bottom }, same space.
L332  _pillRestApplies(tuck, f)  {pill-rest}  — Is the question asked at all?
L343  _pillRestStep(s, covers)  {pill-rest}  — One look in, the next state out.
L354  _pillBox()  — Where the pill RESTS, whatever is drawn right now (tucked it is slid down, hovered it is scaled): its layout size…
L364  _pillBookRects()  — The card's ONE pill, on both tabs (Book / Join waitlist, Cancel booking / Leave waitlist).
L374  _pillKeyFocused()  — Focus a KEYBOARD put there (a tap focuses the pill too, and must not pin it over a button for as long as nothing else…
L379  _pillRestCheck()
L392  var _pillTuck
L393  var _pillRest
L394  var _pillRestClear
L395  var _pillRestTimer
L396  var _pillScrollSrc
L397  var _pillScrollQueued
L404  _pillDialogUp()  — A dialog, sheet or panel is up.
L420  _pillDraw()  — Out of the way for either reason: on the way down a list, or at rest on a Book button.
L428  _pillRestQueue(ms)  — "Ask again once things have settled" — every caller re-arms the one timer, so a run of scroll frames or of DOM writes…
L436  _pillRendered()  — The page was written to (a search streams its studios in, one render after another): what the last look saw is gone,…
L444  _pillOnScroll()  — Once per frame at most.
L468  _pillScrollEvent(e)  — Capture: `scroll` does not bubble, and .tab-content (the scroller at <=640px) is built by tabs.js.
L480  _pillUntuck()  — Focus landed on it (Tab, a screen reader): it has to be on screen.

## Settings Panel (L487)
L493  var SETTINGS_SECTION_IDS  — The Membership rows each open the panel AT their own section; all four used to land at the top of the same sheet.
L504  scrollSettingsTo(section)  — Bring one section to the top of the panel's own scroller.
L516  window.openSettings(section)  +84  — `section` is optional ('reminders' | 'bike' | 'calendar' | 'data').
L601  window.closeSettings()

## Favourite instructors (Membership) (L609)
L617  window.filterFavList()
L619  favRowHTML(instr, favs)
L628  renderFavList()  +58
L687  window.toggleFavFromSettings(instrId)

## Calendar sync UI (the native apps only: iPhone and Android) (L706)
L711  async renderCalendarSync()  +48  {calendar-sync}
L760  async window.onCalendarSyncToggle(checkbox)  {calendar-sync}
L774  async _confirmCalendarOwnership(calId, name, cancelText, lead)  +30  {calendar-sync}  — The ownership dialog.
L808  _calSyncProblem(r)  {calendar-sync}  — Why a sync did NOT happen, in the member's words — '' when it really ran.
L816  async window.onCalendarTargetChange(select)  +39  {calendar-sync}
L856  async window.onCalendarCleanupDupes(btn)  +42  {calendar-sync}
L899  async window.onCalendarResync(btn)  +41  {calendar-sync}

## Bike Preference UI (L943)
L947  async populateStudioSelect()  +61
L1009  window.renderBikePrefGrid()  +62
L1072  window.toggleBikePref(studioId, slotId)

## Integration: Bike Picker Highlights (L1101)
L1105  var _origShowBikePicker

## Export / Import Settings (L1169)
L1173  var EXPORT_KEYS
L1184  window.exportSettings()  +35
L1226  _shareSettingsExport(json, fileName)  +33  {settings-export}  — Native export.
L1276  var IMPORT_MAX_FILE  {import-validate}  — What an import file may do.
L1277  var IMPORT_MAX_VALUE  {import-validate}
L1283  _planSettingsImport(data, deviceGet, opts)  +157  {import-validate}  — data: the parsed file.
L1442  _importSummary(added)  {import-validate}  — "120 classes and 2 favourites" — what the plan adds, in words.
L1460  _applySettingsImport(plan)  — Writes an accepted plan, says what changed, reloads (the app reads most of these keys once, at launch).
L1478  window.importSettings(input)  +53

## Bug Report (L1533)

### Build id (window.APP_VERSION) (L1538)
L1546  var BUILD_ID_RE  {build-id}  — Nothing stamps a version into the page, so every report said "App version: unknown".
L1547  var _appVersionPending  {build-id}
L1554  var _swSwappedSinceLoad  {build-id}  — A worker that takes over mid-session deletes the old cache while the page keeps running the previous build's scripts,…
L1562  _buildIdsFromCaches()  {build-id}
L1572  _buildIdFromSwFile()  {build-id}  — Same-origin file (the app bundle on iOS).
L1585  resolveAppVersion()  {build-id}  — Resolves to the build id (also left on window.APP_VERSION), or null.
L1608  window.getAppVersion = resolveAppVersion  {build-id}
L1611  buildBugReport()  +51
L1663  async window.downloadBugReport()  +38
L1702  window.copyBugReport()
L1724  _fallbackCopy(text, status, what)  +30  — Returns whether the text really reached the clipboard, so a caller with its own status line (copyDiagnostics) never…

## Diagnostics Panel (L1756)
L1770  var DIAG_MAX_LOG_ROWS
L1772  esc(v)
L1779  diagHasToken()  — Boolean-only check for whether an auth token is present.
L1789  diagGetDiagnostics()  — Pull the diagnostics object from PsycleDiag, fully guarded.
L1800  diagRecentLog(getterName, key)  — Last N entries (newest first) from a log getter that returns [{timestamp, <key>}].
L1824  diagApiHealthHTML(diag)  +52  — Build the API-health section HTML from a diagnostics object (may be null).
L1878  diagEnvironmentHTML(diag)  — Build the environment section HTML.
L1913  var PRIVACY_POLICY_URL  — The iOS bridge's getDiagnosticReport() is async, but "Copy diagnostics" has to stay synchronous: awaiting inside the…
L1914  window.openPrivacyPolicy()
L1924  var _diagTaps  — Diagnostics is the owner's tool, in engineering words, so nothing in Settings advertises it: five quick taps on the…
L1925  var _diagTapAt
L1937  var _diagReportText
L1938  var _diagReportSeq
L1945  diagPlatform()  — Where this is running: 'ios' | 'android' (the two native apps, in Capacitor's words) or 'web'.
L1954  diagPrefetchReport()
L1976  diagBuildCopyBlob(diag)  +50  — Assemble the JSON blob copied by "Copy diagnostics".
L2027  window.openDiagnostics()  +60
L2088  window.closeDiagnostics()
L2093  diagStatus(msg, ok)
L2102  copyDiagnostics()
L2117  copyDiagnosticsFallback(blob)  — The status line used to say "copied" whatever the fallback did.
L2122  clearDiagnosticLogs()
