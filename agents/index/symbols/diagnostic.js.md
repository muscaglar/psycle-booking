# js/diagnostic.js — symbols
Purpose: the shape of `js/diagnostic.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

596 lines · 36 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## Storage keys (we introduce two; both store field NAMES only, no PII) (L23)
L30  var SCHEMA_LOG_KEY  — psycle_api_schema_log: live, rolling record of observed shapes.
L35  var CONTRACT_KEY  — psycle_api_contract: a frozen snapshot of the schema log taken once after a healthy authenticated load, used as the…
L38  var LAST_DRIFT_KEY  — Where the last drift was observed (ISO) — surfaced in getDiagnostics().
L40  var MAX_FIELDS_PER_KIND
L41  var MAX_KINDS
L42  var MISSING_THRESHOLD
L43  var CONTRACT_DELAY_MS
L44  var INIT_CHECK_DELAY_MS

### Session-scoped counters (reset on reload; not persisted) (L46)
L49  var _sessionMisses  — Tracks how many times a given field path has been reported missing THIS session, so transient single misses don't flip…
L50  var _recordedAny
L51  var _contractTimer
L52  var _safeModeActive

## Small storage helpers — never throw (L54)
L58  readJSON(key, fallback)
L67  writeJSON(key, value)
L71  nowISO()
L76  softError(msg)  — Safely report an internal problem without ever throwing.

## Shape extraction — field NAMES only, never values (L83)
L94  shapeOf(sample)  — Derive the set of top-level field names that describe `sample`.
L117  getSchemaLog()

## Access to PsycleAPI.SCHEMAS — fully guarded (L122)
L127  getSchemas()  — Return PsycleAPI.SCHEMAS or null if unavailable.
L141  requiredFieldsFor(kind)  — Return the array of required field names declared for a kind, or [].

## 1. record(kind, sampleData) (L153)
L160  record(kind, sampleData)  +30

## 2. noteMissingField(path) (L191)
L199  noteMissingField(path)  +35
L236  handleDrift(reason)  — Central drift reaction: record timestamp, re-check, enter safe mode.

## 3. captureContract() (L251)
L259  captureContract()
L280  getContract()
L286  scheduleContractCapture()  — Arm a one-shot delayed self-capture if no contract exists yet.

## 4. checkContract() (L296)
L305  checkContract()  +53

## 5. enterSafeMode(reason) / exitSafeMode() (L359)
L366  var SAFE_BANNER_ID
L368  enterSafeMode(reason)  +74
L443  removeBanner()
L450  exitSafeMode()
L465  openDiagnostics()  — Open the diagnostics view.

## 6. getDiagnostics() (L481)
L486  getDiagnostics()  +50

## 7. Self-init — run one contract check after the app's initial calls (L537)
L543  var _initialCheckDone
L545  runInitialCheck()

## Public surface (L582)
L586  window.PsycleDiag = {…}  — keys: record, noteMissingField, captureContract, checkContract, enterSafeMode, exitSafeMode, getDiagnostics
