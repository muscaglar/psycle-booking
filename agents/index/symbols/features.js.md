# js/features.js — symbols
Purpose: the shape of `js/features.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

1067 lines · 54 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.

L20  escapeHtml(s)  — Alias the global escapeHTML from security.js (must be at top for hoisting)

## INIT — wire everything up (L30)
L33  init()

## A. CLASS HISTORY (L41)
L45  const HISTORY_KEY
L54  getHistory()
L64  saveHistory(arr)
L71  addHistoryEntry(eventId, slots)
L92  markHistoryCancelled(eventId)
L115  const HISTORY_CANCEL_MARGIN_MS  {history}  — History only ever heard about bookings made inside this install, so a class booked on another device or on Psycle's…
L117  _historyReconcile(history, bookings, cache, nowMs, startMs, max)  +52  {history}
L178  const HISTORY_OWNER_KEY  {history}  — Whose history this install holds (a customer id).
L179  _historyOwnership(stored, me)  {history}
L188  _historyOwnerId()  — The signed-in customer, or null while the session is unverified.
L195  _historyIsMine()  — May a BACKGROUND pass (the reconcile below, explore.js's silent top-up) write this account's classes into the stored…
L203  window._historyIsMine = _historyIsMine
L206  window.openHistoryModal = openHistoryModal
L212  let _histMonthFmt, _histDayFmt  — Two formatters for the whole list, made on the first open — never at module load (tests/suites/booking-extras.js runs…
L213  const HISTORY_FIRST_ROWS, HISTORY_MORE_ROWS
L219  historyChunks(total, first, step)  {history-chunks}  — Row ranges [from, to) the modal is filled in: a first screenful that opens at once, then the rest in bigger steps, one…
L231  openHistoryModal()  +100

### Monkey-patch submitBooking (L332)
L333  patchSubmitBooking()  +32

### Monkey-patch cancel functions (L366)
L370  hasSeat(eventId)  — History tracks SEATS.
L375  patchCancelFunctions()  +46

## B. INSTRUCTOR PROFILES (L422)
L432  classStatusChip(evt)  — [label, class] for one row of the modal's class list, or null.
L448  instructorRankHtml(instrId)  — ★ + S–F under the name: the Membership tab's own controls (.tier-fav / .tier-btn) driven by its own writers, so a rank…
L461  openInstructorModal(instrName, instrId)  +172
L635  window._features_filterByInstructor(instrId)  — Filter main results to just this instructor
L658  window._features_openInstructorModal = openInstructorModal

### Monkey-patch eventCard to make instructor name clickable (L660)
L661  patchEventCard()

## C. PUSH NOTIFICATION SCAFFOLD (L687)
L691  const NOTIFY_KEY
L693  getNotifyWatchlist()
L702  saveNotifyWatchlist(list)
L710  async window.requestNotificationPermission()  — Request notification permission
L730  _bellTitle(watching)  — The bell's glyph is a CSS emoji and its only name was a title, so the emoji is what a screen reader read out.
L731  _bellLabel(watching)
L732  _setBellState(btn, watching)
L739  async window._features_toggleNotify(eventId, btn)  +37  — Toggle notify watchlist for an event
L777  _hasToken()
L784  _classStartMs(startAt)  — API class times are naive London wall clock.
L792  const WATCH_THROTTLE_MS  {notify}
L793  const WATCH_MAX_PER_PASS  {notify}
L798  _watchPlan(watchlist, bookings, cache, nowMs, startMs, checkedAt, cap)  {notify}  — Which watched ids can go without asking Psycle, and which (at most `cap`) to ask about now.
L822  _watchSpotOpened(detail, studio)  {notify}  — Has a spot opened?
L832  let _watchLastPassAt
L833  let _watchPassRunning
L834  const _watchCheckedAt
L835  const _watchOpened
L837  _unwatchIds(ids)
L855  _watchCacheEntry(detail)  — Event detail as _eventCache holds it, for a watched class the cache never had (the watchlist outlives the timetable…
L878  let _watchDialogUp  — One dialog at a time, and never over a dialog the member is answering (confirmModal is single-instance: showing ours…
L879  let _watchDialogTimer
L880  _showOpenedSpots()  +46

### Check watched events for openings (L927)
L934  async checkWatchedEvents(fresh)  +77  — The bell used to re-read _eventCache only — a timetable copy that can be a day old — so it effectively never fired.

### Line class history up with the bookings Psycle just reported (L1012)
L1015  reconcileHistoryWithBookings(t0, ownerAtT0)  — Every guard here is a data-corruption trap that was found, not caution for its own sake — see _historyReconcile for…

### Monkey-patch fetchMyBookings to check watchlist + reconcile history (L1043)
L1044  patchFetchMyBookings()
