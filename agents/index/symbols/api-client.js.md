# js/api-client.js — symbols
Purpose: the shape of `js/api-client.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `9836e6a`). Do not edit; rebuild with `npm run agents:index`.

179 lines · 4 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## 1. SCHEMAS — expected response shapes (drift baseline) (L22)
L30  var SCHEMAS  +51  — Each entry: { required: [...], optional: [...] }. `required` fields are the ones existing code reads unconditionally…

## 2. categorizeError(responseOrError) → {type, userMessage} (L82)
L85  var USER_MESSAGES
L102  categorizeError(responseOrError)  +68  — Map a Response, an Error, or a number (status code) to a category and a short, friendly, actionable message.

## Public surface (L171)
L174  window.PsycleAPI = {…}  — keys: SCHEMAS, categorizeError
