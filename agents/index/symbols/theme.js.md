# js/theme.js — symbols
Purpose: the shape of `js/theme.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `427bc69`). Do not edit; rebuild with `npm run agents:index`.

801 lines · 53 symbols. Top level, plus the declarations one level inside its top-level IIFEs. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


### A. Themes (L15)
L22  const THEME_KEY
L26  const APP_THEMES  — `mono: true` = a near-monochrome theme by design: class types wear the theme's own accent ladder there unless the…
L34  const DEFAULT_THEME
L35  window.APP_THEMES = APP_THEMES
L45  var RETIRED_THEMES  {theme-retired}  — Two themes were retired in September 2026.
L46  _currentThemeId(id)  {theme-retired}
L51  window.RETIRED_THEMES = RETIRED_THEMES
L53  _themeById(id)
L60  _applyTheme(id)
L77  let _themeRewrittenTo  — The saved id.
L78  _savedThemeId()
L87  _resolveTheme()
L95  window.getAppTheme = _resolveTheme
L97  window.setAppTheme(id)
L112  initTheme()  +51
L164  toggleTheme()
L172  injectThemeToggle()
L197  updateThemeIcon()

### A2. Class-type colours (L219)
L261  var CLASS_COLOUR_KEY  {class-colours}
L262  var CLASS_COLOUR_KEYS  {class-colours}
L263  var CLASS_COLOUR_INTENSITIES  {class-colours}
L264  var CLASS_COLOUR_ROLES  {class-colours}
L265  var CLASS_COLOUR_DEFAULT_INTENSITY  {class-colours}
L266  var CLASS_COLOUR_DEFAULTS  {class-colours}
L272  var CLASS_COLOUR_PALETTE  +57  {class-colours}  — In hue order (the order a swatch row shows them), the neutral last.
L330  var CLASS_COLOUR_ON_BASE  {class-colours}  — The label on a base fill.
L332  _ccOwn(obj, k)  {class-colours}
L335  _ccKey(key)  {class-colours}  — "RIDE" / "ride" / anything else → one of CLASS_COLOUR_KEYS ("other" when unknown).
L345  _ccChoices(value)  {class-colours}  — What a stored or imported value may say: { intensity?, map: { key: swatch } } holding ONLY known keys, known swatches…
L367  _ccStorable(value)  {class-colours}  — For settings import and for storage: a parsed value → the object worth storing ({ v: 1, intensity, map: only the…
L373  _ccClean(value)  {class-colours}  — Any value → the full state in effect: { v, intensity, map: all eight keys }.
L383  _ccMerge(stored, partial)  {class-colours}  — The object to store after set(partial): what was stored plus the valid parts of `partial` ({ intensity?, map?: { key:…
L395  _ccPropNames()  {class-colours}  — Every custom property apply() manages (written, or removed on a mono theme).
L409  _ccPlan(state, base, monoTheme)  {class-colours}  — state (from _ccClean) + the theme's base ('light' | 'dark') + whether the theme is mono → { intensity, mono, base,…
L431  var _ccLastSig
L432  var _ccResolved
L434  _ccReadStored()
L442  _applyClassColours(unsaved)  — Writes the plan for the theme on screen.
L461  _ccSave(choices)
L470  window.PsycleClassColours = {…}  +65  — keys: KEY, KEYS, INTENSITIES, PALETTE, DEFAULTS, DEFAULT_INTENSITY, get, set, reset, apply, clean, resolve

### B. Skeleton Loading Cards (L537)
L544  skeletonCardHTML()
L561  skeletonStudioHTML(name, slotCount)  — A faint bike-grid placeholder, sized from a studio layout's slot count, hinting at the shape of the studio currently…
L579  showSkeletonLoading()  +31  — Context-aware: when exactly one studio is selected and its layout is known, prepend a bike-grid placeholder shaped…
L611  hideSkeletonLoading()

### C. Empty State Illustration (L617)
L621  const EMPTY_STATE_ACTIONS  — The ways out of an empty list.
L630  renderEmptyState(message)

### D. Haptic Feedback (L657)
L659  haptic(type)

### Hook into existing functions (L675)
L677  (iife) hookSearch()
L704  wrapSearch()
L720  wrapRender()
L744  wrapSubmitBooking()

### Also hook into setStatus to remove skeleton (L764)
L765  (iife) hookSetStatus()
L781  doHookSetStatus()
