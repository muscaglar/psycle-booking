# js/security.js — symbols
Purpose: the shape of `js/security.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `754fbb0`). Do not edit; rebuild with `npm run agents:index`.

549 lines · 42 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## HTML Escaping — prevents XSS from API-sourced strings (L27)
L36  var _ESC_MAP  — String-replace rather than the textContent→innerHTML div trick: browsers only escape & < > when serializing text…
L38  window.escapeHTML(str)
L51  window.escapeForJsString(str)  — For interpolating a value into a single-quoted JS string INSIDE an HTML attribute (onclick="f('<here>')").

## Token Encryption — AES-GCM with key in IndexedDB (L57)
L61  var ENC_TOKEN_KEY
L62  var LEGACY_TOKEN_KEY
L63  var DB_NAME
L64  var DB_STORE
L66  var _token
L67  var _cryptoKey
L68  var _cryptoAvailable

### IndexedDB helpers (L70)
L72  _openDB()
L81  _idbGet(db, key)
L90  _idbPut(db, key, val)

### AES-GCM helpers (L99)
L111  var KEY_BACKUP_KEY  — In the native app the AES key is also backed up to localStorage (which native-bridge mirrors into Capacitor…
L112  var _isNativeApp
L114  _bufToB64(buf)
L117  _b64ToBuf(b64)
L121  async _getOrCreateKey()
L150  async _encrypt(plaintext)
L161  async _decrypt(b64)

### XOR fallback for non-secure contexts (file://) (L169)
L171  var _xorKey
L173  _xorEncode(str)
L181  _xorDecode(b64)

### Error-log bridge (L192)
L197  var ERROR_LOG_KEY
L198  var MAX_ERROR_LOG_ENTRIES
L200  _logSecurityError(msg)

### Storage-full handling (L214)
L223  _freeAppCaches(deep)  — localStorage is one small bucket (~5 MB; about half that on WebKit, which counts UTF-16) and the app's own timetable…
L237  _safeSetItem(key, value)
L244  window._psycleSafeSetItem = _safeSetItem
L252  var _storedToken  — The token could not be written even with the caches gone.
L253  _tokenNotSaved(token)

### Secure Token Store (L271)
L273  window._secureTokenStore = {…}  +44  — keys: get, set, clear

### Initialization: decrypt stored token (L318)
L321  _withTimeout(promise, ms)  — Timeout wrapper: if crypto init takes >3s, fall back to legacy storage
L341  async _awaitNativeRestore()

## JWT Parsing & Expiry Monitoring (L418)
L422  window.parseJWT(token)
L432  window.getTokenExpiry()
L438  var _expiryTimer
L439  var _expiryTimer2
L443  window.isTokenExpiringSoon(withinMs)  — True when the token is within the warning window of expiry.
L450  _emitExpiring()
L461  window.cancelTokenExpiryCheck()  — Cancel BOTH expiry timers (warning + final).
L467  window.scheduleTokenExpiryCheck()  +54
