# ios-app/www/native-bridge.js — symbols
Purpose: the shape of `ios-app/www/native-bridge.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `427bc69`). Do not edit; rebuild with `npm run agents:index`.

2649 lines · 170 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## Native Bridge — Capacitor integration layer (L1)

### Gym Time (Europe/London) (L16)
L37  var GYM_TZ
L38  var _gymWallFmt
L48  _gymWallToUtcMs(y, mo, d, h, mi, s)  — Absolute UTC ms for a wall-clock time in the gym's zone (DST-correct, independent of the device timezone).
L79  _classStartMs(startAt)  — Absolute UTC ms of an API class time.
L95  window._psycleClassStartMs = _classStartMs

### Calendar ownership decisions (pure) (L97)
L107  _calForeignEvents(events, calId, markerOf)  — Unmarked events in calendar `calId` — what a full-ownership reconcile would delete that isn't ours.
L114  _calSweepVictimIds(events, calId, ownedAck, markerOf)  — Native ids to clear out of `calId` when it stops being the sync target.
L137  _calAckCovers(stored, calId)  — Does the stored ownership ack hand over calendar `calId`?
L140  window._psycleCalForeignEvents = _calForeignEvents
L141  window._psycleCalSweepVictimIds = _calSweepVictimIds
L142  window._psycleCalAckCovers = _calAckCovers

### Platform (L152)
L170  var PLATFORM  — Both native apps run this file.
L174  var IS_ANDROID
L176  var Preferences
L177  var Haptics
L178  var Share

### Fix Login Flow (L193)
L196  window.openLoginPopup()  — window.open() in WKWebView opens Safari, breaking postMessage.

### Fix window.open calls (L200)
L204  var Browser  — calendar.js uses window.open for Google Calendar and ICS blob.
L205  var _origWindowOpen
L206  window.open(url, target)

### Persistent Storage (L221)
L226  var SYNC_KEYS  +45
L277  async restoreFromNative()  — On startup: restore from native storage to localStorage.
L297  syncToNative(key, value)  — Sync localStorage changes to native storage
L307  var _origSetItem  — Intercept localStorage.setItem and removeItem
L308  var _origRemoveItem

### Native Share (L357)
L360  async window.nativeShare(title, text, url)

### Native Calendar Integration (L371)
L410  var Calendar
L411  var CAL_EVENT_MAP_KEY
L412  var CAL_ID_KEY
L413  var CAL_TARGET_KEY
L414  var CAL_MODE_KEY
L415  var CAL_ENABLED_KEY
L423  var CAL_OWNED_ACK_KEY  — '2:<calendarId>' once the user has confirmed that target under the full-ownership contract — only then may the…
L424  _calIsOwned(calId)
L427  var PSYCLE_CAL_TITLE
L428  var PSYCLE_CAL_COLOR
L429  var PSYCLE_EVENT_MARKER
L431  _loadCalMap()
L434  _saveCalMap(map)
L443  _nativeSlotLabel(typeName)  — Slot label for a class type.

### Permissions (L448)
L450  var _calPermissionGranted
L452  async _ensureCalendarPermission()  +30

### Dedicated Psycle Calendar (L483)
L485  async _listNativeCalendars()
L500  hasChosenCalendar()  — Has the user explicitly chosen a calendar to write into?
L504  async _resolveTargetCalendarId()

### Add / Update / Remove Events (L515)
L517  _buildCalEventData(eventId)  +55

### v6-plugin-correct primitives (L573)
L578  _markerEventId(ev)  — Extract the Psycle event id from a native event's notes, or null.
L585  _nativeStartMs(ev)  — Millisecond start time of a native event, across plugin shapes.
L593  _createdEventId(result)  — The created-event id from a createEvent() result, across versions.
L605  async _deleteNativeEvents(ids)  — Delete native events by id.
L637  var _androidDeletedEventIds  — ANDROID ONLY.
L638  _androidRememberDeleted(ids)
L645  async _listNativeEvents(startMs, endMs)  — Normalized listEventsInRange (ms timestamps in, array out).
L657  calendarSyncEnabled()

### Hook into booking / cancel / seat-cancel flows (L665)
L672  var _origSubmitBookingNative
L683  var _origConfirmUnbookNative
L691  var _origUpcomingCancelNative
L699  var _origCancelBikeSlotNative
L707  var _origUpcomingSeatCancelNative

### Full Calendar Reconcile (L715)
L732  async syncAllBookingsToCalendar()  +118
L852  var _origRenderNative  — Run sync after bookings load
L853  var _calSynced
L869  async window.psycleResyncCalendar(opts)  — Force a fresh sync.
L900  async window.psycleCleanupDuplicates()  — Settings → "Remove duplicates".
L909  window.syncAllBookingsToCalendar = syncAllBookingsToCalendar

### Public API for Settings UI (L911)
L914  async window.psycleListCalendars()  — List iOS calendars the user can choose from.
L932  window.psycleGetCalendarConfig()  — Get the current calendar sync config for the Settings UI.
L952  async window.psycleCountForeignEvents(calId)  — READ-ONLY: how many upcoming events in calendar `calId` are NOT Psycle bookings — what handing that calendar over…
L977  async window.psycleSetCalendarConfig(cfg)  +62  — Update the calendar target.

### Weekly Booking Reminder (L1041)
L1046  var LocalNotifications

### Android: notification channels, the small icon and its tint (L1048)
L1066  var ANDROID_CHANNEL_CLASSES  — Android 8+ files every notification under a CHANNEL, which is what the member sees (and can silence) in the system's…
L1067  var ANDROID_CHANNEL_NEW_DATES
L1068  var ANDROID_NOTIF_ICON
L1072  var ANDROID_NOTIF_TINT  — The mark's ink.
L1073  var _androidChannels
L1075  _ensureAndroidChannels()
L1099  _forAndroid(notification, channelId)  — The Android-only fields of one notification.
L1113  var REMINDER_IDS  — Eight rolling one-shot notifications (the next 8 Mondays).
L1118  var WEEKLY_REMINDER_HOUR, WEEKLY_REMINDER_MINUTE  {weekly-reminder}  — WHEN it fires and WHAT it says.
L1119  var WEEKLY_TEMPLATE_KEY  {weekly-reminder}
L1128  _nextMondaysNoonLondon(count, now, wallToUtcMs)  {weekly-reminder}  — The next `count` occurrences of Monday 12:00 Europe/London as absolute Date instants, DST-correct for any device…
L1146  _hasUsualWeek(raw)  {weekly-reminder}  — Is a usual week saved?
L1156  _weeklyReminderCopy(hasUsualWeek)  {weekly-reminder}  — It fires AT the release, so it says the dates ARE open (and still reads true when the banner is seen later).
L1166  _weeklyTapTab(hasUsualWeek)  {weekly-reminder}  — Where the tap lands at once (the web layer's _onBookingWeekOpened does the rest when it can): the usual-week review…
L1175  _weeklyOfferDecision(f)  {weekly-reminder}  — Put the in-app "remind you on Mondays?" question (_offerWeeklyReminder)?
L1184  _usualWeekSaved()
L1188  var REMINDER_PREF
L1192  var _weeklyArmedWithWeek  — Which body the armed reminders carry (true = "Book your usual week…"), null while nothing is known to be armed — so a…
L1194  async _cancelReminders()
L1208  async scheduleWeeklyReminder(interactive)  +42
L1253  var _weeklyChain  — One pass at a time: each cancels the eight ids and schedules them again, and two interleaved passes could leave the…
L1254  _scheduleWeeklySerial(interactive)
L1263  window._nativeReminder = {…}  — keys: isOn, enable, disable — Exposed to the web layer (Membership tab toggle).
L1288  _rearmWeeklyReminder()  — The body names what the tap opens, so it follows the usual week.

### Notification Action Buttons (Book / Cancel / Snooze) (L1309)
L1321  registerNotificationActions()
L1343  var TAP_ROUTE_WAIT_MS  — Land a tapped notification on what it was about.
L1344  var _cancelTapRoute
L1355  _askDiscoverToRefresh()  — The Monday reminder exists for one moment: the release of new dates.
L1364  _routeNotificationTap(eventId)  +53
L1437  handleNotificationAction(notification)  +36  — Route a tapped notification / action button.
L1492  _parseWidgetLink(url)  {widget-link}  — A Home/Lock Screen widget tap opens psync://bookings?event=<id> (minted in PsycleWidget.swift).
L1511  handleWidgetURL(info)  {widget-link}  — Same landing as a class-reminder tap: My Bookings, then that class's sheet (only while the seat is still held).
L1528  var PsycleDeepLink  — The in-app PsycleDeepLink plugin RETAINS the URL until this listener attaches — a widget tap usually cold-launches the…

### Widget / Live Activity / Siri Snapshot (L1536)
L1571  var WIDGET_APP_GROUP  — App Group container id.
L1573  var WIDGET_NEXT_KEY
L1574  var WIDGET_WEEK_KEY
L1579  var WIDGET_UPCOMING_KEY  — Next few classes (same shape as widget_next_class, array of up to 5) so the widget can build a MULTI-ENTRY timeline…
L1584  _prefSet(key, value)  — Standard Preferences plugin: lands in UserDefaults.standard under "<group>.<key>" (NOT extension-readable — kept for…
L1597  _appGroupSet(key, value)  — App Group mirror: writes the SAME logical value under the BARE key into the shared App Group suite, so a native…
L1606  _writeSnapshotKey(key, value)
L1621  var WIDGET_UPCOMING_MAX  {ios-polish}  — The widget timeline shows the next few classes; reminders cover every class held.
L1622  var CLASS_REMINDER_MAX  {ios-polish}
L1626  _isHeldSeat(booking)  {ios-polish}  — A real seat, as opposed to a waitlist place — true even when the class's details failed to load this pass (that makes…
L1635  _evtFromSavedItem(item)  {ios-polish}  — Stand-in for an _eventCache entry, built from app.js's saved copy of My Bookings (psycle_bookings_snapshot), which…
L1653  _staleReminderIds(map, wanted, keep)  {ios-polish}  — Event ids whose armed reminder should go: the class is no longer wanted, or its time moved (it is re-armed for the new…
L1675  var SNAPSHOT_NEUTRAL_TINT  {native-snapshot}  — The card ground at intensity "off": the app's own neutral surface (Cloud / Graphite, css/theme.css) — the widget's…
L1679  _snapHex(value)  {native-snapshot}  — '#2d5fd6' → '#2D5FD6'; anything that is not six hex digits → null.
L1689  _snapClassTypeKey(typeName, keyFn)  {native-snapshot}  — "RIDE: 45" → 'ride', through the app's OWN classTypeKey (js/app.js) — the same answer data-ct gets.
L1709  _snapClassColourFields(key, engine)  {native-snapshot}  — The colour fields for one class type, read from the app's colour engine (`engine` = window.PsycleClassColours: get() →…
L1742  _savedItemsById()  — app.js's saved copy of My Bookings, by event id ({} when there is none).
L1757  _snapshotEventFor(eventId, known)  +35  — Resolve a display-ready event object from the cache, or null.
L1804  var _snapServerConfirmed  — Only trust an EMPTY bookings map after the server confirmed it (bookings:loaded fired this session).
L1806  var _signOutPass  — True only while the clearToken wrapper (a deliberate sign-out) runs its pass.
L1809  var _snapColourSig  — The class colours the last WRITTEN snapshot carries (see the 'classcolours:changed' listener below), and that…
L1810  var _colourSnapTimer
L1811  _classColourSig()
L1820  updateWidgetSnapshot()  +128
L1948  window.updateWidgetSnapshot = updateWidgetSnapshot

### Class-start reminders (T-90 minutes) (L1950)
L1962  var CLASS_REMINDER_PREF
L1963  var CLASS_REMINDER_MAP
L1972  _classReminderBody(c)  — The reminder's body: the instructor, the place and — on the iPhone — what a tap is for.
L1978  _classRemindersEnabled()

### The Android countdown's switch (L1982)
L1997  var COUNTDOWN_ENABLED_KEY  — The Android app has no Live Activity.
L1998  var _countdownSwitchSends
L1999  _androidCountdownSwitch()
L2013  _androidCountdownFlipped(sendsBefore)  — The member flipped Class reminders.
L2024  _loadReminderMap()
L2027  _saveReminderMap(map)
L2031  _reminderIdFor(eventId, map)
L2045  var _reminderChain  — Serialized: overlapping snapshot passes (booking events fire in quick succession) must not interleave the…
L2049  _scheduleClassReminders(upcomingList, unknownSeats)  — `unknownSeats` = {eventId: true} for held seats this pass knew nothing about (see _staleReminderIds) — captured with…
L2056  async _scheduleClassRemindersInner(upcomingList, unknownSeats)  +51
L2109  window._nativeClassReminders = {…}  +33  — keys: isOn, hasPermission, enable, disable — Settings toggle API (rendered by tabs.js next to the weekly reminder).

### First-booking ask (L2143)
L2149  var CLASS_REMINDER_ASKED  — The pref defaults ON, but only the Settings switch ever requested notification permission — and it looked on already,…
L2150  var ASK_POLL_MS
L2151  var ASK_GIVE_UP_MS
L2152  var _reminderAskBusy
L2153  var _reminderAskVoid
L2162  var ASK_BLOCKING_IDS  — Anything the ask would land on top of.
L2167  _askBlocked()
L2176  _askStillWanted()
L2180  async _maybeAskClassReminders()  +56

### Usual-week ask: the Monday reminder, offered in context (L2245)
L2256  var WEEKLY_REMINDER_ASKED  — The weekly reminder defaults OFF and its switch sits in Settings, where nobody looks for it.
L2257  var _weeklyAskBusy
L2258  var _weeklyAskSince
L2260  _weeklyOfferFacts(permission)
L2269  async window._offerWeeklyReminder(reason)  +56
L2335  var _origClearTokenNative  — Deliberate sign-out must blank the snapshot and cancel pending class reminders — otherwise the previous account's…
L2377  var _calReconcileTimer, _calReconcileInFlight, _calReconcilePending  — Auto-reconcile the calendar whenever bookings change — covers classes cancelled on the web / another device and…
L2380  async _runCalSync()  — Single-flight calendar sync.
L2389  _scheduleCalReconcile()
L2397  var _calServerConfirmedEmpty  — Tracks whether the latest bookings state came from a successful server fetch.

### Status Bar (L2420)
L2431  _themeGround(registry, themeId, base)  {android-bridge}  — The ground of the theme in force, for the Android status bar: the `bg` of its APP_THEMES entry (js/theme.js) — the…
L2445  updateStatusBar()  +36
L2483  var observer  — Watch for theme changes

## Analytics / Diagnostics (developer-only, no third-party services) (L2488)
L2492  var ACTION_LOG_NATIVE_KEY
L2493  var MAX_NATIVE_ACTION_LOG
L2497  var _origPushAction  — Persist action log to Capacitor Preferences (survives iOS storage purges).
L2506  _persistActionLogToNative()
L2525  async window.getDiagnosticReport()  +121  — getDiagnosticReport — comprehensive diagnostics string for developer use.
