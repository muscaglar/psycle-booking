# js/tabs.js — symbols
Purpose: the shape of `js/tabs.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `9573454`). Do not edit; rebuild with `npm run agents:index`.

4474 lines · 173 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


### Tab Navigation (L20)
L22  var TABS
L23  var _currentTab
L25  initTabs()  +246
L272  window.switchTab(tab, noHash)  +73
L359  _stillToCome(startAt, now)  — Is this held class still to come?
L365  updateTabBadge()
L427  var _origRender  — Also update badge when renderMyBookings is called directly
L428  _patchRender()

### Stats sub-pages (L440)
L454  var STATS_PAGES  {stats-pages}  — THE grouping.
L462  var STATS_PAGE_DEFAULT  {stats-pages}
L463  var STATS_PAGE_KEY  {stats-pages}
L465  _statsPageById(id)  {stats-pages}
L471  _statsPageId(raw)  {stats-pages}  — Whatever was stored or passed in → a real page id.
L474  _statsPageOfSection(sectionId)  {stats-pages}
L482  _statsPageForKey(currentId, key)  {stats-pages}  — A key pressed in the tab list → the page to move to, or null when the key is not the tab list's.
L493  _statsPageStep(currentId, dir)  {stats-pages}  — The neighbour a horizontal swipe leads to (dir: +1 = next, -1 = previous).
L499  _statsPageIsEmpty(id, shown)  {stats-pages}  — Is there nothing on this page?
L508  _statsEmptyState(id, shown, o)  {stats-pages}  — What a page with nothing on it says: ONE line — never a blank page, never a paragraph.
L519  _statsScrollTarget(current, pageTop, barHeight, pinTop)  {stats-pages}  — Where the scroller goes when a page opens.
L527  var _statsPageNow  — The page on screen.
L532  var _statsPainted  — Pages built since the last renderInsights(): a page is built when it is first opened, and not again until the data is…
L534  var _statsHeroOnly  — Signed out / can't-reach-Psycle with nothing on record: the hero has the tab.
L539  var _STATS_SECTION_PAINT  — Which function fills which section.
L553  _statsPagesHtml(sectionHtml, current)  — The switcher and the three page wrappers, from the table above.
L573  _wireStatsPages(panel)  — Once, from initTabs.
L604  _paintStatsPage(id)  — Build the sections of one page, unless they were built since the last renderInsights().
L616  _applyStatsPage()  — Selected tab, roving tabindex, and which page is displayed.
L632  _syncStatsEmpty()  — The one quiet line of a page with nothing on it (pure:stats-pages decides).
L660  _syncStatsPin()  — Wider than a phone the document scrolls and the tab bar is what sticks to the top: the switcher pins beneath it,…
L671  _scrollToStatsPageTop(page)  — The DOM half of _statsScrollTarget.
L694  _syncStatsPages(heroOnly)  — renderInsights' half of the pages: every call is a fresh look at the data, so nothing built before it is trusted — and…
L708  window.showStatsPage(id)  — Open one of the Stats sub-pages ('overview' | 'habits' | 'instructors').
L725  window._statsPageStep(dir)  — For a swipe helper: the page a swipe in `dir` (+1 / -1) would open, or null.
L738  _statsSwipeMayStart(target)  — Swipe left / right between the pages.
L751  _wireStatsSwipe(panel)  — Once, from initTabs — bound to the panel, which outlives every repaint of its sections.

### Render insights tab content (L765)
L773  var _historyRaw, _historyParsed  — One Stats paint asks getFullHistory() ~11 times (every section below) and the string runs to 400 KB.
L779  getFullHistory()  — Get the full class history from localStorage (synced + locally tracked).
L796  var _tokenReadable  — The stored token is only readable once security.js has decrypted it (on iOS: after the Preferences restore).
L802  window.renderInsights()  +41

### Quick Stats (L844)
L846  renderQuickStats()  +128

### Weekly Calendar View (L975)
L977  var _weekOffset
L979  window.weekNav(dir)
L986  window.planDay(dateStr)  — Interactive weekly planner: tap an empty day to search for classes on that date
L999  renderWeekView()  +112

### Your usual week (weekly template — UI only) (L1112)
L1119  var UW_DAYS
L1123  _uwDateLabel(dateStr)  — 'YYYY-MM-DD' → "Mon 21 Sep".
L1130  _uwPlural(n, one, many)
L1138  _uwTypeOf(label)  {usual-week-crisp}  — A template entry's label is "Type · Instructor" (app.js _templateFromSeats).
L1144  _uwTime(totalMin)  {usual-week-crisp}  — Minutes after midnight → "18:30", through app.js's one formatter (_clock24, pure:clock).
L1152  _uwClassMark(typeName, keyOf, pictogram)  {usual-week-crisp}  — The tile + data-ct of a class type.
L1163  _uwTimeHtml(totalMin)
L1167  _uwMark(typeName)
L1173  _usualWeekTemplate()
L1184  _collectDisplayedWeekTemplate()  — Template entries from the REAL seats held over the next 7 days (it used to read the week shown in the Discover…
L1193  _usualWeekFromHistory()  — Nothing booked this week (a holiday, or the first Monday with the app): fall back to the slots history shows 2+ times.
L1214  _uwLog(action)
L1218  async window.saveWeekAsTemplate()  +42
L1262  async window.clearUsualWeek()  — The card's own handlers (window.clearWeeklyTemplate is app.js's storage call).
L1280  window.removeUsualWeekEntry(idx)
L1294  _placeUsualWeekCard(card)  — Right after #rebookHint — a node app.js removes and re-creates in front of #upcomingPanel whenever it likes.
L1304  var _usualWeekWatch  — The hint is re-made outside any event this module hears (first paint), so watch the tab's own children — not its…
L1305  _watchUsualWeekPlacement(card)
L1318  var UW_COLLAPSED_KEY  {usual-week-card}
L1324  _uwCollapsedFrom(stored, mem)  {usual-week-card}  — Stored '1' = collapsed; anything else there (absent, junk, an import's "true") = expanded.
L1332  _uwCardSeats(value)  {usual-week-card}  — How many seats an entry books: an integer 1–4 (Psycle's own limit per booking); anything else stored there reads as 1.…
L1337  _uwToggleWord(collapsed)  {usual-week-card}  — The word that shows the head is a control (no chevron: the Filters bar's rule).
L1343  _uwCardHtml(template, collapsed)  +49  {usual-week-card}  — The whole card for a saved week.
L1398  var _uwCollapsedMem  — Collapsed or not: localStorage psycle_usual_week_collapsed ('1' | absent), read at every paint — never cached: in the…
L1399  _uwIsCollapsed()
L1404  _uwRememberCollapsed(collapsed)
L1417  window.toggleUsualWeek()  — The head's button.
L1444  _uwFocusedButton(card, doc)  — Which of the card's buttons holds focus (-1: none), and putting it back on the button in that place once the card has…
L1447  _uwRefocusButton(card, idx)
L1455  renderUsualWeekCard()  +38
L1493  window.renderUsualWeekCard = renderUsualWeekCard
L1510  var UW_NOT_OPEN  {usual-week-sheet}  — What the sheet says about a planned row (state → copy).
L1511  var UW_NOT_LISTED  {usual-week-sheet}
L1514  var UW_WAITLIST_NOTE  {usual-week-sheet}  — What joining a waitlist from here means (it leads with a space: it follows the sheet's money sentence).
L1515  _uwPlanNote(row)  +30  {usual-week-sheet}
L1547  _uwTopUpNeed(row)  {usual-week-sheet}  — "1 of 2 seats held": the seat(s) still missing — all a top-up may ask for.
L1554  _uwTickOf(row, need)  {usual-week-sheet}  — "Choose again" lists the same dates afresh.
L1567  _uwKeepTick(kept, row)  {usual-week-sheet}  — …and a kept tick stands only on a row that is still THAT: by template index alone, a seat that went while the class…
L1584  _uwResultNote(result, info)  {usual-week-sheet}  — …and about what happened to a ticked row once the run reached it.
L1607  _uwSeatOptions(saved, max)  {usual-week-sheet}  — The seat counts a row offers: 1 · 2, and 3 · 4 only when the saved entry asked for them — never above the class's own…
L1618  _uwConfirmLabel(t)  {usual-week-sheet}  — The confirm button counts what it will SPEND: "Book 3 classes · 4 seats".
L1630  _uwRangeLabel(range, fmt)  {usual-week-sheet}  — A date range of the sheet's control, as it is labelled.
L1643  _uwLabelHalves(label)  {usual-week-sheet}  — "Newly opened · Fri 25 Sept – Thu 1 Oct" as two halves that each stay whole.
L1657  _uwScrollTopToReveal(scrollTop, viewH, row, used)  {usual-week-sheet}  — The list's scrollTop once the sheet has re-flowed around the row the member is on.
L1679  _uwRowHtml(row, note, checkbox, spots)  — One row of the sheet.
L1710  _uwNoteInner(note)  {usual-week-note}  — What a row's note holds.
L1719  _uwFocusBack(prev, doc)  — Where focus goes when the sheet closes: what held it on open, if that is a real element still in the page — else the…
L1739  _runUsualWeekSheet(opts)  +620  — The sheet between "Book my usual week" and any booking.
L2364  var _templateWeekRunning  — Opens the REVIEW sheet — it books nothing by itself.
L2365  async window.bookTemplateWeek(opts)  +42
L2410  _statsTile(ct)  — The small pictogram tile that leads a class name on a Stats card (app.js classPictogram; css/crisp.css .ct-tile).

### Recommendations (L2415)
L2417  renderRecommendations()  +58

### Habit-slot alerts (Feature 1) (L2476)
L2481  var DAY_NAMES_FULL
L2485  _nextWeekdayDateStr(dow)  — Local-date "YYYY-MM-DD" for the next occurrence of a weekday (0=Sun..6=Sat).
L2495  renderHabitSlots()  +66

### Streaks & milestones (Feature 3) (L2591)
L2600  _weekIndex(date)  — Monday-anchored week index: whole weeks since a fixed epoch Monday.
L2607  renderStreaks()  +82
L2700  var HEAT_BANDS  {stats-charts}  — Five bands of the day, as the board draws them.
L2707  var HEAT_DAYS  {stats-charts}
L2710  _wallParts(s)  {stats-charts}  — 'YYYY-MM-DD HH:MM…' (space or T) → { day: 0–6 Monday first, hour } | null.
L2719  _heatBandIndex(hour)  {stats-charts}
L2726  _heatLevel(count, max)  {stats-charts}  — 0 = never; 1–4 = quarters of the busiest cell (the old grid's own steps).
L2734  _heatmapModel(starts)  {stats-charts}  — Class start times → { total, max, counts[band][day], levels[band][day], top: the (up to three) busiest cells, busiest…
L2757  _heatmapLabel(model)  {stats-charts}  — What the grid says to someone who cannot see it.
L2766  _streakWeeks(weeks, thisWeek, n)  {stats-charts}  — The streak's bars: the last `n` weeks, oldest first.
L2777  _streakWeeksLabel(bars)  {stats-charts}
L2787  _classTypeBreakdown(counts, cats)  {stats-charts}  — counts by CATEGORY_MAP key + the category list → what the class-type bar draws: rows with a class, biggest first (ct =…

### Activity Heatmap (L2801)
L2805  renderHeatmap()  +55

### Membership Info (L2862)
L2864  renderMembershipInfo()  +166

### Cost Per Class Tracker (L3031)
L3033  renderCostTracker()  +94
L3129  var FORECAST_TARGET_GBP  {cost-forecast}
L3130  var FORECAST_MAX_PER_DAY  {cost-forecast}
L3134  _bookableMore(made, max, daysLeft)  {cost-forecast}  — The most classes a "Book N more" line may ask for: what the plan still holds (max 0 = unlimited) and what fits in the…
L3144  _forecastVerdict(made, max, priceGbp, costAtMax, projected, projectedCost, daysLeft)  {cost-forecast}  — The forecast's one-line verdict.
L3167  _forecastHtml(sub, made, max, priceGbp, costAtMax)  +47  — Returns the forecast block HTML (or '' if there isn't enough to forecast).

### Theme picker (Membership → App) (L3215)
L3217  renderThemePicker()
L3242  window._pickTheme(id)

### Class colours (Membership → Appearance) (L3247)
L3256  var CC_INTENSITY_LABELS  {class-colour-control}
L3262  var CC_SWATCH_INK  {class-colour-control}  — What the preview card says for each class type (decoration — never timetable data).
L3263  var CC_PREVIEW  {class-colour-control}
L3268  _ccHas(obj, k)  {class-colour-control}
L3281  _ccControlModel(api, cats, theme, open)  +40  {class-colour-control}  — Everything the control draws, from what the engine says.
L3325  _ccRadioTarget(count, index, key)  {class-colour-control}  — A key pressed on radio `index` of `count` → the radio to move to (and check: in a radio group selection follows…
L3335  var _ccOpenKey
L3337  _ccThemeNow()
L3344  var CC_TICK
L3346  _ccControlHtml(m)  +52
L3402  renderClassColours()  — Rebuilt whole on every change (it is small), with the focus put back where it was: a radio group rebuilt under the…
L3424  window.renderClassColours = renderClassColours
L3426  _ccSet(partial, said)
L3437  _wireClassColours(box)  +42  — Once per container (it outlives every repaint of its contents).
L3490  window.renderThemePicker = renderThemePicker
L3491  window.renderReminderRow = renderReminderRow

### Weekly reminder row (the native apps only — needs the native bridge) (L3493)
L3498  var _classReminderGranted  — Last known answer to "may Psync send notifications?" for the class switch.
L3510  _classReminderSwitch(prefOn, granted, android)  {reminder-row}  — What the class-reminder switch shows.
L3526  _reminderAndroid()  — These rows exist in both native apps.
L3531  _osSettingsWords(text)
L3535  renderReminderRow()
L3560  _paintReminderRow(row)
L3583  async window._toggleClassReminders()
L3610  async window._toggleReminder()
L3622  _formatGbp(amount)
L3626  _daysLeftInBillingPeriod(sub)

### Class Type Distribution (L3643)
L3645  renderClassTypeDistribution()  +50

### Lapsed Favourites (L3696)
L3698  renderLapsedFavourites()  +54

### Instructor Variety Trend (L3753)
L3755  renderVarietyTrend()  +52

### Year in Review (Feature 4) (L3809)
L3821  _yearReviewRows(history, year, nowMs, startMs)  {year-review}  — The rows a year's wrap may count: this year's, not cancelled — and not still to come.
L3830  _takenRows(history, nowMs, startMs)  {year-review}  — The same test over every year: what the Stats tiles ("This month", "All time") and the "Share my stats" image may…
L3837  _historyStartMs()  — app.js's London resolver (h.date is gym wall clock); the device-local parse only when app.js is absent — the same pair…
L3844  _hasTakenHistory()  — Is there a class TAKEN to show?
L3849  _computeYearReview(year)  +62  — Aggregate this year's attended history into a tidy summary object.
L3912  renderYearReview()
L3933  window.openYearReview()  +47

### Share images (year wrap + stats card): shared helpers (L3981)
L3992  async _shareAsTextOnAndroid(title, text)  — The ANDROID app's web view has no Web Share API and no download manager: navigator.share is missing, and the <a…
L4008  var SHARE_FALLBACK  {share}  — Cloud, whole.
L4011  var SHARE_TOKENS  {share}  — Small grey labels read `muted` (--text-muted), never --text-faint: faint is under 3:1 in Handheld, and these labels…
L4014  var SHARE_FOOTER  {share}  — One credit line for both images (they were signed "Psycle Companion" and "Psycle Class Finder" — neither is the app's…
L4018  var SHARE_SCALE  {share}  — Export scale: a fixed integer, not devicePixelRatio — the PNG is looked at on OTHER devices, and a fractional ratio…
L4021  _sharePaletteFrom(read)  {share}  — read(tokenName) → the custom property's text, e.g. ' #efeee9'.
L4032  _sharePalette()
L4042  _shareCanvas(W, H)  — A W x H surface exported at SHARE_SCALE: every coordinate below stays in 640-wide layout units, the pixels are 3x (a…
L4057  var SHARE_WORDMARK_FONT  — Wait for the display face so the wordmark is not drawn in a fallback — but never for long: navigator.share() spends…
L4058  var SHARE_DISPLAY
L4059  var SHARE_BODY
L4060  _shareFontReady()
L4074  _shareWordmark(ctx, pal, y, label)  — "Psync" + the image's section label, on one baseline.
L4088  _shareClassColour(cat, pal)  — The class-type colour in effect, as a literal a canvas can paint: the member's own swatch through the engine…
L4097  async window.shareYearReview()  +114

### Share Insights Card (L4213)
L4234  async window.shareInsights()  +229
