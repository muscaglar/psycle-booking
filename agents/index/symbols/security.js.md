# js/security.js — symbols
Purpose: the shape of `js/security.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

543 lines · 42 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## HTML Escaping — prevents XSS from API-sourced strings (L27)
L36  var _ESC_MAP  — String-replace rather than the textContent→innerHTML div trick: browsers only escape & < > when serializing text…
L38  window.escapeHTML(str)
L51  window.escapeForJsString(str)  — For interpolating a value into a single-quoted JS string INSIDE an HTML attribute (onclick="f('<here>')").

## Token Encryption — AES-GCM with key in IndexedDB (L57)
L61  var ENC_TOKEN_KEY
L62  var LEGACY_TOKEN_KEY
L63  var DB_NAME
L64  var DB_STORE
L66  var _token
L67  var _cryptoKey
L68  var _cryptoAvailable

### IndexedDB helpers (L70)
L72  _openDB()
L81  _idbGet(db, key)
L90  _idbPut(db, key, val)

### AES-GCM helpers (L99)
L107  var KEY_BACKUP_KEY  — In the native app the AES key is also backed up to localStorage (which native-bridge mirrors into Capacitor…
L108  var _isNativeApp
L110  _bufToB64(buf)
L113  _b64ToBuf(b64)
L117  async _getOrCreateKey()
L146  async _encrypt(plaintext)
L157  async _decrypt(b64)

### XOR fallback for non-secure contexts (file://) (L165)
L167  var _xorKey
L169  _xorEncode(str)
L177  _xorDecode(b64)

### Error-log bridge (L188)
L193  var ERROR_LOG_KEY
L194  var MAX_ERROR_LOG_ENTRIES
L196  _logSecurityError(msg)

### Storage-full handling (L210)
L218  _freeAppCaches(deep)  — localStorage is one small bucket (~5 MB; about half that on WebKit, which counts UTF-16) and the app's own timetable…
L232  _safeSetItem(key, value)
L239  window._psycleSafeSetItem = _safeSetItem
L247  var _storedToken  — The token could not be written even with the caches gone.
L248  _tokenNotSaved(token)

### Secure Token Store (L266)
L268  window._secureTokenStore = {…}  +44  — keys: get, set, clear

### Initialization: decrypt stored token (L313)
L316  _withTimeout(promise, ms)  — Timeout wrapper: if crypto init takes >3s, fall back to legacy storage
L335  async _awaitNativeRestore()

## JWT Parsing & Expiry Monitoring (L412)
L416  window.parseJWT(token)
L426  window.getTokenExpiry()
L432  var _expiryTimer
L433  var _expiryTimer2
L437  window.isTokenExpiringSoon(withinMs)  — True when the token is within the warning window of expiry.
L444  _emitExpiring()
L455  window.cancelTokenExpiryCheck()  — Cancel BOTH expiry timers (warning + final).
L461  window.scheduleTokenExpiryCheck()  +54
