# Backlog — candidate work, none of it requested
Purpose: work that is known to be worth doing, so that you can offer it with a size and a risk instead of rediscovering it. **Propose from this list, do not start from it; the owner decides.** **Read this when** the owner asks "what next?", or you have finished and want to suggest a follow-up. **Skip it when** you have a task: nothing here outranks it.

Size: trivial (minutes) · small (an hour or two) · medium (a day) · large (several days, with a device pass). Risk is what can go wrong for a member or for a release, and the rule that contains it. Where things stand: [HANDOVER.md](HANDOVER.md).

Every row was checked against [decisions.md](decisions.md): none reopens its one CLOSED row (section 4, device-local times) or a section 6 left-alone item. To PROPOSE from here you need only its section 7 and "Open questions"; read the rest before you change behaviour.

## 1. Needs the owner

You can prepare these and help read the results; you cannot do them ([decisions.md](decisions.md) section 7: an unsigned build is all you can make; section 2 and [playbooks.md](playbooks.md) P11 step 4: anything that needs a session is the owner's to run).

| Item | Why it matters | Size | Risk | Where to start |
|---|---|---|---|---|
| The App Store submission's owner-only fields | The listing (ios-app/APP_STORE_LISTING.md) is complete except what only the owner holds: a name and contact e-mail in privacy.html's ONE placeholder, a demo Psycle account for App Review (signed out, the app shows a sign-in prompt and nothing else — without one the submission fails Guideline 2.1), the review contact, the copyright line, renaming the App Store Connect record from "PsycleBookingBuddy", the App Privacy and age-rating answers, and whether to ask Psycle for written permission | small | The demo account is on a real booking system: say so in the notes (the listing's text does) | The checklist that ends ios-app/APP_STORE_LISTING.md |
| See the Live Activity go, five minutes after a class starts | The rule is compiled and its arithmetic is checked on a Mac; whether the background task, the widget's reload or `.after(date)` really removes the card on time can only be seen on a phone, with a real class | trivial | None: watching. If it is still up after ten minutes, note whether a Psync widget is placed and whether Background App Refresh is on | ios-app/NATIVE_FEATURES.md → the device checklist, item 7; agents/decisions.md section 6 |
| Switch calendar sync on from a fresh install, on an iPhone | The bridge now asks for calendar access ALONE (it used to ask for Reminders too, with no purpose string, and the first attempt read as denied). Proved against fakes; the real prompt, and that no Reminders prompt appears, needs a phone | trivial | None | tests/suites/calendar-safety.js → "Calendar permission"; `_ensureCalendarPermission` in ios-app/www/native-bridge.js |
| The on-device checks, on a signed build | Widgets, the Live Activity, notifications, the launch screen, safe areas and the seat map's scroll shades have never been seen on a phone: 31 boxes in 8 groups (Appearance, The new mark, Widgets, Live Activity, Notifications, Your usual week, Share sheet, Calendar), none ticked, plus the 12 numbered points of the device checklist | medium | None to the code; each finding becomes a task | [On-device checklist](../IMPROVEMENTS-2026-09.md#on-device-checklist--still-owed); [device checklist](../ios-app/NATIVE_FEATURES.md#device-checklist--crisp-colour-widgets-2026-09-19) in ios-app/NATIVE_FEATURES.md; [playbooks.md](playbooks.md) P7 step 7 |
| The Android on-device checks, on the debug APK | Back in every overlay, the two notification channels and how late a reminder fires, calendar sync on a Google calendar, the bar colours per theme, the themed icon, the launch screen, the storage restore, share as text, sign-in from `https://localhost` — and the widget: placing it, both layouts and resizing, light / dark, each colour intensity, the roll-over after a class starts, the tap on a cold start and a warm one, sign-out, a reboot, a second launcher — and the class countdown: that it appears by itself 90 minutes out with the app closed (and how late: its alarm is inexact and does not wake a sleeping phone), goes at the start with the app dead, what a locked phone shows, the tap, a cancel, a sign-out against an expired session, the Class reminders switch, its own channel switched off, a reboot inside the window. None of it has run on a phone; the emulator run never taps | medium | None to the code; each finding becomes a task. A real booking there is the owner's alone, and optional | [../ios-app/ANDROID.md → "On-device checklist"](../ios-app/ANDROID.md#on-device-checklist); the APK: its "Try it with nothing installed" |
| A first real "Review and book" run, watched | The only proof that exactly the spots shown are the spots booked; best on a Monday just after 12:00 London | small | It spends real credits: the owner's alone. Note what Psycle says to a class past what is open | The "Your usual week" lines of the on-device checklist; [architecture/usual-week.md](architecture/usual-week.md) |
| A first real booking at a studio with no seat map | The count body of `POST /bookings` has never met the live API | small | An odd or refused answer: read `psycle_error_log` afterwards (Settings → Bug report) | [architecture/booking.md](architecture/booking.md) → "No-layout studios" |
| The Capacitor 6 → 8 upgrade | Capacitor's own scene support replaces the hand-written forwarding and one plugin patch — and it is now the gate for Google Play (next row) | large | High: the calendar plugin's call shapes change silently, so sync simply stops unless ios-app/www/native-bridge.js and its suite change with the packages. The lockfile must keep resolving from registry.npmjs.org. Needs a phone — now two: the runbook was written before ios-app/android/ existed and has NO Android steps (variables.gradle, AGP and the Gradle wrapper, the regenerated `capacitor.*.gradle` files, edge-to-edge) | [../ios-app/UPGRADE-CAPACITOR-8.md](../ios-app/UPGRADE-CAPACITOR-8.md) — a runbook, never yet run; its section 9 lists what is UNVERIFIED |
| Google Play submission — BLOCKED on the target API level | Capacitor 6 builds for targetSdk 34 and Play refuses an upload below its floor (35 from 31 August 2025, and it rises each August — read the current figure). Two routes, neither taken: A, the Capacitor upgrade above (recommended: target level, AGP, Gradle and every plugin move together); B, raising `targetSdkVersion` by hand on Capacitor 6, a stopgap. Sideloading the debug APK needs neither | medium, after the upgrade | At targetSdk 35 Android enforces edge-to-edge and ignores the bar colours in res/values*/: the page is drawn under both bars and its `env(safe-area-inset-*)` rules become load-bearing on Android. Never raise `targetSdkVersion` by hand without a phone in hand. The keystore is the owner's and is never committed | ../ios-app/PLAY_STORE_DEPLOY.md, step 0.1 (both routes, and what each costs), then the whole runbook; [../ios-app/ANDROID.md → "Release"](../ios-app/ANDROID.md#release); ../ios-app/PLAY_STORE_LISTING.md |
| Psycle's written permission, before production on Google Play | Psync is an unofficial client that signs in to Psycle's system with the member's Psycle password: exactly what Play's impersonation, trademark and user-data policies look at. The listing says "independent" and "not affiliated" everywhere it can, but the only real protection is permission from Psycle | — (a conversation, the owner's) | Without it the app can be rejected at review or removed later at Psycle's request, and a removal is a strike against a developer account tied to the owner's verified identity. The testing tracks are the lower-stakes place to find out. Never soften or move the "independent" wording; never use a Psycle mark | ../ios-app/PLAY_STORE_DEPLOY.md, step 0.2 (Play Console Help has an "advance notice" form for a permission); ../ios-app/PLAY_STORE_LISTING.md |
| The closed test Google asks of a new personal developer account | A personal account made after 13 November 2023 cannot publish to production until a closed test has run with a minimum number of testers opted in for a minimum number of days without a break — 12 testers for 14 days when the runbook was written: read the current figures | medium, mostly waiting | None to the code. The testers are real people with Google accounts who install the app: plan who they are before the clock starts. The app-access text must give reviewers a credit-less, review-only Psycle account — and never give credentials to Play's pre-launch robot, which taps at random on a real booking system | ../ios-app/PLAY_STORE_DEPLOY.md, step 1 (point 5), step 5.4, step 6 |
| privacy.html still holds its ONE placeholder: the publisher's name and a contact e-mail address | Both stores ask for the policy's public address, and the page must say who publishes the app. Nothing was invented for it: `<span id="publisher">` reads "[OWNER TO COMPLETE: …]" | trivial | The repository is public: an address the owner is content to publish. The page is live only once `main` holds it, and its address (derived, not observed: GitHub Pages of this repository) wants confirming in Settings → Pages. 22-play-release.js accepts the placeholder or exactly one e-mail address there, nothing else | privacy.html; ../ios-app/PLAY_STORE_DEPLOY.md, step 5.3 |
| The first run of the manual release workflow | .github/workflows/android-release.yml has never run: `keytool -storepass:env`, GNU `base64 --ignore-garbage`, `jarsigner -verify`'s words, the two fingerprint greps, `shred` and the `play-release` environment were all written from memory, and release signing has never met a keystore | small | None to a member: it builds and keeps an artifact, it uploads nothing. It needs the upload key and the four `PSYNC_…` secrets, which only the owner can create; a step that fails says why in one red line — fix that line, never loosen what 22-play-release.js holds (no echo of a secret, no `set -x`, no third-party action, no store upload) | ../ios-app/PLAY_STORE_DEPLOY.md, steps 2 to 4b; [architecture/android.md](architecture/android.md) → "Release signing, the target API" |
| App Store submission | Listing copy and six screenshots are ready | small | The owner's call and the owner's copy. The listing still says "when new booking week opens", which the release model has since replaced | ../ios-app/APP_STORE_LISTING.md; ../ios-app/appstore-assets/; [playbooks.md](playbooks.md) P9 |
| Answers to the open questions | Seven usual-week choices and fourteen Android ones were made on the owner's behalf; two more were never asked | trivial each | Low. Ask as short questions with a recommendation | The end of [decisions.md](decisions.md) |

## 2. Product polish

| Item | Why it matters | Size | Risk | Where to start |
|---|---|---|---|---|
| The copy audit's leftovers: wording the owner should rule on, and small things left alone before a release | From the September 2026 audit, NOT changed: "seats" in the usual week where the rest of the app says "spot" (an open question already; recommendation: "spot"); "On the waitlist!" (a place is not a booking: recommendation, no exclamation mark); the Filters quick pill "★ Favs" (the chip says "Favourites"; longer text may wrap at 320 px); four verbs for one act in the seat picker (Select / Pick / Tap / Choose: pinned in booking-races.js and 9c-sheets.js); the iPhone widget's "No upcoming class" against Android's "Nothing booked"; Siri's sentence order (place before time reads better); the favourite star on an instructor card is a `<span>` with a title and no role or aria-label; the calendar EVENT title and notes ("Ride 45 — Maya", "Duration: 45min") — left because they are the member's own calendar data and the reconcile may match on them | small each | Low, except the calendar title: read `_calIsOwned` and the matcher before touching it | tests/suites/23-release-polish.js for the rule; `git grep -n " — " -- js/` for what remains (labels, comments, the two label contracts) |
| Dead markup that still ships: the paste-a-token dialog | psycle-finder.html `#tokenDialog` and its two toasts in js/app.js are unreachable (nothing calls `showTokenDialog`) and are the most developer-sounding text in the bundle. Deleting it is right, but not the week of a submission: check tests/suites/shell.js and the DOM-id index first | small | Low once the ids are checked | `git grep -n tokenDialog` |
| The registered notification actions nobody sees | 'Book' / 'Cancel' / 'Snooze' are registered on iPhone (NotificationCategories.swift, the bridge's `registerActionTypes`) but no scheduled notification carries the category. If one ever does, "Cancel" on a class reminder is ambiguous. Remove the registration or make it one action, "View class" | small | Low | `git grep -n PSYCLE_CLASS` |
| The usual-week review sheet is cramped on an SE-size phone (375 × 667) with a waitlist row ticked | It is the screen where credits are spent, and the one hardest to read there | medium | Medium: layout only — never touch what the run sends. Check Terminal and Handheld, where the list is shortest | `_runUsualWeekSheet`, `pure:usual-week-sheet` (js/tabs.js); css/crisp.css; 14a-usual-week-sheet.js; [playbooks.md](playbooks.md) P8 at 375 × 667 |
| Month abbreviations differ: "21 Sept" in some labels, "18 Sep" in others | Reads as unfinished | small | Low, but suites pin printed strings. `toLocaleDateString('en-GB', { month: 'short' })` prints "Sept" on current engines; four hard-coded arrays print "Sep" | The arrays in `showBikePicker`, `_waitlistClassLine`, `showBookingConfirmation` (js/app.js) and `renderVarietyTrend` (js/tabs.js); `git grep -n "month: 'short'" -- js/`. One formatter, as `pure:clock` is for times |
| The first-paint `themeBoot` script ignores a `?theme=` deep link for one frame | A flash of the saved theme before js/theme.js applies the link | trivial | Low. Both copies (psycle-finder.html, login.html) must stay identical and mirror `APP_THEMES` / `RETIRED_THEMES`; then rebuild www/ | `themeBoot`; `_currentThemeId` (js/theme.js); 10b-themes.js; [architecture/theming.md](architecture/theming.md) |
| The next-class pill, called back by an upward scroll, stays where it lands — over a Book button too — until the next scroll down | A tap meant for Book can open My Bookings | small | Medium: it is deliberate today (the comment above `pure:pill-rest`: "the member asked for it"). Ask before changing it | `_pillScrollStep`, `_pillCoversBook` (js/settings.js, `pure:pill-scroll`, `pure:pill-rest`); 10c-polish.js; [architecture/bookings-tab.md](architecture/bookings-tab.md) |
| The usual week (`psycle_weekly_template`) is not part of a settings export / import | A member who changes phone keeps their settings and loses their usual week | small | Medium: an imported file is untrusted — coerce `seats` by the rule of `_templateSeats`, and import only adds | `EXPORT_KEYS`, `_planSettingsImport`, `_importSummary` (js/settings.js, `pure:import-validate`; without its line the import applies nothing); import-validate.js; the key's row in [architecture/storage-keys.md](architecture/storage-keys.md); [playbooks.md](playbooks.md) P3 |
| Exact alarms on Android, ONLY if reminders prove late on a phone | Reminders are inexact by choice (no special-access screen for the member). From Android 12 the notifications plugin then arms `setAndAllowWhileIdle`, whose delivery window is the SYSTEM's — three quarters of the wait, at most an hour — so "90 minutes before" can in principle arrive with 30 to go; usually it is minutes. (The app's OWN two alarms — the widget's roll-over and the countdown's next look — are `setWindow` with ten minutes, learnings G16; the plugin's are not the app's to change) | small | Medium: `SCHEDULE_EXACT_ALARM` is a special access the member must grant in system settings (the installed notifications plugin has `checkExactNotificationSetting` / `changeExactNotificationSetting` for it), and Play restricts `USE_EXACT_ALARM` to alarm and calendar apps. 19-android-project.js fails on either permission until it is changed with the manifest | The on-device checklist's "note how late" lines first; `_ensureAndroidChannels` and the comment above it (ios-app/www/native-bridge.js); ios-app/android/app/src/main/AndroidManifest.xml |
| An image share card on Android | The stats and year cards go out as text there: an image needs a file on disk | medium | It needs a file plugin — a new Capacitor plugin is a new iOS pod, which [decisions.md](decisions.md) section 8 rules out without the owner's say. Ask first | `_shareAsTextOnAndroid` (js/tabs.js); 18-android.js |
| On Android 12 and earlier, ask before the first reminder — the owner's choice | Those versions have no notification permission, so the first-booking ask never runs (it runs only while the system's answer is still owed): class reminders and the countdown are simply ON from the first booking, the countdown showing class, place and seat on a lock screen left at Android's default. privacy.html and ios-app/ANDROID.md now say so; the alternative is to let that ask also run, on Android only, when the system says "granted" and `psycle_class_reminder_asked` is unset, with "Not now" storing `'off'` — then one answer governs both notifications on every Android version | small | Low, and it is copy and behaviour: ask first ([decisions.md](decisions.md), "Chosen on the owner's behalf"). Behind `IS_ANDROID`, so `IPHONE_LAUNCH_DIGEST` cannot move; 18-android.js and 21-android-countdown.js boot the bridge as `'android'` | `_maybeAskClassReminders` (ios-app/www/native-bridge.js: the `perm.display !== 'prompt'` return); `_classRemindersEnabled` |
| Calendar sync reads every calendar's coming events to find its own | `_listNativeEvents` asks the plugin for `listEventsInRange` over 120 days; @ebarooni/capacitor-calendar 6.7.2 has no calendar filter on either platform, so every event's title, place and times reach the web view before the bridge keeps its own (`calendarId`, the ownership marker). Nothing leaves the device, and privacy.html now says how wide the read is; narrowing it needs a calendar id in the plugin's query | small once the plugin offers it; medium as a patch | Medium: calendar-safety.js pins today's call shapes, a plugin bump moves `PATCHES` and the Podfile (learnings G8), and the 6 → 8 upgrade changes three of those shapes silently (../ios-app/UPGRADE-CAPACITOR-8.md) — do it with or after that upgrade | `_listNativeEvents` (ios-app/www/native-bridge.js); ../ios-app/patch-plugins.js; calendar-safety.js |
| A count of spaces on the widgets, for a studio with no seat map | The Android widget's reader already prints "2 spaces" from an optional `spaces` count, but the bridge writes none, so such a class shows no badge on either phone | small | Low: an optional field with a fallback on BOTH readers ([../ios-app/android/AGENTS.md](../ios-app/android/AGENTS.md) rule 16). The iPhone path's launch digest does not cover the snapshot's VALUES, but 11-native-snapshot.js and 20-android-widget.js pin an entry's fields | `_snapshotEventFor` (ios-app/www/native-bridge.js); `PsyncSnapshot.Entry.seatLabel`; `PsycleSlotLabel` (PsycleShared/PsycleSnapshot.swift) |

**The living lists — read them there; an item is copied here only where it is sized.** Strike an item where it lives, in the commit that fixes it.
- [../IMPROVEMENTS-2026-09.md → "Known small leftovers"](../IMPROVEMENTS-2026-09.md#known-small-leftovers): the four left after the close-out of 2026-09-20; none affects a booking. One is sized above (the SE-size sheet), two are rows of section 1 (the scroll shades on a phone, a no-layout booking), one is deliberate (the plain disc of a photo that cannot load).
- [../IMPROVEMENTS-2026-09.md → "Follow-up: your usual week"](../IMPROVEMENTS-2026-09.md#follow-up-your-usual-week): its paragraphs "Decisions the owner may want to overrule" (mirrored at the end of [decisions.md](decisions.md)) and "Not covered".
- What is deliberately left alone is NOT backlog: [decisions.md](decisions.md) sections 4 and 6.

## 3. Tooling, facts and docs

| Item | Why it matters | Size | Risk | Where to start |
|---|---|---|---|---|
| The app exits within a second of launch in the iOS 26.5 simulator — the UNMODIFIED `main` too | Seen on 2026-09-22 with the scratch-copy build of playbooks P7 step 3, unsigned and ad-hoc signed, on a freshly booted iPhone 17 simulator: `simctl launch` returns a pid, the process is gone within a second, no crash report is written and the app logs nothing. The same recipe ran the app days earlier, so suspect the simulator runtime or the build's debug dylib, not the Swift. Until it is understood, a Swift change can be compiled here but not watched | small | Low: tooling. The TestFlight build on the owner's phone is unaffected | playbooks P7 steps 3–4; try a Release configuration (`-configuration Release`), an older simulator runtime, and `ENABLE_DEBUG_DYLIB=NO` |
| Release builds write `[PsycleLiveActivity]` lines to the device log | Nine `NSLog` calls ship (event ids and minutes only — no names, tokens or addresses). Harmless, and useful when the owner reads a sysdiagnose; a stricter house would compile them out of Release | trivial | Low | `git grep -n NSLog -- ios-app/ios/App` |
| A second team id is left in the Xcode project's PROJECT-level build settings | Every target overrides it, so it changes nothing; it is only untidy in a public repository. Left alone on purpose just before a submission: signing settings are not the place for a cosmetic edit | trivial | Medium if done carelessly: Xcode Cloud signs from these settings | `grep -n DEVELOPMENT_TEAM ios-app/ios/App/PsycleBookingBuddy.xcodeproj/project.pbxproj` |
| tests/tools/fake-psycle.js keeps bookings in memory but not waitlist places: `PUT /waitlists/{id}` answers success and stores nothing, `GET /waitlists` is always empty | A waitlist flow cannot be driven end to end without a hand-made `H.serve` wrapper | small | Low: test tooling only. Keep the data made up, keep `H.unknown` honest, and update the file's header, ../tests/README.md and [playbooks.md](playbooks.md) P8 step 6 with it | tests/tools/fake-psycle.js `H.serve` — its `/waitlists` branches (`GET /waitlists` → an empty list, `PUT /waitlists/{id}` → `waitlist.id` 555). Proposed: keep places in a new array on `__H` (no such field yet). Shapes: [architecture/waitlist.md](architecture/waitlist.md), ../tests/README.md |
| The Android copies of the mark are held by no suite | Four vector drawables and two SVGs under assets/ repeat the mark's path data; 12-brand-mark.js reads none of them | small | Low. A drifted copy is a wrong icon, never a wrong booking | ios-app/android/app/src/main/res/drawable/ic_launcher_foreground.xml (its note); 12-brand-mark.js |
| Confirm the booking-horizon model | Observed once, on 19 September 2026; a second Monday after 12:00 London would confirm or correct it | trivial | Low: the model may suggest and explain, never block. Not the owner's alone: a token-free read of the public timetable, a handful of requests — only if your brief allows traffic to the live host ([playbooks.md](playbooks.md) P11 steps 2–3). Record it as OBSERVED, with the date | `pure:horizon` (js/app.js); [architecture/monday-release.md](architecture/monday-release.md) |
| About half the symbols in [index/](index/) carry no description: it is the first sentence of the comment DIRECTLY above a symbol, and the longest functions have theirs inside the body | A grep by purpose misses `renderMyBookings`, `bookClass`, `showBikePicker`, `openClassDetail` | small | Low in the tool. A comment added in js/ moves lines: keep sliced openers at column 0, rebuild www/ and the index | [index/README.md](index/README.md) point 2; tools/build-index.mjs, or one-sentence comments above the openers |
| The first-run welcome's hold at launch is on no device checklist | It can only be seen on a phone, and nobody is asked to look | trivial | None: a line of documentation | [architecture/ios.md](architecture/ios.md) → "First-run welcome at launch"; the on-device checklist |
| Every push to `main` archives: 25 Xcode Cloud compute hours a month | A day of small pushes can spend the month, and then nothing reaches TestFlight | — | A habit, not a change: batch commits into one push ([learnings.md](learnings.md) G6). A tag start condition is the owner's setting | [../ios-app/CICD.md](../ios-app/CICD.md) → "Release flow" |

## How to add to this list

One row, in the group it belongs to: what, why it matters to a member or a maintainer, an honest size and risk, and a real file or symbol to start from — and delete the row in the commit that does the work.
