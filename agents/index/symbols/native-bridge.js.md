# ios-app/www/native-bridge.js — symbols
Purpose: the shape of `ios-app/www/native-bridge.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

2335 lines · 153 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## Native Bridge — Capacitor integration layer (L1)

### Gym Time (Europe/London) (L15)
L36  var GYM_TZ
L37  var _gymWallFmt
L47  _gymWallToUtcMs(y, mo, d, h, mi, s)  — Absolute UTC ms for a wall-clock time in the gym's zone (DST-correct, independent of the device timezone).
L78  _classStartMs(startAt)  — Absolute UTC ms of an API class time.
L94  window._psycleClassStartMs = _classStartMs

### Calendar ownership decisions (pure) (L96)
L106  _calForeignEvents(events, calId, markerOf)  — Unmarked events in calendar `calId` — what a full-ownership reconcile would delete that isn't ours.
L113  _calSweepVictimIds(events, calId, ownedAck, markerOf)  — Native ids to clear out of `calId` when it stops being the sync target.
L136  _calAckCovers(stored, calId)  — Does the stored ownership ack hand over calendar `calId`?
L139  window._psycleCalForeignEvents = _calForeignEvents
L140  window._psycleCalSweepVictimIds = _calSweepVictimIds
L141  window._psycleCalAckCovers = _calAckCovers
L151  var Preferences
L152  var Haptics
L153  var Share

### Fix Login Flow (L168)
L171  window.openLoginPopup()  — window.open() in WKWebView opens Safari, breaking postMessage.

### Fix window.open calls (L175)
L179  var Browser  — calendar.js uses window.open for Google Calendar and ICS blob.
L180  var _origWindowOpen
L181  window.open(url, target)

### Persistent Storage (L196)
L200  var SYNC_KEYS  +45
L251  async restoreFromNative()  — On startup: restore from native storage to localStorage.
L271  syncToNative(key, value)  — Sync localStorage changes to native storage
L281  var _origSetItem  — Intercept localStorage.setItem and removeItem
L282  var _origRemoveItem

### Native Share (L331)
L334  async window.nativeShare(title, text, url)

### Native Calendar Integration (L345)
L352  var Calendar
L353  var CAL_EVENT_MAP_KEY
L354  var CAL_ID_KEY
L355  var CAL_TARGET_KEY
L356  var CAL_MODE_KEY
L357  var CAL_ENABLED_KEY
L365  var CAL_OWNED_ACK_KEY  — '2:<calendarId>' once the user has confirmed that target under the full-ownership contract — only then may the…
L366  _calIsOwned(calId)
L369  var PSYCLE_CAL_TITLE
L370  var PSYCLE_CAL_COLOR
L371  var PSYCLE_EVENT_MARKER
L373  _loadCalMap()
L376  _saveCalMap(map)
L385  _nativeSlotLabel(typeName)  — Slot label for a class type.

### Permissions (L390)
L392  var _calPermissionGranted
L394  async _ensureCalendarPermission()

### Dedicated Psycle Calendar (L414)
L416  async _listNativeCalendars()
L431  hasChosenCalendar()  — Has the user explicitly chosen a calendar to write into?
L435  async _resolveTargetCalendarId()

### Add / Update / Remove Events (L446)
L448  _buildCalEventData(eventId)  +55

### v6-plugin-correct primitives (L504)
L507  _markerEventId(ev)  — Extract the Psycle event id from a native event's notes, or null.
L514  _nativeStartMs(ev)  — Millisecond start time of a native event, across plugin shapes.
L522  _createdEventId(result)  — The created-event id from a createEvent() result, across versions.
L534  async _deleteNativeEvents(ids)  — Delete native events by id.
L552  async _listNativeEvents(startMs, endMs)  — Normalized listEventsInRange (ms timestamps in, array out).
L559  calendarSyncEnabled()

### Hook into booking / cancel / seat-cancel flows (L567)
L574  var _origSubmitBookingNative
L585  var _origConfirmUnbookNative
L593  var _origUpcomingCancelNative
L601  var _origCancelBikeSlotNative
L609  var _origUpcomingSeatCancelNative

### Full Calendar Reconcile (L617)
L634  async syncAllBookingsToCalendar()  +118
L754  var _origRenderNative  — Run sync after bookings load
L755  var _calSynced
L771  async window.psycleResyncCalendar(opts)  — Force a fresh sync.
L802  async window.psycleCleanupDuplicates()  — Settings → "Remove duplicates".
L811  window.syncAllBookingsToCalendar = syncAllBookingsToCalendar

### Public API for Settings UI (L813)
L816  async window.psycleListCalendars()  — List iOS calendars the user can choose from.
L834  window.psycleGetCalendarConfig()  — Get the current calendar sync config for the Settings UI.
L854  async window.psycleCountForeignEvents(calId)  — READ-ONLY: how many upcoming events in calendar `calId` are NOT Psycle bookings — what handing that calendar over…
L879  async window.psycleSetCalendarConfig(cfg)  +62  — Update the calendar target.

### Weekly Booking Reminder (L943)
L948  var LocalNotifications
L955  var REMINDER_IDS  — Eight rolling one-shot notifications (the next 8 Mondays).
L960  var WEEKLY_REMINDER_HOUR, WEEKLY_REMINDER_MINUTE  {weekly-reminder}  — WHEN it fires and WHAT it says.
L961  var WEEKLY_TEMPLATE_KEY  {weekly-reminder}
L970  _nextMondaysNoonLondon(count, now, wallToUtcMs)  {weekly-reminder}  — The next `count` occurrences of Monday 12:00 Europe/London as absolute Date instants, DST-correct for any device…
L988  _hasUsualWeek(raw)  {weekly-reminder}  — Is a usual week saved?
L998  _weeklyReminderCopy(hasUsualWeek)  {weekly-reminder}  — It fires AT the release, so it says the dates ARE open (and still reads true when the banner is seen later).
L1008  _weeklyTapTab(hasUsualWeek)  {weekly-reminder}  — Where the tap lands at once (the web layer's _onBookingWeekOpened does the rest when it can): the usual-week review…
L1017  _weeklyOfferDecision(f)  {weekly-reminder}  — Put the in-app "remind you on Mondays?" question (_offerWeeklyReminder)?
L1026  _usualWeekSaved()
L1030  var REMINDER_PREF
L1034  var _weeklyArmedWithWeek  — Which body the armed reminders carry (true = "Book your usual week…"), null while nothing is known to be armed — so a…
L1036  async _cancelReminders()
L1050  async scheduleWeeklyReminder(interactive)  +41
L1094  var _weeklyChain  — One pass at a time: each cancels the eight ids and schedules them again, and two interleaved passes could leave the…
L1095  _scheduleWeeklySerial(interactive)
L1104  window._nativeReminder = {…}  — keys: isOn, enable, disable — Exposed to the web layer (Membership tab toggle).
L1129  _rearmWeeklyReminder()  — The body names what the tap opens, so it follows the usual week.

### Notification Action Buttons (Book / Cancel / Snooze) (L1150)
L1162  registerNotificationActions()
L1184  var TAP_ROUTE_WAIT_MS  — Land a tapped notification on what it was about.
L1185  var _cancelTapRoute
L1196  _askDiscoverToRefresh()  — The Monday reminder exists for one moment: the release of new dates.
L1205  _routeNotificationTap(eventId)  +53
L1261  handleNotificationAction(notification)  — Route a tapped notification / action button.
L1303  _parseWidgetLink(url)  {widget-link}  — A Home/Lock Screen widget tap opens psync://bookings?event=<id> (minted in PsycleWidget.swift).
L1322  handleWidgetURL(info)  {widget-link}  — Same landing as a class-reminder tap: My Bookings, then that class's sheet (only while the seat is still held).
L1338  var PsycleDeepLink  — The in-app PsycleDeepLink plugin RETAINS the URL until this listener attaches — a widget tap usually cold-launches the…

### Widget / Live Activity / Siri Snapshot (L1346)
L1369  var WIDGET_APP_GROUP  — App Group container id.
L1371  var WIDGET_NEXT_KEY
L1372  var WIDGET_WEEK_KEY
L1377  var WIDGET_UPCOMING_KEY  — Next few classes (same shape as widget_next_class, array of up to 5) so the widget can build a MULTI-ENTRY timeline…
L1382  _prefSet(key, value)  — Standard Preferences plugin: lands in UserDefaults.standard under "<group>.<key>" (NOT extension-readable — kept for…
L1395  _appGroupSet(key, value)  — App Group mirror: writes the SAME logical value under the BARE key into the shared App Group suite, so a native…
L1404  _writeSnapshotKey(key, value)
L1419  var WIDGET_UPCOMING_MAX  {ios-polish}  — The widget timeline shows the next few classes; reminders cover every class held.
L1420  var CLASS_REMINDER_MAX  {ios-polish}
L1424  _isHeldSeat(booking)  {ios-polish}  — A real seat, as opposed to a waitlist place — true even when the class's details failed to load this pass (that makes…
L1433  _evtFromSavedItem(item)  {ios-polish}  — Stand-in for an _eventCache entry, built from app.js's saved copy of My Bookings (psycle_bookings_snapshot), which…
L1451  _staleReminderIds(map, wanted, keep)  {ios-polish}  — Event ids whose armed reminder should go: the class is no longer wanted, or its time moved (it is re-armed for the new…
L1473  var SNAPSHOT_NEUTRAL_TINT  {native-snapshot}  — The card ground at intensity "off": the app's own neutral surface (Cloud / Graphite, css/theme.css) — the widget's…
L1477  _snapHex(value)  {native-snapshot}  — '#2d5fd6' → '#2D5FD6'; anything that is not six hex digits → null.
L1487  _snapClassTypeKey(typeName, keyFn)  {native-snapshot}  — "RIDE: 45" → 'ride', through the app's OWN classTypeKey (js/app.js) — the same answer data-ct gets.
L1507  _snapClassColourFields(key, engine)  {native-snapshot}  — The colour fields for one class type, read from the app's colour engine (`engine` = window.PsycleClassColours: get() →…
L1540  _savedItemsById()  — app.js's saved copy of My Bookings, by event id ({} when there is none).
L1555  _snapshotEventFor(eventId, known)  +35  — Resolve a display-ready event object from the cache, or null.
L1602  var _snapServerConfirmed  — Only trust an EMPTY bookings map after the server confirmed it (bookings:loaded fired this session).
L1604  var _signOutPass  — True only while the clearToken wrapper (a deliberate sign-out) runs its pass.
L1607  var _snapColourSig  — The class colours the last WRITTEN snapshot carries (see the 'classcolours:changed' listener below), and that…
L1608  var _colourSnapTimer
L1609  _classColourSig()
L1618  updateWidgetSnapshot()  +119
L1737  window.updateWidgetSnapshot = updateWidgetSnapshot

### Class-start reminders (T-90 minutes) (L1739)
L1751  var CLASS_REMINDER_PREF
L1752  var CLASS_REMINDER_MAP
L1754  _classRemindersEnabled()
L1758  _loadReminderMap()
L1761  _saveReminderMap(map)
L1765  _reminderIdFor(eventId, map)
L1779  var _reminderChain  — Serialized: overlapping snapshot passes (booking events fire in quick succession) must not interleave the…
L1783  _scheduleClassReminders(upcomingList, unknownSeats)  — `unknownSeats` = {eventId: true} for held seats this pass knew nothing about (see _staleReminderIds) — captured with…
L1790  async _scheduleClassRemindersInner(upcomingList, unknownSeats)  +51
L1843  window._nativeClassReminders = {…}  +30  — keys: isOn, hasPermission, enable, disable — Settings toggle API (rendered by tabs.js next to the weekly reminder).

### First-booking ask (L1874)
L1880  var CLASS_REMINDER_ASKED  — The pref defaults ON, but only the Settings switch ever requested notification permission — and it looked on already,…
L1881  var ASK_POLL_MS
L1882  var ASK_GIVE_UP_MS
L1883  var _reminderAskBusy
L1884  var _reminderAskVoid
L1893  var ASK_BLOCKING_IDS  — Anything the ask would land on top of.
L1898  _askBlocked()
L1907  _askStillWanted()
L1911  async _maybeAskClassReminders()  +53

### Usual-week ask: the Monday reminder, offered in context (L1973)
L1984  var WEEKLY_REMINDER_ASKED  — The weekly reminder defaults OFF and its switch sits in Settings, where nobody looks for it.
L1985  var _weeklyAskBusy
L1986  var _weeklyAskSince
L1988  _weeklyOfferFacts(permission)
L1997  async window._offerWeeklyReminder(reason)  +56
L2060  var _origClearTokenNative  — Deliberate sign-out must blank the snapshot and cancel pending class reminders — otherwise the previous account's…
L2102  var _calReconcileTimer, _calReconcileInFlight, _calReconcilePending  — Auto-reconcile the calendar whenever bookings change — covers classes cancelled on the web / another device and…
L2105  async _runCalSync()  — Single-flight calendar sync.
L2114  _scheduleCalReconcile()
L2122  var _calServerConfirmedEmpty  — Tracks whether the latest bookings state came from a successful server fetch.

### Status Bar (L2145)
L2148  updateStatusBar()
L2176  var observer  — Watch for theme changes

## Analytics / Diagnostics (developer-only, no third-party services) (L2181)
L2185  var ACTION_LOG_NATIVE_KEY
L2186  var MAX_NATIVE_ACTION_LOG
L2190  var _origPushAction  — Persist action log to Capacitor Preferences (survives iOS storage purges).
L2199  _persistActionLogToNative()
L2218  async window.getDiagnosticReport()  +114  — getDiagnosticReport — comprehensive diagnostics string for developer use.
