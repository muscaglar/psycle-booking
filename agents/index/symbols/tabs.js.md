# js/tabs.js — symbols
Purpose: the shape of `js/tabs.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `9836e6a`). Do not edit; rebuild with `npm run agents:index`.

4580 lines · 182 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


### Tab Navigation (L20)
L22  var TABS
L23  var _currentTab
L25  initTabs()  +252
L278  window.switchTab(tab, noHash)  +72
L364  _stillToCome(startAt, now)  — Is this held class still to come?
L370  updateTabBadge()
L432  var _origRender  — Also update badge when renderMyBookings is called directly
L433  _patchRender()

### Stats sub-pages (L445)
L459  var STATS_PAGES  {stats-pages}  — THE grouping.
L467  var STATS_PAGE_DEFAULT  {stats-pages}
L468  var STATS_PAGE_KEY  {stats-pages}
L470  _statsPageById(id)  {stats-pages}
L476  _statsPageId(raw)  {stats-pages}  — Whatever was stored or passed in → a real page id.
L481  _statsPageForKey(currentId, key)  {stats-pages}  — A key pressed in the tab list → the page to move to, or null when the key is not the tab list's.
L492  _statsPageStep(currentId, dir)  {stats-pages}  — The neighbour a horizontal swipe leads to (dir: +1 = next, -1 = previous).
L498  _statsPageIsEmpty(id, shown)  {stats-pages}  — Is there nothing on this page?
L507  _statsEmptyState(id, shown, o)  {stats-pages}  — What a page with nothing on it says: ONE line — never a blank page, never a paragraph.
L518  _statsScrollTarget(current, pageTop, barHeight, pinTop)  {stats-pages}  — Where the scroller goes when a page opens.
L526  var _statsPageNow  — The page on screen.
L531  var _statsPainted  — Pages built since the last renderInsights(): a page is built when it is first opened, and not again until the data is…
L533  var _statsHeroOnly  — Signed out / can't-reach-Psycle with nothing on record: the hero has the tab.
L538  var _STATS_SECTION_PAINT  — Which function fills which section.
L552  _statsPagesHtml(sectionHtml, current)  — The switcher and the three page wrappers, from the table above.
L572  _wireStatsPages(panel)  — Once, from initTabs.
L603  _paintStatsPage(id)  — Build the sections of one page, unless they were built since the last renderInsights().
L615  _applyStatsPage()  — Selected tab, roving tabindex, and which page is displayed.
L631  _syncStatsEmpty()  — The one quiet line of a page with nothing on it (pure:stats-pages decides).
L659  _syncStatsPin()  — Wider than a phone the document scrolls and the tab bar is what sticks to the top: the switcher pins beneath it,…
L670  _scrollToStatsPageTop(page)  — The DOM half of _statsScrollTarget.
L693  _syncStatsPages(heroOnly)  — renderInsights' half of the pages: every call is a fresh look at the data, so nothing built before it is trusted — and…
L707  window.showStatsPage(id)  — Open one of the Stats sub-pages ('overview' | 'habits' | 'instructors').
L724  window._statsPageStep(dir)  — For a swipe helper: the page a swipe in `dir` (+1 / -1) would open, or null.
L737  _statsSwipeMayStart(target)  — Swipe left / right between the pages.
L750  _wireStatsSwipe(panel)  — Once, from initTabs — bound to the panel, which outlives every repaint of its sections.

### Render insights tab content (L764)
L772  var _historyRaw, _historyParsed  — One Stats paint asks getFullHistory() ~11 times (every section below) and the string runs to 400 KB.
L778  getFullHistory()  — Get the full class history from localStorage (synced + locally tracked).
L795  var _tokenReadable  — The stored token is only readable once security.js has decrypted it (on iOS: after the Preferences restore).
L801  window.renderInsights()  +41

### Quick Stats (L843)
L845  renderQuickStats()  +128

### Weekly Calendar View (L974)
L976  var _weekOffset
L978  window.weekNav(dir)
L985  window.planDay(dateStr)  — Interactive weekly planner: tap an empty day to search for classes on that date
L998  renderWeekView()  +112

### Your usual week (weekly template — UI only) (L1111)
L1118  var UW_DAYS
L1122  _uwDateLabel(dateStr)  — 'YYYY-MM-DD' → "Mon 21 Sep".
L1129  _uwPlural(n, one, many)
L1137  _uwTypeOf(label)  {usual-week-crisp}  — A template entry's label is "Type · Instructor" (app.js _templateFromSeats).
L1143  _uwTime(totalMin)  {usual-week-crisp}  — Minutes after midnight → "18:30", through app.js's one formatter (_clock24, pure:clock).
L1151  _uwClassMark(typeName, keyOf, pictogram)  {usual-week-crisp}  — The tile + data-ct of a class type.
L1162  _uwTimeHtml(totalMin)
L1166  _uwMark(typeName)
L1172  _usualWeekTemplate()
L1183  _collectDisplayedWeekTemplate()  — Template entries from the REAL seats held over the next 7 days (it used to read the week shown in the Discover…
L1192  _usualWeekFromHistory()  — Nothing booked this week (a holiday, or the first Monday with the app): fall back to the slots history shows 2+ times.
L1213  _uwLog(action)
L1217  async window.saveWeekAsTemplate()  +42
L1261  async window.clearUsualWeek()  — The card's own handlers (window.clearWeeklyTemplate is app.js's storage call).
L1279  window.removeUsualWeekEntry(idx)
L1293  _placeUsualWeekCard(card)  — Right after #rebookHint — a node app.js removes and re-creates in front of #upcomingPanel whenever it likes.
L1303  var _usualWeekWatch  — The hint is re-made outside any event this module hears (first paint), so watch the tab's own children — not its…
L1304  _watchUsualWeekPlacement(card)
L1317  var UW_COLLAPSED_KEY  {usual-week-card}
L1323  _uwCollapsedFrom(stored, mem)  {usual-week-card}  — Stored '1' = collapsed; anything else there (absent, junk, an import's "true") = expanded.
L1331  _uwCardSeats(value)  {usual-week-card}  — How many seats an entry books: an integer 1–4 (Psycle's own limit per booking); anything else stored there reads as 1.…
L1336  _uwToggleWord(collapsed)  {usual-week-card}  — The word that shows the head is a control (no chevron: the Filters bar's rule).
L1342  _uwCardHtml(template, collapsed)  +49  {usual-week-card}  — The whole card for a saved week.
L1397  var _uwCollapsedMem  — Collapsed or not: localStorage psycle_usual_week_collapsed ('1' | absent), read at every paint — never cached: in the…
L1398  _uwIsCollapsed()
L1403  _uwRememberCollapsed(collapsed)
L1416  window.toggleUsualWeek()  — The head's button.
L1443  _uwFocusedButton(card, doc)  — Which of the card's buttons holds focus (-1: none), and putting it back on the button in that place once the card has…
L1446  _uwRefocusButton(card, idx)
L1454  renderUsualWeekCard()  +38
L1492  window.renderUsualWeekCard = renderUsualWeekCard
L1509  var UW_NOT_OPEN  {usual-week-sheet}  — What the sheet says about a planned row (state → copy).
L1510  var UW_NOT_LISTED  {usual-week-sheet}
L1513  var UW_WAITLIST_NOTE  {usual-week-sheet}  — What joining a waitlist from here means (it leads with a space: it follows the sheet's money sentence).
L1514  _uwPlanNote(row)  +30  {usual-week-sheet}
L1546  _uwTopUpNeed(row)  {usual-week-sheet}  — "1 of 2 seats held": the seat(s) still missing — all a top-up may ask for.
L1553  _uwTickOf(row, need)  {usual-week-sheet}  — "Choose again" lists the same dates afresh.
L1566  _uwKeepTick(kept, row)  {usual-week-sheet}  — …and a kept tick stands only on a row that is still THAT: by template index alone, a seat that went while the class…
L1583  _uwResultNote(result, info)  {usual-week-sheet}  — …and about what happened to a ticked row once the run reached it.
L1606  _uwSeatOptions(saved, max)  {usual-week-sheet}  — The seat counts a row offers: 1 · 2, and 3 · 4 only when the saved entry asked for them — never above the class's own…
L1617  _uwConfirmLabel(t)  {usual-week-sheet}  — The confirm button counts what it will SPEND: "Book 3 classes · 4 seats".
L1629  _uwRangeLabel(range, fmt)  {usual-week-sheet}  — A date range of the sheet's control, as it is labelled.
L1642  _uwLabelHalves(label)  {usual-week-sheet}  — "Newly opened · Fri 25 Sept – Thu 1 Oct" as two halves that each stay whole.
L1656  _uwScrollTopToReveal(scrollTop, viewH, row, used)  {usual-week-sheet}  — The list's scrollTop once the sheet has re-flowed around the row the member is on.
L1678  _uwRowHtml(row, note, checkbox, spots)  — One row of the sheet.
L1709  _uwNoteInner(note)  {usual-week-note}  — What a row's note holds.
L1718  _uwFocusBack(prev, doc)  — Where focus goes when the sheet closes: what held it on open, if that is a real element still in the page — else the…
L1738  _runUsualWeekSheet(opts)  +620  — The sheet between "Book my usual week" and any booking.
L2363  var _templateWeekRunning  — Opens the REVIEW sheet — it books nothing by itself.
L2364  async window.bookTemplateWeek(opts)  +42
L2409  _statsTile(ct)  — The small pictogram tile that leads a class name on a Stats card (app.js classPictogram; css/crisp.css .ct-tile).

### Recommendations (L2414)
L2416  renderRecommendations()  +58

### Habit-slot alerts (Feature 1) (L2475)
L2480  var DAY_NAMES_FULL
L2484  _nextWeekdayDateStr(dow)  — Local-date "YYYY-MM-DD" for the next occurrence of a weekday (0=Sun..6=Sat).
L2494  renderHabitSlots()  +66

### Streaks & milestones (Feature 3) (L2590)
L2599  _weekIndex(date)  — Monday-anchored week index: whole weeks since a fixed epoch Monday.
L2606  renderStreaks()  +82
L2699  var HEAT_BANDS  {stats-charts}  — Five bands of the day, as the board draws them.
L2706  var HEAT_DAYS  {stats-charts}
L2709  _wallParts(s)  {stats-charts}  — 'YYYY-MM-DD HH:MM…' (space or T) → { day: 0–6 Monday first, hour } | null.
L2718  _heatBandIndex(hour)  {stats-charts}
L2725  _heatLevel(count, max)  {stats-charts}  — 0 = never; 1–4 = quarters of the busiest cell (the old grid's own steps).
L2733  _heatmapModel(starts)  {stats-charts}  — Class start times → { total, max, counts[band][day], levels[band][day], top: the (up to three) busiest cells, busiest…
L2756  _heatmapLabel(model)  {stats-charts}  — What the grid says to someone who cannot see it.
L2765  _streakWeeks(weeks, thisWeek, n)  {stats-charts}  — The streak's bars: the last `n` weeks, oldest first.
L2776  _streakWeeksLabel(bars)  {stats-charts}
L2786  _classTypeBreakdown(counts, cats)  {stats-charts}  — counts by CATEGORY_MAP key + the category list → what the class-type bar draws: rows with a class, biggest first (ct =…

### Activity Heatmap (L2800)
L2804  renderHeatmap()  +55

### Support Psync: optional tips (L2864)
L2878  var TIP_PRODUCTS  {tips}  — The allow-list, cheapest first: the same three ids as TipJarPlugin.swift and PsyncTips.java.
L2887  _tipRows(answer)  {tips}  — What the store answered → the rows to draw, in OUR order.
L2901  _tipOutcome(status)  {tips}  — The plugin's status → what to say.
L2908  _isTipId(id)  {tips}
L2913  _tipJar()
L2920  var _tipAnswer
L2921  var _tipAsking
L2922  var _tipBusy
L2924  renderSupportPsync()
L2949  async _leaveTip(id)
L2971  renderMembershipInfo()  +167

### Cost Per Class Tracker (L3139)
L3141  renderCostTracker()  +94
L3237  var FORECAST_TARGET_GBP  {cost-forecast}
L3238  var FORECAST_MAX_PER_DAY  {cost-forecast}
L3242  _bookableMore(made, max, daysLeft)  {cost-forecast}  — The most classes a "Book N more" line may ask for: what the plan still holds (max 0 = unlimited) and what fits in the…
L3252  _forecastVerdict(made, max, priceGbp, costAtMax, projected, projectedCost, daysLeft)  {cost-forecast}  — The forecast's one-line verdict.
L3275  _forecastHtml(sub, made, max, priceGbp, costAtMax)  +47  — Returns the forecast block HTML (or '' if there isn't enough to forecast).

### Theme picker (Membership → App) (L3323)
L3325  renderThemePicker()
L3350  window._pickTheme(id)

### Class colours (Membership → Appearance) (L3355)
L3364  var CC_INTENSITY_LABELS  {class-colour-control}
L3370  var CC_SWATCH_INK  {class-colour-control}  — What the preview card says for each class type (decoration — never timetable data).
L3371  var CC_PREVIEW  {class-colour-control}
L3376  _ccHas(obj, k)  {class-colour-control}
L3389  _ccControlModel(api, cats, theme, open)  +40  {class-colour-control}  — Everything the control draws, from what the engine says.
L3433  _ccRadioTarget(count, index, key)  {class-colour-control}  — A key pressed on radio `index` of `count` → the radio to move to (and check: in a radio group selection follows…
L3443  var _ccOpenKey
L3445  _ccThemeNow()
L3452  var CC_TICK
L3454  _ccControlHtml(m)  +52
L3510  renderClassColours()  — Rebuilt whole on every change (it is small), with the focus put back where it was: a radio group rebuilt under the…
L3532  window.renderClassColours = renderClassColours
L3534  _ccSet(partial, said)
L3545  _wireClassColours(box)  +42  — Once per container (it outlives every repaint of its contents).
L3598  window.renderThemePicker = renderThemePicker
L3599  window.renderReminderRow = renderReminderRow

### Weekly reminder row (the native apps only — needs the native bridge) (L3601)
L3606  var _classReminderGranted  — Last known answer to "may Psync send notifications?" for the class switch.
L3618  _classReminderSwitch(prefOn, granted, android)  {reminder-row}  — What the class-reminder switch shows.
L3634  _reminderAndroid()  — These rows exist in both native apps.
L3639  _osSettingsWords(text)
L3643  renderReminderRow()
L3668  _paintReminderRow(row)
L3691  async window._toggleClassReminders()
L3718  async window._toggleReminder()
L3730  _formatGbp(amount)
L3734  _daysLeftInBillingPeriod(sub)

### Class Type Distribution (L3751)
L3753  renderClassTypeDistribution()  +50

### Lapsed Favourites (L3804)
L3806  renderLapsedFavourites()  +53

### Instructor Variety Trend (L3860)
L3862  renderVarietyTrend()  +51

### Year in Review (Feature 4) (L3915)
L3927  _yearReviewRows(history, year, nowMs, startMs)  {year-review}  — The rows a year's wrap may count: this year's, not cancelled — and not still to come.
L3936  _takenRows(history, nowMs, startMs)  {year-review}  — The same test over every year: what the Stats tiles ("This month", "All time") and the "Share my stats" image may…
L3943  _historyStartMs()  — app.js's London resolver (h.date is gym wall clock); the device-local parse only when app.js is absent — the same pair…
L3950  _hasTakenHistory()  — Is there a class TAKEN to show?
L3955  _computeYearReview(year)  +62  — Aggregate this year's attended history into a tidy summary object.
L4018  renderYearReview()
L4039  window.openYearReview()  +47

### Share images (year wrap + stats card): shared helpers (L4087)
L4098  async _shareAsTextOnAndroid(title, text)  — The ANDROID app's web view has no Web Share API and no download manager: navigator.share is missing, and the <a…
L4114  var SHARE_FALLBACK  {share}  — Cloud, whole.
L4117  var SHARE_TOKENS  {share}  — Small grey labels read `muted` (--text-muted), never --text-faint: faint is under 3:1 in Handheld, and these labels…
L4120  var SHARE_FOOTER  {share}  — One credit line for both images (they were signed "Psycle Companion" and "Psycle Class Finder" — neither is the app's…
L4124  var SHARE_SCALE  {share}  — Export scale: a fixed integer, not devicePixelRatio — the PNG is looked at on OTHER devices, and a fractional ratio…
L4127  _sharePaletteFrom(read)  {share}  — read(tokenName) → the custom property's text, e.g. ' #efeee9'.
L4138  _sharePalette()
L4148  _shareCanvas(W, H)  — A W x H surface exported at SHARE_SCALE: every coordinate below stays in 640-wide layout units, the pixels are 3x (a…
L4163  var SHARE_WORDMARK_FONT  — Wait for the display face so the wordmark is not drawn in a fallback — but never for long: navigator.share() spends…
L4164  var SHARE_DISPLAY
L4165  var SHARE_BODY
L4166  _shareFontReady()
L4180  _shareWordmark(ctx, pal, y, label)  — "Psync" + the image's section label, on one baseline.
L4194  _shareClassColour(cat, pal)  — The class-type colour in effect, as a literal a canvas can paint: the member's own swatch through the engine…
L4203  async window.shareYearReview()  +114

### Share Insights Card (L4319)
L4340  async window.shareInsights()  +229
