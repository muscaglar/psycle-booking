# Handover — where things stand
Purpose: where the tree stood when this was last written, what has and has not been proved, what is the owner's to do, and how to prove your setup. **Read this when** you are picking the project up after a gap. **Skip it when** you only need to find code ([repo-map.md](repo-map.md), [index/](index/)).

## Session log

Newest on top. **Add an entry when you finish a stretch of work; keep the ten most recent, fold older ones into "How we got here".** Keep an entry to about 6 lines: detail belongs in ../IMPROVEMENTS-2026-09.md or the commit body — link to it. In the same commit delete the [backlog.md](backlog.md) row you did and the "Not verified" cell it settles. 16-agents-docs.js caps this file at 160 lines and holds its ≈ tokens in ../AGENTS.md and [README.md](README.md) to within a quarter: when an entry moves the size, correct both cells. Template:

```
### YYYY-MM-DD — the stretch, in a few words
- **State:** `main` at "<subject>"; pushed yes / no; branch ahead of `origin/main` by N.
- **Changed:** what moved, by area; commits by subject line. **Verified:** what was proved, and how. **Not verified:** what only a phone or Psycle can prove.
- **Left:** what is unfinished or now the owner's; anything the owner decided → decisions.md, in their words.
```

### 2026-09-20 — the Android class countdown (compiled, tested and posted by CI; not yet on a phone), and the road to Google Play written down
- **State:** `main` at "Android countdown, and the road to Google Play"; pushed yes — it went to `main` at the owner's request ("Then push it all etc.") once CI had compiled it, run its tests and posted the notification (hard rule 6); branch ahead of `origin/main` by 0. Check it: the command under "State of play".
- **Changed:** (A) the countdown the owner asked for ("Go ahead with the countdown notification too"): ONE silent, ongoing notification with the system's countdown chronometer, from 90 minutes before the next held class until it starts — Android's stand-in for the Live Activity. Plain Java under ios-app/android/…/countdown/ (a PURE `PsyncCountdownPlan`, `PsyncCountdownNotifier`, an unexported `PsyncCountdownReceiver` that plans and an unexported `PsyncCountdownDismissReceiver` that hears a swipe), the widget's tap shared through `PsyncTapIntent`; no foreground service, no exact alarm, no new permission, no new package, no `PsycleLiveActivity` twin. The bridge, on Android only, hands over the Class reminders switch as `countdown_enabled`. (B) Google Play, as the owner asked ("Add deployment to Play store details somewhere too for me"): the owner's runbook ios-app/PLAY_STORE_DEPLOY.md, a MANUAL `main`-only workflow that builds the signed bundle from four secrets only the owner can create, in a `main`-only `play-release` environment (.github/workflows/android-release.yml — it uploads nothing to a store), privacy.html, the Play icon and feature graphic. A review before the first push then changed it ([learnings.md](learnings.md) G16): both alarms are `setWindow` with ten minutes (the bare `AlarmManager.set` is an HOUR wide), a swiped-away countdown stays away for that class, never written = off, the planning receiver also hears an update and a changed zone or clock, `mayPost` is pure and tested, CI's verdict judges the timeout, the keystore is shredded before the upload action, and privacy.html claims only what the code does (22-play-release.js holds it to the shells' `connect-src`, the hosts js/ names and ios-app/package.json). How it works: [architecture/android.md](architecture/android.md) → "The class countdown", "Release signing, the target API"; what and why: [../IMPROVEMENTS-2026-09.md → "Follow-up: a countdown on Android, and the road to Google Play"](../IMPROVEMENTS-2026-09.md#follow-up-a-countdown-on-android-and-the-road-to-google-play).
- **Verified:** `npm run ci` (8,608 → 8,944 checks), `npm run agents:check`; `IPHONE_LAUNCH_DIGEST` unchanged and the iPhone's three sentences to the letter; every res/ XML (43) and both manifests parsed by a second, real parser (`xmllint`), every `@type/name` and `R.<type>.<name>` resolved (19-android-project.js); off CI: the three pure classes and all 81 JVM tests (36 the countdown's) with javac 21 at `--release 17` — 81 of 81 against Android's own org.json classes, and 25 of 25 deliberate mutations caught over the two rounds — every Java file of the app compiled against an API 34 framework jar with stand-ins for Capacitor and `R` (no warning from the app's own sources under `-Xlint:all`), and the real planner, notifier and both receivers run under Robolectric at API 34 (one notification, its channel, flags, timeout, immutable tap and delete intent, an `RTC` alarm with a 600,000 ms window; a swipe left down by later plan runs; never written = nothing). None of that is Gradle, the manifest merger or a phone; @capacitor/local-notifications 6.1.3's own manifest READ for `POST_NOTIFICATIONS` and `RECEIVE_BOOT_COMPLETED`; package.json and the lockfile untouched. The four work streams met disagreeing about the debug hook (its name, an action nobody read, typed `adb` extras against `getStringExtra`): reconciled to the Java, and each side of that contract — and of the bridge's key against the Java's allow-list — was seen to FAIL when changed alone ([learnings.md](learnings.md) G14).
- **Proved by CI since:** the countdown was compiled by CI's `android-build` job, which first ran all 81 JVM tests against the real org.json (36 of them the countdown's; all pass), and POSTED on an emulator by `android-smoke`: Android's own record (`dumpsys notification`) shows one notification, id 7090, tag `psync-countdown`, on the channel `class-countdown` at importance LOW with no sound, vibration or badge, flags ongoing + only-alert-once, the class colour, a count-down chronometer and a timeout equal to the time left; its private version reads "RIDE 45" / "Shoreditch · Bike 9" and its PUBLIC version only "Next class" and the time; the shade screenshot shows it under "Silent" counting down; and after the class was marked started the app had no notification left. The six `--play` screenshots were built (1080 × 1920, zero leaks) and looked at, with the icon and the feature graphic. In a desktop browser on the fake server with recording twins: each pass writes the three keys, then `countdown_enabled`, then ONE reload; the app's own Class reminders switch writes '0' and '1'; as `'ios'` the key is never written.
- **Not verified:** nobody has seen the countdown on a phone — [../ios-app/ANDROID.md → "On-device checklist"](../ios-app/ANDROID.md#on-device-checklist), "The class countdown". The release workflow has never run: keytool, jarsigner and the signing itself are unproved until the owner's first run. `npm run typecheck` was not run.
- **Left:** the owner's: the checklist on a phone; privacy.html's ONE placeholder (a name and a contact e-mail address); the developer account, the upload key and the four secrets (PLAY_STORE_DEPLOY.md) — and a Play submission still waits for the target API level. Choices made on the owner's behalf: the end of [decisions.md](decisions.md).

### 2026-09-20 — the Android widget, "level 3": compiled, tested and drawn by CI; not yet on a home screen
- **State:** `main` at "Android widget, level 3: the next class on the home screen"; pushed yes — it went to `main` at the owner's request once CI had compiled, tested and drawn it (hard rule 6); branch ahead of `origin/main` by 0. Check it: the command under "State of play".
- **Changed:** ONE home-screen widget, "Next class", in the look of the iPhone widget: a classic App Widget (`NextClassWidgetProvider`, RemoteViews, XML layouts, plain Java, no new runtime dependency) fed by the SAME snapshot through native TWINS of three iPhone plugins — `AppGroupPreferences`, `WidgetCenter`, `PsycleDeepLink`, local Java classes under the same JS names — so the bridge's diff is comments only; a pure snapshot class (`PsyncSnapshot`, `PsyncWidgetPlan`) with 45 JVM tests; one RemoteViews builder shared with a debug-only preview activity; one inexact repaint alarm; the tap an explicit intent with an untrusted id. Web: the Android welcome's last page reads "with a widget and reminders", and the waitlist "You're in" notice is one sentence again. CI: `android-build` runs the JVM tests first, blocking; `android-smoke` photographs the preview in nine states, on a phone-sized emulator. Then a review round, staged on top: the alarm's instant moved into the pure class and gained MIDNIGHT ("Tomorrow 07:30" was wrong from 00:00) and is armed in a `finally`; the plan became a ladder of row SUMS that counts the pictogram tile and the font scale; one-line text ends in an ellipsis, seats a badge has no room for are counted ("4 bikes"), a one-line name prints its head; `widget_next_class` alone is read only when the list is empty (the iPhone's rule); the `initialLayout` claims nothing; every cold start asks for a repaint (Clear storage left the last class up); the provider is NOT exported; the preview needs adb's DUMP permission and logs what it cannot draw, which the smoke greps for. 20-android-widget.js is new; 18-android.js and 19-android-project.js grew. What and why: [../IMPROVEMENTS-2026-09.md → "Follow-up: the Android widget"](../IMPROVEMENTS-2026-09.md#follow-up-the-android-widget); the owner's words → [decisions.md](decisions.md) section 8.
- **Verified:** `npm run ci` (8,388 → 8,608 checks), `npm run agents:check`; the bridge booted as `'android'` WITH the twins (three keys then one reload per pass, a retained tap and a hostile one, sign-out against expiry), the iPhone path by recorded plugin calls (`IPHONE_LAUNCH_DIGEST` unchanged); the Java and XML as far as they can be READ (19-android-project.js); the two PURE classes' 45 tests failing as they should with three of the fixes taken out. Then the real thing: compiled by CI's `android-build` job, which first ran its 45 JVM tests against the real org.json (all pass), and drawn on an emulator by `android-smoke`: the debug preview rendered the widget's own RemoteViews in nine states — compact and wide, day and night, intensity off, a narrow and the smallest box, large type, four seats, empty — with no crash and no preview error in the log, and every one was looked at: the time leads, the card wears the class tint, the pictogram tile and the seat badge are drawn in the class colour, night picks the dark literals, large type keeps every row whole (a long class name ends in an ellipsis), four seats read "4 bikes", and the empty card says "Nothing booked" and names nobody. In a desktop browser on the fake server with recording twins: one booking (one POST) writes the three keys with a correct entry and reloads; a widget tap opens My Bookings and that class's sheet, a hostile link opens My Bookings only; a deliberate sign-out rewrites the snapshot to empty; an APK without the twins stays silent; as `'ios'` nothing changes. package.json's dependencies, the lockfile, all Swift and ios-app/ios are untouched.
- **Not verified:** nobody has put it on a home screen. Launcher or phone only: a local plugin appearing as `Capacitor.Plugins.<name>`, the picker, which layout a launcher's sizes pick, the unexported provider painting at all, the roll-over and midnight alarm, the card before its first paint and after Clear storage, a one-row card and a large font, the tap cold and warm, sign-out — [../ios-app/ANDROID.md → "On-device checklist"](../ios-app/ANDROID.md#on-device-checklist) → "The widget", none ticked ([learnings.md](learnings.md) G9, G11, G12).
- **Left:** the owner adds it on a phone (long-press the home screen → Widgets → Psync) and works through "The widget" in the checklist. Not built with it: a countdown notification in place of the Live Activity — the owner then asked for it (the entry above). Three more choices made on the owner's behalf are at the end of [decisions.md](decisions.md).

### 2026-09-20 — an Android app, "level 2": compiled by CI and launched on an emulator, not yet seen on a phone
- **State:** `main` at "Android: say what CI and the emulator proved" (the app itself is the commit before it, "Android app, level 2: …"); pushed yes — both went to `main` at the owner's request once CI had compiled it (hard rule 6); branch ahead of `origin/main` by 0. Check it: the command under "State of play".
- **Changed:** ios-app/android/ customised (Back in `MainActivity`, backups and cleartext off, calendar permissions, themes and bar colours, vector icons and launch screen, release signing from the environment); the bridge and web layer made platform-aware (`IS_ANDROID`: channels, status-bar colour, calendar shapes; `window._psycleAndroidBack`; share as text; no service worker; Android copy); two CI jobs (`android-build`, advisory `android-smoke`), the bootstrap workflow deleted; 18-android.js, 19-android-project.js; these docs. Then a review round, staged on top: Back answers a confirm BEFORE any busy mark (`bookClass` keeps its button busy under its own confirm), counts a spot swap and a picker cancel as busy, ends a wait when the waiting ends, is the welcome's own Back, and hands focus back; a forged notification tap is refused; portrait only; the navigation bar follows a light / dark flip; `*.p12` / `*.pfx` ignored; 19-android-project.js reads both backup rule files. What and why: [../IMPROVEMENTS-2026-09.md → "Follow-up: an Android app"](../IMPROVEMENTS-2026-09.md#follow-up-an-android-app); the owner's words → [decisions.md](decisions.md) section 8.
- **Verified:** `npm run ci` (8,061 → 8,382 checks; each check of the review round seen to fail without its fix), `npm run agents:check`; the web layer in desktop Chromium on tests/tools/fake-psycle.js with a fake Capacitor answering `'android'` (Back through every layer, over "Book this class?" on a busy button, through a whole spot swap, on the welcome, with focus; `H.leaked` and `H.liveHits()` empty; every write counted, on the FAKE server); the iPhone path by recorded plugin calls, identical to the commit before (`IPHONE_LAUNCH_DIGEST`). Then the real thing: compiled by CI's `android-build` job and run on an emulator by `android-smoke`: it launches into the first-run welcome, one Back key skips the welcome and leaves the app alive on Discover, the log shows no crash or script error, and natively the bridge created its two channels, set the status-bar colour and read its mirrored keys through Preferences (the emulator is 320 dp wide, which is how the welcome's time tile was found cut and fixed; a 360 dp pass was made in a desktop browser). package.json's dependencies, the lockfile and all Swift are untouched.
- **Not verified:** nothing has been seen on a PHONE, and the emulator run never taps: no sign-in, no booking, no notification firing, no calendar write, no share sheet, no storage restore after a purge, release signing has never met a keystore — "compiled, not seen on a device" ([learnings.md](learnings.md) G9). `npm run typecheck` was not run. Everything a phone must show: [../ios-app/ANDROID.md → "On-device checklist"](../ios-app/ANDROID.md#on-device-checklist), none ticked.
- **Left:** the owner installs the `psync-debug-apk` artifact of the latest `main` run on a phone and works through the checklist ([../ios-app/ANDROID.md](../ios-app/ANDROID.md)). A Play submission is blocked on the target API level until the Capacitor upgrade; Level 3 (a widget) was not requested ([backlog.md](backlog.md)). Six choices made on the owner's behalf are at the end of [decisions.md](decisions.md).

### 2026-09-20 — the small leftovers closed out; documentation for whoever comes next
- **State:** `main` at "Add folder guides, a handover and a backlog: a warm start for whoever comes next"; pushed yes — it and the close-out before it went to `main` together, at the owner's request (hard rule 6); branch ahead of `origin/main` by 0. Check it: the command under "State of play".
- **Changed:** "Close out the small leftovers: what was already gone, what is fixed, what remains" — product code; what and why: [../IMPROVEMENTS-2026-09.md → "Follow-up: the small leftovers"](../IMPROVEMENTS-2026-09.md#follow-up-the-small-leftovers). Then documentation and its guards only: a folder guide in js/, css/, tests/, ios-app/ and ios-app/ios/App/, this file, [backlog.md](backlog.md), 16-agents-docs.js; tools/check-split.mjs tells a line edited since the split from a lost one.
- **Verified:** the close-out by 17-leftovers.js (each check seen to fail without its fix) and in a real browser on tests/tools/fake-psycle.js with every write counted, with real key presses for both date-picker focus fixes (`calStep`, `pickCalDate`). The documentation: `npm run ci`, `npm run agents:check`, `node agents/tools/check-split.mjs` (0 missing, 0 duplicated).
- **Not verified:** the seat map's scroll shades in the iOS web view; everything on the two device checklists. The `Xcode Cloud` check on this push had not been read when this was written: read it ([../ios-app/CICD.md](../ios-app/CICD.md) → "Notes / gotchas").
- **Left:** nothing half-made. Living list: [../IMPROVEMENTS-2026-09.md → "Known small leftovers"](../IMPROVEMENTS-2026-09.md#known-small-leftovers). Candidate work, none of it requested: [backlog.md](backlog.md).

## State of play

- **What `main` holds — check, do not trust this line:** `git fetch && git status -sb && git log --oneline origin/main..HEAD` (and `git stash list`, `git worktree list`). No output and nothing staged = everything is on `main`. Anything else is unshipped work: report it, and do not commit or push it (hard rule 6). This file is written just before its own commit, so a commit or push it describes may not have happened: the newest entry's **State** line says what was so then.
- The September 2026 improvement programme is finished; its audit trail is [../IMPROVEMENTS-2026-09.md](../IMPROVEMENTS-2026-09.md).
- **How it ships:** every push to `main` is archived by Xcode Cloud and uploaded to TestFlight, and the web app is published from `main` by a GitHub Actions deploy that is not defined in this repository — two independent legs ([decisions.md](decisions.md#7-release-and-process) section 7; [../ios-app/CICD.md](../ios-app/CICD.md)).
- **Nothing is half-made:** no pending migration, no open question blocking work. The iOS wrapper is still Capacitor 6 and needs iOS 15 or later; the 6 → 8 upgrade is written up and has not been run.
- **There is an Android app, proved as far as CI can prove it:** "level 2" (everything but the widgets, the Live Activity and Siri) around the same ios-app/www/ and the same bridge, with NO change to the iPhone build; then "level 3", one home-screen widget; then, staged and not yet compiled, a class countdown — ONE silent notification in the Live Activity's place. Written without a compiler: CI's `android-build` job is its only one (it compiles, runs the widget's JVM tests, and `android-smoke` launches the app and draws the widget's preview on an emulator), and nothing has run on a phone ([architecture/android.md](architecture/android.md); the owner's guide is [../ios-app/ANDROID.md](../ios-app/ANDROID.md)). It ships nowhere: CI makes a debug APK to sideload, and a release is by hand — the owner's runbook is [../ios-app/PLAY_STORE_DEPLOY.md](../ios-app/PLAY_STORE_DEPLOY.md), and a MANUAL workflow can build the signed bundle once the owner has created its four secrets (it uploads nothing).
- **Checks:** 126 DOM-free checks before the programme, about 8,500 now. CI is the syntax check, the unit suites, the www/ drift check and the plugin patch check, plus advisory smoke, typecheck and index-freshness steps, an unsigned iOS compile, the Android project's JVM unit tests and debug compile, and an advisory emulator smoke ([architecture/testing-and-ci.md](architecture/testing-and-ci.md)).

## Verified / not verified

| Verified — and how | NOT verified — needs a phone or the real service |
|---|---|
| Every web-layer change of the programme, the close-out of the small leftovers included: the unit suites (run as `TZ=America/New_York`), then a real browser against a stubbed API with every write counted — stubs made by hand at first, later tests/tools/fake-psycle.js (`H.writes` as expected; `H.leaked` and `H.liveHits()` empty). | [The on-device checklist](../IMPROVEMENTS-2026-09.md#on-device-checklist--still-owed): widgets, the Live Activity, notifications (the Monday 12:00 reminder and its tap included), the launch screen, safe areas, status bar, share sheet, the calendar hand-over. None of it has run on a phone. |
| Native changes: a full App-scheme simulator build and launch, unsigned; `sh ios-app/native-checks/run.sh` (the previous build's payloads decode; `18:30` prints and parses under a 12-hour phone setting). | Anything behind entitlements — a widget reading the App Group, a Live Activity on a Lock Screen: the [device checklist](../ios-app/NATIVE_FEATURES.md#device-checklist--crisp-colour-widgets-2026-09-19) in ios-app/NATIVE_FEATURES.md. An unsigned build proves the code compiles, no more ([learnings.md](learnings.md) G7). |
| The Android work: compiled by CI's `android-build` job and run on an emulator by `android-smoke`: it launches into the first-run welcome, one Back key skips the welcome and leaves the app alive on Discover, the log shows no crash or script error, and natively the bridge created its two channels, set the status-bar colour and read its mirrored keys through Preferences; and, without a compiler: the web layer and the bridge booted as `'android'` (18-android.js; then desktop Chromium on the fake server with a fake Capacitor); the iPhone path's plugin calls unchanged to the byte (`IPHONE_LAUNCH_DIGEST`); the Gradle project as far as it can be READ — manifest, `MainActivity`, every res/ XML and reference, no signing material (19-android-project.js). The unmodified Capacitor template compiled once on a GitHub runner. | Everything else about Android: [../ios-app/ANDROID.md → "On-device checklist"](../ios-app/ANDROID.md#on-device-checklist). Nothing has run on a PHONE, the emulator run never taps, and release signing has never met a keystore: Back's round trip, the channels and how late a reminder fires, the calendar shapes read off the plugin's Kotlin, the bar colours, the themed icon, the launch screen, the storage restore, sign-in from `https://localhost`. |
| The Android widget: compiled by CI's `android-build` job, which first ran its 45 JVM tests against the real org.json (all pass), and drawn on an emulator by `android-smoke`: the debug preview rendered the widget's own RemoteViews in nine states — compact and wide, day and night, intensity off, a narrow and the smallest box, large type, four seats, empty — with no crash and no preview error in the log, and every one was looked at: the time leads, the card wears the class tint, the pictogram tile and the seat badge are drawn in the class colour, night picks the dark literals, large type keeps every row whole (a long class name ends in an ellipsis), four seats read "4 bikes", and the empty card says "Nothing booked" and names nobody. By reading: what the bridge writes through the twins, one reload per pass, a retained tap and a hostile one, sign-out against expiry, the iPhone digest unchanged (20-android-widget.js); one receiver and its provider-info, RemoteViews-safe layouts, a pictogram per class type, immutable PendingIntents, an inexact alarm armed in a `finally` for an instant the pure class names, the plan's constants against the XML, one-line styles that end in an ellipsis, an explicit tap, the twins registered before `super.onCreate`, no `android.*` in the pure class, the preview in debug builds only and behind DUMP (19-android-project.js). | The Android widget on a real launcher — nobody has put it on a home screen: the "The widget" block of [../ios-app/ANDROID.md → "On-device checklist"](../ios-app/ANDROID.md#on-device-checklist). |
| The Android class countdown: compiled by CI's `android-build` job, which first ran all 81 JVM tests against the real org.json (36 of them the countdown's; all pass), and POSTED on an emulator by `android-smoke`: Android's own record (`dumpsys notification`) shows one notification, id 7090, tag `psync-countdown`, on the channel `class-countdown` at importance LOW with no sound, vibration or badge, flags ongoing + only-alert-once, the class colour, a count-down chronometer and a timeout equal to the time left; its private version reads "RIDE 45" / "Shoreditch · Bike 9" and its PUBLIC version only "Next class" and the time; the shade screenshot shows it under "Silent" counting down; and after the class was marked started the app had no notification left. And, without a compiler: the bridge's half booted as `'android'` with recording twins — `countdown_enabled` after the three keys and before the ONE reload, '0' with Class reminders off, a flip that goes out by itself, an older app served without a word, never a call on the iPhone (21-android-countdown.js; `IPHONE_LAUNCH_DIGEST` unchanged); the Java and both manifests as far as they can be READ — a pure planner, ONE channel at IMPORTANCE_LOW, the builder calls and their `SDK_INT` tests, two unexported receivers, a ten-minute alarm window, no new permission, ONE tap builder, the debug hook held to ci.yml's three broadcasts, the verdict's patterns run against a record written the way the platform prints one (19-android-project.js); the release workflow, privacy.html, the runbook and the Play graphics as far as they can be READ (22-play-release.js). | The countdown COMPILED: `android-build` has not run on it (Gradle's javac against the real android.jar, the manifest merger, its 36 JVM tests against the real org.json, the step that proves they ran); `android-smoke` has not posted one, and On a phone: everything in the "The class countdown" block of [../ios-app/ANDROID.md → "On-device checklist"](../ios-app/ANDROID.md#on-device-checklist). The release workflow has never run (keytool, jarsigner, the signing itself); nothing was checked against a live Play Console; privacy.html has been opened from file:// only, and its wording is unreviewed by a lawyer. |
| No write has ever been sent to `psycle.codexfit.com` by a test, a script or a browser check. | A first real "Review and book" run: that exactly the spots shown are the spots booked, that a two-seat row books two, that "Stop after this class" stops. |
| The Swift copies of the fallback palette, class-type words, pictogram numbers and chrome colours match the web originals (11-native-snapshot.js). | A first real booking at a studio with no seat map: the count body of `POST /bookings` has never met the live API — from the usual-week sheet or from Discover. |
| The `Xcode Cloud` archive of the agents/ commit succeeded (read on 2026-09-20). It holds everything before the close-out, including the push of 19 September 2026 that got no check of its own ([../ios-app/CICD.md](../ios-app/CICD.md) → "Notes / gotchas"). | The `Xcode Cloud` check of the close-out and of this documentation. A commit that is not yet on `origin/main` HAS no check: run `git branch -r --contains <sha>` first — [playbooks.md](playbooks.md) P10 step 4's "no check" diagnosis applies only to a pushed commit. And that any archived build launches: a green archive proves nothing about launch ([learnings.md](learnings.md) G5). |
| | The seat map's scroll shades in the iOS web view and at a real wide studio: seen only in desktop Chrome at 375px, in all five themes, with a made-up 40-seat layout. They are on the on-device checklist. |
| | The booking-horizon model: observed once, on 19 September 2026, from public timetable occupancy. Advisory only — it may suggest and explain, never block ([architecture/monday-release.md](architecture/monday-release.md)). |
| | The first-run welcome's hold at launch while iOS restores storage ([architecture/ios.md](architecture/ios.md) → "First-run welcome at launch"). It is on no written checklist yet. |
| | **Not run at all:** the Capacitor 6 → 8 upgrade ([../ios-app/UPGRADE-CAPACITOR-8.md](../ios-app/UPGRADE-CAPACITOR-8.md)). It needs the public npm registry and a phone; the calendar plugin's call shapes change between the majors and fail silently. |

## In the owner's hands

Not yours to do; offer help, do not start: the rows of [backlog.md](backlog.md) section 1; committing or pushing anything you find staged or ahead of `origin/main` (a push is a TestFlight archive and a web publish); anything in App Store Connect or Xcode Cloud — no keys for it exist in the repository, by design; and the Android release: the developer account, the keystore, the four repository secrets and every run of the release workflow, Play Console, Psycle's permission, the name and e-mail address in privacy.html ([../ios-app/PLAY_STORE_DEPLOY.md](../ios-app/PLAY_STORE_DEPLOY.md)) — and every box of the Android on-device checklist.

## Your first hour

1. Where the tree really is: the commands of "State of play".
2. Read in the order of [README.md](README.md) → "The first five minutes". Then prove your setup:
3. `npm run ci` — nothing to install, no network. Expect `[check] OK`, `… passed, 0 failed`, `www/ is in sync`.
4. Serve the repository and run the smoke page: `python3 -m http.server 8080 --bind 127.0.0.1`, open `http://127.0.0.1:8080/tests/smoke.html`; the title must read `SMOKE: PASS`.
5. Boot the fake server once: [playbooks.md](playbooks.md#p8-verify-a-change-in-a-real-browser-on-the-fake-server) P8, steps 1–5. Book one class and read `H.writes`: exactly one `POST /bookings`, with the slots you picked.

Working agreement: [playbooks.md](playbooks.md#p0-every-change-the-loop) P0, [decisions.md](decisions.md#7-release-and-process) section 7. What the owner decided goes into decisions.md in their words.

## How we got here

The September 2026 programme in order — each line points at the file that describes the RESULT. The commit subjects in `git log` tell the same story; [../IMPROVEMENTS-2026-09.md](../IMPROVEMENTS-2026-09.md) has the reasons. Before it: a full-app review in July 2026 ([../REVIEW.md](../REVIEW.md)) and the August move of waitlists onto Psycle's own `/waitlists` API.

1. Booking, cancel and waitlist integrity: verified bookings, a `POST /bookings` never re-sent, seat-scoped cancel ids → [architecture/booking.md](architecture/booking.md), [architecture/waitlist.md](architecture/waitlist.md).
2. Reliability: retry rules, the offline queue, saved copies → [architecture/offline.md](architecture/offline.md).
3. Sessions and per-account data → [architecture/session-and-accounts.md](architecture/session-and-accounts.md), [architecture/storage-keys.md](architecture/storage-keys.md).
4. Accessibility plumbing: overlays, focus, announcements → [architecture/accessibility.md](architecture/accessibility.md).
5. Discover: live filters, the window cache, the Filters bar; then one day at a time, with a day strip and a swipe → [architecture/discover.md](architecture/discover.md), [architecture/day-pager.md](architecture/day-pager.md).
6. Stats as three sub-pages; a full-screen first-run welcome; leaner copy → [architecture/stats.md](architecture/stats.md), [architecture/welcome.md](architecture/welcome.md).
7. Three shipping failures and their fixes: the deployment-target floor, the UIScene life cycle, Lock Screen widget text cut off → [architecture/ios.md](architecture/ios.md) ([learnings.md](learnings.md) G3–G5; [../ios-app/CICD.md](../ios-app/CICD.md) → "Notes / gotchas").
8. The "Crisp Colour" redesign: one class card, class-type colours the member can tune, two typefaces → [architecture/design-system.md](architecture/design-system.md), [architecture/class-card.md](architecture/class-card.md), [architecture/class-type-colours.md](architecture/class-type-colours.md).
9. 24-hour times, a quieter clash line, Linen and Synthwave removed (five themes remain), a desktop layout → [architecture/time.md](architecture/time.md), [architecture/theming.md](architecture/theming.md).
10. The "Engraved Halves" mark, app icon, launch screen, App Store screenshots, and the tooling that rebuilds them → [decisions.md](decisions.md) section 3, [playbooks.md](playbooks.md) P7 step 5 and P9.
11. Widgets and the Live Activity in the new look, with one fixed-format date parser → [architecture/ios.md](architecture/ios.md) ([learnings.md](learnings.md) G1, G2).
12. Two small guards: a safe-area inset lost to a padding shorthand (13-safe-area.js; [learnings.md](learnings.md) D3) and an array-shaped profile body (`_profileFrom`; [learnings.md](learnings.md) A9).
13. "Your usual week" redone: review first, seats per class, suggested spots with a choose-only seat map, up to three weeks ahead, a card that folds, the Monday 12:00 reminder → [architecture/usual-week.md](architecture/usual-week.md), [architecture/monday-release.md](architecture/monday-release.md).
14. The small leftovers closed out: focus in the date picker, scroll shades on a wide seat map, the seat word in the offline-booking dialog (17-leftovers.js, offline-queue.js) → [architecture/sheets-and-dialogs.md](architecture/sheets-and-dialogs.md), [architecture/offline.md](architecture/offline.md).
15. These documents: the entry, this folder, the generated index, the folder guides → [README.md](README.md).
16. An Android app at "level 2", around the same www/ and bridge, with the iPhone build untouched; written by reading, compiled only by CI → [architecture/android.md](architecture/android.md) ([learnings.md](learnings.md) G9, G10; [../ios-app/ANDROID.md](../ios-app/ANDROID.md)).
17. "Level 3": a home-screen widget for it, fed by the iPhone widget's snapshot through native twins of three plugins → [architecture/android.md](architecture/android.md) → "The home-screen widget" ([learnings.md](learnings.md) G11, G12).
18. A class countdown for it — one silent notification in the Live Activity's place, planned natively from the same snapshot — and the road to Google Play written down, with a manual workflow that builds the signed bundle and uploads nothing → [architecture/android.md](architecture/android.md) → "The class countdown", "Release signing, the target API" ([learnings.md](learnings.md) G14; [../ios-app/PLAY_STORE_DEPLOY.md](../ios-app/PLAY_STORE_DEPLOY.md)).
