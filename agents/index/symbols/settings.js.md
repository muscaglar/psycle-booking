# js/settings.js — symbols
Purpose: the shape of `js/settings.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

2185 lines · 103 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.

L20  var TIER_KEY
L21  var BIKE_PREF_KEY
L22  var TIERS

### Data Persistence (L24)
L30  _store(key, value)  — Reads are coerced (app.js, pure:stored-data — both keys can come out of an imported file); writes go through…
L35  loadTiers()
L41  saveTiers(t)
L43  loadBikePrefs()
L49  saveBikePrefs(p)

### Public API (L51)
L53  window.getInstructorTier(instrId)
L57  window.getBikePrefs(studioId)
L62  window.tierBadgeHTML(instrId)

## Floating Next Class Pill (L71)
L75  var _pillEl
L76  var _pillTimer
L78  createPill()
L110  _pillA11y(label)  — The pill is a div that acts as a button (it holds div children, so it stays a div).
L130  updatePill()  +84
L215  startPillTimer()

### The pill steps aside (L254)
L265  var PILL_TUCK_AFTER  {pill-scroll}
L266  var PILL_SHOW_AFTER  {pill-scroll}
L267  var PILL_EDGE  {pill-scroll}
L272  _pillScrollStep(prev, f)  {pill-scroll}  — One scroll position in, the pill's next state out.
L302  _pillCalledBack(s)  {pill-scroll}  — A state in, "did the member just call it back?" out: mid-list, a run UP of PILL_SHOW_AFTER — the run that un-tucks it,…

### At rest it never sits on a Book button (L307)
L322  var PILL_REST_MS  {pill-rest}
L323  var PILL_REST_AGAIN_MS  {pill-rest}
L325  var PILL_OVERLAP_MIN  {pill-rest}
L333  _pillCoversBook(box, rects)  {pill-rest}  — box: where the pill rests; rects: the Book buttons' boxes — { left, top, right, bottom }, same space.
L350  _pillRestApplies(tuck, f)  {pill-rest}  — Is the question asked at all?
L361  _pillRestStep(s, covers)  {pill-rest}  — One look in, the next state out.
L372  _pillBox()  — Where the pill RESTS, whatever is drawn right now (tucked it is slid down, hovered it is scaled): its layout size…
L382  _pillBookRects()  — The card's ONE pill, on both tabs (Book / Join waitlist, Cancel booking / Leave waitlist).
L392  _pillKeyFocused()  — Focus a KEYBOARD put there (a tap focuses the pill too, and must not pin it over a button for as long as nothing else…
L397  _pillRestCheck()
L410  var _pillTuck
L411  var _pillRest
L412  var _pillRestClear
L413  var _pillRestTimer
L414  var _pillScrollSrc
L415  var _pillScrollQueued
L422  _pillDialogUp()  — A dialog, sheet or panel is up.
L438  _pillDraw()  — Out of the way for either reason: on the way down a list, or at rest on a Book button.
L446  _pillRestQueue(ms)  — "Ask again once things have settled" — every caller re-arms the one timer, so a run of scroll frames or of DOM writes…
L454  _pillRendered()  — The page was written to (a search streams its studios in, one render after another): what the last look saw is gone,…
L462  _pillOnScroll()  — Once per frame at most.
L486  _pillScrollEvent(e)  — Capture: `scroll` does not bubble, and .tab-content (the scroller at <=640px) is built by tabs.js.
L498  _pillUntuck()  — Focus landed on it (Tab, a screen reader): it has to be on screen.

## Settings Panel (L505)
L511  var SETTINGS_SECTION_IDS  — The Membership rows each open the panel AT their own section; all four used to land at the top of the same sheet.
L522  scrollSettingsTo(section)  — Bring one section to the top of the panel's own scroller.
L534  window.openSettings(section)  +89  — `section` is optional ('reminders' | 'bike' | 'calendar' | 'data').
L624  window.closeSettings()

## Instructor Tier UI (L632)
L636  window.filterTierList()
L638  tierRowHTML(instr, tiers, favs)
L657  renderTierList()  +83
L741  window.toggleFavFromSettings(instrId)
L756  window.setInstructorTier(instrId, tier)

## Calendar Sync UI (iOS only) (L772)
L777  async renderCalendarSync()  +48  {calendar-sync}
L826  async window.onCalendarSyncToggle(checkbox)  {calendar-sync}
L840  async _confirmCalendarOwnership(calId, name, cancelText, lead)  +30  {calendar-sync}  — The ownership dialog.
L874  _calSyncProblem(r)  {calendar-sync}  — Why a sync did NOT happen, in the member's words — '' when it really ran.
L882  async window.onCalendarTargetChange(select)  +39  {calendar-sync}
L922  async window.onCalendarCleanupDupes(btn)  +42  {calendar-sync}
L965  async window.onCalendarResync(btn)  +41  {calendar-sync}

## Bike Preference UI (L1009)
L1013  async populateStudioSelect()  +61
L1075  window.renderBikePrefGrid()  +62
L1138  window.toggleBikePref(studioId, slotId)

## Integration: Bike Picker Highlights (L1167)
L1171  var _origShowBikePicker

## Integration: Tier Badges on Class Cards (L1235)
L1243  window.tierBadgeHTML(instructorId)  — app.js (eventCard / renderMyBookings) calls this hook directly next to the instructor name.

## Export / Import Settings (L1254)
L1258  var EXPORT_KEYS
L1270  window.exportSettings()  +35
L1312  _shareSettingsExport(json, fileName)  +33  {settings-export}  — Native export.
L1362  var IMPORT_MAX_FILE  {import-validate}  — What an import file may do.
L1363  var IMPORT_MAX_VALUE  {import-validate}
L1369  _planSettingsImport(data, deviceGet, opts)  +158  {import-validate}  — data: the parsed file.
L1529  _importSummary(added)  {import-validate}  — "120 classes, 4 rankings and 2 favourites" — what the plan adds, in words.
L1548  _applySettingsImport(plan)  — Writes an accepted plan, says what changed, reloads (the app reads most of these keys once, at launch).
L1566  window.importSettings(input)  +53

## Bug Report (L1621)

### Build id (window.APP_VERSION) (L1626)
L1634  var BUILD_ID_RE  {build-id}  — Nothing stamps a version into the page, so every report said "App version: unknown".
L1635  var _appVersionPending  {build-id}
L1642  var _swSwappedSinceLoad  {build-id}  — A worker that takes over mid-session deletes the old cache while the page keeps running the previous build's scripts,…
L1650  _buildIdsFromCaches()  {build-id}
L1660  _buildIdFromSwFile()  {build-id}  — Same-origin file (the app bundle on iOS).
L1673  resolveAppVersion()  {build-id}  — Resolves to the build id (also left on window.APP_VERSION), or null.
L1696  window.getAppVersion = resolveAppVersion  {build-id}
L1699  buildBugReport()  +51
L1751  async window.downloadBugReport()  +38
L1790  window.copyBugReport()
L1812  _fallbackCopy(text, status, what)  +30  — Returns whether the text really reached the clipboard, so a caller with its own status line (copyDiagnostics) never…

## Diagnostics Panel (L1844)
L1858  var DIAG_MAX_LOG_ROWS
L1860  esc(v)
L1867  diagHasToken()  — Boolean-only check for whether an auth token is present.
L1877  diagGetDiagnostics()  — Pull the diagnostics object from PsycleDiag, fully guarded.
L1888  diagRecentLog(getterName, key)  — Last N entries (newest first) from a log getter that returns [{timestamp, <key>}].
L1912  diagApiHealthHTML(diag)  +52  — Build the API-health section HTML from a diagnostics object (may be null).
L1966  diagEnvironmentHTML(diag)  — Build the environment section HTML.
L1998  var _diagReportText  — The iOS bridge's getDiagnosticReport() is async, but "Copy diagnostics" has to stay synchronous: awaiting inside the…
L1999  var _diagReportSeq
L2001  diagPrefetchReport()
L2023  diagBuildCopyBlob(diag)  +47  — Assemble the JSON blob copied by "Copy diagnostics".
L2071  window.openDiagnostics()  +60
L2132  window.closeDiagnostics()
L2137  diagStatus(msg, ok)
L2146  copyDiagnostics()
L2161  copyDiagnosticsFallback(blob)  — The status line used to say "copied" whatever the fallback did.
L2166  clearDiagnosticLogs()
