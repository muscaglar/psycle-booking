# ios-app/www/native-bridge.js — symbols
Purpose: the shape of `ios-app/www/native-bridge.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `ebc2926`). Do not edit; rebuild with `npm run agents:index`.

2653 lines · 171 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## Native Bridge — Capacitor integration layer (L1)

### Gym Time (Europe/London) (L16)
L37  var GYM_TZ
L38  var _gymWallFmt
L48  _gymWallToUtcMs(y, mo, d, h, mi, s)  — Absolute UTC ms for a wall-clock time in the gym's zone (DST-correct, independent of the device timezone).
L79  _classStartMs(startAt)  — Absolute UTC ms of an API class time.
L95  window._psycleClassStartMs = _classStartMs

### Calendar ownership decisions (pure) (L97)
L107  _calForeignEvents(events, calId, markerOf)  — Unmarked events in calendar `calId` — what a full-ownership reconcile would delete that isn't ours.
L114  _calSweepVictimIds(events, calId, ownedAck, markerOf)  — Native ids to clear out of `calId` when it stops being the sync target.
L137  _calAckCovers(stored, calId)  — Does the stored ownership ack hand over calendar `calId`?
L140  window._psycleCalForeignEvents = _calForeignEvents
L141  window._psycleCalSweepVictimIds = _calSweepVictimIds
L142  window._psycleCalAckCovers = _calAckCovers

### Platform (L152)
L170  var PLATFORM  — Both native apps run this file.
L174  var IS_ANDROID
L176  var Preferences
L177  var Haptics
L178  var Share

### Fix Login Flow (L193)
L196  window.openLoginPopup()  — window.open() in WKWebView opens Safari, breaking postMessage.

### Fix window.open calls (L200)
L204  var Browser  — calendar.js uses window.open for Google Calendar and ICS blob.
L205  var _origWindowOpen
L206  window.open(url, target)

### Persistent Storage (L221)
L226  var RETIRED_KEYS
L227  var SYNC_KEYS  +45
L278  async restoreFromNative()  — On startup: restore from native storage to localStorage.
L301  syncToNative(key, value)  — Sync localStorage changes to native storage
L311  var _origSetItem  — Intercept localStorage.setItem and removeItem
L312  var _origRemoveItem

### Native Share (L361)
L364  async window.nativeShare(title, text, url)

### Native Calendar Integration (L375)
L414  var Calendar
L415  var CAL_EVENT_MAP_KEY
L416  var CAL_ID_KEY
L417  var CAL_TARGET_KEY
L418  var CAL_MODE_KEY
L419  var CAL_ENABLED_KEY
L427  var CAL_OWNED_ACK_KEY  — '2:<calendarId>' once the user has confirmed that target under the full-ownership contract — only then may the…
L428  _calIsOwned(calId)
L431  var PSYCLE_CAL_TITLE
L432  var PSYCLE_CAL_COLOR
L433  var PSYCLE_EVENT_MARKER
L435  _loadCalMap()
L438  _saveCalMap(map)
L447  _nativeSlotLabel(typeName)  — Slot label for a class type.

### Permissions (L452)
L454  var _calPermissionGranted
L456  async _ensureCalendarPermission()  +30

### Dedicated Psycle Calendar (L487)
L489  async _listNativeCalendars()
L504  hasChosenCalendar()  — Has the user explicitly chosen a calendar to write into?
L508  async _resolveTargetCalendarId()

### Add / Update / Remove Events (L519)
L521  _buildCalEventData(eventId)  +55

### v6-plugin-correct primitives (L577)
L582  _markerEventId(ev)  — Extract the Psycle event id from a native event's notes, or null.
L589  _nativeStartMs(ev)  — Millisecond start time of a native event, across plugin shapes.
L597  _createdEventId(result)  — The created-event id from a createEvent() result, across versions.
L609  async _deleteNativeEvents(ids)  — Delete native events by id.
L641  var _androidDeletedEventIds  — ANDROID ONLY.
L642  _androidRememberDeleted(ids)
L649  async _listNativeEvents(startMs, endMs)  — Normalized listEventsInRange (ms timestamps in, array out).
L661  calendarSyncEnabled()

### Hook into booking / cancel / seat-cancel flows (L669)
L676  var _origSubmitBookingNative
L687  var _origConfirmUnbookNative
L695  var _origUpcomingCancelNative
L703  var _origCancelBikeSlotNative
L711  var _origUpcomingSeatCancelNative

### Full Calendar Reconcile (L719)
L736  async syncAllBookingsToCalendar()  +118
L856  var _origRenderNative  — Run sync after bookings load
L857  var _calSynced
L873  async window.psycleResyncCalendar(opts)  — Force a fresh sync.
L904  async window.psycleCleanupDuplicates()  — Settings → "Remove duplicates".
L913  window.syncAllBookingsToCalendar = syncAllBookingsToCalendar

### Public API for Settings UI (L915)
L918  async window.psycleListCalendars()  — List iOS calendars the user can choose from.
L936  window.psycleGetCalendarConfig()  — Get the current calendar sync config for the Settings UI.
L956  async window.psycleCountForeignEvents(calId)  — READ-ONLY: how many upcoming events in calendar `calId` are NOT Psycle bookings — what handing that calendar over…
L981  async window.psycleSetCalendarConfig(cfg)  +62  — Update the calendar target.

### Weekly Booking Reminder (L1045)
L1050  var LocalNotifications

### Android: notification channels, the small icon and its tint (L1052)
L1070  var ANDROID_CHANNEL_CLASSES  — Android 8+ files every notification under a CHANNEL, which is what the member sees (and can silence) in the system's…
L1071  var ANDROID_CHANNEL_NEW_DATES
L1072  var ANDROID_NOTIF_ICON
L1076  var ANDROID_NOTIF_TINT  — The mark's ink.
L1077  var _androidChannels
L1079  _ensureAndroidChannels()
L1103  _forAndroid(notification, channelId)  — The Android-only fields of one notification.
L1117  var REMINDER_IDS  — Eight rolling one-shot notifications (the next 8 Mondays).
L1122  var WEEKLY_REMINDER_HOUR, WEEKLY_REMINDER_MINUTE  {weekly-reminder}  — WHEN it fires and WHAT it says.
L1123  var WEEKLY_TEMPLATE_KEY  {weekly-reminder}
L1132  _nextMondaysNoonLondon(count, now, wallToUtcMs)  {weekly-reminder}  — The next `count` occurrences of Monday 12:00 Europe/London as absolute Date instants, DST-correct for any device…
L1150  _hasUsualWeek(raw)  {weekly-reminder}  — Is a usual week saved?
L1160  _weeklyReminderCopy(hasUsualWeek)  {weekly-reminder}  — It fires AT the release, so it says the dates ARE open (and still reads true when the banner is seen later).
L1170  _weeklyTapTab(hasUsualWeek)  {weekly-reminder}  — Where the tap lands at once (the web layer's _onBookingWeekOpened does the rest when it can): the usual-week review…
L1179  _weeklyOfferDecision(f)  {weekly-reminder}  — Put the in-app "remind you on Mondays?" question (_offerWeeklyReminder)?
L1188  _usualWeekSaved()
L1192  var REMINDER_PREF
L1196  var _weeklyArmedWithWeek  — Which body the armed reminders carry (true = "Book your usual week…"), null while nothing is known to be armed — so a…
L1198  async _cancelReminders()
L1212  async scheduleWeeklyReminder(interactive)  +42
L1257  var _weeklyChain  — One pass at a time: each cancels the eight ids and schedules them again, and two interleaved passes could leave the…
L1258  _scheduleWeeklySerial(interactive)
L1267  window._nativeReminder = {…}  — keys: isOn, enable, disable — Exposed to the web layer (Membership tab toggle).
L1292  _rearmWeeklyReminder()  — The body names what the tap opens, so it follows the usual week.

### Notification Action Buttons (Book / Cancel / Snooze) (L1313)
L1325  registerNotificationActions()
L1347  var TAP_ROUTE_WAIT_MS  — Land a tapped notification on what it was about.
L1348  var _cancelTapRoute
L1359  _askDiscoverToRefresh()  — The Monday reminder exists for one moment: the release of new dates.
L1368  _routeNotificationTap(eventId)  +53
L1441  handleNotificationAction(notification)  +36  — Route a tapped notification / action button.
L1496  _parseWidgetLink(url)  {widget-link}  — A Home/Lock Screen widget tap opens psync://bookings?event=<id> (minted in PsycleWidget.swift).
L1515  handleWidgetURL(info)  {widget-link}  — Same landing as a class-reminder tap: My Bookings, then that class's sheet (only while the seat is still held).
L1532  var PsycleDeepLink  — The in-app PsycleDeepLink plugin RETAINS the URL until this listener attaches — a widget tap usually cold-launches the…

### Widget / Live Activity / Siri Snapshot (L1540)
L1575  var WIDGET_APP_GROUP  — App Group container id.
L1577  var WIDGET_NEXT_KEY
L1578  var WIDGET_WEEK_KEY
L1583  var WIDGET_UPCOMING_KEY  — Next few classes (same shape as widget_next_class, array of up to 5) so the widget can build a MULTI-ENTRY timeline…
L1588  _prefSet(key, value)  — Standard Preferences plugin: lands in UserDefaults.standard under "<group>.<key>" (NOT extension-readable — kept for…
L1601  _appGroupSet(key, value)  — App Group mirror: writes the SAME logical value under the BARE key into the shared App Group suite, so a native…
L1610  _writeSnapshotKey(key, value)
L1625  var WIDGET_UPCOMING_MAX  {ios-polish}  — The widget timeline shows the next few classes; reminders cover every class held.
L1626  var CLASS_REMINDER_MAX  {ios-polish}
L1630  _isHeldSeat(booking)  {ios-polish}  — A real seat, as opposed to a waitlist place — true even when the class's details failed to load this pass (that makes…
L1639  _evtFromSavedItem(item)  {ios-polish}  — Stand-in for an _eventCache entry, built from app.js's saved copy of My Bookings (psycle_bookings_snapshot), which…
L1657  _staleReminderIds(map, wanted, keep)  {ios-polish}  — Event ids whose armed reminder should go: the class is no longer wanted, or its time moved (it is re-armed for the new…
L1679  var SNAPSHOT_NEUTRAL_TINT  {native-snapshot}  — The card ground at intensity "off": the app's own neutral surface (Cloud / Graphite, css/theme.css) — the widget's…
L1683  _snapHex(value)  {native-snapshot}  — '#2d5fd6' → '#2D5FD6'; anything that is not six hex digits → null.
L1693  _snapClassTypeKey(typeName, keyFn)  {native-snapshot}  — "RIDE: 45" → 'ride', through the app's OWN classTypeKey (js/app.js) — the same answer data-ct gets.
L1713  _snapClassColourFields(key, engine)  {native-snapshot}  — The colour fields for one class type, read from the app's colour engine (`engine` = window.PsycleClassColours: get() →…
L1746  _savedItemsById()  — app.js's saved copy of My Bookings, by event id ({} when there is none).
L1761  _snapshotEventFor(eventId, known)  +35  — Resolve a display-ready event object from the cache, or null.
L1808  var _snapServerConfirmed  — Only trust an EMPTY bookings map after the server confirmed it (bookings:loaded fired this session).
L1810  var _signOutPass  — True only while the clearToken wrapper (a deliberate sign-out) runs its pass.
L1813  var _snapColourSig  — The class colours the last WRITTEN snapshot carries (see the 'classcolours:changed' listener below), and that…
L1814  var _colourSnapTimer
L1815  _classColourSig()
L1824  updateWidgetSnapshot()  +128
L1952  window.updateWidgetSnapshot = updateWidgetSnapshot

### Class-start reminders (T-90 minutes) (L1954)
L1966  var CLASS_REMINDER_PREF
L1967  var CLASS_REMINDER_MAP
L1976  _classReminderBody(c)  — The reminder's body: the instructor, the place and — on the iPhone — what a tap is for.
L1982  _classRemindersEnabled()

### The Android countdown's switch (L1986)
L2001  var COUNTDOWN_ENABLED_KEY  — The Android app has no Live Activity.
L2002  var _countdownSwitchSends
L2003  _androidCountdownSwitch()
L2017  _androidCountdownFlipped(sendsBefore)  — The member flipped Class reminders.
L2028  _loadReminderMap()
L2031  _saveReminderMap(map)
L2035  _reminderIdFor(eventId, map)
L2049  var _reminderChain  — Serialized: overlapping snapshot passes (booking events fire in quick succession) must not interleave the…
L2053  _scheduleClassReminders(upcomingList, unknownSeats)  — `unknownSeats` = {eventId: true} for held seats this pass knew nothing about (see _staleReminderIds) — captured with…
L2060  async _scheduleClassRemindersInner(upcomingList, unknownSeats)  +51
L2113  window._nativeClassReminders = {…}  +33  — keys: isOn, hasPermission, enable, disable — Settings toggle API (rendered by tabs.js next to the weekly reminder).

### First-booking ask (L2147)
L2153  var CLASS_REMINDER_ASKED  — The pref defaults ON, but only the Settings switch ever requested notification permission — and it looked on already,…
L2154  var ASK_POLL_MS
L2155  var ASK_GIVE_UP_MS
L2156  var _reminderAskBusy
L2157  var _reminderAskVoid
L2166  var ASK_BLOCKING_IDS  — Anything the ask would land on top of.
L2171  _askBlocked()
L2180  _askStillWanted()
L2184  async _maybeAskClassReminders()  +56

### Usual-week ask: the Monday reminder, offered in context (L2249)
L2260  var WEEKLY_REMINDER_ASKED  — The weekly reminder defaults OFF and its switch sits in Settings, where nobody looks for it.
L2261  var _weeklyAskBusy
L2262  var _weeklyAskSince
L2264  _weeklyOfferFacts(permission)
L2273  async window._offerWeeklyReminder(reason)  +56
L2339  var _origClearTokenNative  — Deliberate sign-out must blank the snapshot and cancel pending class reminders — otherwise the previous account's…
L2381  var _calReconcileTimer, _calReconcileInFlight, _calReconcilePending  — Auto-reconcile the calendar whenever bookings change — covers classes cancelled on the web / another device and…
L2384  async _runCalSync()  — Single-flight calendar sync.
L2393  _scheduleCalReconcile()
L2401  var _calServerConfirmedEmpty  — Tracks whether the latest bookings state came from a successful server fetch.

### Status Bar (L2424)
L2435  _themeGround(registry, themeId, base)  {android-bridge}  — The ground of the theme in force, for the Android status bar: the `bg` of its APP_THEMES entry (js/theme.js) — the…
L2449  updateStatusBar()  +36
L2487  var observer  — Watch for theme changes

## Analytics / Diagnostics (developer-only, no third-party services) (L2492)
L2496  var ACTION_LOG_NATIVE_KEY
L2497  var MAX_NATIVE_ACTION_LOG
L2501  var _origPushAction  — Persist action log to Capacitor Preferences (survives iOS storage purges).
L2510  _persistActionLogToNative()
L2529  async window.getDiagnosticReport()  +121  — getDiagnosticReport — comprehensive diagnostics string for developer use.
