# ios-app/www/native-bridge.js — symbols
Purpose: the shape of `ios-app/www/native-bridge.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `db0805f`). Do not edit; rebuild with `npm run agents:index`.

2573 lines · 166 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## Native Bridge — Capacitor integration layer (L1)

### Gym Time (Europe/London) (L16)
L37  var GYM_TZ
L38  var _gymWallFmt
L48  _gymWallToUtcMs(y, mo, d, h, mi, s)  — Absolute UTC ms for a wall-clock time in the gym's zone (DST-correct, independent of the device timezone).
L79  _classStartMs(startAt)  — Absolute UTC ms of an API class time.
L95  window._psycleClassStartMs = _classStartMs

### Calendar ownership decisions (pure) (L97)
L107  _calForeignEvents(events, calId, markerOf)  — Unmarked events in calendar `calId` — what a full-ownership reconcile would delete that isn't ours.
L114  _calSweepVictimIds(events, calId, ownedAck, markerOf)  — Native ids to clear out of `calId` when it stops being the sync target.
L137  _calAckCovers(stored, calId)  — Does the stored ownership ack hand over calendar `calId`?
L140  window._psycleCalForeignEvents = _calForeignEvents
L141  window._psycleCalSweepVictimIds = _calSweepVictimIds
L142  window._psycleCalAckCovers = _calAckCovers

### Platform (L152)
L167  var PLATFORM  — Both native apps run this file.
L171  var IS_ANDROID
L173  var Preferences
L174  var Haptics
L175  var Share

### Fix Login Flow (L190)
L193  window.openLoginPopup()  — window.open() in WKWebView opens Safari, breaking postMessage.

### Fix window.open calls (L197)
L201  var Browser  — calendar.js uses window.open for Google Calendar and ICS blob.
L202  var _origWindowOpen
L203  window.open(url, target)

### Persistent Storage (L218)
L223  var SYNC_KEYS  +45
L274  async restoreFromNative()  — On startup: restore from native storage to localStorage.
L294  syncToNative(key, value)  — Sync localStorage changes to native storage
L304  var _origSetItem  — Intercept localStorage.setItem and removeItem
L305  var _origRemoveItem

### Native Share (L354)
L357  async window.nativeShare(title, text, url)

### Native Calendar Integration (L368)
L405  var Calendar
L406  var CAL_EVENT_MAP_KEY
L407  var CAL_ID_KEY
L408  var CAL_TARGET_KEY
L409  var CAL_MODE_KEY
L410  var CAL_ENABLED_KEY
L418  var CAL_OWNED_ACK_KEY  — '2:<calendarId>' once the user has confirmed that target under the full-ownership contract — only then may the…
L419  _calIsOwned(calId)
L422  var PSYCLE_CAL_TITLE
L423  var PSYCLE_CAL_COLOR
L424  var PSYCLE_EVENT_MARKER
L426  _loadCalMap()
L429  _saveCalMap(map)
L438  _nativeSlotLabel(typeName)  — Slot label for a class type.

### Permissions (L443)
L445  var _calPermissionGranted
L447  async _ensureCalendarPermission()

### Dedicated Psycle Calendar (L467)
L469  async _listNativeCalendars()
L484  hasChosenCalendar()  — Has the user explicitly chosen a calendar to write into?
L488  async _resolveTargetCalendarId()

### Add / Update / Remove Events (L499)
L501  _buildCalEventData(eventId)  +55

### v6-plugin-correct primitives (L557)
L562  _markerEventId(ev)  — Extract the Psycle event id from a native event's notes, or null.
L569  _nativeStartMs(ev)  — Millisecond start time of a native event, across plugin shapes.
L577  _createdEventId(result)  — The created-event id from a createEvent() result, across versions.
L589  async _deleteNativeEvents(ids)  — Delete native events by id.
L621  var _androidDeletedEventIds  — ANDROID ONLY.
L622  _androidRememberDeleted(ids)
L629  async _listNativeEvents(startMs, endMs)  — Normalized listEventsInRange (ms timestamps in, array out).
L641  calendarSyncEnabled()

### Hook into booking / cancel / seat-cancel flows (L649)
L656  var _origSubmitBookingNative
L667  var _origConfirmUnbookNative
L675  var _origUpcomingCancelNative
L683  var _origCancelBikeSlotNative
L691  var _origUpcomingSeatCancelNative

### Full Calendar Reconcile (L699)
L716  async syncAllBookingsToCalendar()  +118
L836  var _origRenderNative  — Run sync after bookings load
L837  var _calSynced
L853  async window.psycleResyncCalendar(opts)  — Force a fresh sync.
L884  async window.psycleCleanupDuplicates()  — Settings → "Remove duplicates".
L893  window.syncAllBookingsToCalendar = syncAllBookingsToCalendar

### Public API for Settings UI (L895)
L898  async window.psycleListCalendars()  — List iOS calendars the user can choose from.
L916  window.psycleGetCalendarConfig()  — Get the current calendar sync config for the Settings UI.
L936  async window.psycleCountForeignEvents(calId)  — READ-ONLY: how many upcoming events in calendar `calId` are NOT Psycle bookings — what handing that calendar over…
L961  async window.psycleSetCalendarConfig(cfg)  +62  — Update the calendar target.

### Weekly Booking Reminder (L1025)
L1030  var LocalNotifications

### Android: notification channels, the small icon and its tint (L1032)
L1050  var ANDROID_CHANNEL_CLASSES  — Android 8+ files every notification under a CHANNEL, which is what the member sees (and can silence) in the system's…
L1051  var ANDROID_CHANNEL_NEW_DATES
L1052  var ANDROID_NOTIF_ICON
L1056  var ANDROID_NOTIF_TINT  — The mark's ink.
L1057  var _androidChannels
L1059  _ensureAndroidChannels()
L1083  _forAndroid(notification, channelId)  — The Android-only fields of one notification.
L1097  var REMINDER_IDS  — Eight rolling one-shot notifications (the next 8 Mondays).
L1102  var WEEKLY_REMINDER_HOUR, WEEKLY_REMINDER_MINUTE  {weekly-reminder}  — WHEN it fires and WHAT it says.
L1103  var WEEKLY_TEMPLATE_KEY  {weekly-reminder}
L1112  _nextMondaysNoonLondon(count, now, wallToUtcMs)  {weekly-reminder}  — The next `count` occurrences of Monday 12:00 Europe/London as absolute Date instants, DST-correct for any device…
L1130  _hasUsualWeek(raw)  {weekly-reminder}  — Is a usual week saved?
L1140  _weeklyReminderCopy(hasUsualWeek)  {weekly-reminder}  — It fires AT the release, so it says the dates ARE open (and still reads true when the banner is seen later).
L1150  _weeklyTapTab(hasUsualWeek)  {weekly-reminder}  — Where the tap lands at once (the web layer's _onBookingWeekOpened does the rest when it can): the usual-week review…
L1159  _weeklyOfferDecision(f)  {weekly-reminder}  — Put the in-app "remind you on Mondays?" question (_offerWeeklyReminder)?
L1168  _usualWeekSaved()
L1172  var REMINDER_PREF
L1176  var _weeklyArmedWithWeek  — Which body the armed reminders carry (true = "Book your usual week…"), null while nothing is known to be armed — so a…
L1178  async _cancelReminders()
L1192  async scheduleWeeklyReminder(interactive)  +42
L1237  var _weeklyChain  — One pass at a time: each cancels the eight ids and schedules them again, and two interleaved passes could leave the…
L1238  _scheduleWeeklySerial(interactive)
L1247  window._nativeReminder = {…}  — keys: isOn, enable, disable — Exposed to the web layer (Membership tab toggle).
L1272  _rearmWeeklyReminder()  — The body names what the tap opens, so it follows the usual week.

### Notification Action Buttons (Book / Cancel / Snooze) (L1293)
L1305  registerNotificationActions()
L1327  var TAP_ROUTE_WAIT_MS  — Land a tapped notification on what it was about.
L1328  var _cancelTapRoute
L1339  _askDiscoverToRefresh()  — The Monday reminder exists for one moment: the release of new dates.
L1348  _routeNotificationTap(eventId)  +53
L1421  handleNotificationAction(notification)  +36  — Route a tapped notification / action button.
L1476  _parseWidgetLink(url)  {widget-link}  — A Home/Lock Screen widget tap opens psync://bookings?event=<id> (minted in PsycleWidget.swift).
L1495  handleWidgetURL(info)  {widget-link}  — Same landing as a class-reminder tap: My Bookings, then that class's sheet (only while the seat is still held).
L1512  var PsycleDeepLink  — The in-app PsycleDeepLink plugin RETAINS the URL until this listener attaches — a widget tap usually cold-launches the…

### Widget / Live Activity / Siri Snapshot (L1520)
L1552  var WIDGET_APP_GROUP  — App Group container id.
L1554  var WIDGET_NEXT_KEY
L1555  var WIDGET_WEEK_KEY
L1560  var WIDGET_UPCOMING_KEY  — Next few classes (same shape as widget_next_class, array of up to 5) so the widget can build a MULTI-ENTRY timeline…
L1565  _prefSet(key, value)  — Standard Preferences plugin: lands in UserDefaults.standard under "<group>.<key>" (NOT extension-readable — kept for…
L1578  _appGroupSet(key, value)  — App Group mirror: writes the SAME logical value under the BARE key into the shared App Group suite, so a native…
L1587  _writeSnapshotKey(key, value)
L1602  var WIDGET_UPCOMING_MAX  {ios-polish}  — The widget timeline shows the next few classes; reminders cover every class held.
L1603  var CLASS_REMINDER_MAX  {ios-polish}
L1607  _isHeldSeat(booking)  {ios-polish}  — A real seat, as opposed to a waitlist place — true even when the class's details failed to load this pass (that makes…
L1616  _evtFromSavedItem(item)  {ios-polish}  — Stand-in for an _eventCache entry, built from app.js's saved copy of My Bookings (psycle_bookings_snapshot), which…
L1634  _staleReminderIds(map, wanted, keep)  {ios-polish}  — Event ids whose armed reminder should go: the class is no longer wanted, or its time moved (it is re-armed for the new…
L1656  var SNAPSHOT_NEUTRAL_TINT  {native-snapshot}  — The card ground at intensity "off": the app's own neutral surface (Cloud / Graphite, css/theme.css) — the widget's…
L1660  _snapHex(value)  {native-snapshot}  — '#2d5fd6' → '#2D5FD6'; anything that is not six hex digits → null.
L1670  _snapClassTypeKey(typeName, keyFn)  {native-snapshot}  — "RIDE: 45" → 'ride', through the app's OWN classTypeKey (js/app.js) — the same answer data-ct gets.
L1690  _snapClassColourFields(key, engine)  {native-snapshot}  — The colour fields for one class type, read from the app's colour engine (`engine` = window.PsycleClassColours: get() →…
L1723  _savedItemsById()  — app.js's saved copy of My Bookings, by event id ({} when there is none).
L1738  _snapshotEventFor(eventId, known)  +35  — Resolve a display-ready event object from the cache, or null.
L1785  var _snapServerConfirmed  — Only trust an EMPTY bookings map after the server confirmed it (bookings:loaded fired this session).
L1787  var _signOutPass  — True only while the clearToken wrapper (a deliberate sign-out) runs its pass.
L1790  var _snapColourSig  — The class colours the last WRITTEN snapshot carries (see the 'classcolours:changed' listener below), and that…
L1791  var _colourSnapTimer
L1792  _classColourSig()
L1801  updateWidgetSnapshot()  +122
L1923  window.updateWidgetSnapshot = updateWidgetSnapshot

### Class-start reminders (T-90 minutes) (L1925)
L1937  var CLASS_REMINDER_PREF
L1938  var CLASS_REMINDER_MAP
L1943  _classReminderTail()  — How the reminder's body ends.
L1947  _classRemindersEnabled()
L1951  _loadReminderMap()
L1954  _saveReminderMap(map)
L1958  _reminderIdFor(eventId, map)
L1972  var _reminderChain  — Serialized: overlapping snapshot passes (booking events fire in quick succession) must not interleave the…
L1976  _scheduleClassReminders(upcomingList, unknownSeats)  — `unknownSeats` = {eventId: true} for held seats this pass knew nothing about (see _staleReminderIds) — captured with…
L1983  async _scheduleClassRemindersInner(upcomingList, unknownSeats)  +52
L2037  window._nativeClassReminders = {…}  +30  — keys: isOn, hasPermission, enable, disable — Settings toggle API (rendered by tabs.js next to the weekly reminder).

### First-booking ask (L2068)
L2074  var CLASS_REMINDER_ASKED  — The pref defaults ON, but only the Settings switch ever requested notification permission — and it looked on already,…
L2075  var ASK_POLL_MS
L2076  var ASK_GIVE_UP_MS
L2077  var _reminderAskBusy
L2078  var _reminderAskVoid
L2087  var ASK_BLOCKING_IDS  — Anything the ask would land on top of.
L2092  _askBlocked()
L2101  _askStillWanted()
L2105  async _maybeAskClassReminders()  +55

### Usual-week ask: the Monday reminder, offered in context (L2169)
L2180  var WEEKLY_REMINDER_ASKED  — The weekly reminder defaults OFF and its switch sits in Settings, where nobody looks for it.
L2181  var _weeklyAskBusy
L2182  var _weeklyAskSince
L2184  _weeklyOfferFacts(permission)
L2193  async window._offerWeeklyReminder(reason)  +56
L2259  var _origClearTokenNative  — Deliberate sign-out must blank the snapshot and cancel pending class reminders — otherwise the previous account's…
L2301  var _calReconcileTimer, _calReconcileInFlight, _calReconcilePending  — Auto-reconcile the calendar whenever bookings change — covers classes cancelled on the web / another device and…
L2304  async _runCalSync()  — Single-flight calendar sync.
L2313  _scheduleCalReconcile()
L2321  var _calServerConfirmedEmpty  — Tracks whether the latest bookings state came from a successful server fetch.

### Status Bar (L2344)
L2355  _themeGround(registry, themeId, base)  {android-bridge}  — The ground of the theme in force, for the Android status bar: the `bg` of its APP_THEMES entry (js/theme.js) — the…
L2369  updateStatusBar()  +36
L2407  var observer  — Watch for theme changes

## Analytics / Diagnostics (developer-only, no third-party services) (L2412)
L2416  var ACTION_LOG_NATIVE_KEY
L2417  var MAX_NATIVE_ACTION_LOG
L2421  var _origPushAction  — Persist action log to Capacitor Preferences (survives iOS storage purges).
L2430  _persistActionLogToNative()
L2449  async window.getDiagnosticReport()  +121  — getDiagnosticReport — comprehensive diagnostics string for developer use.
