# js/settings.js — symbols
Purpose: the shape of `js/settings.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `427bc69`). Do not edit; rebuild with `npm run agents:index`.

2228 lines · 108 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.

L20  var TIER_KEY
L21  var BIKE_PREF_KEY
L22  var TIERS

### Data Persistence (L24)
L30  _store(key, value)  — Reads are coerced (app.js, pure:stored-data — both keys can come out of an imported file); writes go through…
L35  loadTiers()
L41  saveTiers(t)
L43  loadBikePrefs()
L49  saveBikePrefs(p)

### Public API (L51)
L53  window.getInstructorTier(instrId)
L57  window.getBikePrefs(studioId)
L62  window.tierBadgeHTML(instrId)

## Floating Next Class Pill (L71)
L75  var _pillEl
L76  var _pillTimer
L78  createPill()
L110  _pillA11y(label)  — The pill is a div that acts as a button (it holds div children, so it stays a div).
L130  updatePill()  +88
L219  startPillTimer()

### The pill steps aside (L258)
L269  var PILL_TUCK_AFTER  {pill-scroll}
L270  var PILL_SHOW_AFTER  {pill-scroll}
L271  var PILL_EDGE  {pill-scroll}
L276  _pillScrollStep(prev, f)  {pill-scroll}  — One scroll position in, the pill's next state out.
L306  _pillCalledBack(s)  {pill-scroll}  — A state in, "did the member just call it back?" out: mid-list, a run UP of PILL_SHOW_AFTER — the run that un-tucks it,…

### At rest it never sits on a Book button (L311)
L326  var PILL_REST_MS  {pill-rest}
L327  var PILL_REST_AGAIN_MS  {pill-rest}
L329  var PILL_OVERLAP_MIN  {pill-rest}
L337  _pillCoversBook(box, rects)  {pill-rest}  — box: where the pill rests; rects: the Book buttons' boxes — { left, top, right, bottom }, same space.
L354  _pillRestApplies(tuck, f)  {pill-rest}  — Is the question asked at all?
L365  _pillRestStep(s, covers)  {pill-rest}  — One look in, the next state out.
L376  _pillBox()  — Where the pill RESTS, whatever is drawn right now (tucked it is slid down, hovered it is scaled): its layout size…
L386  _pillBookRects()  — The card's ONE pill, on both tabs (Book / Join waitlist, Cancel booking / Leave waitlist).
L396  _pillKeyFocused()  — Focus a KEYBOARD put there (a tap focuses the pill too, and must not pin it over a button for as long as nothing else…
L401  _pillRestCheck()
L414  var _pillTuck
L415  var _pillRest
L416  var _pillRestClear
L417  var _pillRestTimer
L418  var _pillScrollSrc
L419  var _pillScrollQueued
L426  _pillDialogUp()  — A dialog, sheet or panel is up.
L442  _pillDraw()  — Out of the way for either reason: on the way down a list, or at rest on a Book button.
L450  _pillRestQueue(ms)  — "Ask again once things have settled" — every caller re-arms the one timer, so a run of scroll frames or of DOM writes…
L458  _pillRendered()  — The page was written to (a search streams its studios in, one render after another): what the last look saw is gone,…
L466  _pillOnScroll()  — Once per frame at most.
L490  _pillScrollEvent(e)  — Capture: `scroll` does not bubble, and .tab-content (the scroller at <=640px) is built by tabs.js.
L502  _pillUntuck()  — Focus landed on it (Tab, a screen reader): it has to be on screen.

## Settings Panel (L509)
L515  var SETTINGS_SECTION_IDS  — The Membership rows each open the panel AT their own section; all four used to land at the top of the same sheet.
L526  scrollSettingsTo(section)  — Bring one section to the top of the panel's own scroller.
L538  window.openSettings(section)  +84  — `section` is optional ('reminders' | 'bike' | 'calendar' | 'data').
L623  window.closeSettings()

## Instructor Tier UI (L631)
L635  window.filterTierList()
L637  tierRowHTML(instr, tiers, favs)
L656  renderTierList()  +83
L740  window.toggleFavFromSettings(instrId)
L755  window.setInstructorTier(instrId, tier)

## Calendar sync UI (the native apps only: iPhone and Android) (L771)
L776  async renderCalendarSync()  +48  {calendar-sync}
L825  async window.onCalendarSyncToggle(checkbox)  {calendar-sync}
L839  async _confirmCalendarOwnership(calId, name, cancelText, lead)  +30  {calendar-sync}  — The ownership dialog.
L873  _calSyncProblem(r)  {calendar-sync}  — Why a sync did NOT happen, in the member's words — '' when it really ran.
L881  async window.onCalendarTargetChange(select)  +39  {calendar-sync}
L921  async window.onCalendarCleanupDupes(btn)  +42  {calendar-sync}
L964  async window.onCalendarResync(btn)  +41  {calendar-sync}

## Bike Preference UI (L1008)
L1012  async populateStudioSelect()  +61
L1074  window.renderBikePrefGrid()  +62
L1137  window.toggleBikePref(studioId, slotId)

## Integration: Bike Picker Highlights (L1166)
L1170  var _origShowBikePicker

## Integration: Tier Badges on Class Cards (L1234)
L1242  window.tierBadgeHTML(instructorId)  — app.js (eventCard / renderMyBookings) calls this hook directly next to the instructor name.

## Export / Import Settings (L1253)
L1257  var EXPORT_KEYS
L1269  window.exportSettings()  +35
L1311  _shareSettingsExport(json, fileName)  +33  {settings-export}  — Native export.
L1361  var IMPORT_MAX_FILE  {import-validate}  — What an import file may do.
L1362  var IMPORT_MAX_VALUE  {import-validate}
L1368  _planSettingsImport(data, deviceGet, opts)  +158  {import-validate}  — data: the parsed file.
L1528  _importSummary(added)  {import-validate}  — "120 classes, 4 rankings and 2 favourites" — what the plan adds, in words.
L1547  _applySettingsImport(plan)  — Writes an accepted plan, says what changed, reloads (the app reads most of these keys once, at launch).
L1565  window.importSettings(input)  +53

## Bug Report (L1620)

### Build id (window.APP_VERSION) (L1625)
L1633  var BUILD_ID_RE  {build-id}  — Nothing stamps a version into the page, so every report said "App version: unknown".
L1634  var _appVersionPending  {build-id}
L1641  var _swSwappedSinceLoad  {build-id}  — A worker that takes over mid-session deletes the old cache while the page keeps running the previous build's scripts,…
L1649  _buildIdsFromCaches()  {build-id}
L1659  _buildIdFromSwFile()  {build-id}  — Same-origin file (the app bundle on iOS).
L1672  resolveAppVersion()  {build-id}  — Resolves to the build id (also left on window.APP_VERSION), or null.
L1695  window.getAppVersion = resolveAppVersion  {build-id}
L1698  buildBugReport()  +51
L1750  async window.downloadBugReport()  +38
L1789  window.copyBugReport()
L1811  _fallbackCopy(text, status, what)  +30  — Returns whether the text really reached the clipboard, so a caller with its own status line (copyDiagnostics) never…

## Diagnostics Panel (L1843)
L1857  var DIAG_MAX_LOG_ROWS
L1859  esc(v)
L1866  diagHasToken()  — Boolean-only check for whether an auth token is present.
L1876  diagGetDiagnostics()  — Pull the diagnostics object from PsycleDiag, fully guarded.
L1887  diagRecentLog(getterName, key)  — Last N entries (newest first) from a log getter that returns [{timestamp, <key>}].
L1911  diagApiHealthHTML(diag)  +52  — Build the API-health section HTML from a diagnostics object (may be null).
L1965  diagEnvironmentHTML(diag)  — Build the environment section HTML.
L2000  var PRIVACY_POLICY_URL  — The iOS bridge's getDiagnosticReport() is async, but "Copy diagnostics" has to stay synchronous: awaiting inside the…
L2001  window.openPrivacyPolicy()
L2011  var _diagTaps  — Diagnostics is the owner's tool, in engineering words, so nothing in Settings advertises it: five quick taps on the…
L2012  var _diagTapAt
L2024  var _diagReportText
L2025  var _diagReportSeq
L2032  diagPlatform()  — Where this is running: 'ios' | 'android' (the two native apps, in Capacitor's words) or 'web'.
L2041  diagPrefetchReport()
L2063  diagBuildCopyBlob(diag)  +50  — Assemble the JSON blob copied by "Copy diagnostics".
L2114  window.openDiagnostics()  +60
L2175  window.closeDiagnostics()
L2180  diagStatus(msg, ok)
L2189  copyDiagnostics()
L2204  copyDiagnosticsFallback(blob)  — The status line used to say "copied" whatever the fallback did.
L2209  clearDiagnosticLogs()
