# js/api-client.js — symbols
Purpose: the shape of `js/api-client.js` — every top-level function, class, variable and `window.x` export, in source order under the file's own banners, with its line. Read this when you need to find code by what it DOES, or to see how the file is laid out. Skip it when you know the exact name — `grep -n` on the source is as cheap.

> Generated by agents/tools/build-index.mjs from working tree (uncommitted changes on top of `124a0b0`). Do not edit; rebuild with `npm run agents:index`.

666 lines · 24 symbols. The whole file is one IIFE: what is listed are the declarations one level inside it. Format: `L<line>  name(params)  +<length in lines, when 30 or more>  {pure blocks it sits in}  — first sentence of its comment`. Members of objects and functions nested in functions are not listed.


## 1. SCHEMAS — expected response shapes (drift baseline) (L33)
L41  var SCHEMAS  +51  — Each entry: { required: [...], optional: [...] }. `required` fields are the ones existing code reads unconditionally…
L95  var KIND_IS_ARRAY  — Map of schema-kind → which response shape it validates (object vs first array element).

## Diagnostic bridge — every call typeof-guarded (PsycleDiag is optional) (L105)
L109  diagNoteMissingField(path)
L122  diagNoteSample(kind, sample, validation)  — diagnostic.js exports record(kind, sample) — there never was a noteSample, so this bridge silently did nothing.

## 3. field(obj, path, fallback) — safe nested access + drift detection (L132)
L147  field(obj, path, fallback)  +33  — Read a nested value from `obj` by `path`.

## 5. validate(kind, data) — required-field check against SCHEMAS (L181)
L194  validate(kind, data)  — Compare `data` against SCHEMAS[kind].required.

## 4. categorizeError(responseOrError) → {type, userMessage} (L223)
L226  var USER_MESSAGES
L243  categorizeError(responseOrError)  +78  — Map a Response, an Error, or a number (status code) to a category and a short, friendly, actionable message.
L323  makeError(categorized, contextMsg)  — Build an Error carrying a categorized `.psycleError` for the UI.

## 6. parseJson(res) — content-type-aware, defensive JSON parse (L332)
L343  parseJson(res)  +56  — Parse a Response body as JSON, but FIRST guard against the corsproxy (or a gateway) returning an HTML error page with…

## 2. Typed getters (L400)
L410  getApiFetch()
L421  request(path, opts)  — Core request → categorized-on-failure → parsed JSON.
L455  process(kind, envelope, extract)  — Validate + report drift for a parsed payload, then return the data the callers actually consume.
L483  getInstructors()  — GET /instructors → unwrapped data[]
L492  getLocations()  — GET /locations → unwrapped data[]
L501  getEventTypes()  — GET /event-types → unwrapped data[]
L510  getProfile()  — GET /profile → unwrapped object (data.data || data)
L522  getBookings(query)  — GET /bookings[?query] → unwrapped list (array or data[]).
L540  getEvents(paramsObj)  — GET /events?<params> → { events, relations } (search code reads both).
L572  getEventDetail(id)  — GET /events/{id} → full detail envelope.
L588  getWaitlists(page)  — GET /waitlists?page=N → { entries: data[], meta } (one page of the customer's active waitlist places; meta.last_page…
L605  createBooking(bodyObj)  — POST /bookings → created booking envelope (callers read data.data.id || data.id).
L626  deleteBooking(pathOrId)  — DELETE a booking.

## Public surface (L641)
L644  window.PsycleAPI = {…}  — keys: SCHEMAS, getInstructors, getLocations, getEventTypes, getProfile, getBookings, getEvents, getEventDetail, getWaitlists, createBooking, deleteBooking, field, categorizeError, validate, parseJson
